---
title: "GENSR: Symbolic Regression Based in Equation Generative Space"
title_zh: GENSR：基于方程生成空间的符号回归
authors: "Qian Li, Yuxiao Hu, Juncheng Liu, Yuntian Chen"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20557v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于生成潜空间的符号回归框架
tldr: 符号回归（SR）旨在从数据中发现数学方程。针对传统方法在离散空间搜索导致反馈噪声大的问题，本文提出GenSR框架。该框架通过预训练条件变分自编码器（CVAE）构建具有符号连续性和局部数值平滑性的生成潜空间，将SR任务转化为“地图构建、粗略定位、精细搜索”的过程。GenSR在预测精度、表达式简洁度和计算效率上表现优异，且对噪声具有鲁棒性。
selection_source: fresh_fetch
motivation: 传统符号回归在离散空间搜索，方程结构微调与数值行为不一致，导致拟合误差反馈难以有效引导搜索。
method: 提出基于CVAE的生成潜空间框架，通过预训练将方程重参数化为平滑空间，并结合CMA-ES算法进行精细化搜索。
result: 实验表明GenSR在保持高预测准确率的同时，显著提升了表达式的简洁性和计算效率，并展现出强抗噪性。
conclusion: GenSR通过将符号回归重构为贝叶斯条件分布最大化问题，为在连续潜空间中高效搜索数学方程提供了理论保证和实践路径。
---

## 摘要
符号回归 (SR) 旨在揭示观测数据背后隐藏的方程。然而，大多数方法在离散方程空间中进行搜索，其中方程的结构修改很少与其数值行为一致，导致拟合误差反馈过于嘈杂，难以引导探索。为了应对这一挑战，我们提出了 GenSR，这是一个基于生成潜空间的符号回归框架，遵循“地图构建 -> 粗略定位 -> 精细搜索”的范式。具体而言，GenSR 首先预训练一个双分支条件变分自编码器 (CVAE)，将符号方程重新参数化为具有符号连续性和局部数值平滑性的生成潜空间。该空间可以被视为方程空间中结构良好的“地图”，为搜索提供方向信号。在推理时，CVAE 将输入数据粗略定位到潜空间中有前景的区域。随后，改进的 CMA-ES 利用平滑的潜梯度对候选区域进行精细化。从贝叶斯角度来看，GenSR 将符号回归任务重新定义为最大化条件分布 $p(\mathrm{Equ.} \mid \mathrm{Num.})$，而 CVAE 的训练通过证据下界 (ELBO) 实现了这一目标。这一新视角为 GenSR 的有效性提供了理论保证。大量实验表明，GenSR 在保持对噪声鲁棒性的同时，共同优化了预测准确性、表达式简洁性和计算效率。

## Abstract
Symbolic Regression (SR) tries to reveal the hidden equations behind observed data. However, most methods search within a discrete equation space, where the structural modifications of equations rarely align with their numerical behavior, leaving fitting error feedback too noisy to guide exploration. To address this challenge, we propose GenSR, a generative latent space-based SR framework following the `map construction -> coarse localization -> fine search'' paradigm. Specifically, GenSR first pretrains a dual-branch Conditional Variational Autoencoder (CVAE) to reparameterize symbolic equations into a generative latent space with symbolic continuity and local numerical smoothness. This space can be regarded as a well-structured `map'' of the equation space, providing directional signals for search. At inference, the CVAE coarsely localizes the input data to promising regions in the latent space. Then, a modified CMA-ES refines the candidate region, leveraging smooth latent gradients. From a Bayesian perspective, GenSR reframes the SR task as maximizing the conditional distribution $p(\mathrm{Equ.} \mid \mathrm{Num.})$, with CVAE training achieving this objective through the Evidence Lower Bound (ELBO). This new perspective provides a theoretical guarantee for the effectiveness of GenSR. Extensive experiments show that GenSR jointly optimizes predictive accuracy, expression simplicity, and computational efficiency, while remaining robust under noise.

---

## 论文详细总结（自动生成）

这是一份关于论文《GENSR: Symbolic Regression Based in Equation Generative Space》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
符号回归（SR）的核心挑战在于如何在庞大的数学方程空间中找到最拟合数据的表达式。
*   **研究动机**：传统方法（如遗传算法、强化学习）大多在**离散方程空间**中搜索。这种空间存在“不连续性”：方程结构的微小改变（编辑距离小）可能导致数值行为的剧烈波动。这使得拟合误差的反馈信号充满了噪声，难以引导搜索算法走向全局最优。
*   **核心目标**：将离散的方程空间重构为一个**连续且有组织的生成潜空间**（即“方程地图”），使方程的结构相似性和数值行为相似性在空间中得到统一，从而将复杂的组合优化问题转化为高效的连续空间搜索问题。

### 2. 方法论
GenSR 遵循“地图构建 $\rightarrow$ 粗略定位 $\rightarrow$ 精细搜索”的范式：

*   **双分支条件变分自编码器 (CVAE)**：
    *   **后验分支**：输入数值样本和符号方程，学习方程结构的分布，确保潜空间的**符号连续性**。
    *   **先验分支**：仅输入数值样本，学习数值特征的分布，通过 KL 散度与后验分支对齐，确保潜空间的**局部数值平滑性**。
*   **贝叶斯视角**：论文证明了训练 CVAE 等价于最大化证据下界（ELBO），从而在理论上保证了从数值数据推断方程概率分布的有效性。
*   **搜索算法（改进型 CMA-ES）**：
    *   **粗略定位**：推理时，先验分支将输入数据映射到潜空间的一个高概率区域。
    *   **精细搜索**：使用改进的协方差矩阵自适应进化策略（CMA-ES）在该区域内迭代。
    *   **关键优化**：为了应对高维潜空间（512维），采用了**对角协方差假设**和**Top-k 方差更新策略**，仅优化最相关的维度，显著提升了搜索效率。

### 3. 实验设计
*   **数据集**：使用了符号回归的标准基准测试集 **SRBench**，包含：
    *   **Feynman**：119 个物理方程。
    *   **Strogatz**：14 个非线性动力系统挑战。
    *   **Black-box**：57 个真实世界的黑盒回归任务。
*   **对比方法 (Baselines)**：共对比了 18 种算法，包括：
    *   **启发式/遗传算法**：GPlearn, Operon, GP-GOMEA 等。
    *   **深度学习/强化学习**：DSR, E2ESR, SNIP, TPSR, RAG-SR 等。
    *   **物理启发式**：AI Feynman 2.0。
*   **评估指标**：拟合优度 ($R^2$)、时间复杂度（运行耗时）、方程复杂度（符号长度）。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H800 (80GB)** GPU。
*   **训练细节**：
    *   预训练数据集包含约 **500 万个** 合成方程-样本对。
    *   训练 200 个 epoch，每个 epoch 包含 1000 个 step。
    *   每个 epoch 耗时约 1 小时（总训练时长约 200 小时）。
*   **模型规模**：参数量为 **1.621 亿 (162.1M)**。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽，涵盖了从标准基准测试到极端噪声环境的全面评估。
*   **充分性**：
    *   **消融实验**：深入探讨了潜空间维度、CMA-ES 的种群大小 ($s$)、步长 ($t$)、Top-k 更新频率等超参数的影响。
    *   **鲁棒性测试**：在 0 到 0.1 四个等级的噪声水平下进行了对比。
    *   **可视化分析**：通过 t-SNE 降维展示了潜空间的结构分离度和数值连续性。
    *   **额外任务**：验证了潜空间在方程分类任务中的迁移能力。
*   **客观性**：采用第三方维护的 SRBench 框架，确保了对比的公平性。

### 6. 主要结论与发现
*   **帕累托最优**：GenSR 在准确性、简洁性和效率之间达到了最佳平衡，始终处于帕累托前沿（Pareto Front）的第一梯队。
*   **高效搜索**：相比于纯离散搜索，GenSR 的连续潜空间搜索速度提升了数倍甚至数十倍。
*   **强鲁棒性**：在存在高噪声的情况下，GenSR 的 $R^2$ 评分下降最慢，且生成的方程依然保持简洁，不易过拟合。
*   **生成能力**：潜空间插值实验证明，GenSR 能够生成语法正确且物理意义平滑过渡的中间方程，这是判别式模型（如 SNIP）无法做到的。

### 7. 优点
*   **范式创新**：成功将符号回归从“大海捞针”式的离散组合优化转变为“地图导航”式的连续空间优化。
*   **理论完备**：提供了坚实的贝叶斯推导，将深度生成模型与经典进化策略有机结合。
*   **实用性强**：改进的 CMA-ES 解决了高维潜空间搜索的计算瓶颈，使其能够处理复杂的多元方程。

### 8. 不足与局限
*   **预训练依赖**：模型性能高度依赖于预训练合成数据的质量和覆盖范围。如果目标方程包含预训练中未出现的特殊算子，可能难以发现。
*   **复杂度限制**：为了防止搜索发散，训练时对方程长度进行了限制，这可能导致模型在发现极长、极复杂的嵌套方程时存在瓶颈。
*   **算力门槛**：预训练过程需要高性能 GPU 集群支持，对于资源受限的研究者来说门槛较高。

（完）
