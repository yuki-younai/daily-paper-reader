---
title: An information-based model selection criterion for data-driven model discovery
title_zh: 一种用于数据驱动模型发现的基于信息的模型选择准则
authors: "Michael C Chung, Alen Zacharia, Juan Guan"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21393v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用稀疏回归和信息准则从数据中提取可解释的符号模型
tldr: 针对数据驱动模型发现中难以平衡模型复杂性与准确性的问题，本文提出了一种新型信息准则 SLIC 及其配套的稀疏回归算法。该方法能自动生成候选模型并利用 SLIC 准则自适应地筛选出最优模型，有效避免了人工调参和过拟合。通过在多种非线性微分方程及流体力学、纳米技术实验数据上的验证，证明了 SLIC 在提取可解释物理模型和生成可测试预测方面显著优于传统准则。
selection_source: fresh_fetch
motivation: 现有的数据驱动模型发现算法在平衡拟合度与稀疏性时常需繁琐的人工调参，且容易出现过拟合现象。
method: 提出一种自动生成候选模型的稀疏回归算法，并引入样本长度缩放对数信息准则（SLIC）来识别最优模型。
result: 实验表明 SLIC 在处理非线性微分方程和真实实验数据时，其模型提取准确率显著超过了其他流行的方法选择准则。
conclusion: 该研究为从复杂数据中自动发现具有可解释性和预测能力的科学模型提供了一种稳健且高效的自动化工具。
---

## 摘要
数据驱动的模型发现 (DDMD) 算法是从数据中提取可解释符号模型的强大工具。然而，识别出最佳平衡拟合优度与稀疏性的模型通常是一个费力的过程，需要用户进行微调，且容易出现过拟合，结果可能因模型初始化和特定训练过程而产生显著差异。在此，我们提出了一种稀疏回归算法，该算法能够自动且自适应地生成候选模型，并使用一种新型的样本长度缩放对数信息准则 (SLIC) 从这些候选模型中识别出最佳模型。我们证明了在从多个非线性常微分方程和偏微分方程的数据中提取正确模型方面，SLIC 的表现大大优于其他流行的信息准则。随后，我们展示了 SLIC 从流体动力学和纳米技术的实验数据集中发现可解释模型的能力，这些模型能够产生新的可测试预测。

## Abstract
Data-driven model discovery (DDMD) algorithms are powerful tools for extracting interpretable symbolic models from data. However, identifying the model that best balances goodness-of-fit and sparsity is often a laborious process requiring user fine-tuning, is prone to overfitting, and results may significantly vary depending on model initialization and specific training procedure. Here, we present a sparse regression algorithm that automatically and adaptively generates candidate models, and uses a novel sample-length-scaling logarithmic information criterion (SLIC) to identify the best model from these candidates. We demonstrate that SLIC greatly outperforms other popular information criteria in extracting the correct model from the data of several nonlinear ordinary and partial differential equations. Then, we demonstrate SLIC's ability to discover interpretable models from experimental datasets in fluid dynamics and nanotechnology that generate new testable predictions.

---

## 论文详细总结（自动生成）

这是一份关于论文《An information-based model selection criterion for data-driven model discovery》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在科学发现中，从数据中自动提取可解释的数学模型（如微分方程）是数据驱动模型发现（DDMD）的核心目标。然而，现有的算法面临两个主要挑战：
*   **调参繁琐**：现有的稀疏回归方法（如 SINDy）通常需要用户手动调整稀疏性参数（阈值），这在处理未知系统或实时应用时非常困难。
*   **过拟合风险**：传统的信息准则（如 AIC、BIC）在处理大规模数据时，其拟合优度项的增长速度远超复杂度惩罚项，导致算法倾向于选择包含冗余项的复杂模型，从而失去可解释性和泛化能力。
*   **研究动机**：开发一种无需人工干预、能自动平衡模型准确性与简洁性，且在大数据量下依然稳健的模型选择准则。

### 2. 论文提出的方法论
论文提出了一套自动化的稀疏回归框架，核心包含两个部分：

*   **SLIC 准则（核心思想）**：
    提出了“样本长度缩放对数信息准则”（Sample-Length-scaling logarithmic Information Criterion, SLIC）。其数学表达式为：
    $$SLIC(\hat{\epsilon}, k) = n \log(\hat{\epsilon}k)$$
    其中，$n$ 是观测样本数，$\hat{\epsilon}$ 是均方误差（MSE），$k$ 是模型复杂度（非零参数数量）。
    *   **关键特性**：SLIC 要求惩罚项随 $n$ 线性缩放，并对 $k$ 取对数。这种设计确保了模型选择在数据量增大时保持一致性，且在数学上等价于最小化模型方差（Bias-Variance Tradeoff 中的 Variance）。

*   **自动剪枝算法流程**：
    1.  **初始化**：通过伪逆（Pseudoinverse）获得初始全量模型估计。
    2.  **候选生成**：利用当前模型系数的绝对值作为自适应阈值 $\lambda$。
    3.  **迭代优化**：根据生成的阈值对模型项进行剪枝，重新计算系数，并使用 SLIC 进行评分。
    4.  **模型选择**：在多次迭代中保留 SLIC 分数最低的模型作为最终输出。

### 3. 实验设计
论文通过模拟数据和真实实验数据进行了广泛验证：

*   **模拟场景（Benchmark）**：
    *   **常微分方程 (ODEs)**：Lorenz 系统、Rössler 吸引子、Lotka-Volterra 模型、Brusselator、Van der Pol 振子、非线性摆。
    *   **偏微分方程 (PDEs)**：Burger's 方程、KdV 方程、Kuramoto-Sivashinsky (KS) 方程、非线性薛定谔方程 (NLS)、Sine-Gordon 方程。
*   **对比方法**：AIC, AICc, HQIC, BIC, KIC, 以及 Bridge Criterion (BC)。
*   **真实实验数据**：
    *   **流体力学**：矩形坦克内液体晃动的质心动力学（Sloshing tank）。
    *   **纳米技术**：RNA-脂质体集群聚合过程中的相互作用核函数（Kernel function）发现。

### 4. 资源与算力
*   **软件环境**：算法使用 Julia 语言实现，利用了 `OrdinaryDiffEq.jl` 进行数值模拟，`DiffEqFlux.jl` 进行参数估计，以及 `Optimization.jl` 进行非线性优化。
*   **硬件说明**：论文**未明确说明**具体的 GPU 或 CPU 型号及具体的训练时长。但由于稀疏回归和 SLIC 计算主要涉及线性代数运算（伪逆）和简单的对数运算，其计算开销通常远低于深度学习模型，可在普通工作站上快速运行。

### 5. 实验数量与充分性
*   **实验规模**：对于每种模拟系统，研究者在不同的噪声水平（0% 到 40%）下进行了 **25 次独立随机实验**，以获取统计意义上的准确率和误报率（FPR）。
*   **充分性评价**：实验设计非常充分。研究不仅覆盖了经典的动力学系统，还探讨了下采样、采样频率变化、轨迹长度变化以及参数敏感性。
*   **客观性**：通过引入真实实验数据（非模拟数据）来验证模型的预测能力（如预测未见过的强迫振动条件下的系统行为），增强了结果的说服力和客观性。

### 6. 论文的主要结论与发现
*   **性能卓越**：在所有测试的 ODE 和 PDE 系统中，SLIC 在识别正确模型项方面的准确率显著高于 AIC 和 BIC，尤其是在高噪声环境下。
*   **极低误报**：SLIC 几乎不引入虚假的模型项（FPR 接近 0），而传统准则在大数据量下极易引入冗余项导致过拟合。
*   **物理发现能力**：
    *   在流体实验中，SLIC 自动发现了 Duffing 方程，并准确预测了系统在不稳定区域的滞后行为。
    *   在纳米材料实验中，SLIC 确定了聚合过程受扩散控制（恒定核函数），并据此成功预测了稀释样本的生长速率。

### 7. 优点
*   **自适应性**：无需人工预设稀疏阈值，实现了 DDMD 的高度自动化。
*   **理论优雅**：将信息准则与偏置-方差权衡直接联系起来，为 $n \log(k)$ 的缩放形式提供了物理/统计学解释。
*   **鲁棒性**：对噪声和数据量不敏感，解决了“数据越多越容易过拟合”的悖论。

### 8. 不足与局限
*   **库函数依赖**：该方法仍依赖于预定义的候选函数库（Library），如果真实的物理项不在库中，算法无法发现。
*   **计算复杂度**：虽然单次回归快，但在处理具有海量候选项的超大规模库时，迭代剪枝的搜索效率可能面临挑战。
*   **应用范围**：目前主要验证了基于回归的符号发现，对于基于神经网络的黑盒模型压缩或选择是否同样有效尚待研究。

（完）
