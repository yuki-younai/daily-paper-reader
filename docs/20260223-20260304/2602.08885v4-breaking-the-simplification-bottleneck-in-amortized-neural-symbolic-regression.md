---
title: Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression
title_zh: 突破摊销神经符号回归中的简化瓶颈
authors: "Paul Saegert, Ullrich Köthe"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08885v4"
tags: ["query:sr"]
score: 10.0
evidence: 用于摊销神经符号回归的高性能简化引擎
tldr: 符号回归旨在发现可解释的数学表达式，但摊销式神经符号回归在处理复杂科学问题时受限于表达式简化的效率瓶颈。本文提出 SimpliPy，一个比 SymPy 快 100 倍的规则化简化引擎，并构建了 Flash-ANSR 框架。该方法显著提升了训练和推理速度，支持更大规模的数据集训练和有效的测试集去重，在保持高精度的同时生成更简洁的表达式，性能可媲美最先进的遗传算法 PySR。
selection_source: fresh_fetch
motivation: 现有的摊销式符号回归方法受限于通用计算机代数系统极慢的简化速度，导致其难以扩展到复杂的科学计算场景。
method: 提出了一种名为 SimpliPy 的高效规则化简化引擎，并基于此构建了 Flash-ANSR 框架以实现大规模训练和高效的表达式规范化。
result: SimpliPy 实现了 100 倍的加速，使 Flash-ANSR 在基准测试中显著优于同类神经模型，并达到了与 PySR 相当的精度。
conclusion: 通过突破表达式简化的计算瓶颈，摊销式神经符号回归能够在大规模数据集上实现更高效、更准确且更简洁的公式发现。
---

## 摘要
符号回归 (SR) 旨在发现能够准确描述观测数据的可解释解析表达式。摊销 SR 有望比主流的遗传编程 SR 方法更高效，但目前在扩展到现实科学复杂度方面仍面临挑战。我们发现，一个关键障碍是缺乏将等价表达式快速约简为简洁规范形式的方法。摊销 SR 曾通过 SymPy 等通用计算机代数系统 (CAS) 来解决这一问题，但高昂的计算成本严重限制了训练和推理速度。我们提出了 SimpliPy，这是一种基于规则的简化引擎，在质量相当的情况下，其速度比 SymPy 快 100 倍。这为摊销 SR 带来了实质性的改进，包括对更大训练集的扩展性、对每个表达式 token 预算更高效的利用，以及针对等价测试表达式的系统性训练集去污染。我们在 Flash-ANSR 框架中展示了这些优势，该框架在 FastSRB 基准测试上的准确率远高于摊销基准模型（NeSymReS、E2E）。此外，它的性能与最先进的直接优化方法 (PySR) 相当，且随着推理预算的增加，它能恢复出更简洁而非更复杂的表达式。

## Abstract
Symbolic regression (SR) aims to discover interpretable analytical expressions that accurately describe observed data. Amortized SR promises to be much more efficient than the predominant genetic programming SR methods, but currently struggles to scale to realistic scientific complexity. We find that a key obstacle is the lack of a fast reduction of equivalent expressions to a concise normalized form. Amortized SR has addressed this by general-purpose Computer Algebra Systems (CAS) like SymPy, but the high computational cost severely limits training and inference speed. We propose SimpliPy, a rule-based simplification engine achieving a 100-fold speed-up over SymPy at comparable quality. This enables substantial improvements in amortized SR, including scalability to much larger training sets, more efficient use of the per-expression token budget, and systematic training set decontamination with respect to equivalent test expressions. We demonstrate these advantages in our Flash-ANSR framework, which achieves much better accuracy than amortized baselines (NeSymReS, E2E) on the FastSRB benchmark. Moreover, it performs on par with state-of-the-art direct optimization (PySR) while recovering more concise instead of more complex expressions with increasing inference budget.

---

## 论文详细总结（自动生成）

这是一份关于论文《Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：符号回归（SR）旨在从数据中发现简洁的数学公式。目前的“摊销式（Amortized）”神经符号回归方法（通过预训练 Transformer 直接预测公式）虽然推理速度快，但面临**简化瓶颈**。
*   **核心矛盾**：为了让模型学到规范的表达，训练数据必须经过简化（如将 $x+x$ 简化为 $2x$）。现有的计算机代数系统（如 SymPy）基于复杂的对象树操作，速度极慢，导致训练数据生成成为整个流水线的瓶颈。为了妥协，现有方法要么使用规模极小的静态数据集，要么放弃简化导致模型输出冗余且难以解释。

### 2. 方法论
论文提出了 **Flash-ANSR** 框架，其核心由以下三个部分组成：
*   **SimpliPy 简化引擎**：
    *   **离线规则发现**：利用 Kruskal 算法变体，在有限的符号空间内预先搜索并构建一个“最小生成森林”，将复杂的子表达式映射到最简形式。
    *   **在线模式匹配**：在训练/推理时，通过高效的哈希查找和模式匹配执行简化，避免了昂贵的树遍历。其速度比 SymPy 快约 100 倍，且简化质量相当。
*   **模型架构**：
    *   **编码器**：采用改进的 **Set Transformer**，引入了 **Masked RMSSetNorm**（掩码均方根集合归一化），能够高效处理变长的观测数据集。
    *   **解码器**：标准的 Transformer Decoder，使用旋转位置嵌入（RoPE）和 FlashAttention 优化。
*   **训练与去污染流程**：
    *   **在线生成**：在训练过程中实时生成并简化表达式，总计训练量达 5.12 亿个样本。
    *   **严格去污染**：通过符号和数值双重校验，确保训练集中不包含任何与测试集等价的表达式，保证了实验的严谨性。

### 3. 实验设计
*   **基准测试**：使用 **FastSRB** 基准，包含 115 个具有物理意义的科学表达式，数据域基于真实的物理场景。
*   **对比方法**：
    *   **摊销式基准**：NeSymReS、E2E（代表了目前最先进的神经符号回归模型）。
    *   **直接优化基准**：PySR（目前工业界和学术界公认的遗传编程黄金标准）。
*   **评估维度**：数值恢复率（NRR）、推理时间与精度的 Pareto 前沿、数据稀疏性影响、噪声鲁棒性。

### 4. 资源与算力
论文详细列出了不同规模模型的训练资源：
*   **v23.0-3M**：RTX 2080Ti，耗时 112 小时。
*   **v23.0-20M**：RTX 4090，耗时 56 小时。
*   **v23.0-120M**：A100，耗时 340 小时。
*   **v23.0-1B**：**H200**，耗时 **657 小时**。
*   **推理环境**：单台配备 RTX 4090 和 AMD Ryzen 9 9950X 的工作站。

### 5. 实验数量与充分性
*   **实验规模**：针对 115 个测试表达式，每个表达式在不同设置下重复 5-10 次实验，并使用自助法（Bootstrapping）计算 95% 置信区间。
*   **充分性**：实验覆盖了从毫秒级到 1000 秒级的推理预算全谱系，并进行了详尽的消融实验（如先验分布对常数预测的影响）。
*   **公平性**：通过“推理时间-恢复率”曲线进行对比，避免了因硬件或搜索步数设置不同导致的比较偏差。

### 6. 主要结论与发现
*   **性能突破**：Flash-ANSR 彻底碾压了之前的神经符号回归模型（NeSymReS, E2E），并在 120M 及以上参数规模时，在数值精度上达到了与遗传算法 PySR 相当的水平。
*   **简洁性反转（Parsimony Inversion）**：这是一个重要发现。传统方法（如 PySR）随着搜索时间增加，往往会生成更复杂的公式来拟合残差；而 Flash-ANSR 随着推理预算（采样数）增加，找到的公式反而**越来越简洁**，更趋向于真实的物理定律。
*   **双下降现象**：在数据稀疏性实验中观察到了类似于深度学习的“双下降”现象，在极少数据点时模型倾向于简单插值，在特定点数时复杂度激增，随后随数据增多回归真实规律。

### 7. 优点
*   **效率极高**：SimpliPy 解决了困扰该领域多年的 CAS 瓶颈，使得大规模在线训练成为可能。
*   **评估严谨**：在数据去污染和数值恢复标准（要求达到机器精度 $10^{-7}$）上设立了新的行业标杆。
*   **模型扩展性**：证明了符号回归任务同样符合 Scaling Laws，大模型在处理复杂公式时表现出更强的泛化力。

### 8. 不足与局限
*   **噪声鲁棒性**：在处理高噪声数据（$\eta \ge 10^{-2}$）时，Flash-ANSR 的表现不如 PySR。这是因为模型在纯净数据上训练，会将噪声误认为是复杂的函数结构（分布偏移）。
*   **算力门槛**：1B 参数模型的训练成本极高（H200 运行近一个月），对于普通研究者来说难以复现。
*   **算子限制**：虽然支持 30 多种算子，但对于超出预定义算子集的公式仍无法处理。

（完）
