---
title: "Beyond Error-Based Optimization: Experience-Driven Symbolic Regression with Goal-Conditioned Reinforcement Learning"
title_zh: 超越基于误差的优化：基于目标条件强化学习的经验驱动符号回归
authors: "Jianwen Sun, Xinrui Li, Fuqing Li, Xiaoxuan Shen"
date: 2026-01-21
pdf: "https://arxiv.org/pdf/2601.14693v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于经验驱动的强化学习符号回归
tldr: 针对符号回归中传统误差驱动搜索易陷入局部最优且结构模糊的问题，本文提出EGRL-SR框架。该框架将符号回归建模为目标条件强化学习问题，利用事后经验回放和动作价值网络从历史轨迹中学习映射模式。通过设计全点满足的二元奖励函数和结构引导的启发式探索策略，EGRL-SR能更有效地引导搜索过程，在恢复率和鲁棒性上显著优于现有方法。
selection_source: fresh_fetch
motivation: 传统的基于误差的搜索方法在面对具有相似误差但结构迥异的候选表达式时，难以提供明确的搜索方向，导致收敛困难。
method: 提出一种基于目标条件强化学习的框架，结合事后经验回放、二元奖励函数及结构引导的启发式探索，利用历史经验主动引导搜索。
result: 在公开基准测试中，EGRL-SR在恢复率、鲁棒性以及复杂表达式的恢复能力上均一致优于当前最先进的方法。
conclusion: 实验证明动作价值网络能有效引导搜索，且所设计的奖励函数和探索策略对提升符号回归性能至关重要。
---

## 摘要
符号回归旨在自动识别紧凑且可解释的数学表达式，以建模输入和输出变量之间的函数关系。大多数现有的基于搜索的符号回归方法通常依赖拟合误差来指导搜索过程。然而，在广阔的表达式空间中，许多候选表达式可能表现出相似的误差值，但在结构上却存在巨大差异，这导致了模糊的搜索方向，并阻碍了向底层真实函数的收敛。为了应对这一挑战，我们提出了一个名为 EGRL-SR（经验驱动的目标条件强化学习符号回归）的新型框架。与传统的误差驱动方法不同，EGRL-SR 引入了一个新的视角：利用精确的历史轨迹并优化动作价值网络，以主动引导搜索过程，从而实现更稳健的表达式搜索。具体而言，我们将符号回归建模为一个目标条件强化学习问题，并结合了事后经验回放（hindsight experience replay），使动作价值网络能够从多样的输入输出对中泛化出通用的映射模式。此外，我们设计了一个全点满足二元奖励函数，鼓励动作价值网络关注结构模式而非低误差表达式，并同时提出了一种结构引导的启发式探索策略，以增强搜索多样性和空间覆盖率。在公共基准测试上的实验表明，EGRL-SR 在恢复率和稳健性方面始终优于现有最先进的方法，并且在相同的搜索预算下可以恢复更复杂的表达式。消融实验结果验证了动作价值网络能有效地引导搜索，且奖励函数和探索策略都发挥了关键作用。

## Abstract
Symbolic Regression aims to automatically identify compact and interpretable mathematical expressions that model the functional relationship between input and output variables. Most existing search-based symbolic regression methods typically rely on the fitting error to inform the search process. However, in the vast expression space, numerous candidate expressions may exhibit similar error values while differing substantially in structure, leading to ambiguous search directions and hindering convergence to the underlying true function. To address this challenge, we propose a novel framework named EGRL-SR (Experience-driven Goal-conditioned Reinforcement Learning for Symbolic Regression). In contrast to traditional error-driven approaches, EGRL-SR introduces a new perspective: leveraging precise historical trajectories and optimizing the action-value network to proactively guide the search process, thereby achieving a more robust expression search. Specifically, we formulate symbolic regression as a goal-conditioned reinforcement learning problem and incorporate hindsight experience replay, allowing the action-value network to generalize common mapping patterns from diverse input-output pairs. Moreover, we design an all-point satisfaction binary reward function that encourages the action-value network to focus on structural patterns rather than low-error expressions, and concurrently propose a structure-guided heuristic exploration strategy to enhance search diversity and space coverage. Experiments on public benchmarks show that EGRL-SR consistently outperforms state-of-the-art methods in recovery rate and robustness, and can recover more complex expressions under the same search budget. Ablation results validate that the action-value network effectively guides the search, with both the reward function and the exploration strategy playing critical roles.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Error-Based Optimization: Experience-Driven Symbolic Regression with Goal-Conditioned Reinforcement Learning》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：符号回归（SR）旨在从数据中自动发现简洁、可解释的数学表达式。
*   **核心问题**：现有的搜索类方法（如遗传算法、强化学习）主要依赖**拟合误差**来指导搜索。然而，在广阔的表达式空间中，许多结构完全不同的表达式在特定区间内可能具有极低的相似误差（如图 2 所示），这导致搜索方向模糊，容易陷入局部最优，且难以恢复真实的底层函数。
*   **整体含义**：本文提出从“误差驱动”转向“经验驱动”，通过学习历史搜索轨迹中的结构模式来主动引导搜索，提高复杂表达式的恢复成功率。

### 2. 论文提出的方法论：EGRL-SR 框架
核心思想是将符号回归建模为**目标条件强化学习（GCRL）**问题，利用历史经验学习输入 $x$ 到输出 $y$ 的映射模式。
*   **目标条件马尔可夫决策过程（GC-MDP）**：
    *   **状态 (State)**：定义为当前中间数值输出 $x_{now}$ 与目标输出 $y$ 的拼接。
    *   **动作 (Action)**：采用后缀表达式生成策略，包括变量、一元算子和二元算子。
    *   **奖励函数 (APSR)**：设计了“全点满足二元奖励”。只有当表达式在所有样本点上均满足精度阈值时才给予奖励 1，否则为 0。这避免了相似误差带来的结构误导。
*   **关键技术细节**：
    *   **事后经验回放 (HER)**：将失败的尝试（未达到目标 $y$）重新标记为成功的尝试（达到了某个中间值 $y'$），从而让模型从大量“失败”中学习通用的数学构造模式。
    *   **优化算法**：采用 **Double Dueling DQN** 处理离线经验，缓解 Q 值高估问题。
    *   **结构引导启发式探索 (SGHE)**：将搜索空间按结构属性（如嵌套深度、算子位置）划分为多个子空间，并分配独立的价值网络进行探索，确保搜索的多样性和覆盖率。

### 3. 实验设计
*   **数据集/场景**：使用了符号回归领域公认的三个基准测试集：**Nguyen**、**Livermore** 和 **Keijzer**。
*   **对比方法 (Baselines)**：涵盖了当前主流的四大类方法，共 10 种：
    *   **强化学习类**：DSR, DSO。
    *   **预训练/Transformer类**：NeSymReS, End-to-End (E2E)。
    *   **遗传算法类**：GOMEA, PySR。
    *   **方程学习/神经网络类**：EQL-Div, DySymNet。
    *   **蒙特卡洛树搜索类**：TPSR。
*   **评估指标**：采用最严格的**精确恢复率 (Exact Recovery Rate)**，即要求生成的表达式在数学形式上（含常数）与目标完全一致。

### 4. 资源与算力
*   **搜索预算**：每个任务分配统一的 160 万步（1.6 million steps）搜索预算。
*   **硬件信息**：论文正文及提取文本中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。通常此类强化学习任务在单张消费级 GPU（如 RTX 3090/4090）上即可运行，但文中缺乏具体参数。

### 5. 实验数量与充分性
*   **实验规模**：针对不同长度区间（从长度 $\le 8$ 到 $\ge 31$）的表达式进行了分层测试。
*   **重复性**：每个目标表达式均进行了 **12 次独立重复实验**，并记录了平均值和标准差。
*   **消融实验**：针对 HER（经验回放）、APSR（奖励函数）和 SGHE（探索策略）分别做了对比实验，验证了各组件的必要性。
*   **鲁棒性测试**：引入了不同等级的噪声（0 到 0.1），测试模型在非理想数据下的表现。
*   **评价**：实验设计较为全面、客观，对比了多种不同范式的 SOTA 方法，具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能领先**：EGRL-SR 在几乎所有长度区间内的恢复率均显著优于对比方法，尤其是在处理复杂长表达式时优势明显。
*   **抗噪性强**：在噪声环境下，EGRL-SR 的性能下降比基于遗传算法的方法（如 PySR）更缓慢，表现出更好的鲁棒性。
*   **价值引导有效**：消融实验证明，动作价值网络确实学习到了有效的搜索启发式信息，显著降低了找到目标所需的轨迹数（TtS）。
*   **传统方法局限**：发现预训练模型和 MCTS 方法在精确恢复任务上表现较差，主要受限于训练分布不匹配或搜索效率低下。

### 7. 优点（亮点）
*   **范式创新**：首次将目标条件强化学习（GCRL）引入符号回归，改变了单纯依赖误差反馈的搜索逻辑。
*   **解决模糊性**：通过二元奖励和 HER，有效地解决了“低误差但不正确结构”对搜索过程的干扰。
*   **结构化探索**：SGHE 策略通过划分结构子空间，解决了强化学习在巨大搜索空间中容易陷入局部最优的问题。

### 8. 不足与局限
*   **常数处理**：论文提到目前常数是通过变量和算子的组合间接表示的，这在处理含有复杂浮点常数的表达式时会极大地增加搜索空间，导致性能下降。
*   **算力开销**：虽然未详述，但 160 万步的搜索预算对于某些实时性要求高的场景可能仍然偏大。
*   **算子集限制**：实验主要基于预定义的 8 个基础算子，对于包含特殊函数（如 Bessel 函数等）的极复杂科学公式，其泛化能力尚待验证。

（完）
