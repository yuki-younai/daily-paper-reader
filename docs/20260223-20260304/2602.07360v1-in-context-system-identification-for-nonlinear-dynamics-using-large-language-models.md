---
title: In-Context System Identification for Nonlinear Dynamics Using Large Language Models
title_zh: 基于大语言模型的非线性动力学上下文系统辨识
authors: Linyu Lin
date: 2026-02-07
pdf: "https://arxiv.org/pdf/2602.07360v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用大语言模型自动发现控制方程并精炼符号形式
tldr: 本研究针对非线性动力系统识别中SINDy方法依赖专家调优候选库的局限，提出了一种LLM辅助的SINDy流水线。该方法通过上下文学习将大语言模型引入迭代优化循环，利用LLM的符号推理能力根据误差反馈和领域约束建议新的方程结构。在63个动力系统数据集上的实验表明，该方法在符号恢复准确性和预测精度上均优于传统SINDy，实现了数据驱动误差反馈与领域启发式推理的有效结合。
selection_source: fresh_fetch
motivation: 传统的SINDy方法在发现非线性动力学方程时，高度依赖于人工设计的候选函数库，缺乏自动化和灵活性。
method: 提出一种将LLM引入循环的迭代优化框架，利用上下文学习根据当前误差和约束自动调整并建议方程的符号形式。
result: 在ODEBench等多个数据集上的测试显示，该方法显著提高了方程的结构相似度并降低了测试均方根误差。
conclusion: 大语言模型能够有效引导SINDy在方程空间中的搜索，通过结合数据反馈与符号推理，发现更准确且具可解释性的控制方程。
---

## 摘要
非线性动力学稀疏识别（SINDy）是从数据中发现简洁控制方程的强大方法，但它通常需要专家对候选库进行调优。我们提出了一种 LLM 辅助的 SINDy 流水线，通过上下文学习（in-context learning）将大语言模型（LLM）引入循环，迭代地优化候选方程。该流水线首先使用自适应库拟合基准 SINDy 模型，然后进入 LLM 引导的优化循环。在每次迭代中，当前的最佳方程、误差指标和领域特定约束被汇总到 LLM 的提示词中，由其建议新的方程结构。这些候选方程根据定义的符号形式进行解析，并在训练和测试数据上进行评估。该流水线将基于模拟的误差作为主要指标，同时也评估与真实情况（ground truth）的结构相似性，包括匹配函数形式、关键项、耦合关系和定性行为。如果测试误差低于阈值（NRMSE < 0.1）或达到最大 10 次迭代，迭代停止标准将提前结束优化。最后，选择最佳模型，我们在 63 个动力系统数据集（ODEBench）以及用于沸水核反应堆的 March-Leuba 模型上评估了这种 LLM 辅助的 SINDy。结果与经典 SINDy 进行了对比，表明在复杂动力学情况下，LLM 循环一致地提高了符号恢复能力，与基准 SINDy 相比，具有更高的方程结构相似性和更低的测试均方根误差（RMSE）。这项工作证明了 LLM 可以有效地引导 SINDy 在方程空间中的搜索，将数据驱动的误差反馈与受领域启发的符号推理相结合，从而发现不仅准确而且在结构上具有可解释性的控制方程。

## Abstract
Sparse Identification of Nonlinear Dynamics (SINDy) is a powerful method for discovering parsimonious governing equations from data, but it often requires expert tuning of candidate libraries. We propose an LLM-aided SINDy pipeline that iteratively refines candidate equations using a large language model (LLM) in the loop through in-context learning. The pipeline begins with a baseline SINDy model fit using an adaptive library and then enters a LLM-guided refinement cycle. At each iteration, the current best equations, error metrics, and domain-specific constraints are summarized in a prompt to the LLM, which suggests new equation structures. These candidate equations are parsed against a defined symbolic form and evaluated on training and test data. The pipeline uses simulation-based error as a primary metric, but also assesses structural similarity to ground truth, including matching functional forms, key terms, couplings, qualitative behavior. An iterative stopping criterion ends refinement early if test error falls below a threshold (NRMSE < 0.1) or if a maximum of 10 iterations is reached. Finally, the best model is selected, and we evaluate this LLM-aided SINDy on 63 dynamical system datasets (ODEBench) and march leuba model for boiling nuclear reactor. The results are compared against classical SINDy and show the LLM-loop consistently improves symbolic recovery with higher equation similarity to ground truth and lower test RMSE than baseline SINDy for cases with complex dynamics. This work demonstrates that an LLM can effectively guide SINDy's search through equation space, integrating data-driven error feedback with domain-inspired symbolic reasoning to discover governing equations that are not only accurate but also structurally interpretable.

---

## 论文详细总结（自动生成）

以下是对论文《In-Context System Identification for Nonlinear Dynamics Using Large Language Models》（基于大语言模型的非线性动力学上下文系统辨识）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的非线性动力学稀疏识别（SINDy）方法高度依赖于人工预设的候选函数库。如果真实的物理项不在预设库中，模型将无法准确识别系统方程。
*   **研究动机**：现有的深度学习方法（如 Neural ODEs）虽能拟合轨迹，但缺乏可解释的符号公式；而纯符号回归方法在处理复杂动力学时搜索空间巨大。
*   **整体含义**：本文旨在利用大语言模型（LLM）的科学常识和符号推理能力，将其作为“假设生成器”引入 SINDy 的迭代循环中，实现候选库的自适应扩展和方程结构的自动精炼。

### 2. 方法论：核心思想与技术细节
该论文提出了一种 **LLM-aided SINDy 流水线**，其核心是一个闭环迭代优化过程：
*   **初始基准拟合**：首先使用包含多项式、三角函数等基础项的固定库进行初步 SINDy 拟合，获取基准方程和误差指标。
*   **LLM 提示词构建（Prompt Engineering）**：将当前方程、状态变量的统计特征（如是否振荡、饱和）、模拟误差（NRMSE）、领域先验知识以及失败的尝试记录汇总，构建提示词发送给 LLM。
*   **受限候选生成**：LLM 建议新的函数项（如指数项、有理项或特定的耦合项）。这些建议必须符合线性参数化形式：$\dot{x}_i(t) = \sum \theta_{i,k} \phi_k(x, u, t)$，以便后续使用稀疏回归求解系数 $\theta$。
*   **验证与筛选**：通过语法解析器检查 LLM 生成的方程是否合法，剔除重复项，并进行稀疏回归拟合。
*   **多目标评分与选择**：使用综合评分函数 $J = \max(NRMSE_i) + \lambda_c \mathcal{C} + \lambda_p \mathcal{P}$，平衡预测精度（NRMSE）、符号复杂度（$\mathcal{C}$）和先验一致性（$\mathcal{P}$）。
*   **迭代停止**：当测试误差低于阈值（NRMSE < 0.1）或达到最大迭代次数（10次）时停止。

### 3. 实验设计
*   **数据集/场景**：
    *   **ODEBench**：包含 63 个不同的常微分方程（ODE）系统，涵盖 1D 到 4D 动力学，包括线性振荡器、混沌系统（如 Lorenz 变体）、捕食者-猎物模型等。
    *   **March-Leuba 模型**：一个复杂的 7 状态沸水核反应堆（BWR）稳定性模型，具有强耦合和非线性反馈特征。
*   **Benchmark（基准）**：经典 SINDy（使用固定且广泛的候选库）。
*   **对比指标**：预测精度（$R^2$、NRMSE）和结构恢复质量（是否找回了真实的物理项）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：由于核心算法涉及 LLM 调用（通过 API）和数值积分（ODE 求解器），其主要开销在于 LLM 的推理延迟和多次迭代中的模拟评估。文中提到设置了严格的模拟超时限制以防止病态模型导致程序卡死。

### 5. 实验数量与充分性
*   **实验规模**：在 63 个标准基准系统上进行了全面测试，并增加了一个复杂的核工程实际案例。
*   **充分性评价**：实验设计较为充分。通过大规模的 ODEBench 测试，证明了方法在不同维度和动力学类型下的通用性。
*   **客观性**：对比了基准 SINDy 在相同初始条件下的表现，并使用了独立的测试集评估泛化能力，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **精度提升**：在 63 个系统中，LLM 辅助方法使 48 个系统达到了停止准则，而基准 SINDy 仅为 30 个。
*   **误差降低**：中值测试误差从基准的 62% 大幅降至 3.4%；在基准 SINDy 导致模拟发散（无限误差）的案例中，该方法均能找到稳定模型。
*   **结构恢复**：LLM 辅助方法成功恢复了 36 个系统的正确物理结构，显著高于基准的 26 个。
*   **发现**：LLM 能够像专家一样通过观察误差（如相位滞后、阻尼不匹配）来建议正确的函数变换（如将三角函数替换为线性项或引入指数衰减）。

### 7. 优点（亮点）
*   **自适应性**：突破了 SINDy 对固定库的依赖，能够自动发现库外（out-of-library）的函数项。
*   **可解释性**：保留了符号回归的透明度，每一个迭代生成的模型都是人类可读的数学公式。
*   **鲁棒性**：引入了模拟验证环节，确保生成的方程不仅在导数拟合上准确，在长程预测（rollout）中也稳定。
*   **知识融合**：成功将 LLM 的符号推理能力与数值优化的严谨性结合在一起。

### 8. 不足与局限
*   **LLM 依赖性**：系统的成功很大程度上取决于 LLM 建议相关项的能力，对于极其罕见或复杂的复合函数（如 $cot(y) \cos(x)$），LLM 仍可能失效。
*   **计算成本**：相比一键式的 SINDy，迭代过程（涉及多次 LLM 调用和 ODE 模拟）计算开销更高。
*   **潜在偏见**：LLM 可能会倾向于建议其训练数据中常见的函数形式，从而在处理全新的物理现象时产生搜索偏见。
*   **噪声敏感性**：虽然实验中使用了无噪声数据，但在高噪声环境下的表现尚未得到充分验证。

（完）
