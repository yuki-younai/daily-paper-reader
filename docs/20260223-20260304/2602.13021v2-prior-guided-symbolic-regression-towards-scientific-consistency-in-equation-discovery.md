---
title: "Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery"
title_zh: 先验引导的符号回归：迈向方程发现中的科学一致性
authors: "Jing Xiao, Xinhai Chen, Jiaming Peng, Qinglin Wang, Menghan Jia, Zhiquan Lai, Guangping Yu, Dongsheng Li, Tiejun Li, Jie Liu"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13021v2"
tags: ["query:sr"]
score: 10.0
evidence: 具有科学一致性约束的先验引导符号回归框架
tldr: 符号回归旨在从数据中发现方程，但现有方法易陷入拟合良好却违背科学原理的“伪方程陷阱”。本文提出PG-SR框架，通过三阶段流程引入先验约束检查器和先验退火约束评估机制，将领域知识转化为可执行约束。理论上该方法降低了假设空间复杂度，实验表明其在多领域优于基线，且对噪声和数据稀缺具有强鲁棒性。
selection_source: fresh_fetch
motivation: 现有符号回归方法过度依赖经验风险最小化，导致生成的方程虽然拟合度高但缺乏科学一致性。
method: 提出PG-SR框架，利用先验约束检查器和先验退火约束评估机制，在三阶段演化过程中引导方程发现。
result: 实验证明PG-SR在多个领域显著超越了现有最先进基线，并在噪声和数据稀缺环境下表现出极强的鲁棒性。
conclusion: 通过引入显式的科学先验约束，PG-SR有效解决了伪方程问题，提升了符号回归在科学发现中的可靠性与泛化能力。
---

## 摘要
符号回归 (SR) 旨在从观测数据中发现可解释的方程，具有揭示自然现象背后潜在原理的潜力。然而，现有方法往往会陷入“伪方程陷阱”：生成的方程虽然能很好地拟合观测数据，但与基本科学原理不一致。一个关键原因是这些方法由经验风险最小化主导，缺乏确保科学一致性的显式约束。为了弥补这一差距，我们提出了 PG-SR，这是一个基于预热、进化和精炼三阶段流水线的先验引导符号回归框架。在整个流水线中，PG-SR 引入了一个先验约束检查器，将领域先验显式编码为可执行的约束程序，并在进化阶段采用先验退火约束评估 (PACE) 机制，逐步引导发现过程转向科学一致的区域。在理论上，我们证明了 PG-SR 降低了假设空间的 Rademacher 复杂度，从而产生了更紧致的泛化界，并为防止伪方程提供了保证。在实验上，PG-SR 在多个领域均优于最先进的基准方法，并对不同的先验质量、噪声数据和数据稀缺性保持了鲁棒性。

## Abstract
Symbolic Regression (SR) aims to discover interpretable equations from observational data, with the potential to reveal underlying principles behind natural phenomena. However, existing approaches often fall into the Pseudo-Equation Trap: producing equations that fit observations well but remain inconsistent with fundamental scientific principles. A key reason is that these approaches are dominated by empirical risk minimization, lacking explicit constraints to ensure scientific consistency. To bridge this gap, we propose PG-SR, a prior-guided SR framework built upon a three-stage pipeline consisting of warm-up, evolution, and refinement. Throughout the pipeline, PG-SR introduces a prior constraint checker that explicitly encodes domain priors as executable constraint programs, and employs a Prior Annealing Constrained Evaluation (PACE) mechanism during the evolution stage to progressively steer discovery toward scientifically consistent regions. Theoretically, we prove that PG-SR reduces the Rademacher complexity of the hypothesis space, yielding tighter generalization bounds and establishing a guarantee against pseudo-equations. Experimentally, PG-SR outperforms state-of-the-art baselines across diverse domains, maintaining robustness to varying prior quality, noisy data, and data scarcity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：论文指出了符号回归（SR）中的**“伪方程陷阱”（Pseudo-Equation Trap）**。即现有方法在面对噪声或稀疏数据时，往往能生成数值拟合度极高（低 MSE）但完全违背基本科学原理（如物理守恒、生物单峰响应等）的方程。
*   **研究动机**：现有的搜索类、Transformer类及大模型（LLM）类方法大多由**经验风险最小化（ERM）**主导，缺乏显式的科学一致性约束。这导致模型在分布外（OOD）预测时容易发生灾难性发散，无法真正用于科学发现。

### 2. 方法论：PG-SR 框架
PG-SR 提出了一个结合领域先验引导的三阶段流水线：
*   **核心思想**：将人类验证的领域知识转化为可执行的 Python 约束程序，并通过动态调整约束强度的机制引导搜索。
*   **关键技术细节**：
    1.  **先验约束检查器（Prior Constraint Checker）**：利用 LLM 提取领域知识，结合专家验证和数据统计分析，生成可执行的约束代码（如检查方程在特定条件下的单调性、边界值等）。
    2.  **先验退火约束评估（PACE）**：这是核心评分机制。它不直接剔除违背约束的方程，而是通过一个随进化时间 $t$ 变化的函数 $S(f, D, C)$ 来调整评分。初期允许违背约束以保持多样性，后期（退火过程）严厉惩罚违背者，强制模型收敛到科学一致的区域。
    3.  **三阶段流程**：
        *   **预热（Warm-up）**：基于数据统计特征生成初始方程骨架。
        *   **进化（Evolution）**：利用 LLM 作为算子，结合 PACE 评分在多个并行“岛屿”中演化方程。
        *   **精炼（Refinement）**：通过残差分析和反射机制，针对性地修正方程的结构缺陷。
*   **理论支撑**：论文证明了引入先验约束能有效降低假设空间的 **Rademacher 复杂度**，从而提供更紧致的泛化界，从理论上保证了对伪方程的抑制。

### 3. 实验设计
*   **数据集与场景**：涵盖了生物（大肠杆菌生长）、材料（铝合金应力-应变）、化学（反应动力学）和物理（非线性振荡器）四个领域的 5 个复杂数据集。
*   **Benchmark 与对比方法**：
    *   **搜索类**：PySR, GPLearn, DSR, uDSR, Operon, GP-GOMEA。
    *   **Transformer 类**：TPSR, E2E, NeSymReS, PhyE2E。
    *   **LLM 类**：LLM-SR, LaSR, DrSR。
*   **评估指标**：重点考察 **ID（分布内）** 和 **OOD（分布外）** 的归一化均方误差（NMSE）。

### 4. 资源与算力
*   **硬件环境**：使用本地工作站，配置为 Intel Core i9-14900HX CPU 和 **NVIDIA GeForce RTX 4090 GPU**。
*   **模型后端**：主要使用 `gpt-4o-mini`，同时也测试了 `Llama-3.3-70B` 以验证鲁棒性。
*   **训练/推理时长**：在标准基准测试（如振荡器）上，单次完整运行大约需要 **10 小时**。
*   **成本**：单次运行消耗约 1785 万个 Token，按商业 API 价格计算约为 4.70 美元。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括 5 个数据集上的性能对比、针对 13 个基线的横向评测。
*   **消融实验**：分别验证了预热阶段、PACE 机制和精炼阶段的必要性。
*   **鲁棒性测试**：
    *   **先验质量敏感性**：测试了在“无先验”、“弱先验”、“错误先验”和“全先验”下的表现，证明了 PACE 机制能防止模型被错误先验误导。
    *   **噪声与稀缺性**：在不同噪声水平（0.01 到 0.1）和不同数据量（5% 到 100%）下进行了压力测试。
*   **评价**：实验设计非常充分且客观，特别是 OOD 泛化实验和噪声鲁棒性实验，直接回应了符号回归在现实科学场景中的核心痛点。

### 6. 主要结论与发现
*   **性能领先**：PG-SR 在所有数据集的 ID 和 OOD 测试中均取得了最低的 NMSE，尤其在 OOD 场景下优势巨大。
*   **成功逃离陷阱**：通过显式约束，PG-SR 能够发现符合物理规律的方程（如准确找回振荡器的基准方程），而基线方法往往在 OOD 区域发生数值发散。
*   **鲁棒性强**：即使在数据极度稀缺（仅 5% 数据）或高噪声环境下，依靠先验引导，模型依然能保持稳定的预测性能。

### 7. 优点与亮点
*   **科学一致性**：将“软”的 LLM 生成与“硬”的程序化约束结合，解决了符号回归长期存在的不可靠问题。
*   **PACE 机制**：巧妙地平衡了搜索初期的探索（Exploration）和后期的规范（Exploitation），避免了硬过滤导致的搜索停滞。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了基于统计学习理论的复杂度证明。

### 8. 不足与局限
*   **人工干预**：虽然使用了 LLM 辅助，但在构建“可执行先验约束程序”时仍需要一定程度的人类专家介入和验证，尚未实现完全自动化的“端到端”发现。
*   **推理成本**：相比于传统的遗传算法（如 PySR），基于 LLM 的迭代过程在时间和 Token 费用上成本较高。
*   **应用限制**：对于那些完全缺乏领域先验知识的“黑盒”系统，该方法的优势可能无法充分发挥。

（完）
