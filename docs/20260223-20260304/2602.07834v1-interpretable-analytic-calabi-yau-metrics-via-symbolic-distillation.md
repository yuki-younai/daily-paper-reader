---
title: Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation
title_zh: 通过符号蒸馏获得的可解释解析卡拉比-丘度规
authors: D Yang Eng
date: 2026-02-08
pdf: "https://arxiv.org/pdf/2602.07834v1"
tags: ["query:sr"]
score: 9.0
evidence: 将神经近似符号蒸馏为可解释公式
tldr: 本研究针对弦理论中难以计算的卡拉比-丘流形度规，提出了一种基于符号回归的蒸馏方法。通过将复杂的神经网络近似模型转化为简洁的解析公式，研究者仅用五个项就实现了与神经网络相当的精度（R²=0.9994），且参数量减少了3000倍。该公式不仅在不同模参数范围内保持稳定，还能准确复现体积积分和汤川耦合等物理观测值，为获取可解释的几何物理模型提供了新途径。
selection_source: fresh_fetch
motivation: 针对弦理论中卡拉比-丘流形度规难以获得解析解且数值模型缺乏可解释性的挑战。
method: 采用符号回归技术从预训练的神经网络教师模型中蒸馏出由幂和与对称多项式组成的解析表达式。
result: 获得的五项解析式在极低参数量下实现了极高的拟合精度，并成功验证了其在物理量计算中的有效性。
conclusion: 符号蒸馏能够将黑盒神经网络转化为紧凑且具有物理洞察力的解析模型，推动了数学物理问题的符号化求解。
---

## 摘要
卡拉比-丘流形对弦理论至关重要，但其度规的计算往往难以处理。本文展示了符号回归可以将神经网络近似蒸馏为简单且可解释的公式。我们的五项表达式在参数量减少 3000 倍的情况下，达到了与神经网络相当的精度（$R^2 = 0.9994$）。多种子验证确认了几何约束选择了关键特征（特别是幂和与对称多项式），同时允许结构上的多样性。在所研究的模空间范围（$ψ \in [0, 0.8]$）内，该函数形式保持不变且系数平滑变化；我们将这些趋势解释为在局部训练的教师模型精度范围内（当 $ψ \neq 0$ 时，$σ \approx 8-9\%$）的经验假设。该公式能够重现物理可观测物理量——体积积分和汤川耦合——从而验证了符号蒸馏可以为此前仅能由黑盒网络计算的物理量恢复出紧凑且可解释的模型。

## Abstract
Calabi--Yau manifolds are essential for string theory but require computing intractable metrics. Here we show that symbolic regression can distill neural approximations into simple, interpretable formulas. Our five-term expression matches neural accuracy ($R^2 = 0.9994$) with 3,000-fold fewer parameters. Multi-seed validation confirms that geometric constraints select essential features, specifically power sums and symmetric polynomials, while permitting structural diversity. The functional form can be maintained across the studied moduli range ($ψ\in [0, 0.8]$) with coefficients varying smoothly; we interpret these trends as empirical hypotheses within the accuracy regime of the locally-trained teachers ($σ\approx 8-9\%$ at $ψ\neq 0$). The formula reproduces physical observables -- volume integrals and Yukawa couplings -- validating that symbolic distillation recovers compact, interpretable models for quantities previously accessible only to black-box networks.

---

## 论文详细总结（自动生成）

### 论文总结：通过符号蒸馏获得的可解释解析卡拉比-丘度规

#### 1. 核心问题与整体含义（研究动机和背景）
卡拉比-丘（Calabi-Yau, CY）流形的 Ricci 平坦度规是弦理论中计算粒子物理观测值（如汤川耦合、模空间几何）的核心。然而，求解定义度规的 Monge-Ampère 方程在计算上极其困难。虽然近年来神经网络（NN）作为代理模型实现了毫秒级的评估速度，但其“黑盒”性质导致物理直觉缺失，且参数量巨大。
**核心问题：** 能否从复杂的神经代理模型中提取出紧凑、解析且具有物理可解释性的度规公式，并验证其在整个模空间（Moduli Space）中的有效性？

#### 2. 论文提出的方法论
研究者采用了一种**符号蒸馏（Symbolic Distillation）**框架，将高维数值模型转化为低维解析表达式：
*   **核心思想：** 使用高精度的 Donaldson 算法（H-矩阵，k=10）作为“教师模型”，通过符号回归（Symbolic Regression）寻找能够拟合度规行列式比值 $\log(\det g_{alg} / \det g_{FS})$ 的最简函数。
*   **特征工程：** 构造了两个规范不变的几何不变量作为输入特征：
    *   **幂和（Power Sum）$p_2$：** 捕捉径向各向异性。
    *   **第三初等对称多项式 $\sigma_3$：** 捕捉角结构。
*   **算法流程：** 
    1. 在不同模参数 $\psi$ 下独立训练 Donaldson 教师模型。
    2. 使用 **PySR** 库进行符号搜索，通过 Pareto 准则平衡损失函数与模型复杂度。
    3. 提取出通用的五项解析公式，并观察其系数随模参数 $\psi$ 的演化趋势。

#### 3. 实验设计
*   **数据集/场景：** 研究对象为 Dwork 家族的五次超曲面（Quintic Hypersurfaces），模参数范围 $\psi \in [0, 0.8]$。
*   **Benchmark（基准）：** 
    *   Donaldson H-matrix（教师模型，875个参数）。
    *   Ashmore (2021) 的神经网络代理模型（约 15,000 个参数）。
    *   三阶多项式拟合（10个参数）。
*   **对比指标：** 拟合优度 $R^2$、均方根误差（RMSE）、Ricci 平坦度指标 $\sigma$、以及物理观测值（体积积分、汤川耦合 $\kappa_{111}$）的准确性。

#### 4. 资源与算力
*   **硬件：** 使用了 **NVIDIA A100 GPU**（通过 Google Colab Pro 平台）。
*   **训练细节：** 符号回归运行了 200 次迭代，种群大小为 60。Donaldson 教师模型在每个 $\psi$ 点训练 15 个迭代（Adam 优化器）。
*   **评估速度：** 符号公式在处理 $10^6$ 个点时仅需 0.045 秒，比神经网络快 3,000 倍，比原始 H-矩阵快 10,000 倍。

#### 5. 实验数量与充分性
*   **实验组数：** 
    *   在 $\psi=0$（Fermat 点）进行了 **10 个随机种子**的独立运行，验证特征发现的稳健性。
    *   在 $\psi \in \{0.0, 0.2, 0.4, 0.6, 0.8\}$ 五个点进行了跨模分析。
    *   进行了 **特征消融实验**（证明了 $\sigma_3$ 对降低 50% 误差的关键作用）。
    *   进行了 **跨阶数（k=6, 8, 10）验证**，证明了公式结构在不同近似精度下的稳定性。
*   **充分性评价：** 实验设计较为全面，涵盖了统计显著性测试（Permutation Test）、残差分析和物理基准测试，能够客观证明符号公式的有效性。

#### 6. 主要结论与发现
*   **极度压缩：** 发现了一个仅含 **5 个项** 的解析公式，在参数量减少 3,000 倍的情况下，达到了与 NN 相当的精度（$R^2 = 0.9994$）。
*   **因子分解原理：** 证明了度规结构可以分解为“固定的函数骨架”和“随模参数变化的系数”。
*   **分层调制响应：** 随着 $\psi$ 增加，奇异项（如 $1/p_2^2$）发生符号反转，而对称项 $\sigma_3$ 单调增强（增强 15 倍），揭示了度规在模空间中的几何演化规律。
*   **物理保真度：** 该公式计算的体积积分误差约 2%，汤川耦合在 Fermat 点达到精确匹配，证明了其在唯象学研究中的实用价值。

#### 7. 优点
*   **高可解释性：** 相比黑盒 NN，公式中的每一项（如奇异项、块体项、角项）都有明确的几何含义。
*   **泛化能力：** 公式结构在 $\psi \in [0, 0.8]$ 范围内保持稳定，无需为每个模点重新搜索函数形式。
*   **计算效率极高：** 极大地降低了弦理论参数空间扫描的计算成本。

#### 8. 不足与局限
*   **教师模型限制：** 在 $\psi \neq 0$ 时，教师模型的 Ricci 误差较大（8-9%），这限制了符号公式在变形区域的绝对几何精度。
*   **覆盖范围：** 研究仅限于 Dwork 家族（单参数模空间），尚未验证其在多参数模空间或其他 CY 流形家族（如双三次流形）上的通用性。
*   **奇异点风险：** 实验未触及 $\psi \to 1$ 的锥形奇异点（Conifold limit），在该极限下线性系数假设可能会失效。
*   **观测值单一：** 目前主要验证了行列式相关的物理量，对于需要完整度规张量或曲率积分的更复杂观测值（如拉普拉斯特征值），尚需进一步扩展。

（完）
