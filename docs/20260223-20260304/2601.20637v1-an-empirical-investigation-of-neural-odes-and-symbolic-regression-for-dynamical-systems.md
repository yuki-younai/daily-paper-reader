---
title: An Empirical Investigation of Neural ODEs and Symbolic Regression for Dynamical Systems
title_zh: 神经常微分方程与符号回归在动力系统中的实证研究
authors: "Panayiotis Ioannou, Pietro Liò, Pietro Cicuta"
date: 2026-01-28
pdf: "https://arxiv.org/pdf/2601.20637v1"
tags: ["query:sr"]
score: 10.0
evidence: 神经常微分方程与符号回归在方程恢复方面的比较
tldr: 本研究探讨了利用神经常微分方程（NODEs）和符号回归（SR）建模复杂动力系统并发现其控制方程的方法。通过对阻尼振荡系统的实验，研究发现NODEs在动力学相似的情况下具有良好的外推能力，而SR能有效从噪声数据中恢复方程。最重要的是，即使在训练数据极少的情况下，利用NODE生成的数据也能辅助SR推断出物理定律，为科学发现提供了新路径。
selection_source: fresh_fetch
motivation: 准确建模复杂动力系统并发现其背后的控制微分方程是加速科学发现的关键任务。
method: 通过阻尼振荡系统的噪声合成数据，评估了神经常微分方程的外推性能以及符号回归恢复底层方程的能力。
result: "NODEs在相似动力学条件下外推效果显著，且SR能从仅基于10%原始数据训练的NODE所生成的数据中成功恢复大部分方程。"
conclusion: 结合NODEs丰富有限数据与符号回归推断物理定律的方法，为科学探索和规律发现提供了一种极具前景的新策略。
---

## 摘要
准确建模复杂系统的动力学并发现其控制微分方程是加速科学发现的关键任务。利用来自两个阻尼振荡系统的带噪声合成数据，我们探索了神经常微分方程（NODEs）的外推能力以及符号回归（SR）恢复底层方程的能力。我们的研究得出了三个关键见解。首先，我们证明了只要生成的轨迹与训练数据具有动力学相似性，NODEs 就能有效地外推到新的边界条件。其次，SR 成功地从带噪声的真实数据中恢复了方程，尽管其性能取决于输入变量的正确选择。最后，我们发现，当使用仅在 10% 的完整模拟数据上训练的 NODE 所生成的数据时，SR 恢复了三个控制方程中的两个，并对第三个方程给出了良好的近似。虽然最后这一发现指出了未来研究的方向，但我们的结果表明，利用 NODEs 丰富有限的数据并使符号回归能够推断物理定律，代表了一种极具前景的科学发现新方法。

## Abstract
Accurately modelling the dynamics of complex systems and discovering their governing differential equations are critical tasks for accelerating scientific discovery. Using noisy, synthetic data from two damped oscillatory systems, we explore the extrapolation capabilities of Neural Ordinary Differential Equations (NODEs) and the ability of Symbolic Regression (SR) to recover the underlying equations. Our study yields three key insights. First, we demonstrate that NODEs can extrapolate effectively to new boundary conditions, provided the resulting trajectories share dynamic similarity with the training data. Second, SR successfully recovers the equations from noisy ground-truth data, though its performance is contingent on the correct selection of input variables. Finally, we find that SR recovers two out of the three governing equations, along with a good approximation for the third, when using data generated by a NODE trained on just 10% of the full simulation. While this last finding highlights an area for future work, our results suggest that using NODEs to enrich limited data and enable symbolic regression to infer physical laws represents a promising new approach for scientific discovery.

---

## 论文详细总结（自动生成）

这篇论文题为《神经常微分方程与符号回归在动力系统中的实证研究》，由剑桥大学的研究人员完成。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在科学发现中，准确建模复杂动力系统并识别其背后的控制方程至关重要。然而，实验数据通常具有**高噪声**和**稀疏性**。
*   **核心问题**：
    1.  神经常微分方程（NODEs）在面对噪声数据时的插值与外推能力如何？
    2.  符号回归（SR）能否从有限且带噪声的数据中恢复精确的物理方程？
    3.  **NODE + SR 管道**：能否利用 NODE 作为数据增强工具，在仅有极少量（如 10%）原始数据的情况下，通过 NODE 生成高质量数据来辅助 SR 发现物理定律？
*   **整体含义**：论文探索了黑盒模型（NODE）与可解释模型（SR）的协同效应，旨在为数据匮乏领域的科学探索提供新路径。

### 2. 方法论
*   **核心思想**：构建一个两阶段管道。首先用 NODE 学习系统的动力学特征，然后利用训练好的 NODE 生成密集、去噪的数据，最后由 SR 从中提取显式数学表达式。
*   **关键技术细节**：
    *   **NODE 建模**：使用基于 JAX 的 Diffrax 库。模型采用多层感知机（MLP）架构（2 层隐藏层，每层 20 个神经元）来近似向量场 $f(z(t), t, \theta)$。
    *   **数据增强与去噪**：NODE 不仅用于填补时间序列中的空白（插值），还被发现能起到“去噪过滤器”的作用，平滑原始数据中的随机噪声。
    *   **符号回归（SR）**：使用 PySR 库，通过进化算法在数学算子空间中搜索最匹配的方程。
    *   **导数计算**：在 SR 阶段，使用 NumPy 的梯度函数从 NODE 生成的状态数据中计算时间导数，作为 SR 的目标值。

### 3. 实验设计
*   **实验场景**：
    1.  **倒立摆（Cart-pole）**：经典的物理力学系统，涉及角度和角速度的非线性动力学。
    2.  **生物模型（Bio-model）**：模拟大肠杆菌在营养环境变化下的适应性行为，包含三个状态变量（氨基酸比例、核糖体质量分数、基因调节函数）。
*   **Benchmark（基准）**：
    *   使用数值积分（SciPy 的 `odeint`）生成的地面真值（Ground Truth）数据。
    *   在真值中加入 -5% 到 5% 的均匀分布噪声。
*   **对比实验**：
    *   SR 直接作用于带噪声的真值数据 vs. SR 作用于 NODE 生成的数据。
    *   不同采样频率（每小时 5 到 100 个数据点）对 NODE 预测性能的影响。
    *   NODE 在训练区域内外的外推表现（基于相空间轨迹分析）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或数量。
*   **训练细节**：提到了训练迭代次数。例如，倒立摆模型训练了 100,000 次迭代；生物模型在 SR 数据生成阶段训练了 150,000 次迭代。使用了 Adabelief 优化器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对倒立摆，通过热图分析了大量不同初始条件下的均方误差（MSE）。
    *   针对生物模型，测试了 6 种不同的采样频率，每种频率进行 10 次独立运行，并在 19 种不同的环境转换（Shifts）下评估。
*   **充分性评价**：实验设计较为系统，特别是对“动力学相似性”与外推能力的关联分析非常深入。但在 SR 部分，主要集中在单一转换过程的数据，作者也承认若能涵盖更多样化的多条件数据，结果会更具鲁棒性。

### 6. 主要结论与发现
*   **NODE 的外推逻辑**：NODE 的外推能力并不取决于初始条件的物理接近度，而取决于新轨迹与训练轨迹在**相空间（Phase Space）中的动力学相似性**。
*   **采样频率的鲁棒性**：即使采样非常稀疏，NODE 也能实现高质量的长期预测，这证明了其在处理低频实验数据时的潜力。
*   **NODE 的去噪效应**：SR 在 NODE 生成的数据上表现优于直接在带噪声的原始数据上表现。NODE 实际上过滤了噪声，捕获了底层的物理信号。
*   **SR 的局限性**：SR 的成功高度依赖于输入变量的选择。如果缺少关键中间变量（如生长速率 $\lambda$），SR 很难恢复复杂的有理分式方程。

### 7. 优点
*   **协同效应**：成功证明了 NODE 可以作为 SR 的前置处理器，解决了 SR 对大数据量和低噪声的苛刻要求。
*   **深度见解**：提出了“采样多样化的动力学特征比单纯增加初始条件数量更重要”的观点，对实验设计有指导意义。
*   **实用性**：在仅使用 10% 数据的情况下仍能恢复大部分物理方程，极具应用价值。

### 8. 不足与局限
*   **变量依赖**：SR 阶段仍需人工干预来选择输入变量，自动化程度有待提高。
*   **复杂方程恢复不全**：对于生物模型中最复杂的方程（方程 2），SR 只能找到良好近似，未能完全恢复原始结构（丢失了低信号项）。
*   **实验覆盖面**：SR 分析主要基于合成数据，尚未在真实的、具有未知物理规律的实验数据集上进行验证。
*   **架构单一**：主要研究了标准 NODE，未深入探讨更先进的变体（如 Neural CDEs）在噪声处理上的差异。

（完）
