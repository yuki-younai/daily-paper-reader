---
title: "Learning Gradient Flow: Using Equation Discovery to Accelerate Engineering Optimization"
title_zh: 学习梯度流：利用方程发现加速工程优化
authors: "Grant Norman, Conor Rowan, Kurt Maute, Alireza Doostan"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13513v2"
tags: ["query:sr"]
score: 9.0
evidence: 动力系统的驱动方程发现
tldr: 本研究针对工程优化中目标函数及其梯度计算成本高昂的问题，提出了一种基于数据驱动方程发现的方法。通过学习梯度下降、牛顿法和ADAM等算法的连续时间动力学，引入了学习梯度流（LGF）优化器。该优化器利用优化变量的轨迹数据构建全维或降维空间的代理模型，从而预测优化路径。在结构拓扑优化和逆问题等多个工程力学及科学机器学习任务中的实验表明，该方法能显著加速收敛，大幅降低计算开销。
selection_source: fresh_fetch
motivation: 旨在解决传统工程优化中频繁评估复杂目标函数及其梯度所导致的巨大计算成本问题。
method: 提出LGF优化器，通过方程发现技术从优化轨迹中学习梯度流动力学，并将其作为代理模型进行求解。
result: 在逆问题、拓扑优化和前向求解等多个标准工程问题上，该方法成功捕捉了优化轨迹特征并显著提升了收敛效率。
conclusion: 学习梯度流是一种有效的代理建模策略，能够通过模拟优化动力学来加速复杂工程系统的优化过程。
---

## 摘要
在这项工作中，我们研究了利用动力系统的数据驱动方程发现技术，来建模和预测无约束优化问题的连续时间动力学。为了避免对目标函数及其梯度进行昂贵的评估，我们利用优化变量的轨迹数据来学习与梯度下降、牛顿法和 ADAM 优化相关的连续时间动力学。随后，将发现的梯度流作为原始优化问题的代理模型进行求解。为此，我们引入了学习梯度流（LGF）优化器，该优化器能够在优化过程中以用户定义的时间间隔，在全维或降维空间中构建变多项式阶数的代理模型。我们在工程力学和科学机器学习的几个标准问题上证明了该方法的有效性，包括两个反问题、结构拓扑优化以及两个具有不同离散化的正向求解问题。我们的结果表明，学习到的梯度流通过捕捉优化轨迹的关键特征，同时避免对目标及其梯度进行昂贵的评估，可以显著加速收敛。

## Abstract
In this work, we investigate the use of data-driven equation discovery for dynamical systems to model and forecast continuous-time dynamics of unconstrained optimization problems. To avoid expensive evaluations of the objective function and its gradient, we leverage trajectory data on the optimization variables to learn the continuous-time dynamics associated with gradient descent, Newton's method, and ADAM optimization. The discovered gradient flows are then solved as a surrogate for the original optimization problem. To this end, we introduce the Learned Gradient Flow (LGF) optimizer, which is equipped to build surrogate models of variable polynomial order in full- or reduced-dimensional spaces at user-defined intervals in the optimization process. We demonstrate the efficacy of this approach on several standard problems from engineering mechanics and scientific machine learning, including two inverse problems, structural topology optimization, and two forward solves with different discretizations. Our results suggest that the learned gradient flows can significantly expedite convergence by capturing critical features of the optimization trajectory while avoiding expensive evaluations of the objective and its gradient.

---

## 论文详细总结（自动生成）

这篇论文题为《学习梯度流：利用方程发现加速工程优化》，由科罗拉多大学博尔德分校的研究团队撰写。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在工程优化（如拓扑优化、反问题求解）中，每次迭代通常需要求解复杂的偏微分方程（PDE）以获取目标函数及其梯度，计算成本极其高昂。
*   **研究动机**：优化算法（如梯度下降、牛顿法）在连续时间极限下可被视为动力系统（即“梯度流”）。如果能从已有的优化轨迹中“发现”并建模这一动力学方程，就可以利用廉价的代理模型预测未来的优化路径，从而跳过大量昂贵的真实梯度计算。

### 2. 方法论
*   **核心思想**：引入**学习梯度流（Learned Gradient Flow, LGF）**优化器。它将优化过程视为一个常微分方程（ODE）系统 $\frac{da}{dt} = f(a)$，并利用数据驱动的方程发现技术（如 SINDy）在线构建 $f(a)$ 的代理模型。
*   **关键技术细节**：
    *   **数据采集**：先运行 $K$ 步标准优化算法，记录优化变量的轨迹数据。
    *   **方程发现（SINDy）**：构建多项式候选基函数库，通过稀疏回归（如 STLSQ 算法）建立轨迹变化率与当前状态之间的映射关系。
    *   **时间积分预测**：求解发现的廉价 ODE，将优化变量从当前状态预测到 $M$ 步之后的状态（$M > K$）。
    *   **定期重训练（Scheduled Retraining）**：交替进行“真实梯度步”和“代理模型预测步”，以修正代理模型的漂移。
    *   **降维处理**：针对高维问题（如数万个变量），先利用 SVD（奇异值分解）将数据投影到低维潜空间，在潜空间内学习动力学，再映射回原空间。
    *   **多算法适配**：论文推导了如何将此框架应用于梯度下降（GD）、牛顿法（Newton）和 ADAM 优化器。

### 3. 实验设计
*   **实验场景**：
    1.  **1D 动态反问题**：恢复加热棒的材料导热系数。
    2.  **3D 结构拓扑优化**：最小化悬臂梁的柔度（变量数 > 13,000）。
    3.  **非线性热传导前向求解**：测试牛顿法的连续动力学学习。
    4.  **全波反演（FWI）**：地球物理中的非凸优化问题，测试 ADAM 适配性。
    5.  **Deep Ritz 方法**：利用神经网络求解弹性力学问题，测试随机/噪声梯度下的表现。
*   **Benchmark（基准）**：各场景对应的标准优化算法（Vanilla GD, Newton, ADAM）。
*   **对比指标**：收敛速度、目标函数值、参数恢复准确度、计算加速比（节省的梯度评估次数）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现工具**：使用了 PyTorch 框架和 `torchdiffeq` 库进行 ODE 积分。实验主要通过“迭代次数（Epochs）”和“梯度评估节省比例”来衡量效率提升。

### 5. 实验数量与充分性
*   **实验规模**：共设计了 5 组具有代表性的工程和机器学习实验。
*   **充分性评价**：实验覆盖了线性与非线性物理、谱方法与有限元离散化、确定性与随机梯度等多种维度。
*   **客观性**：论文不仅展示了成功案例，还通过消融实验展示了如果“加速比”设置过高（即 $M$ 远大于 $K$）会导致模型失稳或误差增大，体现了客观性。

### 6. 主要结论与发现
*   **显著加速**：LGF 能够节省 33% 到 250% 的昂贵梯度评估次数，同时保持与标准算法相当的精度。
*   **低维流形**：即使是上万维的拓扑优化问题，其优化动力学往往存在于极低维（如 2 维）的潜空间中，这证明了降维 LGF 的有效性。
*   **通用性**：该方法能有效捕捉包括动量（ADAM）和二阶信息（Newton）在内的复杂优化轨迹特征。

### 7. 优点与亮点
*   **创新性**：首次将 SINDy 方程发现技术直接嵌入到在线优化循环中作为加速器。
*   **非侵入性**：不需要修改底层的 PDE 求解器，只需观测优化变量的轨迹即可。
*   **高效性**：通过潜空间建模解决了 SINDy 在处理超高维数据时的维度灾难问题。

### 8. 不足与局限
*   **超参数敏感性**：历史窗口大小 $K$ 和预测步长 $M$ 的选择依赖经验，缺乏自动调节机制。
*   **外推风险**：代理模型仅在局部轨迹有效，若优化景观（Landscape）发生剧烈变化（如遇到约束边界或非光滑区域），模型可能失效。
*   **内存开销**：需要存储一定长度的轨迹历史，对于超大规模模型可能会增加内存压力。
*   **约束处理**：目前主要针对无约束优化，对于带有复杂不等式约束的问题，轨迹可能出现不连续，学习难度较大。

（完）
