---
title: "Chain-of-Adaptation: Surgical Vision-Language Adaptation with Reinforcement Learning"
title_zh: 适应链：基于强化学习的手术视觉-语言适配
authors: "Jiajie Li, Chenhui Xu, Meihuan Liu, Jinjun Xiong"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20116v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 利用强化学习进行视觉语言适配与领域对齐
tldr: 针对传统微调在特定领域（如手术视觉）中容易破坏预训练多模态先验并降低泛化能力的问题，本文提出了Chain-of-Adaptation (CoA) 框架。该框架通过强化学习和结构化推理格式，在整合领域知识的同时保留了模型的通用推理与感知能力。在手术基准测试中的实验证明，CoA在准确性、泛化性和稳定性方面均优于监督微调，为视觉语言模型的领域专业化提供了可靠路径。
selection_source: fresh_fetch
motivation: 传统的监督微调会损害预训练模型的多模态先验知识，导致在特定领域应用时泛化性能下降。
method: 提出CoA框架，利用强化学习和结构化推理格式来实现领域对齐，同时保留模型的核心视觉语言能力。
result: 在手术视觉基准测试的分布内和分布外设置下，CoA均表现出比监督微调更高的准确率和更强的泛化能力。
conclusion: CoA有效地平衡了领域专业化与通用能力的保留，是视觉语言模型进行领域自适应的一种稳健且高效的方法。
---

## 摘要
在特定领域数据集上的传统微调可能会无意中改变模型的预训练多模态先验，导致泛化能力下降。为了解决这一问题，我们提出了适应链（Chain-of-Adaptation, CoA），这是一种旨在整合领域知识并保持模型固有推理和感知能力的适配框架。CoA 引入了一种结构化推理格式，通过强化学习在不牺牲通用多模态能力的情况下增强领域对齐。在标准手术基准上的实验表明，在分布内和分布外设置下，CoA 比监督微调具有更高的准确性、更强的泛化能力和更稳定的表现。此外，消融研究证实 CoA 有效地保留了模型的核心视觉-语言能力，为视觉语言模型（VLM）的领域专业化提供了一条可靠的途径。

## Abstract
Conventional fine-tuning on domain-specific datasets can inadvertently alter a model's pretrained multimodal priors, leading to reduced generalization. To address this, we propose Chain-of-Adaptation (CoA), an adaptation framework designed to integrate domain knowledge while maintaining the model's inherent reasoning and perceptual capabilities. CoA introduces a structured reasoning format that enhances domain alignment without sacrificing general multimodal competence by reinforcement learning. Experiments on standard surgical benchmarks, under both in-distribution and out-of-distribution settings, demonstrate that CoA achieves higher accuracy, stronger generalization, and more stable behavior than supervised fine-tuning. Furthermore, ablation studies confirm that CoA effectively preserves the model's core visual-language abilities, providing a reliable pathway for domain specialization in VLMs.

---

## 论文详细总结（自动生成）

### 论文总结：Chain-of-Adaptation (CoA)

#### 1. 核心问题与研究动机
*   **核心问题**：通用的视觉-语言模型（VLM）在应用于手术等专业领域时，面临严重的分布偏移（图像模态、术语、任务需求）。
*   **研究动机**：传统的监督微调（SFT）在手术领域存在三大困境：
    1.  **数据稀缺与标注简单**：手术数据集多为低级分类标签，缺乏丰富的描述性或推理逻辑。
    2.  **模型坍缩与遗忘**：SFT 容易导致模型输出变短、丧失语言多样性，并产生“灾难性遗忘”，损害模型原有的通用推理能力。
    3.  **幻觉与过拟合**：模型容易过度拟合训练集中的常见类别，在面对罕见器械或新场景（分布外数据）时表现极差。
*   **整体含义**：本文旨在探索如何在不破坏 VLM 预训练多模态先验的前提下，通过强化学习实现领域知识的精准对齐。

#### 2. 方法论
CoA 框架由**结构化推理格式**和**两阶段训练流程**组成：
*   **结构化推理格式（CoA Format）**：
    1.  `<general description>`：仅描述颜色、几何、纹理等低级视觉属性，不涉及专业术语，用于保留通用感知能力。
    2.  `<evidence>`：将观察结果与领域知识结合，列出推理线索。
    3.  `<thought>`：进行逻辑合成与深度推理。
    4.  `<answer>`：给出最终的专业答案。
*   **两阶段训练流程**：
    1.  **冷启动（Cold Start）**：利用强模型（如 Gemini-Flash-2.5）对 10k 张互联网手术图像生成伪标签，让模型初步熟悉 CoA 格式和手术词汇。
    2.  **强化学习（RLVR）**：采用 **GRPO（Group Relative Policy Optimization）** 算法。模型针对同一输入生成多组响应，根据答案的准确性（可验证奖励）和格式合规性计算奖励，通过相对优势更新策略，避免了对静态标注的过度依赖。

#### 3. 实验设计
*   **数据集/场景**：
    *   **分布内（In-distribution）**：EndoVis2018（机器人手术）、CholecT50（腹腔镜手术）。
    *   **分布外（Out-of-distribution）**：GraSP（前列腺切除术视频帧）。
    *   **通用能力评估**：MMBench、MMStar。
*   **Benchmark 与对比方法**：
    *   基座模型：Qwen3-VL-8B-Instruct。
    *   对比方法：原始基座模型、标准 SFT、仅冷启动、冷启动+SFT、不带思维链的 RLVR、带传统 CoT 的 RLVR。

#### 4. 资源与算力
*   **算力设备**：使用了 **8 × NVIDIA H100 GPU**。
*   **训练细节**：采用混合精度训练，推理使用 vLLM 引擎。冷启动阶段训练 1 个 epoch，RLVR 阶段训练 1 个 epoch。
*   **超参数**：RLVR 阶段 batch size 为 112，KL 散度系数 $\beta=0.001$，每条提示生成 8 个响应。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个主流手术识别基准、1 个跨领域泛化测试以及 2 个通用 VLM 评测集。
*   **消融实验**：详细对比了 SFT 与 RLVR 的差异，以及 CoA 格式与传统 CoT 格式的优劣。
*   **充分性评价**：实验设计较为充分且客观。通过雷达图展示了模型在长尾类别（罕见器械）上的表现，证明了方法在处理类别不平衡问题上的公平性。

#### 6. 主要结论与发现
*   **性能提升**：CoA 在 EndoVis2018 和 CholecT50 上的 F1 分数分别提升了 **27%** 和 **10%**。
*   **缓解坍缩**：相比 SFT 导致输出长度骤减 64%，CoA 保持了稳定的输出长度和语言多样性。
*   **泛化性强**：在未见过的 GraSP 数据集上，CoA 的表现显著优于 SFT，证明其学习的是推理逻辑而非简单的模式匹配。
*   **保留通用性**：在通用 VLM 榜单上，CoA 的得分不降反升，说明结构化推理起到了正则化作用。

#### 7. 优点
*   **创新的推理结构**：通过将通用描述与专业推理分离，有效地平衡了“领域专业化”与“通用能力保留”。
*   **数据效率高**：利用 RLVR 机制，在缺乏高质量专家标注的情况下，通过模型自演化提升了性能。
*   **鲁棒性**：在处理手术场景中常见的类别不平衡和分布偏移问题上表现优异。

#### 8. 不足与局限
*   **依赖强模型初始化**：冷启动阶段高度依赖 Gemini 等闭源模型生成的伪标签质量。
*   **任务覆盖面**：目前主要集中在手术器械/步骤识别等分类任务，对于更复杂的开放式手术咨询或实时手术导航的验证尚不充分。
*   **计算成本**：RL 训练（尤其是 GRPO 采样）相比 SFT 需要更多的计算资源和更长的训练时间。

（完）
