---
title: "Adaptive Layerwise Perturbation: Unifying Off-Policy Corrections for LLM RL"
title_zh: 自适应逐层扰动：统一大语言模型强化学习中的离策修正
authors: "Chenlu Ye, Xuanchang Zhang, Yifan Hao, Zhou Yu, Ziji Zhang, Abhinav Gullapalli, Hao Chen, Jing Huang, Tong Zhang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19470v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 使用层级扰动统一LLM强化学习的离策修正
tldr: 针对大语言模型强化学习中由于策略陈旧和训练推理不匹配导致的训练不稳定问题，本文提出了自适应逐层扰动（ALP）方法。该方法通过在更新期间向每一层的输入隐藏状态注入可学习的微小扰动，平滑策略分布并缩小更新策略与推理策略之间的差距，从而有效抑制重要性采样比率的重尾现象。实验证明，ALP在数学和工具集成推理任务中显著提升了性能和训练稳定性。
selection_source: fresh_fetch
motivation: 解决LLM强化学习中因策略分布过尖锐导致的重要性采样比率重尾及训练不稳定性问题。
method: 在模型更新过程中，向各层隐藏状态注入可学习的微小扰动，以平滑策略分布并缩小分布差距。
result: 在数学和多轮工具推理任务中，该方法显著提升了模型性能，并有效抑制了KL散度激增和重要性比率爆炸。
conclusion: 逐层表示级的扰动能有效统一离策校正，增强LLM强化学习的稳定性和探索能力。
---

## 摘要
离策（Off-policy）问题，如策略陈旧和训练-推理不匹配，已成为大语言模型强化学习（LLM RL）训练稳定性和进一步探索的主要瓶颈。为了提高推理效率，推理策略与更新策略之间的分布差距不断扩大，导致重要性采样比率呈现重尾分布。当策略在局部过于尖锐时，会出现重尾比率，这会进一步放大尖锐梯度，并可能使更新超出置信区域。为了解决这一问题，我们提出了自适应逐层扰动（ALP），通过在更新过程中向每一层的输入隐藏状态注入微小的可学习扰动，并将其作为目标函数中相对于未改变推理策略的重要性比率的分子。直观地说，通过向中间表示添加受控噪声，ALP 防止了更新后的策略与推理策略发生过于剧烈的偏离，并扩大了策略族，使其能够覆盖带有不匹配噪声的推理策略族。因此，平滑后的分布可以自然地缩小更新策略与推理策略之间的差距，并减少重要性比率的尾部，从而维持训练稳定性。这一点在实证中得到了进一步验证。在单轮数学和多轮工具集成推理任务上的实验表明，ALP 不仅提高了最终性能，还避免了迭代训练过程中重要性比率尾部的爆炸和 KL 散度激增，同时增强了探索能力。消融实验表明，跨所有层的表示级扰动最为有效，显著优于部分层和仅 Logits 的变体。

## Abstract
Off-policy problems such as policy staleness and training-inference mismatch, has become a major bottleneck for training stability and further exploration for LLM RL. To enhance inference efficiency, the distribution gap between the inference and updated policy grows, leading to heavy-tailed importance ratios. Heavy-tailed ratios arise when the policy is locally sharp, which further inflates sharp gradients and can push updates outside the trust region. To address this, we propose Adaptive Layerwise Perturbation(ALP) by injecting small learnable perturbations into input hidden states of each layer during updates, which is used as the numerator of the importance ratio against the unchanged inference policy in the objective. Intuitively, by adding controlled noise to intermediate representations, ALP prevents the updated policy from deviating too sharply from the inference policy, and enlarges the policy family to cover the inference policy family with mismatch noises. Hence, the flattened distribution can naturally tighten the updated and inference policy gap and reduce the tail of importance ratios, thus maintaining training stability. This is further validated empirically. Experiments on single-turn math and multi-turn tool-integrated reasoning tasks show that ALP not only improves final performance, but also avoid blow up of importance ratio tail and KL spikes during iterative training, along with boosted exploration. Ablations show that representation-level perturbations across all layers are most effective, substantially outperforming partial-layer and logits-only variants.

---

## 论文详细总结（自动生成）

### 论文总结：自适应逐层扰动（ALP）统一 LLM RL 的离策修正

#### 1. 核心问题与整体含义（研究动机和背景）
在大语言模型（LLM）的强化学习（RL）过程中，为了提高训练效率，通常采用**离策（Off-policy）**训练。然而，这引入了两个主要挑战：
*   **策略陈旧（Policy Staleness）：** 同一批轨迹被用于多次策略更新，导致当前策略与生成数据的策略脱节。
*   **训练-推理不匹配（Training-Inference Mismatch）：** 推理引擎（如 vLLM）由于量化、批处理和内核差异，导致推理时的分布与训练时的分布存在微小偏差。
这些因素会导致**重要性采样比率（Importance Ratio）出现重尾分布**，引发 KL 散度激增、梯度爆炸和训练崩溃。论文的核心动机是通过引入结构化噪声来平滑策略分布，从而统一解决上述离策偏差问题。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Adaptive Layerwise Perturbation (ALP)**，其核心思想是“以噪声对抗噪声”：
*   **核心操作：** 在策略更新阶段，向模型每一层（Layer）的输入隐藏状态（Hidden States）注入微小的可学习高斯扰动 $\delta \sim N(0, \sigma^2 I)$。
*   **重要性比率构造：** 在计算目标函数时，仅在分子（更新后的训练策略 $\pi_{train}$）中加入扰动，而分母（推理策略 $\pi_{infer}$）保持不变。
*   **直观理解：** 扰动扩大了训练策略的覆盖范围（Policy Family），使其能够包容推理系统带来的偏差。这种做法平滑了优化景观（Optimization Landscape），防止策略变得过于“尖锐（Sharp）”，从而自然地抑制了重要性比率的极端值。
*   **理论支撑：** 论文证明了当扰动方差 $\sigma^2$ 匹配或超过推理系统的偏差范数时，训练与推理分布之间的 KL 散度是有界的，这保证了更新能维持在置信区域（Trust Region）内。

#### 3. 实验设计
实验涵盖了单轮和多轮推理场景：
*   **场景与数据集：**
    *   **单轮数学推理：** 使用 Guru RL-92k 和 OpenR1 (NuminaMath) 的子集进行训练。
    *   **多轮工具集成推理 (TIR)：** 使用 Python 解释器解决数学问题，数据集整合了 SimpleRL、Deepscaler 和 rStar2-Agent。
*   **Benchmark：** 在 Math500、Minerva Math、Olympiad Bench、AIME2024 和 AIME2025 五个极具挑战性的数学榜单上进行评估。
*   **对比方法：** 
    *   **GRPO：** 标准的组相对策略优化。
    *   **Bypass：** 直接计算训练与推理策略比率的基线。
    *   **MIS (Masked Importance Sampling)：** 现有的通过掩码或截断处理不匹配的方法（分为 Token 级和 Seq 级）。
    *   **ALP (本文方法)：** 分为 Token-ALP 和 Seq-ALP。

#### 4. 资源与算力
*   **硬件设备：** 论文明确提到使用了 **8 台 H100 GPU 和 4 台 A100 GPU** 进行训练。
*   **训练细节：** 采用 FSDP（全分片数据并行）策略，学习率为 $1 \times 10^{-6}$，扰动参数的学习率为 $5 \times 10^{-4}$。每次迭代采样 512 个 Prompt，每个 Prompt 生成 8 个响应。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在单轮和多轮两个大类场景下进行了详尽测试，每类场景均包含 5 个 Benchmark。
*   **消融实验：** 进行了多组深入的消融研究，包括：
    *   扰动位置（全层 vs. 部分层 vs. 仅 Logits）。
    *   扰动规模（$\sigma$ 的初始值与学习率敏感性）。
    *   Token 级与 Seq 级的对比。
*   **充分性评价：** 实验设计非常充分且客观。不仅对比了最终准确率，还详细记录了训练动态（KL 散度、熵、梯度范数、重要性比率分布），从多个维度验证了 ALP 的稳定性。

#### 6. 主要结论与发现
*   **性能领先：** ALP 在单轮和多轮任务中均优于所有基线方法。Token-ALP 在单轮任务表现最佳，而 Seq-ALP 在复杂的多轮 TIR 任务中表现最强。
*   **稳定性显著：** ALP 有效防止了训练后期的 KL 激增和熵崩溃，重要性比率的尾部被显著压缩。
*   **增强探索：** 通过 Pass@k 分析发现，ALP 提高了生成结果的多样性和正确路径的覆盖率，证明了扰动有助于模型进行更有效的探索。
*   **层级效应：** 扰动**所有层**的效果显著优于仅扰动部分层或仅扰动 Logits。在部分扰动时，底层（靠近输入）的扰动比顶层更有效。

#### 7. 优点（亮点）
*   **统一性：** 用一个简单的扰动机制同时解决了策略陈旧和系统不匹配两个复杂问题，无需像 MIS 那样精细调节多个截断阈值。
*   **实现简单：** 仅需在模型前向传播中加入微小噪声，对现有 RL 框架（如 verl）改动极小。
*   **理论与实证结合：** 既有严谨的数学证明（关于 KL 有界性和损失平滑性），又有直观的训练动态可视化。

#### 8. 不足与局限
*   **任务覆盖面：** 实验主要集中在数学和逻辑推理任务，尚未在通用对话、创意写作或多模态 RL 任务中验证其普适性。
*   **计算开销：** 虽然扰动本身计算量极小，但在训练期间需要维护额外的扰动参数，且对于超大规模模型，全层注入噪声的内存开销虽小但仍需考虑。
*   **异步场景验证：** 论文提到了完全异步 RL 系统是未来的方向，但本文实验主要还是在同步/半同步框架下完成的。

（完）
