---
title: Teaching an Agent to Sketch One Part at a Time
title_zh: 教授智能体逐部分绘制草图
authors: "Xiaodan Du, Ruize Xu, David Yunis, Yael Vinker, Greg Shakhnarovich"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19500v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 多模态语言模型智能体的多轮过程奖励强化学习
tldr: 该研究提出了一种按部件逐步生成矢量草图的新方法。通过开发自动标注流水线构建了 ControlSketch-Part 数据集，并训练了一个基于多模态语言模型的智能体。该智能体采用监督微调与多轮过程奖励强化学习相结合的训练策略，实现了具有高度可解释性、可控性及局部编辑能力的文本到矢量草图生成，为精细化图形创作提供了新工具。
selection_source: fresh_fetch
motivation: 旨在解决现有矢量草图生成方法缺乏语义部件层面的可控性、可解释性以及局部编辑能力的问题。
method: 构建了包含部件级标注的新数据集，并利用多模态语言模型结合多轮过程奖励强化学习来训练绘图智能体。
result: 该方法成功实现了按部件顺序生成草图，并允许用户通过视觉反馈对特定局部进行精确控制和修改。
conclusion: 结合结构化部件数据与过程化反馈机制，显著提升了文本驱动矢量草图生成的交互性和实用价值。
---

## 摘要
我们开发了一种逐部分生成矢量草图的方法。为此，我们在监督微调的基础上，使用一种新颖的多轮过程奖励强化学习来训练基于多模态语言模型的智能体。我们的方法得益于一个名为 ControlSketch-Part 的新数据集，该数据集包含丰富的草图部分级标注，是通过一种新颖且通用的自动标注流水线获得的，该流水线将矢量草图分割成语义部分，并通过结构化的多阶段标注过程将路径分配给各个部分。我们的结果表明，结合结构化的部分级数据并在过程中为智能体提供视觉反馈，可以实现可解释、可控且可局部编辑的文本到矢量草图生成。

## Abstract
We develop a method for producing vector sketches one part at a time. To do this, we train a multi-modal language model-based agent using a novel multi-turn process-reward reinforcement learning following supervised fine-tuning. Our approach is enabled by a new dataset we call ControlSketch-Part, containing rich part-level annotations for sketches, obtained using a novel, generic automatic annotation pipeline that segments vector sketches into semantic parts and assigns paths to parts with a structured multi-stage labeling process. Our results indicate that incorporating structured part-level data and providing agent with the visual feedback through the process enables interpretable, controllable, and locally editable text-to-vector sketch generation.

---

## 论文详细总结（自动生成）

### 论文总结：教授智能体逐部分绘制草图 (Teaching an Agent to Sketch One Part at a Time)

#### 1. 核心问题与整体含义
*   **研究动机**：现有的文本到矢量草图生成方法大多采用“一次性生成”模式，忽略了人类绘画时逐部分（part-by-part）构思的自然过程。这种一阶段生成方式缺乏可解释性，且难以进行局部编辑（如修改某个特定部件而不影响整体）。
*   **核心目标**：开发一个能够根据文本指令，在观察当前画布状态的基础上，逐个语义部件（如：先画头、再画身体、最后画腿）生成矢量草图的视觉语言模型（VLM）智能体。

#### 2. 方法论
*   **自动标注流水线**：提出了一种通用的多阶段标注方法，将现有的矢量草图数据集（ControlSketch）转化为包含语义部件信息的新数据集 **ControlSketch-Part**。
    *   **流程**：初始部件分解 -> 部件审查与精炼 -> 路径到部件的初始分配 -> 诊断性视觉反馈下的路径分配审查 -> 路径分配精炼 -> 生成整体描述。
*   **两阶段训练框架**：
    *   **阶段 1：监督微调 (SFT)**：在随机排列的部件序列上训练模型，使其学习输出格式（Bézier 曲线坐标）并具备根据当前画布和部件描述生成对应路径的能力。
    *   **阶段 2：多轮过程奖励 GRPO 强化学习**：
        *   **核心思想**：使用组相对策略优化（GRPO）算法，并引入**过程奖励（Process-Reward）**。
        *   **奖励函数**：结合了 **DreamSim 奖励**（衡量当前生成步骤与真值部件的视觉感知相似度）和 **路径计数奖励**（惩罚路径过多或过少，保持简洁性）。
        *   **优势**：通过中间状态的密集信用分配（Dense Credit Assignment），解决了 SFT 模型在多轮推理中误差累积导致的质量下降问题。

#### 3. 实验设计
*   **数据集**：基于 ControlSketch 构建的 ControlSketch-Part，涵盖 15 个物体类别，包含 35,000 个图像-草图对。
*   **对比方法**：
    *   **SketchAgent**：基于 Claude Sonnet 的零样本顺序绘图方法。
    *   **Gemini 3.1 Pro**：直接进行全图生成的通用大模型。
    *   **SDXL + SwiftSketch**：先生成位图再转矢量的扩散模型组合。
*   **评估指标**：Long-CLIP 余弦相似度（衡量文本一致性）、双盲人类偏好研究（针对步骤质量和最终质量）。

#### 4. 资源与算力
*   **模型骨干**：Qwen2-VL-30B-A3B。
*   **微调技术**：使用 LoRA（秩为 64）。
*   **训练细节**：
    *   **SFT 阶段**：学习率 2e-4，Batch Size 128，训练 5400 步。
    *   **RL 阶段**：学习率 3e-6，Batch Size 8，组大小（Group Size）为 8，训练 1000 步。
*   **算力平台**：使用了 Thinking Machines Lab 的 Tinker 框架进行训练（具体 GPU 型号和数量未在文中直接标明，但 30B 参数模型通常需要多块 A100/H100 级别的显卡）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 15 类物体，进行了超过 3000 组人类偏好对比实验。
*   **消融实验**：针对单轮 RL、多轮结果奖励（Outcome-reward）与多轮过程奖励（Process-reward）进行了对比，验证了过程奖励在多轮生成中的必要性。
*   **客观性**：采用了双盲测试和多种自动化指标，实验设计较为严谨，对比了当前最先进的闭源模型和开源流水线。

#### 6. 主要结论与发现
*   **部件级数据的价值**：引入结构化的部件标注是提升矢量草图生成可控性的关键。
*   **过程奖励的优越性**：在多轮生成任务中，对中间状态进行奖励（过程奖励）比仅对最终结果进行奖励（结果奖励）能显著提升视觉质量和鲁棒性。
*   **交互式编辑**：该智能体天然支持局部编辑，用户可以通过删除或替换特定部件的描述来精确修改草图。

#### 7. 优点
*   **高度可控与可编辑**：实现了真正的逐部件生成，支持任意路径的删除和替换。
*   **创新的标注流水线**：提出了一种利用 VLM 自我审查（Critique）来生成高质量矢量标注的通用方法。
*   **视觉反馈机制**：在 RL 阶段引入 DreamSim 视觉奖励，使模型生成的线条更符合人类审美和几何结构。

#### 8. 不足与局限
*   **路径计数偏差**：由于奖励函数鼓励匹配真值路径数，模型在某些情况下可能为了凑数而提前停止或生成多余线条。
*   **泛化范围受限**：目前主要局限于 ControlSketch 数据集中的物体类别，对于罕见概念或复杂拓扑结构的生成仍存在失败案例。
*   **缺乏推理自省**：智能体在推理时还不具备自我修正能力，前期步骤的错误可能会累积到后续步骤。

（完）
