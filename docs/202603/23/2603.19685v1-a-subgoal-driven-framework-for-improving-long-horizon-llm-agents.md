---
title: A Subgoal-driven Framework for Improving Long-Horizon LLM Agents
title_zh: 一种用于提升长程 LLM 智能体的子目标驱动框架
authors: "Taiyi Wang, Sian Gooding, Florian Hartmann, Oriana Riva, Edward Grefenstette"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19685v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 自主LLM智能体的强化学习微调
tldr: 针对LLM智能体在长程任务中规划能力不足和强化学习奖励稀疏的问题，本文提出了一种子目标驱动的框架。该框架包含在线推理时的子目标分解机制，以及名为MiRA的强化学习训练方法，通过引入密集的里程碑奖励信号，显著提升了智能体在复杂环境下的执行成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 现有LLM智能体在长程任务中容易丢失目标，且强化学习过程中面临奖励稀疏和延迟的问题。
method: 提出在线子目标分解规划机制，并结合基于里程碑密集奖励的强化学习框架MiRA。
result: "MiRA将Gemma3-12B在WebArena-Lite上的成功率从6.4%提升至43.0%，超越了GPT-4o等闭源模型。"
conclusion: 显式的推理规划与里程碑式奖励相结合，是提升自主智能体处理长程复杂任务能力的有效途径。
---

## 摘要
基于大语言模型（LLM）的智能体已成为数字环境（包括移动界面、操作系统和网络浏览器）中强大的自主控制器。例如，网络导航需要处理动态内容和长动作序列，这使其极具挑战性。现有的基于 LLM 的智能体在长程规划方面面临两个主要困难：在在线执行过程中，随着新信息的到来，它们往往会迷失方向，缺乏通往最终目标的清晰且自适应的路径；这一问题在强化学习（RL）微调过程中进一步加剧，稀疏且延迟的奖励使得智能体难以识别哪些动作导致了成功，从而阻碍了它们在扩展任务中保持连贯的推理。为了应对这些挑战，我们提出了两项贡献。首先，我们引入了一个智能体框架，该框架利用专有模型通过子目标分解进行在线规划。其次，我们提出了 MiRA（Milestoning your Reinforcement Learning Enhanced Agent），这是一个使用密集的、基于里程碑的奖励信号的强化学习训练框架。实时规划机制使 Gemini 等专有模型在 WebArena-Lite 基准测试中的成功率（SR）绝对值提升了约 10%。同时，将 MiRA 应用于开源的 Gemma3-12B 模型，使其成功率从 6.4% 提升至 43.0%。这一性能超过了 GPT-4-Turbo (17.6%) 和 GPT-4o (13.9%) 等专有系统，以及之前的开源模型最先进水平 WebRL (38.4%)。总之，我们的研究结果表明，将显式的推理时规划与基于里程碑的奖励相结合，可以显著提高智能体的长程能力，为构建更稳健、更通用的自主系统铺平了道路。

## Abstract
Large language model (LLM)-based agents have emerged as powerful autonomous controllers for digital environments, including mobile interfaces, operating systems, and web browsers. Web navigation, for example, requires handling dynamic content and long sequences of actions, making it particularly challenging. Existing LLM-based agents struggle with long-horizon planning in two main ways. During online execution, they often lose track as new information arrives, lacking a clear and adaptive path toward the final goal. This issue is further exacerbated during reinforcement learning (RL) fine-tuning, where sparse and delayed rewards make it difficult for agents to identify which actions lead to success, preventing them from maintaining coherent reasoning over extended tasks. To address these challenges, we propose two contributions. First, we introduce an agent framework that leverages proprietary models for online planning through subgoal decomposition. Second, we present MiRA (Milestoning your Reinforcement Learning Enhanced Agent), an RL training framework that uses dense, milestone-based reward signals. The real-time planning mechanism improves proprietary models such as Gemini by approximately a 10% absolute increase in success rate (SR) on the WebArena-Lite benchmark. Meanwhile, applying MiRA to the open Gemma3-12B model increases its success rate from 6.4% to 43.0%. This performance surpasses proprietary systems such as GPT-4-Turbo (17.6%) and GPT-4o (13.9%), as well as the previous open-model state of the art, WebRL (38.4%). Overall, our findings demonstrate that combining explicit inference-time planning with milestone-based rewards significantly improves an agent's long-horizon capabilities, paving the way for more robust and general-purpose autonomous systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Subgoal-driven Framework for Improving Long-Horizon LLM Agents》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于提升大语言模型（LLM）智能体在**长程（Long-Horizon）任务**（如复杂的网页导航）中的表现。
*   **研究动机**：现有的 LLM 智能体在处理长序列动作时存在两大瓶颈：
    1.  **在线执行迷失**：随着任务步骤增加，智能体容易丢失最终目标，缺乏清晰且自适应的路径。
    2.  **强化学习（RL）训练困难**：网页导航的奖励通常是稀疏且延迟的（仅在任务结束时给出），导致智能体难以识别哪些中间动作对成功有贡献（信用分配问题）。
*   **核心目标**：通过引入“子目标（Subgoal）”机制，在推理时提供动态规划，在训练时提供密集的里程碑奖励。

### 2. 方法论：核心思想与关键技术
论文提出了一个结合在线规划与离线强化学习的统一框架：

*   **自动化失败分析器（Automated Failure Analyzer）**：利用 Gemini-2.5-Flash 对失败轨迹进行分类（如“中途卡住”、“错误终止”等），并定位导致失败的关键决策步骤，为后续改进提供依据。
*   **在线推理：动态里程碑（Dynamic Milestoning）**：
    *   **子目标生成**：利用强力模型（如 Gemini-2.5-Pro）将高层意图分解为 4 个具体的子目标。
    *   **回顾性反思**：在每一步执行前，智能体通过“自我检查”模块回顾历史动作和截图，确认已完成的里程碑，并动态调整下一步计划。
*   **离线训练：MiRA 框架（Milestoning your RL Enhanced Agent）**：
    *   **潜在评论家（Potential Critic, $P_\psi$）**：训练一个模型来预测任务进度（0 到 1 之间的连续值）。通过对离散子目标完成点进行线性插值，将稀疏信号转化为密集的进度标签。
    *   **奖励塑造（Reward Shaping）**：利用 $P_\psi$ 的输出差值作为辅助奖励：$r'_t = r_t + \alpha(P_\psi(s_{t+1}) - P_\psi(s_t))$。
    *   **策略优化**：采用基于均方误差（MSE）的对数概率回归目标，而非传统的 KL 散度优化，以更好地支持离线数据学习。
    *   **迭代细化循环**：通过“失败驱动”的课程学习，不断从失败案例中采样相似任务进行迭代训练。

### 3. 实验设计
*   **数据集/场景**：主要使用 **WebArena-Lite** 基准测试，包含 165 个真实网页任务，涵盖 Reddit、Gitlab、购物管理（CMS）、地图（Map）和开源购物平台（OSS）。
*   **对比方法**：
    *   **闭源模型**：GPT-4-Turbo, GPT-4o, Gemini-2.5-Flash/Pro。
    *   **开源模型及 SOTA**：Llama-3.1 (8B), Gemma-3 (12B), WebRL, DigiRL, AWR, 以及标准的监督微调（SFT）。
*   **评估指标**：任务成功率（SR）和 Pass@k。

### 4. 资源与算力
*   **算力说明**：论文提到实验是在 Google DeepMind 团队的支持下完成的，使用了其内部计算资源。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 H100/A100）、具体数量或总训练时长。但从模型规模（Gemma3-12B）和迭代训练（6 轮在线交互）来看，所需的算力资源相当可观。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多轮迭代训练（Phase 0 到 Phase 6）。
    *   针对 5 个不同的网页领域进行了细分性能分析。
    *   **消融实验**：非常充分。分别验证了 Potential Critic（进度预测）、双重鲁棒优势估计（Doubly-Robust Advantage Estimation）、MSE 损失函数以及奖励塑造因子 $\alpha$ 的影响。
    *   **分析实验**：包括子目标完成的动态演变分析、推理时“思考预算（Thinking Budget）”与延迟的权衡分析。
*   **客观性**：实验采用了 WebArena-Lite 这一公认较难且稳定的基准，并与当前最强的开源（WebRL）和闭源模型进行了直接对比，结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：MiRA 将 Gemma3-12B 的成功率从 **6.4% 提升至 43.0%**，不仅远超之前的开源 SOTA（WebRL 的 38.4%），甚至击败了 GPT-4o（13.9%）和 GPT-4-Turbo（17.6%）。
*   **规划增强**：在线子目标规划机制（SGO）使 Gemini-2.5-Pro 的成功率绝对值提升了约 **10%**。
*   **失败模式转变**：MiRA 显著减少了“中途卡住（Stuck Midway）”的错误，证明了子目标引导能有效帮助智能体跳出局部循环。
*   **推理权衡**：动态分配推理计算量（Thinking）可以在保持高成功率的同时，比固定高预算推理更节省时间。

### 7. 优点
*   **系统性强**：从失败诊断到在线规划，再到离线强化学习，形成了一个完整的闭环。
*   **奖励设计巧妙**：通过线性插值将离散的子目标逻辑转化为连续的进度信号，解决了长程任务中信用分配的难题。
*   **实用性高**：提出的 MSE 回归目标比传统的 PPO 等算法在处理离线智能体数据时更稳定、更高效。

### 8. 不足与局限
*   **依赖强力教师模型**：子目标的生成和验证高度依赖于 Gemini-2.5-Pro 等顶级模型，这在完全脱离闭源 API 的环境下可能难以实施。
*   **冷启动问题**：如果智能体连第一个子目标都无法通过随机探索达到，MiRA 的奖励塑造将无法发挥作用。
*   **评估规模限制**：虽然 WebArena-Lite 质量高，但 165 个任务的规模相对较小，可能存在对特定网页结构的过拟合风险。
*   **延迟增加**：在线推理时的“自我检查”和“思考”步骤显著增加了单步操作的延迟。

（完）
