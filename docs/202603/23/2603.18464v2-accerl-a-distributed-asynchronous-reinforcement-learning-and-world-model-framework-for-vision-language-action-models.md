---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v2"
tags: ["query:rlaif"]
score: 9.0
evidence: 视觉-语言-动作模型与世界模型的强化学习
tldr: 针对大规模视觉-语言-动作（VLA）模型在强化学习中面临的计算效率低和数据获取难等挑战，本文提出了AcceRL框架。该框架采用完全异步且解耦的设计，通过物理隔离训练、推理和采样过程消除同步瓶颈，并首次将可训练的世界模型集成到分布式异步流水线中以生成虚拟经验。实验证明，AcceRL在LIBERO基准上达到了SOTA水平，展现出超线性的吞吐量扩展能力和极高的采样效率。
selection_source: fresh_fetch
motivation: 解决大规模VLA模型在强化学习训练过程中存在的计算效率低下和数据获取困难的问题。
method: 提出一种完全异步且解耦的分布式框架，通过物理隔离核心环节并集成可插拔的世界模型来生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，并实现了超线性的吞吐量增长和卓越的硬件利用率。
conclusion: AcceRL为大规模VLA模型的高效训练提供了强有力的分布式异步强化学习与世界模型集成方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样（rollouts）过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，用于生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它表现出吞吐量的超线性扩展和高效的硬件利用率；在算法层面，其世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。代码已在 https://github.com/distanceLu/AcceRL 公开。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO~\cite{liu2023libero} benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks. Code is publicly available at https://github.com/distanceLu/AcceRL.

---

## 论文详细总结（自动生成）

### AcceRL：分布式异步强化学习与世界模型框架论文总结

#### 1. 论文的核心问题与整体含义
*   **研究背景**：具身智能中的视觉-语言-动作（VLA）模型虽然在模仿学习（IL）上取得了进展，但在处理长程任务和泛化性时仍面临“协变量偏移”问题。强化学习（RL）是解决这一问题的关键，但将 RL 应用于大规模 VLA 模型存在严重的系统瓶颈。
*   **核心问题**：
    1.  **同步障碍**：传统 RL 框架在环境交互、策略推理和模型更新之间存在严格的同步等待（长尾效应），导致昂贵的 GPU 资源在等待缓慢的物理模拟器时处于闲置状态。
    2.  **数据饥渴**：大规模 VLA 模型需要海量交互数据，而物理模拟器的运行速度和推理延迟限制了采样频率。
*   **整体含义**：本文提出了 **AcceRL**，这是首个将完全异步解耦架构与可训练世界模型（World Model）相结合的分布式 RL 框架，旨在通过“想象中学习”和物理隔离计算任务来极大提升训练效率和样本利用率。

#### 2. 论文提出的方法论
*   **核心思想**：通过物理隔离**训练（Training）**、**推理（Inference）**和**采样（Rollout）**三个过程，消除同步瓶颈，并引入世界模型生成虚拟经验。
*   **关键技术细节**：
    *   **宏观异步（Macro-Asynchrony）**：训练器（Trainer）与采样工人（Rollout Workers）解耦。采样工人持续将轨迹存入重放池，训练器独立采样更新，利用 ZeRO 技术优化内存。
    *   **微观异步（Micro-Asynchrony）**：采用“推理即服务（IaaS）”模式。采样工人仅负责环境交互，将观测值发送至集中的推理池（Inference Pool），通过动态批处理（Dynamic Batching）最大化 GPU 利用率。
    *   **世界模型集成**：集成基于扩散模型（Diffusion-based）的观测模型和奖励模型。采样工人在真实环境交互间隙，利用世界模型进行“想象采样”，生成高保真虚拟轨迹。
    *   **算法优化**：
        *   **策略滞后处理**：引入价值重计算（Value Re-computation）和全局优势归一化。
        *   **GIPO 算法**：使用高斯重要性采样策略优化（GIPO）替代标准 PPO 的硬截断，以应对异步带来的策略滞后。
        *   **VLA 适配**：词表瘦身（只保留动作 Token）、动作感知注意力池化价值头、Token 级策略优化。

#### 3. 实验设计
*   **数据集/场景**：使用了 **LIBERO** 机器人操作基准测试，涵盖四个子集：LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **Benchmark**：在 MuJoCo 物理引擎中进行模拟，使用 OSMesa 进行离屏渲染。
*   **对比方法**：
    *   **OpenVLA-OFT**：基于正交微调的监督学习基准。
    *   **RLinf-VLA**：高效的大规模 RL 框架。
    *   **SimpleVLA-RL**：典型的同步训练 RL 方案。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   训练器扩展实验中使用了多达 **7 台 NVIDIA H200 GPU**。
    *   采样端扩展至 **64 个 Rollout Workers**。
*   **训练表现**：在 7 张 H200 上达到了 **104.22 SPS**（每秒样本数）的吞吐量，表现出超线性扩展特性。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **性能对比**：在 LIBERO 的四个维度上进行了全量对比。
    *   **消融实验**：针对“价值重计算机制”、“GIPO 算法 vs PPO”以及“世界模型增益”分别做了深入对比。
    *   **系统扩展性实验**：测试了从 1 到 7 个 GPU 的训练吞吐量，以及从 1 到 64 个工人的采样效率。
*   **充分性评价**：实验设计较为充分，涵盖了从系统吞吐量到算法收敛性的多维度验证。通过在长程任务上的对比，客观证明了 RL 相比 IL 的优势，以及异步架构相比同步架构的效率提升。

#### 6. 论文的主要结论与发现
*   **系统性能**：AcceRL 实现了超线性的吞吐量扩展，硬件利用率保持在 94% 以上，有效消除了 GPU 空转现象。
*   **算法效率**：引入世界模型后，在线采样效率提升了 **200 倍（20,000%）**。在仅 10,000 步真实交互下，模型性能即可快速突破。
*   **任务表现**：在 LIBERO-Long 长程任务中，AcceRL 达到了 **99.1%** 的成功率，显著优于 OpenVLA-OFT（90.7%）和其他 RL 变体，证明了其在复杂控制中的稳定性。

#### 7. 优点
*   **架构创新**：首次在分布式异步 RL 流水线中无缝集成可训练的世界模型，实现了“零卸载（Zero-offload）”的想象训练。
*   **系统优化**：通过 IaaS 模式和词表瘦身等手段，极大地降低了 VRAM 占用并提升了推理速度。
*   **算法稳健**：GIPO 算法和价值重计算机制有效解决了异步架构中常见的策略滞后和训练不稳定问题。

#### 8. 不足与局限
*   **模型支持范围**：目前主要针对 VLA 模型的动作输出进行优化，尚未支持大规模语言模型（LLM）的全量对齐（Post-training）。
*   **世界模型开销**：虽然世界模型减少了物理模拟需求，但训练和运行高保真扩散模型本身也需要额外的计算资源。
*   **应用限制**：实验主要在模拟器（LIBERO）中完成，尚未展示在真实物理机器人上的部署表现及现实世界中的误差累积情况。

（完）
