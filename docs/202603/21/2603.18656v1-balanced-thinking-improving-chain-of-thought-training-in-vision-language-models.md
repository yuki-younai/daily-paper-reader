---
title: "Balanced Thinking: Improving Chain of Thought Training in Vision Language Models"
title_zh: 平衡思考：改进视觉语言模型中的思维链训练
authors: "Shaked Perek, Ben Wiesel, Avihu Dekel, Nimrod Shabtay, Eli Schwartz"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18656v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 视觉语言模型多模态推理的强化学习与微调
tldr: 本研究针对视觉语言模型在思维链（CoT）训练中存在的推理与答案Token不平衡问题，提出了SCALe方法。该方法通过动态的余弦调度策略，在SFT阶段显式分离并调整推理与答案段的损失权重，使模型从关注推理逻辑逐渐转向确保答案准确。实验表明，SCALe仅需极短的训练时间即可达到传统SFT+RL流程的性能，显著提升了多模态推理的效率与精度。
selection_source: fresh_fetch
motivation: 传统SFT训练中长篇幅的推理过程会掩盖短小但关键的答案部分，导致模型推理冗长且准确性不足。
method: 提出SCALe方法，利用余弦调度策略动态调整推理与答案段的损失权重，实现长度无关的平衡监督。
result: SCALe在仅需SFT+GRPO流程约七分之一训练时间的情况下，达到了与之相当的性能，且结合GRPO后效果最优。
conclusion: SCALe为视觉语言模型提供了一种轻量且高效的推理训练方案，是提升模型逻辑性与准确性的强力基准。
---

## 摘要
视觉语言模型（VLM）中的多模态推理通常依赖于两阶段过程：有监督微调（SFT）和强化学习（RL）。在标准 SFT 中，所有 token 对损失函数的贡献是均等的，尽管推理数据在 token 分布上本质上是不平衡的。冗长的 <think> 轨迹掩盖了短小但对任务至关重要的 <answer> 片段，导致推理过程冗长且答案不准确。我们提出了 SCALe（Scheduled Curriculum Adaptive Loss，调度课程自适应损失），它利用动态且与长度无关的加权方式，显式地分离了对推理片段和答案片段的监督。与过度关注 <think> 片段的原始 SFT 不同，SCALe-SFT 通过余弦调度策略在整个训练过程中逐渐将重点从 <think> 转移到 <answer>，从而鼓励简洁且有据可依的推理。我们在多种基准测试和架构上对 SCALe 进行了评估。结果表明，SCALe 的准确率始终优于原始 SFT，并且在仅需约七分之一训练时间的情况下，达到了完整的 SFT + GRPO 两阶段流程的性能，使其成为一种轻量且有效的替代方案。当与 GRPO 结合时，SCALe 实现了最佳的整体性能，突显了其作为独立方法以及作为强化精炼坚实基础的价值。

## Abstract
Multimodal reasoning in vision-language models (VLMs) typically relies on a two-stage process: supervised fine-tuning (SFT) and reinforcement learning (RL). In standard SFT, all tokens contribute equally to the loss, even though reasoning data are inherently token-imbalanced. Long <think> traces overshadow short but task-critical <answer> segments, leading to verbose reasoning and inaccurate answers. We propose SCALe (Scheduled Curriculum Adaptive Loss), which explicitly separates supervision over reasoning and answer segments using dynamic, length-independent weighting. Unlike vanilla SFT, which overweights the <think> segment, SCALe-SFT gradually shifts the focus from <think> to <answer> throughout training via a cosine scheduling policy, encouraging concise and well-grounded reasoning. We evaluate SCALe across diverse benchmarks and architectures. Results show that SCALe consistently improves accuracy over vanilla SFT and matches the performance of the full two-phase SFT + GRPO pipeline while requiring only about one-seventh of the training time, making it a lightweight yet effective alternative. When combined with GRPO, SCALe achieves the best overall performance, highlighting its value both as a standalone method and as a strong foundation for reinforcement refinement.

---

## 论文详细总结（自动生成）

以下是对论文《Balanced Thinking: Improving Chain of Thought Training in Vision Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在视觉语言模型（VLM）的思维链（CoT）训练中，存在严重的 **Token 不平衡问题**。
*   **研究动机**：传统的有监督微调（SFT）对所有 Token 使用均等的交叉熵损失。然而，推理数据中 `<think>`（推理过程）通常极长，而 `<answer>`（最终答案）非常短（比例可达 150:1）。这导致模型在训练时过度关注推理的流畅性，而忽视了答案的准确性，容易产生“想得太多但答得不对”或推理冗长且逻辑断裂的问题。

### 2. 方法论
*   **核心思想**：提出 **SCALe-SFT**（调度课程自适应损失），通过动态调整推理段和答案段的损失权重，实现“长度无关”的平衡监督。
*   **关键技术细节**：
    *   **片段分离与长度无关损失**：将输出序列分为推理段和答案段，分别计算平均损失（总损失除以该段 Token 数），确保长推理不会淹没短答案。
    *   **调度加权（Scheduled Weighting）**：引入**余弦退火调度策略**。在训练初期，赋予推理段较高权重以建立结构化思维；随着训练进行，逐渐降低推理权重，增加答案权重的相对影响。
    *   **公式描述**：总损失 $L = w_t L_{think} + w_a L_{answer}$。其中 $w_t$ 随训练步数从 1.0 逐渐降至 0.5，而 $w_a$ 保持为 1.0。
    *   **强化精炼**：SCALe-SFT 产出的模型可作为 GRPO（群体相对策略优化）的初始化检查点，进一步提升性能。

### 3. 实验设计
*   **数据集/场景**：
    *   **ScienceQA**：多模态科学问答，考察视觉理解与科学推理。
    *   **IconQA**：图表与抽象图形理解。
    *   **训练数据**：使用 Vision-R1 推理语料库（约 20 万样本）。
*   **Benchmark 与对比方法**：
    *   **对比基准**：原始模型（Base）、传统 SFT、传统 SFT+GRPO 流程。
    *   **评估指标**：准确率（Exact Match）和无效生成率（如缺失标签）。
*   **测试模型**：Qwen2.5-VL-3B、LLaVA-v1.6-Mistral-7B、Gemma-3-4b。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：采用 DeepSpeed-ZeRO2 框架，使用 LoRA 微调，学习率为 $1 \times 10^{-4}$，最大序列长度 8192。
*   **效率提升**：论文强调 SCALe-SFT 仅需完整 SFT+GRPO 流程约 **1/7 的训练时间**，即可达到相近的性能。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构的模型（包括已做过推理预训练和未做过的模型），在 2 个主流基准上进行了全面测试。
*   **消融实验**：专门对比了“固定权重（FW）”与“动态调度权重（SCALe）”的效果，验证了调度机制的必要性。
*   **充分性评价**：实验设计较为客观、公平，通过跨模型、跨任务的对比证明了方法的普适性。

### 6. 主要结论与发现
*   **性能提升**：SCALe-SFT 相比传统 SFT 准确率提升高达 3%；结合 GRPO 后，性能进一步提升（最高比传统流程高 5.4%）。
*   **高效替代**：在算力受限时，仅使用 SCALe-SFT 即可超越传统的“SFT + 强化学习”组合。
*   **结构稳定性**：显著降低了模型生成无效格式（如忘记关闭标签或陷入推理循环）的概率，无效生成率相对降低了 8.6% 至 87.4%。

### 7. 优点
*   **简单高效**：无需复杂的奖励函数设计，仅通过修改损失权重分配即可获得显著收益。
*   **即插即用**：适用于各种 VLM 架构，且能与现有的强化学习算法（如 GRPO）完美兼容。
*   **解决痛点**：直接针对 CoT 训练中 Token 分布极度不均的本质问题提出了优雅的数学解决方案。

### 8. 不足与局限
*   **模态限制**：目前主要针对单图推理任务，尚未在多图或视频推理场景中验证。
*   **参数敏感性**：权重调度的起始值和终值（如 1.0 到 0.5）目前是手动设定的，未来可能需要自动化的在线调优机制。
*   **任务范围**：虽然计划扩展到纯文本推理，但本文实验仍集中在视觉问答领域。

（完）
