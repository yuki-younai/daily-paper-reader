---
title: "Learning to Reason with Curriculum I: Provable Benefits of Autocurriculum"
title_zh: 课程学习推理 I：自动课程的可证明收益
authors: "Nived Rajaraman, Audrey Huang, Miro Dudik, Robert Schapire, Dylan J. Foster, Akshay Krishnamurthy"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18325v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大模型后训练与强化学习中的自动课程学习
tldr: 本研究针对思维链推理模型训练中高昂的数据与计算成本，提出了“自动课程学习”（Autocurriculum）框架。该方法通过让模型根据自身当前表现自适应地选择训练题目，在理论上证明了其显著优势：在监督微调（SFT）阶段能以指数级减少所需的推理演示数量，在强化学习（RL）阶段则能使计算成本与参考模型质量解耦。这一方法借鉴了提升法等经典技术，无需对提示词分布做预设，为高效培养模型推理能力提供了理论支撑。
selection_source: fresh_fetch
motivation: 旨在解决训练思维链推理模型时面临的极高数据获取成本和计算资源消耗问题。
method: 提出自动课程学习机制，通过模型性能反馈动态选择训练样本，实现自适应的数据监督和强化学习。
result: 证明了该方法能显著减少SFT所需的演示数据，并降低RL训练对高质量参考模型的依赖。
conclusion: 研究表明自适应数据选择策略在提升推理模型训练效率方面具有可证明的理论效益。
---

## 摘要
思维链（Chain-of-thought）推理是指语言模型在给出最终回答前，通过生成思考标记（thinking tokens）来消耗额外的计算量，这一机制显著提升了模型能力。然而，训练这些推理模型在数据和计算方面都极其昂贵，因为它涉及从人类或合成生成器中收集长路径的推理行为轨迹，并进一步通过强化学习对模型进行后训练。这些成本是不可避免的，还是可以通过更好的算法设计来降低？我们证明了自动课程（autocurriculum）——即模型利用自身表现来决定训练应关注哪些问题——在监督微调（SFT）和强化学习（RL）中均能可证明地改进标准训练方案。对于 SFT，我们展示了自动课程通过将教师监督集中在当前模型难以处理的提示词（prompts）上，所需的推理示例比非自适应微调呈指数级减少。对于 RL 微调，自动课程将计算成本与参考模型的质量解耦，将后者降低为几乎与目标准确率无关的预热（burn-in）成本。这些改进纯粹源于自适应数据选择，借鉴了提升法（boosting）和反例学习（learning from counterexamples）的经典技术，且无需对提示词的分布或难度做出任何假设。

## Abstract
Chain-of-thought reasoning, where language models expend additional computation by producing thinking tokens prior to final responses, has driven significant advances in model capabilities. However, training these reasoning models is extremely costly in terms of both data and compute, as it involves collecting long traces of reasoning behavior from humans or synthetic generators and further post-training the model via reinforcement learning. Are these costs fundamental, or can they be reduced through better algorithmic design? We show that autocurriculum, where the model uses its own performance to decide which problems to focus training on, provably improves upon standard training recipes for both supervised fine-tuning (SFT) and reinforcement learning (RL). For SFT, we show that autocurriculum requires exponentially fewer reasoning demonstrations than non-adaptive fine-tuning, by focusing teacher supervision on prompts where the current model struggles. For RL fine-tuning, autocurriculum decouples the computational cost from the quality of the reference model, reducing the latter to a burn-in cost that is nearly independent of the target accuracy. These improvements arise purely from adaptive data selection, drawing on classical techniques from boosting and learning from counterexamples, and requiring no assumption on the distribution or difficulty of prompts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Reason with Curriculum I: Provable Benefits of Autocurriculum》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：训练具备思维链（CoT）推理能力的语言模型成本极高。监督微调（SFT）需要大量高质量的人类或教师模型推理轨迹；强化学习（RL）虽然不需要轨迹，但需要海量的模型采样（Rollouts）和验证。
*   **核心问题**：这些高昂的统计和计算成本是不可避免的吗？是否可以通过算法设计（如“自动课程学习”）来降低？
*   **整体含义**：本文从理论上证明了**自动课程（Autocurriculum）**——即模型根据自身表现自适应选择训练题目——能够显著降低推理模型的训练成本。

### 2. 方法论：核心思想与关键技术
该论文提出了两个主要算法框架，分别针对 SFT 和 RL 场景：

*   **核心思想**：利用**结果验证器（Outcome Verifier）**提供的廉价反馈，识别模型当前的“薄弱环节”，并将昂贵的训练资源（教师演示或模型采样）集中在这些困难题目上。
*   **AutoTune (针对 SFT)**：
    *   **技术细节**：借鉴了经典提升法（Boosting）和反例学习（Learning from Counterexamples）的思想。
    *   **流程**：迭代式训练模型集成。在每一轮中，利用验证器筛选出当前模型预测错误的提示词（Prompts），仅针对这些提示词向教师模型请求 CoT 演示。
    *   **数学基础**：通过拒绝采样（Rejection Sampling）改变提示词分布，使每一轮的新模型都能修正前一轮的错误。
*   **AutoTune.RL (针对 RLVR)**：
    *   **技术细节**：针对带有可验证奖励的强化学习（RLVR）。
    *   **流程**：将自动课程与拒绝采样相结合。它证明了计算成本可以与参考模型的质量（覆盖率）解耦。

### 3. 实验设计：场景与对比
*   **研究性质**：这是一篇**纯理论证明论文**，其“实验”体现为对算法复杂度的数学推导和界限分析。
*   **对比基准（Benchmark）**：对比了 Joshi 等人 (2025) 提出的**非自适应（标准）微调方案**。
*   **分析场景**：
    1.  **确定性模型**：作为基础分析对象。
    2.  **随机性模型**：更贴近真实的语言模型。
    3.  **稀疏验证器**：假设每个问题有唯一正确答案。
    4.  **部分覆盖参考模型**：在 RL 场景下，参考模型仅在部分题目上能生成正确答案。

### 4. 资源与算力
*   **算力说明**：由于本文是理论研究，**未涉及具体的 GPU 型号、数量或实际训练时长**。
*   **计算复杂度分析**：论文通过数学公式给出了“计算成本”（ncomp）和“查询复杂度”（nCoT）的理论上界。

### 5. 实验数量与充分性
*   **理论覆盖度**：论文在 39 页的篇幅中提供了详尽的数学证明（附录占据了约一半篇幅）。
*   **充分性**：分析涵盖了从 SFT 到 RL、从确定性到随机性模型的多种变体，证明了结论在不同假设下的稳健性。
*   **客观性**：结论基于严格的统计学习理论（如 Natarajan 维度、VC 维度等），不依赖于特定的经验性超参数。

### 6. 主要结论与发现
*   **SFT 的指数级优化**：自动课程可以将所需的教师演示数量从 $e^{\Theta(d/\epsilon)}$ 降低到 $e^{O(d)}$。这意味着**教师演示的数量几乎与目标准确率无关**。
*   **RL 的解耦效应**：在 RL 场景下，自动课程将计算成本与参考模型的覆盖率解耦。覆盖率低只产生一个固定的“预热成本”，而提升准确率的边际成本与高质量参考模型相当。
*   **无需分布假设**：这些改进不需要对提示词的分布或难度做任何先验假设，完全由模型自身的训练动力学驱动。

### 7. 优点：亮点与创新
*   **首个理论证明**：首次为大模型推理训练中的自动课程学习提供了可证明的收益分析。
*   **算法效率**：提出的算法在统计上是高效的，且在计算上可实现（基于经验风险最小化 ERM 预言机）。
*   **跨领域结合**：成功将 80-90 年代的经典学习理论（Boosting）应用于现代 LLM 推理训练问题。

### 8. 不足与局限
*   **理想化假设**：假设存在一个完美的验证器（Verifier），但在现实（如创意写作）中，验证器往往是不完美或不存在的。
*   **离线/批处理限制**：目前的理论框架主要针对批处理设置，尚未完全涵盖现代 RL 流程中的在线策略梯度更新（如 PPO/GRPO）。
*   **覆盖率扩张缺失**：论文假设参考模型在目标问题上已有微弱的覆盖，尚未深入探讨如何通过简单问题训练来“扩张”到完全未覆盖的困难问题（作者表示这将在第二部分研究中讨论）。

（完）
