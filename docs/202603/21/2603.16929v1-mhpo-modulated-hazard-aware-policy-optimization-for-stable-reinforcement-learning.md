---
title: "MHPO: Modulated Hazard-aware Policy Optimization for Stable Reinforcement Learning"
title_zh: MHPO：面向稳定强化学习的调制风险感知策略优化
authors: "Hongjun Wang, Wei Liu, Weibo Gu, Xing Sun, Kai Han"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.16929v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 改进 GRPO 策略优化的稳定性和梯度保真度
tldr: 针对GRPO框架中重要性比率调节不稳定的挑战，本文提出MHPO框架。该方法通过对数保真度调制器（LFM）将无界比率映射为有界可微形式以确保梯度稳定性，并引入解耦风险惩罚（DHP）利用生存分析原理调节非对称策略偏移。MHPO在文本与视觉语言推理任务中表现优异，显著增强了强化学习的训练稳健性与性能。
selection_source: fresh_fetch
motivation: 传统硬截断方法因不可微边界和梯度消失导致训练不稳定，且缺乏自适应抑制极端策略偏差的风险感知机制。
method: 提出MHPO框架，结合LFM调制器确保全局梯度保真度，并利用DHP惩罚项独立监管正负策略偏移以稳定信任区域。
result: 在多个推理基准测试中，MHPO在提升模型性能的同时，显著降低了训练过程中的方差并防止了模式崩溃。
conclusion: MHPO通过创新的调制与风险感知机制，为实现稳定且高效的强化学习策略优化提供了有力支持。
---

## 摘要
调节重要性权重比对于基于组相对策略优化（GRPO）框架的训练稳定性至关重要。然而，现有的权重比控制方法（如硬截断）存在不可微边界和梯度消失区域的问题，无法保持梯度保真度。此外，这些方法缺乏风险感知机制来动态抑制极端偏差，使得优化过程容易受到策略突变的影响。为了应对这些挑战，我们提出了调制风险感知策略优化（MHPO），这是一种专为鲁棒且稳定的强化学习设计的新型框架。所提出的 MHPO 引入了对数保真度调制器（LFM），将无界的重要性权重比映射到有界的、可微的域中。该机制有效地防止了高方差的离群 token 破坏损失景观的稳定性，同时确保了全局梯度的稳定性。作为补充，解耦风险惩罚（DHP）集成了生存分析中的累积风险函数，以独立调节正向和负向的策略偏移。通过利用风险感知惩罚来塑造优化景观，MHPO 实现了对非对称策略偏移的细粒度调节，在稳定的置信域内同时缓解了过度扩张导致的模式崩溃，并防止了灾难性收缩导致的策略侵蚀。在文本和视觉语言任务的多种推理基准测试上的广泛评估表明，MHPO 一贯优于现有方法，在显著增强训练稳定性的同时实现了卓越的性能。

## Abstract
Regulating the importance ratio is critical for the training stability of Group Relative Policy Optimization (GRPO) based frameworks. However, prevailing ratio control methods, such as hard clipping, suffer from non-differentiable boundaries and vanishing gradient regions, failing to maintain gradient fidelity. Furthermore, these methods lack a hazard-aware mechanism to adaptively suppress extreme deviations, leaving the optimization process vulnerable to abrupt policy shifts. To address these challenges, we propose Modulated Hazard-aware Policy Optimization (MHPO), a novel framework designed for robust and stable reinforcement learning. The proposed MHPO introduces a Log-Fidelity Modulator (LFM) to map unbounded importance ratios into a bounded, differentiable domain. This mechanism effectively prevents high-variance outlier tokens from destabilizing the loss landscape while ensuring global gradient stability. Complementarily, a Decoupled Hazard Penalty (DHP) integrates cumulative hazard functions from survival analysis to independently regulate positive and negative policy shifts. By shaping the optimization landscape with hazard-aware penalties, the proposed MHPO achieves fine-grained regulation of asymmetric policy shifts simultaneously mitigating mode collapse from over-expansion and preventing policy erosion from catastrophic contraction within a stabilized trust region. Extensive evaluations on diverse reasoning benchmarks across both text-based and vision-language tasks demonstrate that MHPO consistently outperforms existing methods, achieving superior performance while significantly enhancing training stability.

---

## 论文详细总结（自动生成）

### MHPO: 面向稳定强化学习的调制风险感知策略优化 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的强化学习（RL）后训练中，**Group Relative Policy Optimization (GRPO)** 框架因其无需 Critic 模型的特性而备受关注。然而，该框架面临核心挑战：**重要性比率（Importance Ratio）的不稳定性**。
*   **背景**：在长链条推理（CoT）任务中，token 级别的比率累积会导致梯度剧烈波动（Spikes），引发训练崩溃。
*   **动机**：现有的控制方法（如 PPO/GRPO 的硬截断）存在不可微边界和梯度消失问题；同时，它们对“正向偏移”（概率增加）和“负向偏移”（概率减少）采取对称处理，忽略了两者不同的风险特征（如过度扩张导致的模式崩溃与过度收缩导致的策略侵蚀）。

#### 2. 方法论：核心思想与关键技术
MHPO 提出了一种统一的框架，通过两个核心组件实现平滑且风险感知的策略优化：
*   **对数保真度调制器 (LFM, Log-Fidelity Modulator)**：
    *   **核心思想**：在对数空间使用缩放的 `tanh` 函数对重要性比率进行映射。
    *   **技术细节**：公式为 $\psi(r) = c \cdot \tanh(\frac{\log r}{c})$。它将无界比率映射到 $[-c, c]$ 的有界可微流形中。
    *   **作用**：在策略锚点附近保持高保真度（类似标准策略梯度），在远离锚点时平滑衰减梯度，避免离群 token 支配参数更新。
*   **解耦风险惩罚 (DHP, Decoupled Hazard Penalty)**：
    *   **核心思想**：借鉴生存分析中的 **Weibull 累积风险函数**，独立调节正负策略偏移。
    *   **技术细节**：引入惩罚项 $\zeta$，利用 `softplus` 函数分离正负向，并应用 Weibull 分布的形状参数 $k$ 和尺度参数 $\lambda$ 来塑造优化景观。
    *   **作用**：允许对正负偏移进行非对称控制（例如，对负向偏移设置更严厉的惩罚以防止策略侵蚀），在信任区域内保持低惩罚，在区域外快速增加惩罚。
*   **理论保证**：论文证明了 MHPO 的梯度乘子具有有限上界，确保了随机优化中梯度估计器的**二阶矩稳定性**。

#### 3. 实验设计
*   **模型与场景**：
    *   **纯文本推理**：使用 Qwen3-4B-Base、Qwen2.5-7B-Instruct、Qwen2.5-Math-7B-Instruct。
    *   **视觉语言推理**：使用 Qwen2.5-VL-7B-Instruct。
*   **数据集与 Benchmark**：
    *   训练集：DAPO-Math-17k, Geometry3K。
    *   评估集：MATH-500, AIME 2024/2025, AMC 2023, HMMT25（文本）；MathVista, MathVision, MathVerse（多模态）。
*   **对比方法**：GRPO, GSPO, DAPO, SAPO（SOTA 策略优化算法）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及总训练时长。
*   **实现细节**：提到最大响应长度在 Qwen2.5 上为 2048 tokens，在 Qwen3 上为 4096 tokens。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 Base 模型与 Instruct 模型、通用模型与领域专家模型、纯文本与多模态模型，实验覆盖面广。
*   **消融实验**：针对超参数 $c$（保真度边界）、$k$（形状参数）、$\lambda$（尺度参数）进行了详细的敏感性分析。
*   **稳定性分析**：通过梯度范数轨迹、奖励曲线、以及“最佳 vs 最后”Checkpoint 的性能跌落对比，多维度验证了稳定性。
*   **评价**：实验设计严谨、对比基准全面，通过跨模态和跨模型规模的验证，证明了结论的客观性和普适性。

#### 6. 主要结论与发现
*   **性能卓越**：MHPO 在所有基准测试中均显著优于 GRPO 和其他变体。在 Qwen3-4B-Base 上，平均性能比最强基准高出 5.7%。
*   **极致稳定**：MHPO 的梯度范数始终保持低位且平稳，无剧烈尖峰。
*   **防止崩溃**：在训练后期，传统方法（如 DAPO）常出现性能大幅下滑（-12.2%），而 MHPO 的性能跌落仅为 0.8%，几乎消除了对复杂早停（Early Stopping）策略的依赖。

#### 7. 优点（亮点）
*   **数学严谨性**：引入生存分析理论处理策略偏移风险，并提供了梯度稳定性的理论证明。
*   **非对称控制**：识别并解决了正负策略更新风险不一致的问题，比传统的对称截断更符合 RL 训练逻辑。
*   **全局可微**：相比硬截断，LFM 提供了全域连续的梯度流，有利于 Adam 等自适应优化器的动量积累。

#### 8. 不足与局限
*   **超参数复杂性**：引入了较多超参数（$c, k_+, \lambda_+, k_-, \lambda_-$），虽然论文给出了默认推荐值，但在不同规模或性质的任务上可能需要精细调优。
*   **算力细节缺失**：缺乏具体的算力消耗报告，难以评估其相对于基础 GRPO 的计算开销增加情况（尽管理论上增加极小）。
*   **应用范围**：目前主要集中在数学和逻辑推理任务，在创意写作或开放式对话等主观性更强的 RLHF 任务上的表现尚待验证。

（完）
