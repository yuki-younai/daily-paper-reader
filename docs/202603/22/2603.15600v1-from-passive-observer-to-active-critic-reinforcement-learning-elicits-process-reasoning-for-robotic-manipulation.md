---
title: "From Passive Observer to Active Critic: Reinforcement Learning Elicits Process Reasoning for Robotic Manipulation"
title_zh: 从被动观察者到主动评估者：强化学习激发机器人操控的过程推理
authors: "Yibin Liu, Yaxing Lyu, Daqi Gao, Zhixuan Liang, Weiliang Tang, Shilong Mu, Xiaokang Yang, Yao Mu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15600v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 用于机器人操作的视频多模态大模型强化学习
tldr: 针对长程机器人操作中过程监督的挑战，本文提出 PRIMO R1 框架，通过强化学习将视频大模型从被动观察者转变为主动评价者。该方法利用基于结果的 RL 激励模型生成思维链进行进度评估，并采用结构化时空输入。实验表明，该 7B 模型在精度上显著超越 72B 模型，并在故障检测任务中刷新了 SOTA 记录。
selection_source: fresh_fetch
motivation: 现有的视频多模态大模型多采用监督微调，仅能识别事件而缺乏针对任务目标的评估能力，难以胜任长程机器人操作的监督任务。
method: 提出 PRIMO R1 框架，利用基于结果的强化学习诱导模型生成过程推理思维链，并结合初始与当前状态的结构化时空输入进行进度估计。
result: "该 7B 模型在推理误差上比基准降低了 50%，在 RoboFail 故障检测基准上达到 67.0% 的准确率，超越了 OpenAI o1 等闭源大模型。"
conclusion: 通过强化学习激发模型的过程推理能力，可以显著提升视频大模型在机器人复杂任务监控和故障检测中的表现与泛化能力。
---

## 摘要
准确的过程监督仍然是长程机器人操控面临的关键挑战。一个主要的瓶颈在于，目前主要在监督微调（SFT）范式下训练的视频多模态大语言模型（MLLM）仅作为被动的“观察者”，识别正在发生的事件，而非根据最终任务目标评估当前状态。在本文中，我们介绍了 PRIMO R1（过程推理诱导监测），这是一个 7B 规模的框架，旨在将视频 MLLM 转化为主动的“评估者”。我们利用基于结果的强化学习来激励生成显式的思维链（Chain-of-Thought），以进行进度估计。此外，我们的架构通过在初始状态和当前状态图像之间显式锚定视频序列，构建了结构化的时间输入。在所提出的 PRIMO 数据集和基准测试的支持下，在多种域内环境和域外真实世界人形机器人场景中进行的广泛实验表明，PRIMO R1 达到了最先进的性能。在定量分析上，我们的 7B 模型在专门推理基准上的平均绝对误差降低了 50%，与 72B 规模的通用 MLLM 相比，展现出显著的相对准确率提升。此外，PRIMO R1 在困难的故障检测任务上表现出强大的零样本泛化能力。我们在 RoboFail 基准测试上以 67.0% 的准确率确立了最先进的性能，超过了 OpenAI o1 等闭源模型 6.0%。

## Abstract
Accurate process supervision remains a critical challenge for long-horizon robotic manipulation. A primary bottleneck is that current video MLLMs, trained primarily under a Supervised Fine-Tuning (SFT) paradigm, function as passive "Observers" that recognize ongoing events rather than evaluating the current state relative to the final task goal. In this paper, we introduce PRIMO R1 (Process Reasoning Induced Monitoring), a 7B framework that transforms video MLLMs into active "Critics". We leverage outcome-based Reinforcement Learning to incentivize explicit Chain-of-Thought generation for progress estimation. Furthermore, our architecture constructs a structured temporal input by explicitly anchoring the video sequence between initial and current state images. Supported by the proposed PRIMO Dataset and Benchmark, extensive experiments across diverse in-domain environments and out-of-domain real-world humanoid scenarios demonstrate that PRIMO R1 achieves state-of-the-art performance. Quantitatively, our 7B model achieves a 50% reduction in the mean absolute error of specialized reasoning baselines, demonstrating significant relative accuracy improvements over 72B-scale general MLLMs. Furthermore, PRIMO R1 exhibits strong zero-shot generalization on difficult failure detection tasks. We establish state-of-the-art performance on RoboFail benchmark with 67.0% accuracy, surpassing closed-source models like OpenAI o1 by 6.0%.

---

## 论文详细总结（自动生成）

# 论文总结：从被动观察者到主动评估者（PRIMO R1）

### 1. 核心问题与研究背景
*   **研究动机**：在长程机器人操控任务中，获取准确的“过程监督”信号（即任务进行到哪一步了）是强化学习和策略学习的关键瓶颈。
*   **核心痛点**：现有的视频多模态大模型（Video MLLMs）大多采用监督微调（SFT）范式，表现得像“被动观察者”——它们能描述视频中发生了什么，但缺乏定量推理能力，无法准确评估当前状态与最终目标之间的逻辑距离，且容易在任务失败时产生“幻觉”进度。
*   **研究目标**：将模型从“观察者”转变为“主动评价者（Critic）”，使其能够通过显式的逻辑推理来评估任务进度并检测执行故障。

### 2. 方法论：PRIMO R1 框架
*   **核心思想**：利用**基于结果的强化学习（RL）**来诱导模型生成**思维链（Chain-of-Thought, CoT）**，通过结构化的时空输入进行高精度的状态对齐。
*   **关键技术细节**：
    *   **结构化输入锚定**：输入不再仅仅是视频流，而是显式地将视频序列（$V_{seq}$）锚定在初始状态图像（$I_{init}$）和当前状态图像（$I_{curr}$）之间。这种设计为模型提供了清晰的边界条件，解决了长视频中空间细节被稀释的问题。
    *   **推理诱导（CoT）**：模型被要求按照“规划（Planning）— 观察（Observation）— 推理（Reasoning）”的步骤生成推理链，最后给出进度数值。
    *   **强化学习算法**：采用 **GRPO（Group Relative Policy Optimization）**。该算法通过组内相对奖励来估计基准，无需额外的价值网络（Critic Network），显著降低了显存开销。
    *   **奖励函数设计**：
        1.  **格式奖励（$r_{fmt}$）**：强制模型遵循 `<think>` 和 `<answer>` 的结构。
        2.  **准确度奖励（$r_{acc}$）**：采用线性衰减函数，预测值越接近真实进度（0-100），奖励越高。

### 3. 实验设计
*   **数据集与场景**：
    *   **PRIMO Dataset**：包含 116k SFT 数据和 182k RL 训练数据，涵盖 AgiBot（真实）、BEHAVIOR-1k（仿真）、RoboTwin（仿真）等。
    *   **PRIMO Bench**：专门设计的评估基准，包含“域内（同任务）”和“域外（跨任务、跨环境）”测试。
    *   **真实世界验证**：在 Leju KUAVO-MY 全尺寸人形机器人上收集了工厂、服务等非结构化场景的数据。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, OpenAI o1, Gemini 2.0 Flash, Claude-4.5-Haiku。
    *   **开源通用模型**：Qwen2.5-VL (7B/72B), InternVL 2.5 (8B)。
    *   **专用推理/视频模型**：ProgressLM, Video R1, RoboBrain, Cosmos-Reasoning。

### 4. 资源与算力
*   **硬件环境**：使用 8 张 **NVIDIA A100 (80GB)** GPU。
*   **训练配置**：
    *   **SFT 阶段**：学习率 1e-6，Batch Size 64，训练 1 个 Epoch。
    *   **RL 阶段**：学习率 1e-6，GRPO 组大小（G）为 8，每个 Prompt 生成 32 个 Rollouts。
    *   **输入限制**：训练时视频采样为 16 帧，推理时为 32 帧。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同环境（3 仿真 + 1 真实）上进行了大规模测试，涵盖了从原子动作到长程复合任务。
*   **消融实验**：
    *   验证了 **SFT vs RL** 的贡献：证明 RL 显著提升了模型的泛化能力和自纠错能力。
    *   验证了**输入模态**：对比了仅视频、视频+初始帧、视频+当前帧等组合，证明“三位一体”的锚定结构最优。
*   **客观性**：通过跨环境（Sim-to-Real）的零样本测试和 RoboFail 第三方基准测试，验证了模型并非简单的模式记忆，而是具备真正的推理迁移能力。

### 6. 主要结论与发现
*   **性能超越**：7B 规模的 PRIMO R1 在进度估计精度上显著超过了 72B 的通用大模型，平均绝对误差（MAE）降低了约 50%。
*   **故障检测 SOTA**：在 RoboFail 基准上达到 **67.0%** 的准确率，超越了 OpenAI o1（61.0%）和 GPT-4o（63.0%）。
*   **推理的内在联系**：实验发现，优化连续的进度推理能力，能够自然地诱导出检测离散执行故障所需的时空表征。
*   **Sim-to-Real 成功**：模型在完全未见的真实人形机器人场景中依然保持了极高的评估准确度。

### 7. 优点（亮点）
*   **范式转变**：成功将视频 MLLM 从简单的“描述者”提升为具备定量评估能力的“评价者”。
*   **结构化 Prompt 创新**：通过 Planning-Observation-Reasoning 的三段式推理，使模型的判断过程具有极高的可解释性。
*   **高效的 RL 应用**：证明了 GRPO 在多模态领域（尤其是视频推理）的有效性，且 7B 模型在特定任务上展现出极高的“性价比”。

### 8. 不足与局限
*   **时间分辨率限制**：由于计算开销，视频采样限制在 16-32 帧，对于某些极短时间内发生的关键物理交互（如微小的打滑或碰撞）可能捕捉不足。
*   **推理延迟**：虽然比 Cosmos-Reasoning 快，但 0.6s 左右的推理延迟对于需要毫秒级响应的高频闭环控制任务仍有距离。
*   **任务复杂度上限**：虽然在长程任务表现良好，但对于涉及极多步骤（如超过 100 步）的超长任务，CoT 的逻辑一致性可能会受到挑战。

（完）
