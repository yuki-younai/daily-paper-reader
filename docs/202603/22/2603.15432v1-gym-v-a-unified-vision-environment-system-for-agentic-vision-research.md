---
title: "Gym-V: A Unified Vision Environment System for Agentic Vision Research"
title_zh: Gym-V：用于智能体视觉研究的统一视觉环境系统
authors: Fanqing Meng Lingxiao Du Jiawei Gu Jiaqi Liao Linjie Li Zijian Wu Xiangyan Liu Ziqi Zhao Mengkang Hu Yue Zhang Zichen Liu Jiaheng Zhang Michael Qizhe Shieh
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15432v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 用于智能体强化学习的统一视觉环境
tldr: Gym-V是一个统一的视觉环境平台，包含10个领域、179个程序化生成的视觉任务，旨在解决视觉智能体缺乏标准化基础设施的问题。研究发现，观察脚手架（如描述和规则）对训练成功的影响超过了强化学习算法的选择。该平台支持受控实验，揭示了跨领域迁移和多轮交互对模型泛化能力的影响，为视觉语言模型（VLM）智能体的训练和评估提供了重要基础。
selection_source: fresh_fetch
motivation: 视觉智能体研究缺乏标准化的基础设施，导致难以进行系统性的学习驱动因素分析和公平比较。
method: 开发了包含179个程序化生成环境的Gym-V系统，支持可控难度和多领域任务，用于评估和训练视觉智能体。
result: 实验表明观察脚手架比RL算法更关键，且多样化任务训练能显著提升泛化能力，而单一任务训练可能导致负迁移。
conclusion: Gym-V为视觉智能体研究提供了便捷的训练和评估基准，强调了环境设计和任务多样性在提升VLM性能中的核心作用。
---

## 摘要
随着智能体系统日益依赖于基于可验证奖励的强化学习，标准化的“gym”基础设施对于快速迭代、可重复性和公平比较已变得至关重要。视觉智能体缺乏此类基础设施，这限制了对驱动其学习的因素以及当前模型不足之处的系统性研究。我们推出了 Gym-V，这是一个包含 10 个领域、179 个程序化生成的视觉环境的统一平台，具有可控的难度，能够实现以往在碎片化工具包中无法进行的受控实验。通过该平台，我们发现观察支架（observation scaffolding）对训练成功的决定性作用超过了强化学习算法的选择，其中字幕和游戏规则决定了学习是否能够成功。跨领域迁移实验进一步表明，在多样化的任务类别上进行训练具有广泛的泛化能力，而狭隘的训练可能导致负迁移，且多轮交互会放大所有这些效应。Gym-V 作为一个便捷的训练环境和评估工具包基础发布，旨在加速未来对智能体视觉语言模型（agentic VLMs）的研究。

## Abstract
As agentic systems increasingly rely on reinforcement learning from verifiable rewards, standardized ``gym'' infrastructure has become essential for rapid iteration, reproducibility, and fair comparison. Vision agents lack such infrastructure, limiting systematic study of what drives their learning and where current models fall short. We introduce \textbf{Gym-V}, a unified platform of 179 procedurally generated visual environments across 10 domains with controllable difficulty, enabling controlled experiments that were previously infeasible across fragmented toolkits. Using it, we find that observation scaffolding is more decisive for training success than the choice of RL algorithm, with captions and game rules determining whether learning succeeds at all. Cross-domain transfer experiments further show that training on diverse task categories generalizes broadly while narrow training can cause negative transfer, with multi-turn interaction amplifying all of these effects. Gym-V is released as a convenient foundation for training environments and evaluation toolkits, aiming to accelerate future research on agentic VLMs.

---

## 论文详细总结（自动生成）

### Gym-V：用于智能体视觉研究的统一视觉环境系统

#### 1. 研究动机与背景
*   **核心问题**：当前的视觉智能体（Vision Agents）研究缺乏像文本智能体那样标准化的“Gym”基础设施。现有的视觉环境平台（如 VAGEN, VLM-Gym）覆盖任务单一、接口不统一，导致研究者难以系统性地分析驱动视觉智能体学习的关键因素，也难以进行公平的模型比较。
*   **研究背景**：随着视觉语言模型（VLM）向智能体化发展，利用可验证奖励进行强化学习（RLVR）成为主流。然而，从文本转向像素增加了感知的瓶颈，观察结果的呈现方式（如是否提供描述、规则等）对 RL 训练的成功至关重要，这需要一个统一且可控的实验平台。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：构建一个统一的视觉环境系统 Gym-V，通过程序化生成（Procedural Generation）和自动验证，提供跨领域的、难度可控的视觉任务，并将训练、离线监督和基准评估整合在同一接口下。
*   **关键技术细节**：
    *   **统一接口**：遵循 OpenAI Gym 标准，支持 `reset()` 和 `step()`。同时兼容单智能体、多智能体、离线数据集和评估基准。
    *   **可组合包装器（Wrappers）**：将观察设计（如规则说明、图像字幕、历史窗口长度）作为实验变量，无需修改环境逻辑即可调整智能体接收到的上下文。
    *   **分布式奖励服务（EaaS）**：针对生成式任务（如文生图），采用“评估即服务”架构，通过 Ray Serve 部署昂贵的奖励模型（如 CLIP, HPSv3），支持跨 GPU 的自动批处理和水平扩展。
    *   **环境分类**：涵盖 10 个类别，包括单轮推理（算法、几何、逻辑、拼图等）和多轮交互（游戏、空间导航、时序控制）。

#### 3. 实验设计
*   **数据集/场景**：共 179 个程序化生成的环境（105 个单轮，74 个多轮）。
*   **对比模型（Benchmark）**：评估了 9 个主流 VLM，包括 Gemini-3-Pro、Claude Sonnet 4.5、GPT-5.2 以及 Qwen 系列（Qwen3-VL, Qwen2.5-VL 等）。
*   **对比方法**：
    *   **RL 算法对比**：在统一预算下对比了 GRPO、GSPO 和 SAPO。
    *   **消融实验**：对比了有无历史上下文（MDP vs. 3/5 轮历史）、有无游戏规则提示、有无图像字幕（Image-only vs. Image+Caption）。
    *   **迁移学习**：进行了 6×6 的单轮跨领域迁移矩阵和 4×4 的多轮游戏迁移实验。

#### 4. 资源与算力
*   **算力说明**：论文提到使用了 `verl` 强化学习框架和 `Ray` 分布式框架。
*   **具体细节**：RL 训练以 Qwen2.5-VL-7B-Instruct 为基础模型。单轮任务每轮迭代采样 256 个实例，每个实例生成 8 个 rollout；多轮任务每轮采样 256 条完整轨迹，每集最多 200 步。
*   **缺失信息**：文中未明确给出具体的 GPU 型号（如 H100/A100）数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 179 个环境的零样本评估、16 个环境的 RL 算法对比、针对 4 个多轮游戏的深度消融实验，以及大规模的跨领域迁移矩阵。
*   **充分性与公平性**：实验设计非常系统，通过统一的超参数和计算预算对比不同 RL 算法，确保了公平性。消融实验覆盖了感知、逻辑和记忆等多个维度，验证了观察支架的决定性作用，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **观察支架胜过算法选择**：观察支架（如提供字幕和规则）对训练成功的影响远大于 RL 算法的选择。在某些任务中，没有规则或字幕会导致学习完全失败。
*   **模型能力差距**：最新的推理导向模型（如 Qwen3-VL-32B）在视觉推理上显著优于规模更大的旧模型（如 Qwen2.5-VL-72B），但与闭源顶尖模型（Gemini-3-Pro）仍有差距。
*   **迁移规律**：在多样化任务（如认知、拼图）上训练具有较好的泛化性，而在单一任务（如几何）上训练容易产生负迁移。
*   **多轮交互的放大效应**：多轮交互会放大感知错误和策略偏移，使得观察支架和课程设计变得更加关键。

#### 7. 优点
*   **高度统一性**：首次将视觉 RL 训练、离线学习和生成式评估整合进同一个 Gym 框架。
*   **实验可控性**：通过 Wrapper 设计，使研究者能够独立研究“观察呈现方式”这一变量。
*   **规模与多样性**：179 个环境远超同类视觉 Gym 平台，且支持程序化难度调节，避免了静态数据集的污染问题。

#### 8. 不足与局限
*   **时序任务挑战**：在复古街机游戏等时序控制任务中，当前所有 VLM 表现极差（奖励接近零），显示出模型在精细时间控制上的巨大缺陷。
*   **算力成本**：虽然提供了 EaaS，但大规模视觉 RL 训练对算力要求依然极高，文中主要实验集中在 7B 模型上。
*   **现实鸿沟**：环境多为程序化生成的网格或 3D 模拟场景，与真实世界复杂视觉场景的迁移能力尚待进一步验证。

（完）
