---
title: "VC-Soup: Value-Consistency Guided Multi-Value Alignment for Large Language Models"
title_zh: VC-Soup：基于价值一致性引导的大语言模型多价值对齐
authors: "Hefei Xu, Le Wu, Yu Wang, Min Hou, Han Wu, Zhen Zhang, Meng Wang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18113v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 利用奖励重加权和模型合并进行大语言模型多价值对齐
tldr: 针对大语言模型在多价值观对齐中面临的训练成本高和价值观冲突导致性能下降的问题，本文提出了VC-soup框架。该框架通过引入价值一致性指标过滤低质量偏好数据，训练出具有良好线性模式连接性的模型，并利用参数合并与帕累托过滤实现多价值观的均衡对齐，有效提升了对齐效果。
selection_source: fresh_fetch
motivation: 现有的多价值观对齐方法在处理价值观冲突时表现不佳，且为不同价值观组合重复训练模型的成本极高。
method: 提出一种基于奖励差距向量余弦相似度的价值一致性度量，通过过滤低一致性数据并结合线性模型合并与帕累托过滤来优化对齐。
result: 实验证明VC-soup能有效缓解价值观冲突，在多项指标上均优于现有的多价值观对齐方法。
conclusion: 通过价值一致性引导的数据筛选和模型合并，可以更高效且平衡地实现大语言模型的多价值观对齐。
---

## 摘要
随着大语言模型（LLMs）日益影响互联网上的内容生成、交互和决策，将其与人类价值对齐已成为可信人工智能的核心目标。当需要对齐多个且可能存在冲突的人类价值时，这一挑战变得更加突出。尽管最近的方法（如奖励重加权、基于提示的监督微调和模型合并）尝试解决多价值对齐问题，但它们仍面临两个主要局限性：（1）为每种价值组合训练单独的模型成本极高；（2）价值冲突会显著降低对齐性能。这些局限性使得在多种人类价值之间实现理想的权衡变得十分困难。为了应对这些挑战，我们从数据价值一致性的角度重新审视多价值对齐，并提出了 VC-soup，这是一个基于价值一致性学习的数据过滤和参数合并框架。我们首先设计了一种价值一致性指标，该指标基于每个偏好对的奖励差距向量与全 1 向量之间的余弦相似度，从而量化其跨价值的连贯性。然后，我们过滤掉每个价值数据集中一致性较低的偏好对，并利用剩余数据进行训练，以获得平滑且价值一致的策略模型，从而更好地保持线性模式连接性（linear mode connectivity）。最后，我们对这些策略进行线性组合，并在不同价值之间应用帕累托过滤（Pareto filtering），以获得具有平衡多价值性能的解决方案。广泛的实验和理论分析表明，VC-soup 有效地缓解了冲突，并始终优于现有的多价值对齐方法。

## Abstract
As large language models (LLMs) increasingly shape content generation, interaction, and decision-making across the Web, aligning them with human values has become a central objective in trustworthy AI. This challenge becomes even more pronounced when aligning multiple, potentially conflicting human values. Although recent approaches, such as reward reweighting, prompt-based supervised fine-tuning, and model merging, attempt to tackle multi-value alignment, they still face two major limitations: (1) training separate models for each value combination is prohibitively expensive; (2) value conflicts substantially degrade alignment performance. These limitations make it difficult to achieve favorable trade-offs across diverse human values. To address these challenges, we revisit multi-value alignment from the perspective of value consistency in data and propose VC-soup, a data filtering and parameter merging framework grounded in value-consistent learning. We first design a value consistency metric based on the cosine similarity between the reward-gap vector of each preference pair and an all-ones vector, which quantifies its cross-value coherence. We then filter out low-consistency preference pairs in each value dataset and train on the remaining data to obtain smooth, value-consistent policy models that better preserve linear mode connectivity. Finally, we linearly combine these policies and apply Pareto filtering across values to obtain solutions with balanced multi-value performance. Extensive experiments and theoretical analysis demonstrate that VC-soup effectively mitigates conflicts and consistently outperforms existing multi-value alignment methods.

---

## 论文详细总结（自动生成）

以下是对论文《VC-Soup: Value-Consistency Guided Multi-Value Alignment for Large Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在对齐人类价值观时，往往面临多个价值观（如“有用性”与“无害性”）之间的冲突。
*   **研究背景**：
    *   **现有局限**：传统的奖励重加权或多目标优化方法要么训练成本极高（需为每种权重组合重训），要么在模型合并（Model Merging）时因参数干扰导致性能大幅下降。
    *   **冲突根源**：作者发现，参数干扰主要源于训练数据中的“价值不一致性”——即某些样本在提升一个维度的同时会损害另一个维度，导致不同价值观模型的梯度方向相反，破坏了参数空间的线性模式连接性（Linear Mode Connectivity）。

### 2. 方法论
VC-soup 提出了一种基于“价值一致性”引导的数据过滤与参数合并框架，包含四个关键阶段：
*   **核心思想**：通过筛选出对所有目标价值观都有正向贡献的“一致性样本”来训练模型，从而确保生成的参数增量（Value Vectors）在方向上更接近，便于无损合并。
*   **关键技术细节**：
    1.  **价值一致性度量（VC Metric）**：计算每个偏好对在 $n$ 个价值观下的奖励差距（Reward Gap），构建差距向量 $g$。通过计算 $g$ 与全 1 向量的**余弦相似度**来量化一致性。得分接近 1 表示该样本同时符合所有价值观。
    2.  **一致性数据过滤**：设定阈值 $\tau$，从原始数据集中剔除低一致性（冲突）样本，构建一致性子集。
    3.  **训练价值一致性向量（VC Vectors）**：在过滤后的子集上使用 DPO（直接偏好优化）训练各价值观的专用模型，得到相对于基座模型的参数增量 $\theta_i$。
    4.  **参数合并与帕累托过滤**：将不同价值观的 VC 向量进行线性加权合并。通过在验证集上进行帕累托采样，选出最优的权衡模型集合。

### 3. 实验设计
*   **数据集**：
    *   **Anthropic-HH**：包含有用性（Helpfulness）和无害性（Harmlessness）的 160K 对话数据。
    *   **BeaverTails**：侧重于安全对齐，包含有用性与安全性（Safety）。
    *   **Honest Dataset**：用于扩展到三维价值观（诚实性）的实验。
*   **基准模型**：LLaMA2-7B。
*   **对比方法（Baselines）**：
    *   单维度 DPO。
    *   **SeqT**：顺序微调。
    *   **DPO-LW**：线性加权损失函数。
    *   **SOUP**：朴素的模型合并。
    *   **MODPO**：基于边际的多目标 DPO。
    *   **MVA**：基于价值解耦与外推的合并方法。
*   **评估指标**：奖励模型评分（Reward Scores）、GPT-4 胜率评估、帕累托前沿曲线。

### 4. 资源与算力
*   **硬件配置**：使用了 **8 台 NVIDIA RTX 5880 Ada GPU**。
*   **训练细节**：
    *   采用 **LoRA** 轻量化微调（Anthropic-HH 秩为 64，BeaverTails 秩为 16）。
    *   学习率 1e-5，Batch Size 为 2，DPO 温度参数 $\beta=0.1$。
    *   代码基于 HuggingFace 的 `trl` 库实现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了二维（有用 vs 无害/安全）和三维（有用 vs 无害 vs 诚实）价值观对齐实验。
*   **分析深度**：
    *   绘制了完整的**帕累托前沿曲线**，直观展示了不同方法在冲突价值间的权衡能力。
    *   进行了**参数级干扰分析**：计算了不同层级下 VC 向量间的 L2 距离和余弦相似度，证明了 VC-soup 训练出的模型在参数空间更接近。
    *   **消融实验**：分别验证了“仅过滤”、“仅合并”以及不同阈值 $\tau$ 对结果的影响。
*   **评价**：实验设计客观、公平，通过自动化指标（奖励模型）与人类偏好模拟（GPT-4）双重验证，充分证明了方法的有效性。

### 6. 主要结论与发现
*   **性能领先**：VC-soup 在所有数据集上的帕累托前沿均优于现有基准，能够以更低的数据量实现更好的多价值平衡。
*   **线性模式连接性**：通过过滤冲突数据，不同价值观模型的参数更新方向更加一致（余弦相似度更高），从而显著减少了合并时的性能损耗。
*   **数据效率**：实验表明，即使过滤掉部分数据，由于剩余数据质量更高且冲突更少，最终对齐效果反而提升。

### 7. 优点
*   **原理简洁且深刻**：从数据层面的价值冲突切入，解决了模型合并中参数干扰的本质问题。
*   **高效性**：训练完成后，用户可以通过调整合并权重 $\lambda$ 快速获得不同偏好的模型，无需重新训练。
*   **理论支撑**：提供了关于线性模式连接性的理论分析，证明了缩小向量间距可以降低合并后的损失上限。

### 8. 不足与局限
*   **阈值敏感性**：阈值 $\tau$ 的选择在很大程度上依赖于经验，不同数据集可能需要精细调优。
*   **奖励模型依赖**：数据过滤的效果高度依赖于预训练奖励模型的准确性，如果奖励模型本身存在偏差，过滤过程可能会失效。
*   **价值观规模**：虽然验证了三个价值观，但在处理更多维度（如 10 个以上）的极度复杂价值观体系时的扩展性仍有待验证。

（完）
