---
title: "TRUST-SQL: Tool-Integrated Multi-Turn Reinforcement Learning for Text-to-SQL over Unknown Schemas"
title_zh: TRUST-SQL：面向未知模式的工具集成多轮强化学习 Text-to-SQL 框架
authors: "Ai Jian, Xiaoyun Zhang, Wanrou Du, Jingqing Ruan, Jiangbo Pei, Weipeng Zhang, Ke Zeng, Xunliang Cai"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16448v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 用于Text-to-SQL自主智能体的多轮强化学习
tldr: 针对现实中数据库模式庞大且未知的挑战，本文提出TRUST-SQL框架。该框架将Text-to-SQL建模为部分可观测马尔可夫决策过程，通过四阶段协议引导智能体主动探索元数据。结合创新的双轨GRPO强化学习策略，利用令牌级掩码优势解决信用分配问题。实验表明，该方法在无需预加载模式的情况下，性能显著优于基础模型，并能媲美甚至超越依赖模式预填的强基准模型。
selection_source: fresh_fetch
motivation: 传统的Text-to-SQL研究通常假设已知完整模式，但在处理包含海量噪声元数据的真实企业级数据库时，这种假设难以成立。
method: 提出一种集成工具的四阶段协议，并采用双轨GRPO强化学习策略，通过隔离探索奖励与执行结果来优化模型训练。
result: "在五个基准测试中，TRUST-SQL使4B和8B模型分别获得30.6%和16.6%的绝对性能提升，且在未知模式下表现优于依赖模式预填的基准。"
conclusion: TRUST-SQL证明了通过结构化工具集成和改进的强化学习策略，智能体可以在不预知数据库模式的情况下实现高效且准确的SQL生成。
---

## 摘要
在全模式假设（Full Schema Assumption）下，Text-to-SQL 解析已取得了显著进展。然而，在现实世界的企业环境中，这一前提往往失效，因为数据库包含数百张表以及海量的噪声元数据。智能体必须主动识别并验证相关的子集，而不是预先注入完整的模式，这引出了本研究探讨的“未知模式”（Unknown Schema）场景。为此，我们提出了 TRUST-SQL（通过工具进行未知模式下的真实推理）。我们将该任务建模为部分可观测马尔可夫决策过程（POMDP），其中自主智能体采用结构化的四阶段协议，将推理建立在经过验证的元数据之上。至关重要的是，该协议为我们新颖的双轨 GRPO（Dual-Track GRPO）策略提供了结构化边界。通过应用 Token 级掩码优势（token-level masked advantages），该策略将探索奖励与执行结果隔离，以解决信用分配（credit assignment）问题，相比标准 GRPO 实现了 9.9% 的相对提升。在五个基准测试上的广泛实验表明，TRUST-SQL 的 4B 和 8B 变体相比其基础模型分别实现了 30.6% 和 16.6% 的平均绝对提升。值得注意的是，尽管在完全没有预加载元数据的情况下运行，我们的框架仍能持续匹配或超越依赖模式预填充（schema prefilling）的强基准模型。

## Abstract
Text-to-SQL parsing has achieved remarkable progress under the Full Schema Assumption. However, this premise fails in real-world enterprise environments where databases contain hundreds of tables with massive noisy metadata. Rather than injecting the full schema upfront, an agent must actively identify and verify only the relevant subset, giving rise to the Unknown Schema scenario we study in this work. To address this, we propose TRUST-SQL (Truthful Reasoning with Unknown Schema via Tools). We formulate the task as a Partially Observable Markov Decision Process where our autonomous agent employs a structured four-phase protocol to ground reasoning in verified metadata. Crucially, this protocol provides a structural boundary for our novel Dual-Track GRPO strategy. By applying token-level masked advantages, this strategy isolates exploration rewards from execution outcomes to resolve credit assignment, yielding a 9.9% relative improvement over standard GRPO. Extensive experiments across five benchmarks demonstrate that TRUST-SQL achieves an average absolute improvement of 30.6% and 16.6% for the 4B and 8B variants respectively over their base models. Remarkably, despite operating entirely without pre-loaded metadata, our framework consistently matches or surpasses strong baselines that rely on schema prefilling.

---

## 论文详细总结（自动生成）

### TRUST-SQL 论文深度总结

#### 1. 核心问题与研究动机
传统的 Text-to-SQL 研究大多基于**全模式假设（Full Schema Assumption）**，即假设数据库的所有表结构和元数据都能预先加载到模型的上下文窗口中。然而，在现实的企业级场景中，数据库往往包含数百张表和海量的噪声数据，直接注入完整模式会导致上下文溢出或引入严重干扰。
论文的核心问题在于：**如何在“未知模式”下，让智能体通过主动探索和工具调用，自主检索并验证必要的元数据，从而生成准确的 SQL？**

#### 2. 方法论：核心思想与技术细节
TRUST-SQL 将任务建模为**部分可观测马尔可夫决策过程（POMDP）**，核心包含两个关键设计：

*   **四阶段交互协议（EPGC）**：
    1.  **Explore（探索）**：调用工具查询数据库元数据（如表名、列名、采样数据）。
    2.  **Propose（提议）**：**强制性认知检查点**。要求智能体显式列出经过验证的模式子集，防止幻觉。
    3.  **Generate（生成）**：基于提议的模式生成候选 SQL 并观察执行反馈。
    4.  **Confirm（确认）**：提交最终 SQL 答案。
*   **双轨 GRPO 强化学习策略（Dual-Track GRPO）**：
    为了解决长轨迹中的**信用分配（Credit Assignment）**问题，该方法将训练分为两个独立轨道：
    *   **Schema Track**：仅针对“提议阶段”之前的 Token，根据检索到的模式准确度（Rschema）计算优势。
    *   **Full Track**：针对全轨迹 Token，根据最终 SQL 的执行结果（Rexec）和格式规范（Rfmt）计算优势。
    *   **Token 级掩码优势**：通过掩码技术，确保探索奖励只强化探索行为，执行奖励只强化生成逻辑，避免信号混淆。

#### 3. 实验设计与基准对比
*   **数据集**：涵盖了大规模模式落地的 **BIRD-Dev**，以及考察泛化性的 **Spider-Test**。此外，还引入了三个鲁棒性变体：**Spider-Syn**（同义词替换）、**Spider-DK**（领域知识）、**Spider-Realistic**（消除歧义），以及极具挑战性的 **Spider 2.0**。
*   **对比方法**：
    *   **单轮模型**：OmniSQL-7B, SQL-R1-3B/7B。
    *   **多轮强化学习模型**：MTIR-SQL, SQL-Trail。
    *   **注意**：所有基准模型均在“全模式预填”的优待条件下运行，而 TRUST-SQL 在完全不预知模式的情况下与其竞争。

#### 4. 资源与算力消耗
论文在附录 B 中详细列出了算力需求：
*   **模型规模**：基于 Qwen3-4B 和 Qwen3-8B。
*   **硬件**：使用 **NVIDIA A100 GPU**。
*   **训练时长**：
    *   **SFT 阶段**：16 张 A100，耗时约 6.5 - 12 小时。
    *   **RL 阶段**：8 - 32 张 A100，采用同步或异步模式，耗时约 40 - 60 小时。
*   **数据量**：SFT 阶段约 9.2k 样本，RL 阶段约 11.6k 样本。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度评估**：在 5 个主流基准和 1 个企业级基准（Spider 2.0）上进行了测试。
*   **消融实验**：深入探讨了双轨损失权重 $\lambda$ 的影响、模式奖励的耦合方式、SFT 预热的必要性。
*   **测试时缩放分析**：分析了训练/推理轮数预算对性能的影响，以及 Pass@K 的扩展性。
*   **客观性**：通过与拥有“上帝视角”（预填模式）的基准模型对比，证明了其主动探索策略的优越性。

#### 6. 主要结论与发现
*   **性能飞跃**：TRUST-SQL 使 4B 模型在未知模式下的性能提升了 30.6%，8B 模型提升了 16.6%。
*   **超越预填模式**：即使不给模型任何预先信息，TRUST-SQL 的表现依然能匹配甚至超过那些预先加载了完整模式的强基准模型。
*   **抑制幻觉**：引入“Propose”阶段后，幻觉错误率从 26.4% 骤降至 2.8%。
*   **双轨优化的有效性**：相比标准 GRPO，双轨策略使执行准确度相对提升了 9.9%。

#### 7. 优点与亮点
*   **现实意义强**：直接挑战“未知模式”这一工业界痛点，而非实验室理想环境。
*   **算法创新**：双轨 GRPO 巧妙地解决了多轮对话中不同任务（探索 vs 生成）的奖励归因难题。
*   **高效性**：相比于 CHESS 等依赖长上下文的框架，TRUST-SQL 的推理延迟降低了 500 倍，Token 消耗降低了 113 倍。

#### 8. 不足与局限
*   **推理开销**：多轮交互自然比单轮翻译带来更高的推理延迟和 API 调用成本。
*   **方言限制**：目前主要针对 SQLite 进行优化和评估，尚未广泛测试 PostgreSQL 或 MySQL 等其他方言。
*   **固定轮数预算**：训练时使用了固定的最大轮数（如 10 轮），对于极度复杂的数据库，可能需要更动态的轮数分配机制。

（完）
