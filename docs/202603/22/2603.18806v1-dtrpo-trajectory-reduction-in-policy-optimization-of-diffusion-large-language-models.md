---
title: "dTRPO: Trajectory Reduction in Policy Optimization of Diffusion Large Language Models"
title_zh: dTRPO：扩散大语言模型策略优化中的轨迹缩减
authors: "Wenxuan Zhang, Lemeng Wu, Changsheng Zhao, Ernie Chang, Mingchen Zhuge, Zechun Liu, Andy Su, Hanxian Huang, Jun Chen, Chong Zhou, Raghuraman Krishnamoorthi, Vikas Chandra, Mohamed Elhoseiny, Wei Wen"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18806v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 扩散语言模型的策略优化与对齐
tldr: 针对扩散大语言模型（dLLMs）在对齐人类偏好时轨迹概率计算成本高的问题，本文提出了dTRPO。该方法通过理论证明，利用新去掩码标记的概率比作为中间状态的无偏估计，并结合重掩码状态的单次前向传播来估算全轨迹概率。实验表明，dTRPO在7B模型上显著提升了推理、编程和指令遵循能力，同时大幅提高了训练和生成效率。
selection_source: fresh_fetch
motivation: 旨在降低扩散大语言模型在策略优化过程中计算全轨迹概率的高昂成本，以实现大规模离线训练。
method: 提出dTRPO，通过新去掩码标记的概率比和重掩码状态的单次前向传播来简化轨迹概率的估算。
result: "在7B模型上，STEM任务提升达9.6%，编程提升4.3%，指令遵循提升3.0%，且训练效率显著增强。"
conclusion: dTRPO通过轨迹缩减策略有效解决了dLLMs的对齐难题，在提升模型性能的同时优化了计算效率。
---

## 摘要
扩散大语言模型 (dLLMs) 为语言生成引入了一种新范式，这反过来也为将其与人类偏好对齐带来了新挑战。在这项工作中，我们旨在通过降低轨迹概率计算的成本来改进 dLLMs 的策略优化，从而实现大规模离线策略训练。我们证明了：(i) 在参考策略正则化下，新去掩码 (unmasked) token 的概率比是中间扩散状态概率比的无偏估计；(ii) 完整轨迹的概率可以通过对重新掩码 (re-masked) 的最终状态进行单次前向传递来有效估计。通过将这两种轨迹缩减策略整合到策略优化目标中，我们提出了轨迹缩减策略优化 (dTRPO)。我们在指令遵循和推理基准测试中对 7B 参数规模的 dLLMs 评估了 dTRPO。结果表明，它显著提升了最先进 dLLMs 的核心性能，在 STEM 任务上获得了高达 9.6% 的提升，在编程任务上提升高达 4.3%，在指令遵循任务上提升高达 3.0%。此外，由于其离线和单次前向传递的特性，dTRPO 表现出强大的训练效率，并通过高质量的输出实现了生成效率的提升。

## Abstract
Diffusion Large Language Models (dLLMs) introduce a new paradigm for language generation, which in turn presents new challenges for aligning them with human preferences. In this work, we aim to improve the policy optimization for dLLMs by reducing the cost of the trajectory probability calculation, thereby enabling scaled-up offline policy training. We prove that: (i) under reference policy regularization, the probability ratio of the newly unmasked tokens is an unbiased estimate of that of intermediate diffusion states, and (ii) the probability of the full trajectory can be effectively estimated with a single forward pass of a re-masked final state. By integrating these two trajectory reduction strategies into a policy optimization objective, we propose Trajectory Reduction Policy Optimization (dTRPO). We evaluate dTRPO on 7B dLLMs across instruction-following and reasoning benchmarks. Results show that it substantially improves the core performance of state-of-the-art dLLMs, achieving gains of up to 9.6% on STEM tasks, up to 4.3% on coding tasks, and up to 3.0% on instruction-following tasks. Moreover, dTRPO exhibits strong training efficiency due to its offline, single-forward nature, and achieves improved generation efficiency through high-quality outputs.

---

## 论文详细总结（自动生成）

以下是对论文《dTRPO: Trajectory Reduction in Policy Optimization of Diffusion Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：扩散大语言模型（dLLMs）在生成文本时采用多步去噪过程，这使得计算生成轨迹的概率变得极其复杂且昂贵。与自回归模型（ARMs）可以通过单次前向传播计算序列概率不同，dLLMs 通常需要多次前向传播来估计轨迹概率，这严重阻碍了其在大规模离线偏好对齐（如 DPO）中的应用。
*   **研究动机**：旨在通过“轨迹缩减”（Trajectory Reduction）技术，降低 dLLMs 策略优化的计算成本，使其能够像 ARMs 一样高效地进行离线后训练（Post-training），从而提升其指令遵循和推理能力。

### 2. 方法论：核心思想与关键技术
dTRPO 的核心是通过理论证明简化了轨迹概率比的计算，主要包含两个关键策略：
*   **状态缩减（State Reduction）**：证明了 dLLMs 的马尔可夫决策过程（MDP）轨迹对数概率可以通过在每个块（Block）中随机采样一个时间步来做无偏估计。这使得计算量从 $T$ 次前向传播减少到 $N_B$ 次（块的数量）。
*   **比例缩减（Ratio Reduction）**：证明了在参考策略正则化（如 DPO）下，轨迹概率比中的调度相关系数（Schedule-dependent coefficients）会相互抵消。最终，概率比仅取决于**新去掩码标记（Newly unmasked tokens）**的分类概率比。
*   **关键技术细节**：
    *   **块注意力（Block Attention）**：设计了一种特殊的注意力掩码，允许在单次前向传播中同时模拟多个块的中间扩散状态，进一步将计算成本压缩至与 ARM 的 DPO 相当。
    *   **推理对齐调度**：在训练时采用与推理时一致的置信度调度策略（Top-k 策略），确保训练目标与生成动态匹配。
    *   **算法流程**：采样时间步 -> 构建部分掩码输入 -> 单次前向传播获取 Logits -> 计算新去掩码位置的概率比 -> 应用 DPO 损失函数更新参数。

### 3. 实验设计
*   **基准模型**：以 Fast-dLLM-v2-7B 为主干（基于 Qwen2.5-7B-Instruct 适配），同时也验证了在 LLaDA 等长块模型上的效果。
*   **数据集**：使用了约 500K 对偏好数据，涵盖 SmolTalk2（指令遵循）、数学偏好数据和代码偏好数据。
*   **对比方法**：
    *   **开源 dLLMs**：LLaDA-Instruct, LLaDA 1.5, Dream-Instruct, Fast-dLLM-v2。
    *   **训练变体**：ELBO（基于证据下界的 SFT）、VRPO（方差缩减偏好优化）、DPO w/ Mean-Field（均值场近似）。
*   **评估指标**：涵盖 STEM 推理（GPQA, GSM8K, MATH）、代码生成（MBPP+, LCBv6, HumanEval+）和指令遵循（IFEval, ArenaHard, MT-Bench）。

### 4. 资源与算力
*   **硬件**：使用了 **64 台 NVIDIA A100 (80GB) GPU**。
*   **训练规模**：总训练时长约为 **5 小时**。
*   **参数设置**：Batch Size 为 2，梯度累积为 8，上下文长度 4096。采用参数高效微调（PEFT），仅更新 MLP 层和输出投影层。

### 5. 实验数量与充分性
*   **实验规模**：论文在 10 多个主流基准测试上进行了零样本（Zero-shot）评估，并针对 LLaDA 架构进行了迁移验证。
*   **消融实验**：非常充分。涵盖了去掩码策略（Random-k vs Top-k）、每块采样步数、块大小（k 值）、投影函数（DPO, IPO, RSO, APO）以及参数更新策略的对比。
*   **客观性**：通过与多种 SOTA 扩散模型及不同概率估计方法的直接对比，证明了 dTRPO 在性能和效率上的双重优势，实验设计较为公平。

### 6. 主要结论与发现
*   **性能显著提升**：dTRPO 在 7B 模型上相比基座模型，在 STEM 任务上提升高达 9.6%，代码任务提升 4.3%，指令遵循提升 3.0%。
*   **缩小与 ARM 差距**：dTRPO 有效缩小了 dLLMs 与强力自回归模型（如 Qwen2.5-Instruct）之间的性能鸿沟，同时保持了扩散模型特有的并行生成效率。
*   **训练效率极高**：其离线训练成本与 ARMs 的 DPO 处于同一数量级，远低于需要在线 Rollout 的强化学习方法（如 TRaDO 或 d1）。
*   **推理速度优势**：由于生成质量提高，减少了冗余生成，dTRPO 在 Arena-Hard 等测试中表现出比基座模型更短的端到端推理时间。

### 7. 优点
*   **理论扎实**：通过严格的数学证明（状态缩减和比例缩减）简化了复杂的扩散轨迹计算，而非纯启发式改进。
*   **极高效率**：单次前向传播即可完成概率比估计，解决了扩散模型训练慢的核心痛点。
*   **通用性强**：不仅适用于块状扩散模型（Fast-dLLM），也适用于长块扩散模型（LLaDA）。
*   **推理对齐**：通过将训练过程与推理时的 Top-k 采样逻辑对齐，提升了模型在实际部署中的表现。

### 8. 不足与局限
*   **绝对性能仍有差距**：尽管大幅提升了 dLLM 的能力，但在 GPQA 和 MATH 等极高难度任务上，距离顶尖的自回归模型（ARMs）仍有一定距离。
*   **依赖高质量偏好数据**：作为一种离线对齐方法，其上限受限于偏好数据集的质量和覆盖范围。
*   **架构限制**：虽然验证了两种架构，但主要实验集中在基于块（Block-wise）的扩散模型上，对于其他变体扩散架构的适用性仍待进一步探索。

（完）
