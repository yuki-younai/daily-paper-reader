---
title: Advancing Multimodal Agent Reasoning with Long-Term Neuro-Symbolic Memory
title_zh: 利用长期神经符号记忆提升多模态智能体推理能力
authors: "Rongjie Jiang, Jianwei Wang, Gengda Zhao, Chengyang Luo, Kai Wang, Wenjie Zhang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15280v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 结合神经符号记忆的多模态智能体推理
tldr: 针对多模态智能体在长期推理中依赖神经记忆而缺乏演绎推理能力的问题，本文提出NS-Mem框架。该框架结合了神经表示与显式符号结构，通过三层记忆架构（情节、语义、逻辑规则）、SK-Gen知识整合机制以及混合检索方式，显著提升了智能体在复杂决策中的分析推理能力，在多项基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有的多模态智能体记忆多依赖神经向量检索，难以支持现实决策中至关重要的分析性演绎推理。
method: 提出NS-Mem框架，通过三层记忆架构整合情节、语义与逻辑规则，并利用SK-Gen机制实现知识的自动整合与混合检索。
result: "在真实多模态推理基准测试中，NS-Mem比纯神经记忆系统平均准确率提升4.35%，在受限推理任务中提升达12.5%。"
conclusion: 结合神经与符号的长期记忆系统能有效增强多模态智能体的结构化推理能力，是提升复杂决策性能的关键。
---

## 摘要
大语言模型的最新进展推动了在开放世界、多模态环境中运行的智能体的出现。为了支持长期推理，此类智能体通常配备有外部记忆系统。然而，大多数现有的多模态智能体记忆主要依赖于神经表示和基于向量的检索，这虽然适用于归纳式、直觉式推理，但在支持对现实世界决策至关重要的分析式、演绎式推理方面存在根本局限。为了解决这一局限性，我们提出了 NS-Mem，这是一个长期神经符号记忆框架，旨在通过将神经记忆与显式符号结构和规则相结合，来提升多模态智能体的推理能力。具体而言，NS-Mem 围绕记忆系统的三个核心组件运行：(1) 由情景层、语义层和逻辑规则层组成的三层记忆架构；(2) 由 SK-Gen 实现的记忆构建与维护机制，该机制能从积累的多模态经验中自动整合结构化知识，并增量更新神经表示和符号规则；(3) 一种混合记忆检索机制，结合了基于相似性的搜索与确定性符号查询函数，以支持结构化推理。在现实世界多模态推理基准测试上的实验表明，神经符号记忆在整体推理准确率上比纯神经记忆系统平均提高了 4.35%，在受限推理查询上的增益高达 12.5%，验证了 NS-Mem 的有效性。

## Abstract
Recent advances in large language models have driven the emergence of intelligent agents operating in open-world, multimodal environments. To support long-term reasoning, such agents are typically equipped with external memory systems. However, most existing multimodal agent memories rely primarily on neural representations and vector-based retrieval, which are well-suited for inductive, intuitive reasoning but fundamentally limited in supporting analytical, deductive reasoning critical for real-world decision making. To address this limitation, we propose NS-Mem, a long-term neuro-symbolic memory framework designed to advance multimodal agent reasoning by integrating neural memory with explicit symbolic structures and rules. Specifically, NS-Mem is operated around three core components of a memory system: (1) a three-layer memory architecture that consists episodic layer, semantic layer and logic rule layer, (2) a memory construction and maintenance mechanism implemented by SK-Gen that automatically consolidates structured knowledge from accumulated multimodal experiences and incrementally updates both neural representations and symbolic rules, and (3) a hybrid memory retrieval mechanism that combines similarity-based search with deterministic symbolic query functions to support structured reasoning. Experiments on real-world multimodal reasoning benchmarks demonstrate that Neural-Symbolic Memory achieves an average 4.35% improvement in overall reasoning accuracy over pure neural memory systems, with gains of up to 12.5% on constrained reasoning queries, validating the effectiveness of NS-Mem.

---

## 论文详细总结（自动生成）

### 论文总结：利用长期神经符号记忆提升多模态智能体推理能力 (NS-Mem)

#### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态智能体记忆系统主要依赖于**神经表示（向量嵌入）和相似性检索**。这种方式类似于人类思维的“系统 1”（直觉、感性），擅长模糊匹配和常识检索，但在处理需要**演绎推理、约束满足和复杂逻辑依赖**的“系统 2”（理性、分析）任务时表现欠缺。
*   **研究动机**：在现实决策中（如：因工具损坏需寻找替代方案），单纯依靠语义相似性往往会忽略逻辑约束。因此，需要一种能将神经记忆的灵活性与符号逻辑的确定性相结合的长期记忆框架。

#### 2. 方法论：NS-Mem 框架
NS-Mem 整合了神经感知与符号逻辑，包含三个核心组件：
*   **三层记忆架构**：
    1.  **情节层 (Episodic Layer)**：存储带时间戳的细粒度多模态观察（事件描述、原始嵌入）。
    2.  **语义层 (Semantic Layer)**：维护实体属性和抽象知识，通过强化/削弱机制实现知识一致性。
    3.  **逻辑层 (Logic Layer)**：核心创新点。每个逻辑节点包含**索引向量**（用于神经检索）和**程序化有向无环图 (Procedural DAG)**（用于符号查询）。
*   **SK-Gen 自动构建与维护机制**：
    *   **提取与挖掘**：从情节记忆中提取动作序列，利用 **PrefixSpan** 算法挖掘频繁模式，并通过 LLM 过滤生成逻辑节点。
    *   **增量更新**：使用**指数移动平均 (EMA)** 更新神经索引向量，以应对语义漂移；通过**转移统计**更新 DAG 的边权重，并提供后验一致性证明。
    *   **知识融合**：将来自不同视频的相同流程 DAG 进行对齐和合并，形成多路径的复杂逻辑图。
*   **混合检索与推理**：
    *   **查询分类**：将查询分为事实型、约束型和角色型。
    *   **多粒度检索**：结合目标级（Goal）和步骤级（Step）的相似度匹配。
    *   **符号增强**：在检索到逻辑节点后，直接在 DAG 上执行确定性算法（如路径枚举、约束过滤），而非仅依赖 LLM 生成。

#### 3. 实验设计
*   **数据集**：使用 **M3-Bench** 基准测试，包含两个子集：
    *   **M3-Bench-robot**：100 个机器人视角视频。
    *   **M3-Bench-web**：920 个网络来源视频。
*   **评估指标**：使用 GPT-4o 作为裁判，评估推理准确率（Accuracy）。
*   **对比方法**：
    *   **苏格拉底模型**：Qwen2.5-VL/Omni, Gemini-1.5-Pro, GPT-4o（无显式记忆）。
    *   **在线视频理解模型**：MovieChat, MA-LMM, Flash-VStream。
    *   **智能体方法**：M3-Agent（目前最先进的纯向量记忆智能体）。

#### 4. 资源与算力
*   **硬件环境**：服务器配备 Intel Xeon Silver 4314 CPU，512GB 内存。
*   **GPU 型号**：使用 **NVIDIA RTX A5000** GPU。
*   **训练/运行细节**：文中未明确给出具体的训练时长，但提到系统支持在线增量更新，且逻辑层维护复杂度为 $O(d \log |N|)$，具有较高的运行效率。

#### 5. 实验数量与充分性
*   **实验规模**：在 Robot 子集（703 个问题）和 Web 子集（2066 个问题）上进行了大规模测试。
*   **实验类型**：
    *   **总体性能对比**：涵盖 5 种推理类型（多细节、多跳、跨模态等）。
    *   **消融实验**：分别验证了神经组件、符号组件及 DAG 结构的贡献。
    *   **维护性评估**：对比了静态图与动态增量更新图的性能。
    *   **超参数分析**：对验证阈值 $\tau$、门控阈值 $\delta$ 和检索权重 $\alpha$ 进行了敏感度分析。
*   **评价**：实验设计全面，涵盖了准确率、效率（对话轮数、耗时）和鲁棒性分析，对比基准包含了当前最强的闭源和开源模型，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **准确率提升**：NS-Mem 在总体准确率上比 M3-Agent 提升了 **4.35%**。
*   **复杂推理优势明显**：在**程序化查询**和**受限查询**任务中，准确率提升高达 **12.5%**（相对提升 50%）。
*   **效率优化**：由于符号函数能一次性返回完整步骤，减少了智能体与环境/记忆的交互轮数（Robot 场景减少 15.8%），降低了推理耗时。
*   **增量更新有效**：动态维护的记忆系统在准确率和效率上均优于静态系统。

#### 7. 优点与亮点
*   **架构创新**：成功将神经检索的“模糊性”与符号逻辑的“确定性”在多模态记忆系统中统一。
*   **理论支撑**：为增量更新的后验一致性和符号查询的确定性提供了数学证明。
*   **实用性强**：SK-Gen 机制实现了从原始视频流到结构化逻辑图的自动化转化，无需人工干预。
*   **解决长程依赖**：通过逻辑层抽象，使智能体能跨越超长视频序列理解复杂的因果和步骤关系。

#### 8. 不足与局限
*   **计算开销**：虽然检索高效，但前期使用 VLM 和 LLM 进行知识蒸馏（Distillation）和过滤的计算成本仍然较高。
*   **LLM 依赖**：逻辑节点的初步生成和查询分类仍依赖 LLM，若 LLM 在提取动作序列时出错，可能会导致符号结构的污染。
*   **应用范围**：目前主要针对具有明显步骤性（Procedural）的任务，对于完全非结构化、无规律的开放式对话提升可能有限。
*   **评估偏差**：依赖 GPT-4o 作为裁判可能存在模型偏见（LLM-as-a-judge 的固有风险）。

（完）
