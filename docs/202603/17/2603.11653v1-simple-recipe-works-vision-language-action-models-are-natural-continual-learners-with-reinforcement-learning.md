---
title: "Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning"
title_zh: 简单方案行之有效：视觉-语言-动作模型是结合强化学习的天然持续学习者
authors: "Jiaheng Hu, Jay Shim, Chen Tang, Yoonchang Sung, Bo Liu, Peter Stone, Roberto Martin-Martin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11653v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 视觉-语言-动作模型的持续强化学习
tldr: 本研究探讨了视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现。尽管传统观点认为顺序微调会导致灾难性遗忘，但实验发现，结合低秩自适应（LoRA）的简单顺序微调在大型预训练VLA模型上表现极佳。该方法不仅具有高塑性，且几乎没有遗忘，在多个基准测试中优于复杂的CRL策略，为构建可自我改进的具身智能体提供了高效简洁的新范式。
selection_source: fresh_fetch
motivation: 旨在挑战顺序微调必然导致灾难性遗忘的传统认知，探索大型预训练VLA模型在持续强化学习中的潜力。
method: 采用基于低秩自适应（LoRA）的简单顺序微调方法，在五个挑战性的终身强化学习基准上对三种VLA模型进行系统评估。
result: 实验证明简单的顺序微调在保持高塑性的同时几乎不产生遗忘，且保留了强大的零样本泛化能力，表现优于复杂的持续学习算法。
conclusion: 大型预训练模型、参数高效微调与在线强化学习的协同作用，使得VLA模型成为天然的持续学习者，重新定义了稳定性与塑性的权衡。
---

## 摘要
针对视觉-语言-动作（VLA）模型的持续强化学习（CRL）是实现能够在开放式、演化环境中进行适应的自我改进具身智能体的一个极具前景的方向。然而，持续学习的传统观点认为，朴素的顺序微调（Seq. FT）会导致灾难性遗忘，因此需要复杂的 CRL 策略。在这项工作中，我们退后一步，对三种模型和五个具有挑战性的终身强化学习基准上的大型预训练 VLA 的 CRL 进行了系统研究。我们发现，与既有观点相反，结合低秩自适应（LoRA）的简单顺序微调表现异常强劲：它实现了高可塑性，几乎没有表现出遗忘，并保持了强大的零样本泛化能力，经常优于更复杂的 CRL 方法。通过详细分析，我们表明这种鲁棒性源于大型预训练模型、参数高效自适应和同策略（on-policy）强化学习之间的协同作用。这些组件共同重塑了稳定性-可塑性权衡，使持续适应既稳定又具可扩展性。我们的结果将顺序微调定位为 VLA 持续强化学习的一种强大方法，并为大模型时代的终身学习提供了新的见解。代码可在 github.com/UT-Austin-RobIn/continual-vla-rl 获取。

## Abstract
Continual Reinforcement Learning (CRL) for Vision-Language-Action (VLA) models is a promising direction toward self-improving embodied agents that can adapt in openended, evolving environments. However, conventional wisdom from continual learning suggests that naive Sequential Fine-Tuning (Seq. FT) leads to catastrophic forgetting, necessitating complex CRL strategies. In this work, we take a step back and conduct a systematic study of CRL for large pretrained VLAs across three models and five challenging lifelong RL benchmarks. We find that, contrary to established belief, simple Seq. FT with low-rank adaptation (LoRA) is remarkably strong: it achieves high plasticity, exhibits little to no forgetting, and retains strong zero-shot generalization, frequently outperforming more sophisticated CRL methods. Through detailed analysis, we show that this robustness arises from a synergy between the large pretrained model, parameter-efficient adaptation, and on-policy RL. Together, these components reshape the stability-plasticity trade-off, making continual adaptation both stable and scalable. Our results position Sequential Fine-Tuning as a powerful method for continual RL with VLAs and provide new insights into lifelong learning in the large model era. Code is available at github.com/UT-Austin-RobIn/continual-vla-rl.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：Simple Recipe Works

#### 1. 论文的核心问题与整体含义（研究动机和背景）
本文探讨了**视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现**。
*   **研究动机**：虽然大型 VLA 模型在通用机器人控制方面表现出色，但在面对不断演化的新环境时仍显脆弱。传统的持续学习观点认为，简单的“顺序微调”（Sequential Fine-Tuning, Seq. FT）会导致严重的“灾难性遗忘”，因此学术界开发了大量复杂的 CRL 算法（如正则化、重放、参数隔离等）。
*   **核心问题**：在大模型时代，这些复杂的 CRL 策略是否依然必要？大型预训练 VLA 模型是否具备某些天然的特性，使得简单的方案就能奏效？

#### 2. 论文提出的方法论
论文提出了一种极其简单的方案：**基于 LoRA 的顺序微调（Seq. FT with LoRA）结合同策略强化学习（On-policy RL）**。
*   **核心思想**：不需要任何额外的遗忘防止机制，直接在任务流上按顺序进行强化学习微调。
*   **关键技术细节**：
    *   **同策略 RL 算法**：采用 **GRPO**（Group Relative Policy Optimization），这是一种稳定且高效的策略梯度方法，通过组内相对奖励来估计优势函数。
    *   **参数高效微调**：使用 **LoRA**（低秩自适应），仅更新极小比例的参数，保持主干权重冻结。
    *   **协同效应分析**：论文通过实验证明，**大型预训练模型（高维零空间）**、**LoRA（限制更新几何结构）**和**同策略 RL（隐式 KL 正则化）**三者结合，共同重塑了稳定性与塑性的权衡，使得模型在学习新任务时几乎不干扰旧知识。

#### 3. 实验设计
*   **数据集/场景**：涵盖了 5 个具有挑战性的终身强化学习基准：
    1.  **LIBERO-Spatial / Object / Long-Horizon**：机器人操纵任务，侧重知识迁移。
    2.  **RoboCasa**：复杂的厨房场景任务。
    3.  **ManiSkill**：基于物理引擎的物体放置任务。
*   **对比方法（Baselines）**：
    *   **参考基准**：顺序微调（Seq. FT）、多任务训练（Oracle）。
    *   **经典 CRL 算法**：EWC（正则化）、Expert Replay（专家重放）、Dark Experience Replay（DER，逻辑重放）、Dynamic Weight Expansion（DWE，参数隔离）。
    *   **大模型适配方法**：SLCA（层级学习率）、RETAIN（权重合并）。
*   **评估指标**：平均成功率（AVG）、负向后向迁移（NBT，即遗忘率）、前向迁移（FWT）、零样本成功率（ZS）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确给出具体的 GPU 型号和总训练时长**。
*   **规模推测**：实验涉及 7B 参数规模的模型（如 OpenVLA），且每个任务训练 10,240 个 episode，并在 3 个随机种子下运行。这表明实验是在大规模计算集群上完成的。论文致谢中提到了 UT Austin 机器学习实验室和 RLinf 团队提供的基础设施支持。

#### 5. 实验数量与充分性
*   **实验规模**：非常充分。
    *   **多模型验证**：测试了 OpenVLA-OFT、Pi-0 和原始 OpenVLA 三种架构。
    *   **鲁棒性测试**：引入了相机角度、光照条件、机器人初始位置的扰动，以及任务顺序的排列组合。
    *   **消融实验**：系统地移除了 RL、LoRA 或大模型组件，验证了三者缺一不可。
*   **客观性与公平性**：所有方法共享核心超参数（学习率、Batch Size 等），并对基线方法的特定超参数进行了局部搜索以确保对比公平。

#### 6. 论文的主要结论与发现
1.  **简单方案胜出**：在 VLA 模型上，简单的顺序微调（Seq. FT + LoRA）在几乎所有基准测试中都表现优异，不仅塑性高，且遗忘率极低（通常 < 2%）。
2.  **超越复杂算法**：现有的 CRL 算法（如 EWC、RETAIN）由于引入了额外的约束，反而损害了模型学习新任务的“塑性”，导致最终性能不如简单的顺序微调。
3.  **零样本能力保持**：顺序微调能够很好地保留甚至增强预训练模型的零样本泛化能力，表现往往优于多任务训练。
4.  **机制解释**：
    *   **RL 的作用**：同策略数据避免了 SFT 中常见的概率质量偏移，起到隐式正则化作用。
    *   **大模型的作用**：高维空间中梯度方向趋于正交，减少了干扰（Fisher 信息分析证实了这一点）。
    *   **LoRA 的作用**：限制了每层权重的结构性剧变。

#### 7. 优点
*   **挑战直觉**：有力地反驳了“顺序微调必然导致灾难性遗忘”的传统教条，为大模型时代的持续学习提供了新视角。
*   **实用性极强**：提出的方案不需要存储旧数据或增加模型参数，极易于在大规模具身智能系统中部署。
*   **理论与实验结合**：不仅展示了结果，还通过 Fisher 信息、信息论（RL 的低比特需求）等角度给出了深刻的机理解释。

#### 8. 不足与局限
*   **任务多样性限制**：虽然使用了多个基准，但任务类型仍集中在机械臂抓取和放置，缺乏更长程、更复杂的逻辑推理任务。
*   **Sim-to-Real 缺失**：所有实验均在仿真环境中完成，尚未在真实物理机器人上验证环境非平稳性带来的挑战。
*   **性能差距**：虽然顺序微调表现强劲，但在某些任务上与“多任务训练（Oracle）”仍存在约 5% 的差距（尽管论文提到可以通过增加训练步数弥补）。

（完）
