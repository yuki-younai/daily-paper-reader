---
title: "Learning to Negotiate: Multi-Agent Deliberation for Collective Value Alignment in LLMs"
title_zh: 学习协商：大语言模型中集体价值对齐的多智能体审议
authors: "Panatchakorn Anantaprayoon, Nataliia Babina, Nima Asgharbeygi, Jad Tarifi"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10476v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 用于LLM集体价值对齐的多智能体协商
tldr: 针对大语言模型在多利益相关者冲突场景下对齐不足的问题，本文提出一种基于多智能体协商的对齐框架。该方法通过让同一模型的两个不同人格实例进行自我博弈和结构化对话，利用RLAIF和GRPO算法优化模型，使其对齐于“集体能动性（CA）”目标。研究表明，该框架在提升冲突解决能力的同时，保持了通用的语言能力，为处理价值冲突提供了新路径。
selection_source: fresh_fetch
motivation: 现有的单智能体对齐方法（如RLHF）难以处理多利益相关者之间的价值冲突，缺乏必要的协商和审议能力。
method: 通过自我博弈让模型扮演对立角色进行结构化对话，并利用GRPO算法基于集体能动性评分对对话过程进行强化学习优化。
result: 该模型在集体能动性对齐上达到了基准水平，并显著增强了冲突解决能力，同时保持了良好的通用语言性能。
conclusion: 协商驱动的审议训练为开发能够支持复杂价值冲突下集体决策的大语言模型提供了一条切实可行的路径。
---

## 摘要
大语言模型（LLMs）的对齐在单智能体设置中通过 RLHF 和宪法 AI（Constitutional AI）等范式取得了实质性进展，近期研究还探索了 RLAIF 等可扩展的替代方案以及不断演进的对齐目标。然而，这些方法在多利益相关者场景中仍然受限，因为此类场景会出现价值观冲突，且需要审议式协商能力。本研究提出了一种基于多智能体协商的对齐框架，该框架将 LLMs 与集体代理（Collective Agency, CA）进行对齐——这是一种旨在促进代理能力持续扩展的现有对齐目标——同时提升冲突解决能力。为了实现可扩展训练，同一 LLM 的两个自我博弈（self-play）实例被赋予对立的人格，通过结构化的轮次对话来合成互利的解决方案。我们生成了合成的道德困境提示词和冲突的人格对，并利用外部 LLM 奖励模型，通过 GRPO 算法以 RLAIF 方式优化策略。虽然奖励是根据最终生成内容的 CA 评分计算的，但梯度被应用于对话标记（tokens），以直接改善审议式交互动态。实验表明，所得模型在实现与单智能体基准相当的 CA 对齐的同时，显著提升了冲突解决性能，且未降低通用语言能力。这些结果表明，协商驱动的审议训练为开发能更好地支持价值冲突场景下集体决策的 LLMs 提供了一条切实可行的路径。

## Abstract
The alignment of large language models (LLMs) has progressed substantially in single-agent settings through paradigms such as RLHF and Constitutional AI, with recent work exploring scalable alternatives such as RLAIF and evolving alignment objectives. However, these approaches remain limited in multi-stakeholder settings, where conflicting values arise and deliberative negotiation capabilities are required. This work proposes a multi-agent negotiation-based alignment framework that aligns LLMs to Collective Agency (CA)-an existing alignment objective introduced to promote the continual expansion of agency-while simultaneously improving conflict-resolution capability. To enable scalable training, two self-play instances of the same LLM, assigned opposing personas, engage in structured turn-based dialogue to synthesize mutually beneficial solutions. We generate synthetic moral-dilemma prompts and conflicting persona pairs, and optimize the policy via RLAIF using GRPO with an external LLM reward model. While rewards are computed from CA scores assigned to the final completion, gradients are applied to dialogue tokens to directly improve deliberative interaction dynamics. Experiments show that the resulting model achieves CA alignment comparable to a single-agent baseline while substantially improving conflict-resolution performance without degrading general language capabilities. These results suggest that negotiation-driven deliberation training provides a practical path toward LLMs that better support collective decision-making in value-conflict scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Negotiate: Multi-Agent Deliberation for Collective Value Alignment in LLMs》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 LLM 对齐方法（如 RLHF、RLAIF）主要针对单智能体场景，优化静态的“有用性、诚实性、无害性（HHH）”目标。然而，现实世界充满多利益相关者的价值冲突，单智能体对齐往往在面对冲突时产生模糊或不一致的回答。
*   **核心问题**：如何让 LLM 具备在多方价值观冲突下进行“审议式协商”的能力，从而达成既符合道德原则又能解决实际问题的集体共识？
*   **整体含义**：论文提出将对齐目标从静态行为转向“集体能动性（Collective Agency, CA）”，并通过多智能体协商训练，使模型学会在冲突中寻找最优合成方案。

### 2. 方法论
*   **核心思想**：将协商过程嵌入强化学习循环。通过两个具有对立人格（Personas）的模型实例进行自我博弈（Self-play），在结构化对话中达成共识，并根据共识质量进行奖励。
*   **关键技术细节**：
    *   **两阶段任务**：首先是**协商阶段**（多轮对话，最多 7 轮），由一个外部判别器（GPT-4o-mini）判断是否达成一致；其次是**总结阶段**，生成最终的解决方案。
    *   **自我博弈设置**：训练中的智能体（策略模型）与自身的一个冻结副本进行对话，分别赋予冲突的人格（如“效率至上” vs “质量至上”）。
    *   **奖励机制**：使用外部 LLM 作为裁判，根据“集体能动性（CA）”的四个维度（知识、仁慈、力量、生命力）对最终方案打分（0-5 分）。若协商失败（未达成一致），奖励直接设为 0。
*   **算法流程**：
    *   采用 **GRPO（Group Relative Policy Optimization，组相对策略优化）** 算法。
    *   **关键创新**：梯度不仅作用于最终总结，还直接作用于**对话过程中的 Token**。这意味着模型是在学习如何“交流”和“妥协”，而不仅仅是学习如何“总结”。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成课程（Curriculum）**：包含 1,100 个道德与实际困境提示词，分为高风险专业决策、复杂人际冲突、日常微观伦理三类。
    *   **人格库**：构建了 50 个独立人格，组成 25 对对立人格对（如“数据驱动” vs “直觉驱动”）。
*   **Benchmark**：
    *   **冲突解决测试集**：100 个未见过的冲突任务。
    *   **通用能力测试**：IFEval（指令遵循）、GPQA Diamond（科学问答）、AIME 2024/2025（数学竞赛）。
*   **对比方法**：
    *   **Base 模型**：Qwen3-14B-Instruct。
    *   **单智能体对齐模型**：仅针对 CA 目标进行单向对齐，不进行协商训练。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX PRO 6000 GPU (96GB VRAM)**。
*   **训练时长**：
    *   多智能体协商训练：约 **110 小时**。
    *   单智能体基准训练：约 160 小时。
*   **模型规格**：基于 Qwen3-14B-Instruct，使用 4-bit QLoRA 进行微调。

### 5. 实验数量与充分性
*   **实验规模**：进行了 1,900 个梯度步长的训练，每步包含多个采样组（G=8）。
*   **充分性评价**：
    *   **较为充分**：实验不仅评估了对齐质量（胜率），还分析了协商效率（达成共识的轮数）和协议达成率。
    *   **客观性**：引入了 GPT-5.2 作为独立裁判，并进行了跨模型裁判的一致性分析（Cross-judge consistency），证明了奖励信号的可靠性。
    *   **公平性**：在对比中使用了相同的基座模型和超参数设置，并测试了随机采样与贪婪解码两种推理策略。

### 6. 主要结论与发现
*   **冲突解决能力显著提升**：多智能体对齐模型在冲突解决任务上的胜率显著高于 Base 模型和单智能体对齐模型。
*   **协商效率提高**：训练后的模型达成共识所需的平均轮数减少了约 20-25%，且协议达成率从 91% 提升至 97%。
*   **质量下限提升**：实验发现，对齐训练主要提升了模型生成高质量、收敛性回答的**一致性**（即提高了表现的“地板”），而非仅仅提高上限。
*   **通用能力保持**：在 IFEval、GPQA 等标准基准上，模型性能未出现退化，证明了该框架在提升特定能力的同时保持了通用智能。

### 7. 优点
*   **视角独特**：将对齐视为一个动态的协商过程，而非静态的指令遵循。
*   **端到端优化对话**：通过 GRPO 直接优化对话 Token，使模型真正学会了审议技巧（如引导对方、寻找折中点）。
*   **可扩展性**：全合成的数据生成和自我博弈机制，使其不依赖昂贵的人类标注。

### 8. 不足与局限
*   **消融实验不足**：未深入拆解人格对、困境类型、GRPO 算法等单一组件对最终效果的具体贡献占比。
*   **场景受限**：目前仅限于两方（1 vs 1）协商，未涉及多方（N > 2）复杂的博弈或联盟形成。
*   **奖励信号粗糙**：奖励仅基于最终结果，缺乏对对话过程中每一轮表现的细粒度信用分配（Credit Assignment）。
*   **合成数据偏差**：训练完全依赖合成数据和 LLM 裁判，可能存在模型内部的偏见循环。

（完）
