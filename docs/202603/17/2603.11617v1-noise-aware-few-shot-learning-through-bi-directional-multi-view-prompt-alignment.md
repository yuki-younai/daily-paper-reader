---
title: Noise-aware few-shot learning through bi-directional multi-view prompt alignment
title_zh: 通过双向多视图提示对齐的噪声感知少样本学习
authors: "Lu Niu, Cheng Xue"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11617v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 视觉语言模型的双向多视图提示对齐
tldr: 针对视觉语言模型在少样本提示微调中易受噪声标签干扰的问题，本文提出NA-MVP框架。该框架通过双向多视图提示对齐，利用不平衡最优传输实现细粒度的区域对齐并抑制噪声区域，结合双向提示捕捉稳定语义，并通过对齐引导的策略修正错误标签。实验证明该方法在噪声环境下显著提升了少样本学习的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的少样本提示学习方法在处理噪声标签时，缺乏建模细粒度语义及自适应分离噪声信号的能力。
method: 采用多视图提示结合不平衡最优传输实现区域对齐，并利用双向提示设计与选择性精炼策略来捕捉稳定语义并修正误标样本。
result: 在多个合成及真实噪声基准数据集上的实验结果表明，NA-MVP的性能一致优于当前最先进的基线方法。
conclusion: 该研究证明了通过细粒度对齐和双向提示建模可以有效增强视觉语言模型在噪声监督下的少样本学习鲁棒性。
---

## 摘要
视觉语言模型通过提示微调（prompt tuning）提供了强大的少样本能力，但仍容易受到噪声标签的影响，这可能会破坏提示并降低跨模态对齐的效果。现有方法面临挑战，因为它们通常缺乏建模细粒度语义线索以及自适应分离干净信号与噪声信号的能力。为了应对这些挑战，我们提出了 NA-MVP，这是一个通过双向多视图提示对齐实现噪声感知少样本学习的框架。NA-MVP 基于一个关键的概念转变：鲁棒的提示学习需要从全局匹配转向区域感知对齐，从而明确区分干净线索与噪声线索。为实现这一目标，NA-MVP 采用了：(1) 多视图提示结合非平衡最优传输（unbalanced optimal transport），在抑制不可靠区域的同时实现细粒度的补丁到提示（patch-to-prompt）对应；(2) 双向提示设计，捕捉互补的面向干净信号和感知噪声的线索，使模型能够专注于稳定的语义；以及 (3) 一种对齐引导的选择性细化策略，利用最优传输仅纠正错误标记的样本，同时保留可靠数据。在合成和真实世界噪声基准上的实验表明，NA-MVP 一致优于最先进的基准方法，证实了其在噪声监督下实现鲁棒少样本学习的有效性。

## Abstract
Vision-language models offer strong few-shot capability through prompt tuning but remain vulnerable to noisy labels, which can corrupt prompts and degrade cross-modal alignment. Existing approaches struggle because they often lack the ability to model fine-grained semantic cues and to adaptively separate clean from noisy signals. To address these challenges, we propose NA-MVP, a framework for Noise-Aware few-shot learning through bi-directional Multi-View Prompt alignment. NA-MVP is built upon a key conceptual shift: robust prompt learning requires moving from global matching to region-aware alignment that explicitly distinguishes clean cues from noisy ones. To realize this, NA-MVP employs (1) multi-view prompts combined with unbalanced optimal transport to achieve fine-grained patch-to-prompt correspondence while suppressing unreliable regions; (2) a bi-directional prompt design that captures complementary clean-oriented and noise-aware cues, enabling the model to focus on stable semantics; and (3) an alignment-guided selective refinement strategy that uses optimal transport to correct only mislabeled samples while retaining reliable data. Experiments on synthetic and real-world noisy benchmarks demonstrate that NA-MVP consistently outperforms state-of-the-art baselines, confirming its effectiveness in enabling robust few-shot learning under noisy supervision.

---

## 论文详细总结（自动生成）

以下是对论文《Noise-aware few-shot learning through bi-directional multi-view prompt alignment》（通过双向多视图提示对齐的噪声感知少样本学习）的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLM）如 CLIP 在少样本（Few-shot）场景下表现优异，但极易受到**噪声标签（Noisy Labels）**的干扰。少量的错误标签会严重误导提示学习（Prompt Learning）的梯度更新，导致跨模态对齐失效。
*   **研究动机**：现有方法存在三大局限：
    1.  **单一视图依赖**：通常仅使用一两个全局提示，无法捕捉细粒度的语义特征。
    2.  **硬性负样本监督**：强制分配负标签过于死板，在噪声环境下不够鲁棒。
    3.  **粗粒度去噪**：依赖固定阈值或简单的伪标签，容易导致错误传播。
*   **整体含义**：提出 NA-MVP 框架，将鲁棒提示学习从“全局匹配”转向“区域感知对齐”，通过识别并抑制图像中的噪声区域来增强模型鲁棒性。

### 2. 方法论
NA-MVP 框架由三个核心模块组成：
*   **双向多视图提示设计（Bi-directional Multi-view Prompts）**：
    *   为每个类别构建两组可学习提示：**面向干净信号的提示（Clean-oriented）**和**感知噪声的提示（Noise-aware）**。
    *   每组包含多个视图（$N$个提示），以捕捉类内的多样化语义。
*   **基于不平衡最优传输（UOT）的细粒度对齐**：
    *   利用 UOT 将图像的局部补丁（Patches）与多个提示进行对齐。
    *   **关键技术**：UOT 放宽了质量守恒约束，允许部分匹配。这使得模型能够自动下调噪声区域的权重，只让可靠的视觉特征与提示对齐。
*   **对齐引导的选择性标签精炼**：
    *   **自适应阈值 $\phi$**：根据干净提示和噪声提示的相对对齐强度，动态识别误标样本。
    *   **经典 OT 修正**：对于识别出的疑似噪声样本，利用经典最优传输（具有严格质量约束）将全局图像特征与类别提示对齐，从而生成更准确的伪标签进行修正。

### 3. 实验设计
*   **数据集**：
    *   **合成噪声数据集**：Caltech101, DTD, Flowers102, OxfordPets, UCF101。
    *   **真实世界噪声数据集**：Food101N。
*   **噪声设置**：包括对称噪声（Symmetric）和非对称噪声（Asymmetric），噪声比例从 12.5% 跨越到 75%。
*   **对比基准（Benchmarks）**：
    *   基础方法：CoOp。
    *   噪声鲁棒方法：CoOp+GCE, JoAPR, NLPrompt, DEFT。
*   **实验场景**：主要聚焦于 16-shot 的少样本设置。

### 4. 资源与算力
*   **硬件使用**：使用了 **1 台 NVIDIA GeForce RTX 4090 GPU**。
*   **训练细节**：
    *   优化器：SGD（学习率 0.002，动量 0.9）。
    *   训练轮数：50 Epochs。
    *   骨干网络：ResNet-50 作为图像编码器，63M 参数的 Transformer 作为文本编码器。
*   **时长**：文中未明确给出总训练时长，但提到使用了高效的 Sinkhorn 算法来加速 OT 计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个合成数据集上进行了 8 种不同噪声比例/类型的详尽测试。
    *   在 Food101N 上进行了不同 Shot 数（4, 8, 16, 32）的测试。
    *   进行了消融实验，分别验证了双向提示、多视图、UOT 和选择性精炼的有效性。
*   **充分性与客观性**：实验覆盖了从轻微到极端（75%）的噪声水平，且所有结果均为三次随机种子的平均值。对比了 2024/2025 年最新的基线方法（如 NLPrompt），显示了较强的时效性和客观性。

### 6. 主要结论与发现
*   **性能领先**：NA-MVP 在所有测试数据集和噪声设置下均优于现有 SOTA 方法，尤其在**高噪声比例（>50%）**下优势更明显。
*   **多视图价值**：实验发现 $N=4$ 个提示视图时效果最佳，证明了捕捉多样化语义对抵御噪声的重要性。
*   **UOT 的优越性**：相比于 KL 散度或经典 OT，UOT 在处理局部噪声特征对齐时表现出更好的灵活性。
*   **自适应修正**：通过双向对齐信号引导的标签修正，比传统的固定阈值过滤更有效，避免了误删干净样本。

### 7. 优点
*   **概念创新**：将噪声处理从标签层面深入到图像的局部特征层面（Region-aware）。
*   **机制灵活**：引入不平衡最优传输（UOT）解决了图像中“干净补丁”与“噪声补丁”共存的问题。
*   **双向建模**：不仅学习什么是“对”的，还通过噪声提示学习什么是“错”的，增强了判别能力。

### 8. 不足与局限
*   **计算开销**：虽然使用了 Sinkhorn 算法，但相比于简单的全局对齐，引入局部补丁级别的 UOT 和全局级别的 OT 修正仍会增加额外的计算负担。
*   **超参数敏感性**：模型引入了如温度参数 $\tau$、平衡系数 $\lambda_i$ 以及 UOT 的松弛参数，这些超参数在不同领域数据集上的通用性有待进一步验证。
*   **任务局限**：目前仅在图像分类任务上验证，尚未扩展到目标检测或分割等更复杂的视觉任务中。

（完）
