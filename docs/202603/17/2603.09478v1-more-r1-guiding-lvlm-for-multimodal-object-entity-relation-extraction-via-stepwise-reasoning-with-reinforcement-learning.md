---
title: "MORE-R1: Guiding LVLM for Multimodal Object-Entity Relation Extraction via Stepwise Reasoning with Reinforcement Learning"
title_zh: MORE-R1：通过强化学习的逐步推理引导大视觉语言模型进行多模态物体-实体关系抽取
authors: "Xiang Yuan, Xu Chu, Xinrong Chen, Haochen Li, Zonghong Dai, Hongcheng Fan, Xiaoyue Yuan, Weiping Li, Tong Mo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09478v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 多模态视觉语言模型中结合强化学习的分步推理
tldr: 针对多模态对象-实体关系抽取（MORE）任务中复杂推理能力不足的问题，本文提出了 MORE-R1 模型。该模型通过引入显式的分步推理机制，并结合强化学习（RL）来增强大型视觉语言模型（LVLM）的性能。研究采用两阶段训练：首先利用自动构建的高质量分步推理数据集进行冷启动监督微调，随后应用 GRPO 算法及渐进式样本混合策略优化推理能力。实验证明，MORE-R1 在 MORE 基准测试中显著优于现有方法，达到了 SOTA 水平。
selection_source: fresh_fetch
motivation: 现有的多模态关系抽取方法在处理复杂场景时缺乏中间推理的透明度，且难以扩展到更具挑战性的任务。
method: 提出一种结合冷启动监督微调与强化学习（GRPO）的两阶段框架，通过自动构建的分步推理数据和渐进式样本混合策略来优化模型。
result: 在 MORE 基准测试上，MORE-R1 显著超越了现有的分类和生成式基准模型，实现了最先进的性能。
conclusion: 通过显式分步推理和强化学习的结合，可以显著提升大型视觉语言模型在多模态关系抽取任务中的推理能力和准确性。
---

## 摘要
多模态物体-实体关系抽取（MORE）是信息抽取研究中一项具有挑战性的任务。它旨在识别视觉对象与文本实体之间的关系，需要复杂的多模态理解和跨模态推理能力。现有方法主要基于分类或不带推理的生成，难以处理 MORE 任务中的复杂抽取场景，且面临可扩展性有限和中间推理透明度不足的问题。为了应对这些挑战，我们提出了 MORE-R1，这是一种新型模型，它引入了带有强化学习（RL）的显式逐步推理，使大视觉语言模型（LVLM）能够有效地处理 MORE 任务。MORE-R1 集成了一个两阶段训练过程，包括初始的监督微调（SFT）冷启动训练阶段和随后的推理能力优化强化学习阶段。在初始阶段，我们设计了一种高效的方法来自动构建包含针对 MORE 任务定制的细粒度逐步推理的高质量 SFT 数据集，使模型能够学习有效的推理范式。在随后的阶段，我们采用了带有渐进式样本混合策略的群体相对策略优化（GRPO）强化学习算法，以稳定训练并进一步增强模型在困难样本上的推理能力。在 MORE 基准测试上的全面实验表明，MORE-R1 达到了最先进的性能，较基准模型有显著提升。

## Abstract
Multimodal Object-Entity Relation Extraction (MORE) is a challenging task in information extraction research. It aims to identify relations between visual objects and textual entities, requiring complex multimodal understanding and cross-modal reasoning abilities. Existing methods, mainly classification-based or generation-based without reasoning, struggle to handle complex extraction scenarios in the MORE task and suffer from limited scalability and intermediate reasoning transparency. To address these challenges, we propose MORE-R1, a novel model that introduces explicit stepwise reasoning with Reinforcement Learning (RL) to enable Large Vision-Language Model (LVLM) to address the MORE task effectively. MORE-R1 integrates a two-stage training process, including an initial cold-start training stage with Supervised Fine-Tuning (SFT) and a subsequent RL stage for reasoning ability optimization. In the initial stage, we design an efficient way to automatically construct a high-quality SFT dataset containing fine-grained stepwise reasoning tailored to the MORE task, enabling the model to learn an effective reasoning paradigm. In the subsequent stage, we employ the Group Relative Policy Optimization (GRPO) RL algorithm with a Progressive Sample-Mixing Strategy to stabilize training and further enhance model's reasoning ability on hard samples. Comprehensive experiments on the MORE benchmark demonstrate that MORE-R1 achieves state-of-the-art performance with significant improvement over baselines.

---

## 论文详细总结（自动生成）

### MORE-R1：通过强化学习的逐步推理引导大视觉语言模型进行多模态关系抽取

#### 1. 核心问题与研究背景
*   **核心问题**：多模态物体-实体关系抽取（MORE）旨在识别图像中的视觉对象与文本中的实体之间的关系。现有方法主要面临三个瓶颈：
    1.  **可扩展性差**：基于分类的方法依赖预定义的标签集，难以处理新关系。
    2.  **复杂场景处理能力不足**：简单的生成式模型缺乏深度推理，难以区分语义相近的关系（如“同事”与“夫妻”）。
    3.  **缺乏透明度**：模型直接输出结果，决策过程不可见。
*   **研究背景**：随着大视觉语言模型（LVLM）和推理模型（如 OpenAI-o1、DeepSeek-R1）的发展，利用显式思维链（CoT）和强化学习（RL）提升复杂任务的推理能力成为可能。

#### 2. 方法论
MORE-R1 采用了**两阶段训练框架**：
*   **阶段 1：冷启动监督微调（SFT）**
    *   **核心思想**：让模型学习针对 MORE 任务定制的推理范式。
    *   **关键技术**：利用 GPT-4o 作为专家模型，自动构建高质量的逐步推理数据集。推理过程被规范为 **6 个步骤**：图像分析、跨模态相关性评估、跨模态对齐、实体类型识别、初步关系过滤、最终关系确定。
*   **阶段 2：强化学习（RL）优化**
    *   **核心算法**：采用 **GRPO（群体相对策略优化）**。该算法通过组内相对奖励计算优势函数，无需额外的价值模型（Value Model），显著降低了计算开销。
    *   **奖励函数设计**：包含格式奖励（是否符合 `<think>` 标签规范）、长度奖励（鼓励深入思考）和答案奖励（预测标签是否正确）。
    *   **渐进式样本混合策略（Progressive Sample-Mixing）**：这是本文的创新点。在 RL 训练初期混合易、难样本，随着训练进行逐渐增加难样本（SFT 阶段预测错误的样本）的比例，以稳定训练并攻克复杂案例。

#### 3. 实验设计
*   **数据集**：使用 **MORE 基准数据集**，包含 20,264 个样本，涵盖 21 种关系类型（包括 `none`）。
*   **对比方法**：
    *   **分类模型**：MOREformer, HVFormer, MMIB, CGI-MRE, FocalMRE, REMOTE（当前 SOTA）。
    *   **生成式模型**：LLaMA3.2-Vision, Qwen2.5-VL（零样本及直接 SFT 版本）。
*   **评估指标**：准确率（Acc）、精确率（P）、召回率（R）和 F1 分数。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 80GB GPU**。
*   **训练时长**：
    *   **阶段 1 (SFT)**：约 2 小时。
    *   **阶段 2 (RL)**：约 24 小时。
*   **模型底座**：Qwen2.5-VL-7B-Instruct。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了主实验对比、消融实验、样本混合策略对比以及案例分析。
*   **充分性与客观性**：
    *   消融实验涵盖了 SFT 数据量、RL 策略变体（如不同衰减因子 $\alpha$）的对比，证明了各组件的有效性。
    *   对比了同等数据量下的直接 SFT 与推理 SFT，确保了实验的公平性。
    *   通过对 `none` 关系的深入分析，解释了模型在精确率和召回率上的权衡，体现了客观性。

#### 6. 主要结论与发现
*   **性能领先**：MORE-R1 在所有指标上均显著超越了现有的分类和生成式基准模型，F1 分数较之前最强的 REMOTE 模型提升了 **6.1%**。
*   **推理的重要性**：显式的逐步推理显著提升了模型对跨模态实体间隐含竞争或协作关系的理解。
*   **RL 的增益**：通过 GRPO 和渐进式样本混合，模型在处理 SFT 阶段无法解决的“硬样本”上表现出更强的探索能力。

#### 7. 优点（亮点）
*   **首创性**：首次将带有强化学习的显式推理机制引入多模态关系抽取任务。
*   **推理范式**：设计的 6 步推理逻辑非常严密，涵盖了从感知到认知、从过滤到决策的完整链路。
*   **训练稳定性**：提出的渐进式样本混合策略有效解决了 RL 在处理纯硬样本时容易出现的训练崩溃或不收敛问题。

#### 8. 不足与局限
*   **数据依赖**：冷启动阶段高度依赖 GPT-4o 等闭源专家模型生成推理路径，这可能引入专家模型的偏见。
*   **算力成本**：尽管 GRPO 优化了效率，但 24 小时的 A100 集群训练对于资源受限的环境仍有较高门槛。
*   **任务局限**：目前仅在 MORE 单一基准上验证，尚未在更广泛的多模态信息抽取任务（如多模态事件抽取）中进行泛化测试。

（完）
