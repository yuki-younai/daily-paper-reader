---
title: Learning Adaptive LLM Decoding
title_zh: 学习自适应大语言模型解码
authors: "Chloe H. Su, Zhe Ye, Samuel Tenka, Aidan Yang, Soonho Kong, Udaya Ghai"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09065v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 使用强化学习训练的解码适配器
tldr: 针对大语言模型解码时固定采样参数无法适应不同任务难度的问题，本文提出了一种学习自适应解码策略的方法。通过引入轻量级解码适配器并利用强化学习训练，该方法能在推理时根据计算资源动态选择采样策略。研究涵盖了序列级和标记级两种粒度，在数学和编程基准测试中显著提升了准确率与计算预算之间的权衡效率，为高效推理提供了新思路。
selection_source: fresh_fetch
motivation: 传统的固定采样超参数无法应对不同提示词和解码步骤中任务难度及不确定性的巨大差异。
method: 引入轻量级解码适配器，通过强化学习在序列级和标记级动态调整采样策略（如贪婪、top-k、min-p）。
result: "在MATH基准测试中，标记级适配器在固定预算下将Pass@1准确率提升了高达10.2%，序列级适配器也带来了显著提升。"
conclusion: 学习自适应解码策略能有效优化LLM的推理效率，在有限的计算预算内实现更高的任务准确性。
---

## 摘要
大语言模型（LLM）的解码通常依赖于固定的采样超参数（如温度、top-p），尽管在不同的提示词和单个解码步骤中，任务难度和不确定性存在显著差异。我们提出学习自适应解码策略，在推理时根据可用的计算资源动态选择采样策略。我们没有微调语言模型本身，而是引入了通过强化学习和可验证的终端奖励（例如数学和编程任务的正确性）训练的轻量级解码适配器。在序列层面，我们将解码建模为一个上下文老虎机（contextual bandit）问题：策略根据提示词嵌入和并行采样预算，为每个提示词选择一种解码策略（如贪婪搜索、top-k、min-p）。在标记（token）层面，我们将解码建模为一个部分可观测马尔可夫决策过程（POMDP），策略根据内部模型特征和剩余的标记预算，在每个标记步骤选择采样动作。在 MATH 和 CodeContests 基准测试上的实验表明，学习到的适配器改善了准确率与预算之间的权衡：在 MATH 数据集上，在固定标记预算下，标记级适配器比最佳静态基准线提高了高达 10.2% 的 Pass@1 准确率，而序列级适配器在固定并行采样下获得了 2-3% 的提升。消融分析支持了序列级和标记级自适应的贡献。

## Abstract
Decoding from large language models (LLMs) typically relies on fixed sampling hyperparameters (e.g., temperature, top-p), despite substantial variation in task difficulty and uncertainty across prompts and individual decoding steps. We propose to learn adaptive decoding policies that dynamically select sampling strategies at inference time, conditioned on available compute resources. Rather than fine-tuning the language model itself, we introduce lightweight decoding adapters trained with reinforcement learning and verifiable terminal rewards (e.g. correctness on math and coding tasks). At the sequence level, we frame decoding as a contextual bandit problem: a policy selects a decoding strategy (e.g. greedy, top-k, min-p) for each prompt, conditioned on the prompt embedding and a parallel sampling budget. At the token level, we model decoding as a partially observable Markov decision process (POMDP), where a policy selects sampling actions at each token step based on internal model features and the remaining token budget. Experiments on the MATH and CodeContests benchmarks show that the learned adapters improve the accuracy-budget tradeoff: on MATH, the token-level adapter improves Pass@1 accuracy by up to 10.2% over the best static baseline under a fixed token budget, while the sequence-level adapter yields 2-3% gains under fixed parallel sampling. Ablation analyses support the contribution of both sequence- and token-level adaptation.

---

## 论文详细总结（自动生成）

# 《Learning Adaptive LLM Decoding》论文深度总结

## 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）在推理时通常采用固定的采样超参数（如温度 $T$、Top-p、Top-k），这种“一刀切”的方法忽略了不同任务、不同提示词（Prompt）甚至同一序列中不同标记（Token）之间不确定性的巨大差异。
*   **研究动机**：推理过程中的不确定性往往集中在少数关键的“分叉标记”上。在简单步骤使用随机采样可能导致错误，而在复杂决策点使用贪婪搜索则可能限制探索空间。
*   **核心目标**：开发一种能够根据当前上下文和**计算预算**（如剩余 Token 数或并行采样数）动态调整解码策略的机制，以优化准确率与计算成本之间的权衡。

## 2. 方法论：核心思想与技术细节
论文提出了一种**解码适配器（Decoding Adapters）**框架，其核心是不改变底层 LLM 参数，仅训练轻量级的策略网络。

### 核心思想
将解码过程视为受限资源下的策略选择问题，分为两个粒度：
1.  **序列级（Sequence-Level）：上下文老虎机（Contextual Bandit）**
    *   **逻辑**：针对每个 Prompt，适配器观察提示词嵌入和总并行预算 $B$，一次性选择一种最优解码配置（如 `Top-p=0.9, T=0.7`）。
    *   **目标函数**：最大化期望终端奖励（如 Pass@k），并加入熵正则化以鼓励探索。
2.  **标记级（Token-Level）：部分可观测马尔可夫决策过程（POMDP）**
    *   **逻辑**：在生成的每一步，适配器观察当前隐藏状态和剩余 Token 预算 $b_t$，动态选择当前 Token 的采样动作（主要是调整温度）。
    *   **技术细节**：使用 REINFORCE 算法训练，通过可验证的奖励（数学题或代码的正确性）进行反馈。为了稳定训练，采用了过滤稀疏奖励和掩码高置信度 Token 的策略。

### 关键技术
*   **预算感知（Budget-Aware）**：将剩余计算资源作为策略网络的输入，使模型在预算充足时倾向于探索，在预算枯竭时倾向于保守（确定性）输出。
*   **动作空间构建**：通过数据驱动的贪婪覆盖算法，从 180 种超参数组合中筛选出最具代表性的离散动作集。

## 3. 实验设计
*   **数据集**：
    *   **MATH**：竞赛级数学题，考察多步推理。
    *   **CodeContests**：竞赛级编程题，考察逻辑构建。
    *   **AIME 2025**：用于评估模型在极难题目上的泛化能力。
*   **基准模型（Backbone）**：主要使用 Qwen3-4B，辅助测试 Qwen3-8B 和 Qwen2.5-Math-1.5B。
*   **对比方法（Baselines）**：
    *   **Best Static**：在验证集上表现最好的单一固定解码策略。
    *   **Mixed**：在动作空间内进行均匀随机选择的混合策略。
    *   **TURN**：一种现有的优化多样本推理温度的方法。

## 4. 资源与算力
*   **算力工具**：论文提到使用了 **vLLM** 引擎进行快速推理生成，并使用 **Ray Train** 进行分布式数据并行（DDP）训练。
*   **具体配置**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）数量以及总训练时长。但考虑到适配器是轻量级 MLP 且 LLM 处于冻结状态，其训练开销远小于全量微调。

## 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了数学和编程两个主要推理领域。
    *   进行了跨领域泛化实验（在 MATH 上训练，在 CodeContests 上测试）。
    *   针对不同推理模式（Thinking vs Non-thinking）和 Prompt 格式（CoT vs Direct）进行了测试。
*   **消融实验**：对比了“有无预算感知”、“仅使用熵作为特征 vs 使用全隐藏状态特征”等变量。
*   **充分性评价**：实验设计较为全面，通过 3 个独立运行的均值和 95% 置信区间来保证结果的客观性。跨模型和跨领域的测试证明了该方法的鲁棒性。

## 6. 主要结论与发现
1.  **显著提升性能**：在 MATH 任务上，标记级适配器在固定预算下将 Pass@1 准确率提升了 **10.2%**。
2.  **预算敏感性**：适配器学会了根据剩余资源调整策略。例如，在并行预算较大时，序列级适配器会选择更多样化的策略以提高 Pass@8 的成功率。
3.  **特征重要性**：单纯依靠 Token 熵（Entropy）不足以达到最佳效果，模型内部的隐藏状态包含更丰富的上下文信息，对选择解码策略至关重要。
4.  **泛化能力**：在 MATH 上训练的适配器在未见过的 AIME'25 题目上依然表现优异，甚至超过了官方报告的基准值。

## 7. 优点
*   **非侵入性**：无需修改底层 LLM 参数，适用于闭源模型（如果能获取 Logits/Hidden States）或冻结的开源模型。
*   **端到端学习**：直接针对最终任务的正确性（Verifiable Rewards）进行优化，而非模仿人类偏好。
*   **计算高效**：适配器本身是极小的 MLP，推理延迟增加极低，却能显著提升 Token 的利用效率。

## 8. 不足与局限
*   **奖励依赖性**：该方法高度依赖于“可验证奖励”（如数学/代码），对于缺乏客观评价标准的开放式对话或文学创作任务，如何定义奖励函数仍是挑战。
*   **动作空间限制**：目前仅在有限的离散动作空间（如几种固定的温度值）中选择，尚未实现连续空间的超参数优化。
*   **算力细节缺失**：未披露具体的训练资源消耗，不便于其他研究者评估复现成本。
*   **应用限制**：标记级适配器需要访问模型的隐藏状态，这在仅提供 API 访问的商业模型上难以实施。

（完）
