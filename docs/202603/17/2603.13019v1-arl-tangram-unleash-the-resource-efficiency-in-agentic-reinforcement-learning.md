---
title: "ARL-Tangram: Unleash the Resource Efficiency in Agentic Reinforcement Learning"
title_zh: ARL-Tangram：释放智能体强化学习中的资源效率
authors: "Bangjun Xiao, Yihao Zhao, Xiangwei Deng, Shihua Yu, Yuxing Xiang, Huaqiu Liu, Qiying Wang, Liang Zhao, Hailin Zhang, Xuanzhe Liu, Xin Jin, Fuli Luo"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13019v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大语言模型智能体强化学习的资源管理
tldr: 代理强化学习（Agentic RL）通过LLM与现实世界交互解决复杂问题，但其依赖的外部资源（如代码执行CPU、奖励模型GPU）常因静态过度配置导致效率低下。本文提出ARL-Tangram系统，通过动作级编排实现细粒度的资源共享与弹性调度。该系统显著降低了动作完成时间并节省了大量外部资源，已成功应用于MiMo系列模型的训练。
selection_source: fresh_fetch
motivation: 现有的代理强化学习框架在处理外部资源时采用静态分配，导致资源利用率低且难以应对动态负载。
method: 提出动作级编排机制，结合统一的动作表述和弹性调度算法，实现对异构外部资源的细粒度管理与共享。
result: "实验显示ARL-Tangram将平均动作完成时间缩短了4.3倍，训练步长加速1.5倍，并节省了高达71.2%的外部资源。"
conclusion: ARL-Tangram通过精细化的资源管理释放了代理强化学习的效率潜力，为大规模智能体模型训练提供了高效支撑。
---

## 摘要
智能体强化学习（Agentic RL）已成为云集群中一种变革性的工作负载，使大语言模型（LLM）能够通过与现实世界的交互来解决复杂问题。然而，与传统强化学习不同，智能体强化学习需要大量的外部云资源（例如用于代码执行的 CPU 和用于奖励模型的 GPU），这些资源存在于主训练集群之外。现有的智能体强化学习框架通常依赖于静态过度配置，即资源往往与长生命周期的轨迹绑定或按任务隔离，这导致了严重的资源低效。我们提出了动作级编排（action-level orchestration），并将其整合到 ARL-Tangram 中。ARL-Tangram 是一个统一的资源管理系统，能够实现细粒度的外部资源共享和弹性。ARL-Tangram 利用统一的动作级公式和弹性调度算法，在满足异构资源约束的同时最小化动作完成时间（ACT）。此外，异构资源管理器经过专门设计，能够高效支持在具有异构特性和拓扑结构的资源上进行动作级执行。对真实世界智能体强化学习任务的评估表明，ARL-Tangram 将平均 ACT 提高了多达 4.3 倍，将强化学习训练的步时长缩短了多达 1.5 倍，并节省了多达 71.2% 的外部资源。该系统已部署用于支持 MiMo 系列模型的训练。

## Abstract
Agentic reinforcement learning (RL) has emerged as a transformative workload in cloud clusters, enabling large language models (LLMs) to solve complex problems through interactions with real world. However, unlike traditional RL, agentic RL demands substantial external cloud resources, e.g., CPUs for code execution and GPUs for reward models, that exist outside the primary training cluster. Existing agentic RL framework typically rely on static over-provisioning, i.e., resources are often tied to long-lived trajectories or isolated by tasks, which leads to severe resource inefficiency.   We propose the action-level orchestration, and incorporate it into ARL-Tangram, a unified resource management system that enables fine-grained external resource sharing and elasticity. ARL-Tangram utilizes a unified action-level formulation and an elastic scheduling algorithm to minimize action completion time (ACT) while satisfying heterogeneous resource constraints. Further, heterogeneous resource managers are tailored to efficiently support the action-level execution on resources with heterogeneous characteristics and topologies. Evaluation on real-world agentic RL tasks demonstrates that ARL-Tangram improves average ACT by up to 4.3$\times$, speeds up the step duration of RL training by up to 1.5$\times$, and saves the external resources by up to 71.2$\%$. This system has been deployed to support the training of the MiMo series models.

---

## 论文详细总结（自动生成）

这是一份关于论文《ARL-Tangram: Unleash the Resource Efficiency in Agentic Reinforcement Learning》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：智能体强化学习（Agentic RL）在训练过程中需要频繁调用外部资源（如用于代码执行的CPU、用于奖励模型的GPU、搜索API配额等）。
*   **研究背景**：传统的RL框架（如Ray, verl）通常采用**静态过度配置（Static Over-provisioning）**。资源要么与长生命周期的轨迹（Trajectory）绑定，要么按任务隔离。
*   **痛点**：
    1.  **轨迹内浪费**：在AI Coding等任务中，环境仅在47%的时间内被访问，其余时间CPU处于闲置。
    2.  **任务间浪费**：不同RL任务的外部调用具有极强的爆发性和不均匀性，导致资源在某些时段严重闲置，而在高峰期又因排队导致训练停滞。

### 2. 方法论：核心思想与关键技术
ARL-Tangram 提出了**动作级编排（Action-level Orchestration）**，将资源管理粒度从“轨迹/任务”细化到“原子动作（Action）”。
*   **统一动作表述（Unified Action Formulation）**：
    *   将每个动作建模为**向量化资源成本**（CPU、GPU、内存、API配额等）。
    *   引入**弹性建模**：描述动作执行时间与资源分配量（如并行度DoP）之间的函数关系。
*   **弹性调度算法**：
    *   **目标函数**：最小化平均动作完成时间（ACT），即“排队时间 + 执行时间”。
    *   **贪婪驱逐机制**：在FCFS（先来先服务）基础上，通过启发式算法动态决定是否通过减少当前动作的资源分配来容纳更多并发动作，或通过增加资源分配来缩短单个动作时长。
*   **异构资源管理器**：
    *   **CPU管理器（AOE, Allocate-on-Execution）**：利用Docker的cgroup动态更新机制，在执行瞬间调整容器的CPU配额，执行完立即回收。
    *   **GPU管理器（EOE, Evict-on-Execution）**：采用“驱逐-恢复”机制。将不活跃的奖励模型服务状态缓存至CPU内存，仅在有动作请求时加载至GPU，支持多模型共享少量GPU。
    *   **基础管理器**：管理API QPS和配额。

### 3. 实验设计
*   **应用场景与数据集**：
    1.  **AI Coding**：基于内部生产环境数据集（类似SWE-bench），涉及大量代码编译和测试。
    2.  **MOPD**：多教师模型蒸馏任务，涉及多个大模型作为奖励函数。
    3.  **DeepSearch**：基于BrowseComp数据集，涉及大量外部API调用。
*   **Benchmark（对比方法）**：
    *   **Kubernetes (k8s)**：作为CPU管理的基准。
    *   **SGLang**：作为GPU推理服务的基准。
    *   **ServerlessLLM**：作为模型快速切换/无服务器推理的对比基准。
*   **评估指标**：平均动作完成时间（ACT）、训练步时长（Step Duration）、资源节省率。

### 4. 资源与算力
*   **训练集群**：多达48个GPU节点，每个节点配备8张 **NVIDIA Hopper 架构 GPU**（如H100/H800），使用RDMA互联。
*   **外部资源池**：
    *   **CPU**：15个节点，每个节点含256核 **AMD CPU** 和 2.4TB 内存。
    *   **GPU**：5个节点，每个节点含8张 NVIDIA GPU 和 3TB CPU 内存（用于模型缓存）。
*   **模型规模**：使用了 Qwen3-32B 和 MiMo-V2-Flash 等模型进行实验。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了端到端性能对比、可扩展性分析（随Batch Size和资源量变化）、消融实验。
*   **充分性**：实验覆盖了CPU密集、GPU密集和API密集三种典型Agentic RL负载，具有较强的代表性。
*   **客观性**：论文不仅展示了加速效果，还详细分析了系统开销（如GPU恢复开销占执行时间的25%），并对比了在不同并发压力下的表现，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能提升**：平均动作完成时间（ACT）最高提升 **4.3倍**。
*   **训练加速**：端到端训练步时长缩短了 **1.5倍**。
*   **资源节省**：在保持相同性能的前提下，节省了高达 **71.2%** 的外部资源。
*   **部署验证**：该系统已在小米（Xiaomi）部署，用于支持 MiMo 系列模型的实际训练。

### 7. 优点
*   **粒度创新**：打破了传统的“环境绑定轨迹”模式，通过动作级调度极大地提高了资源周转率。
*   **异构支持**：统一处理了CPU、GPU和API等完全不同特性的资源，具有很强的通用性。
*   **弹性调度**：不仅是简单的共享，还能根据资源余量动态调整动作的并行度（DoP），主动缩短关键路径时长。

### 8. 不足与局限
*   **恢复开销**：在GPU管理中，从CPU内存恢复模型到GPU仍有显著延迟（约占ACT的25%），对于极短时长的动作可能成为瓶颈。
*   **预验依赖**：弹性调度算法依赖于预先对动作执行时长和弹性比例的画像（Profiling），对于完全未知的自定义工具调用，初期调度可能不够精准。
*   **内存压力**：为了实现CPU的快速切换，系统选择了保留容器内存而仅回收CPU核，这在内存受限的场景下可能导致并发上限受阻。

（完）
