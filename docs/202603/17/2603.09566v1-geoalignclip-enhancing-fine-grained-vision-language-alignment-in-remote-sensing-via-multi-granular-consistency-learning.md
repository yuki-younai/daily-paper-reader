---
title: "GeoAlignCLIP: Enhancing Fine-Grained Vision-Language Alignment in Remote Sensing via Multi-Granular Consistency Learning"
title_zh: GeoAlignCLIP：通过多粒度一致性学习增强遥感领域中的细粒度视觉-语言对齐
authors: "Xiao Yang, Ronghao Fu, Zhuoran Duan, Zhiwen Lin, Xueyan Liu, Bo Yang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09566v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 遥感领域细粒度视觉语言对齐
tldr: 针对遥感图像-文本预训练中细粒度对齐不足的问题，本文提出GeoAlignCLIP框架。该框架通过多粒度语义对齐和模态内一致性学习，实现了图像区域与文本概念间的精确匹配。同时，构建了包含10万条数据的RSFG-100k数据集，提供分层监督。实验证明，该方法在多项遥感任务中显著优于现有模型，展现了强大的细粒度对齐能力。
selection_source: fresh_fetch
motivation: 现有的遥感视觉语言模型主要依赖全局对齐，难以捕捉图像中的细粒度细节，限制了在复杂任务中的表现。
method: 提出GeoAlignCLIP框架，通过多粒度语义对齐、模态内一致性学习以及构建RSFG-100k分层监督数据集来增强对齐精度。
result: 在多个公共遥感基准测试上的实验表明，GeoAlignCLIP在各项任务中均优于现有的遥感专用方法。
conclusion: GeoAlignCLIP通过多粒度学习和高质量数据集，有效提升了遥感领域的细粒度视觉-语义对齐性能。
---

## 摘要
视觉-语言预训练模型在连接遥感图像与自然语言方面取得了显著进展。然而，现有方法通常无法有效整合多粒度的视觉和文本信息，主要依赖于全局图像-文本对齐。这种局限性阻碍了模型准确捕捉图像中细粒度细节的能力，从而限制了其在复杂细粒度任务中的表现。为了解决这一问题，我们提出了 GeoAlignCLIP，这是一个统一的框架，通过学习多粒度语义对齐并结合模态内一致性，在遥感任务中实现细粒度对齐，从而使图像区域与文本概念之间实现更精确的视觉-语义对齐。此外，我们构建了 RSFG-100k，这是一个细粒度遥感数据集，包含场景描述、区域级标注和具有挑战性的难负样本，为模型训练提供层级化监督。在多个公开遥感基准上进行的广泛实验表明，GeoAlignCLIP 在各种任务中始终优于现有的遥感专用方法，展现出更稳健、更准确的细粒度视觉-语言对齐能力。

## Abstract
Vision-language pretraining models have made significant progress in bridging remote sensing imagery with natural language. However, existing approaches often fail to effectively integrate multi-granular visual and textual information, relying primarily on global image-text alignment. This limitation hinders the model's ability to accurately capture fine-grained details in images, thus restricting its performance in complex, fine-grained tasks. To address this, we propose GeoAlignCLIP, a unified framework that achieves fine-grained alignment in remote sensing tasks by learning multi-granular semantic alignments and incorporating intra-modal consistency, enabling more precise visual-semantic alignment between image regions and text concepts. Additionally, we construct RSFG-100k, a fine-granular remote sensing dataset containing scene descriptions, region-level annotations, and challenging hard-negative samples, providing hierarchical supervision for model training. Extensive experiments conducted on multiple public remote-sensing benchmarks demonstrate that GeoAlignCLIP consistently outperforms existing RS-specific methods across diverse tasks, exhibiting more robust and accurate fine-grained vision-language alignment.

---

## 论文详细总结（自动生成）

以下是对论文《GeoAlignCLIP: Enhancing Fine-Grained Vision-Language Alignment in Remote Sensing via Multi-Granular Consistency Learning》的结构化总结：

### 1. 核心问题与整体含义
**研究动机：** 现有的遥感（RS）视觉-语言模型（如 RemoteCLIP, GeoCLIP）主要依赖全局图像-文本对齐，难以捕捉复杂的空间布局、密集的物体细节以及高类间相似性的细粒度特征。
**核心问题：** 如何在遥感图像中实现精确的“区域-短语”级对齐，并解决由于尺度变化和文本描述粒度不一导致的语义漂移问题。

### 2. 方法论
GeoAlignCLIP 采用两阶段训练框架，核心思想是结合**多粒度对比学习（MGCL）**和**多视图一致性学习（MVCL）**。

*   **第一阶段：全局对比学习**
    *   遵循标准 CLIP 范式，对整幅图像和简短标题进行对齐，建立稳定的跨模态嵌入空间。
*   **第二阶段：细粒度与一致性增强**
    *   **多粒度对比学习 (MGCL)：**
        *   **区域-短语对齐 (RPA)：** 利用 RoIAlign 提取局部视觉特征，并与对应的区域描述短语进行对齐。
        *   **难负样本对齐 (HNA)：** 构建语义相近但细节冲突的难负样本（如改变属性或类别），增强模型对微小差异的辨别力。
    *   **多视图一致性学习 (MVCL)：**
        *   **视觉模态内一致性 (VIC)：** 强制要求从全局特征图提取的 RoI 特征与直接从原始像素裁剪（Crop）得到的特征保持语义一致，以应对尺度变化。
        *   **层级文本一致性 (HTC)：** 确保简短标题（全局）与详细描述（局部）在语义空间中的对齐一致性。
*   **数据集构建：** 提出了 **RSFG-100k**，包含 10 万张图像和 40 万条层级化标注（全局场景、区域描述、难负样本），利用 Qwen3-VL 进行语义增强和质量控制。

### 3. 实验设计
*   **评估任务与数据集：**
    *   **细粒度理解：** RRSIS-HR, CHOICE。
    *   **区域级分类：** NWPU-VHR-10, RRSIS-D。
    *   **开集目标检测 (OVD)：** DIOR, DOTAv1.0。
    *   **图像-文本检索：** RSICD, RSITMD, UCM-Caption。
*   **对比方法：** 包括通用 CLIP 变体（CLIP, LongCLIP, FG-CLIP）、遥感专用模型（RemoteCLIP, SkyCLIP, GeoRSCLIP, LRSCLIP）以及遥感大语言模型（GeoChat, VHM）。

### 4. 资源与算力
*   **训练细节：** 使用 AdamW 优化器，采用 DeepSpeed ZeRO-2 策略、bf16 混合精度训练和梯度检查点（Gradient Checkpointing）。
*   **算力说明：** 论文提到单设备 Batch Size 为 32，但**未明确说明具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 9 个不同的遥感基准数据集上进行了测试，涵盖了分类、检索、检测和细粒度推理等多种任务。
*   **消融实验：** 针对 RPA、VIC、HNA、HTC 四个核心组件分别进行了消融分析，验证了各模块对性能的提升作用。
*   **客观性：** 实验对比了多种 SOTA 模型，并进行了数据泄露分析（MD5 匹配和语义相似度检查），确保了测试集与训练集的严格隔离，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先：** GeoAlignCLIP 在细粒度理解和开集检测任务上显著优于现有遥感 CLIP 模型（如在 RRSIS-HR 上 Acc@1 提升至 33.45%）。
*   **局部对齐的重要性：** 实验证明，显式的细粒度语义建模比单纯的领域预训练对处理密集遥感场景更关键。
*   **一致性学习的价值：** 视觉模态内的一致性约束有效缓解了因尺度变化导致的语义漂移。

### 7. 优点
*   **多粒度框架：** 首次在遥感领域统一了区域-短语对齐与多视图一致性学习。
*   **高质量数据集：** RSFG-100k 提供了丰富的层级化监督信息，尤其是难负样本的引入极大地增强了模型的鲁棒性。
*   **泛化能力强：** 在零样本分类和开集检测任务中表现出卓越的跨域迁移能力。

### 8. 不足与局限
*   **计算开销：** 引入 RoIAlign 和多视图计算增加了训练和推理的 FLOPs（从 5.40T 增加到 13.65T），虽然延迟增加不明显，但对显存有更高要求。
*   **数据生成偏差：** RSFG-100k 的标注高度依赖于 Qwen3-VL 等大模型，可能存在模型固有的幻觉或偏见风险。
*   **硬件细节缺失：** 缺乏具体的算力资源描述，不便于其他研究者评估复现成本。

（完）
