---
title: Evolving Prompt Adaptation for Vision-Language Models
title_zh: 视觉-语言模型的演化提示适配
authors: "Enming Zhang, Jiayang Li, Yanru Wu, Zhenyu Liu, Yang Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09493v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大规模视觉语言模型的适配
tldr: 针对视觉语言模型在少样本迁移中易发生灾难性遗忘的问题，本文提出 EvoPrompt 框架。该方法通过模态共享提示投影器生成层级提示，并采用进化训练策略将低秩更新解耦为方向和幅度，从而在保留预训练语义方向的同时进行微调。结合特征几何正则化，EvoPrompt 在提升少样本学习性能的同时，有效保持了模型的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有的参数高效提示学习方法在适应下游任务时，往往会因灾难性遗忘而丢失预训练模型中的基础知识。
method: 提出 EvoPrompt 框架，利用模态共享投影器生成提示，并通过解耦方向与幅度的进化策略及特征几何正则化来引导提示轨迹。
result: 实验结果显示，EvoPrompt 在少样本学习中取得了最先进的性能，并成功保留了预训练模型的零样本泛化能力。
conclusion: 通过显式控制提示的进化路径，可以实现知识保全且高效的视觉语言模型下游任务适配。
---

## 摘要
将大规模视觉-语言模型（VLMs）适配到标注数据有限的下游任务中仍然是一个重大挑战。虽然参数高效的提示学习方法提供了一条有前景的路径，但它们往往面临预训练知识灾难性遗忘的问题。为了解决这一局限性，本研究基于以下见解：控制提示的演化路径对于实现无遗忘适配至关重要。为此，我们提出了 EvoPrompt，这是一个旨在显式引导提示轨迹以实现稳定且保留知识的微调的新型框架。具体而言，我们的方法采用模态共享提示投影器（MPP），从统一的嵌入空间生成分层提示。关键在于，一种演化训练策略将低秩更新解耦为方向和幅度分量，在仅调整幅度的同时保留早期学习到的语义方向，从而使提示能够在不丢弃基础知识的情况下进行演化。该过程通过特征几何正则化（FGR）进一步稳定，该正则化强制执行特征去相关以防止表示崩溃。大量实验表明，EvoPrompt 在少样本学习中实现了最先进的性能，同时稳健地保留了预训练 VLM 原始的零样本能力。

## Abstract
The adaptation of large-scale vision-language models (VLMs) to downstream tasks with limited labeled data remains a significant challenge. While parameter-efficient prompt learning methods offer a promising path, they often suffer from catastrophic forgetting of pre-trained knowledge. Toward addressing this limitation, our work is grounded in the insight that governing the evolutionary path of prompts is essential for forgetting-free adaptation. To this end, we propose EvoPrompt, a novel framework designed to explicitly steer the prompt trajectory for stable, knowledge-preserving fine-tuning. Specifically, our approach employs a Modality-Shared Prompt Projector (MPP) to generate hierarchical prompts from a unified embedding space. Critically, an evolutionary training strategy decouples low-rank updates into directional and magnitude components, preserving early-learned semantic directions while only adapting their magnitude, thus enabling prompts to evolve without discarding foundational knowledge. This process is further stabilized by Feature Geometric Regularization (FGR), which enforces feature decorrelation to prevent representation collapse. Extensive experiments demonstrate that EvoPrompt achieves state-of-the-art performance in few-shot learning while robustly preserving the original zero-shot capabilities of pre-trained VLMs.

---

## 论文详细总结（自动生成）

## 论文总结：Evolving Prompt Adaptation for Vision-Language Models

### 1. 核心问题与整体含义
本文针对大规模视觉-语言模型（VLM，如 CLIP）在下游任务适配中的**灾难性遗忘**问题展开研究。虽然现有的参数高效微调（PEFT）方法（如提示学习 Prompt Learning）能以极小代价适配模型，但在少样本（Few-shot）场景下，模型往往会过度拟合下游数据，导致其原始强大的零样本（Zero-shot）泛化能力大幅下降。论文的核心动机是：通过**显式引导提示（Prompt）的演化轨迹**，使其从通用语义锚点平滑过渡到任务特定特征，从而在获取新知识的同时保留预训练的知识。

### 2. 方法论
EvoPrompt 框架由三个核心模块组成：

*   **模态共享提示投影器 (MPP)**：
    *   不再为每一层独立设置提示，而是从一个统一的、可学习的嵌入空间出发，通过投影器生成各层提示。
    *   采用类似 LoRA 的低秩分解技术，将投影矩阵分解为全局共享权重 $W_{shared}$ 和层特定适配器 $\Delta W_i$。这种设计增强了跨层和跨模态的协同作用。
*   **进化轨迹感知学习策略 (Evolutionary Strategy)**：
    *   **幅度-方向解耦**：将低秩更新 $\Delta W$ 分解为标量幅度 $\alpha$ 和单位方向矩阵。
    *   **历史冻结**：在训练过程中，冻结早期阶段学习到的“方向”分量（认为其代表了基础语义结构），仅允许优化“幅度”和当前阶段的新方向。
    *   **自适应秩衰减**：随着训练轮数增加，逐步降低适配器的秩 $r$，以减少后期微调对特征空间的剧烈扰动，防止过拟合。
*   **特征几何正则化 (FGR)**：
    *   基于 Soft-HGR 最大相关性理论，引入正则化项来强制特征在模态内去相关（即正交化）。这能有效防止在数据稀缺时出现的表示崩溃（Representation Collapse）。

### 3. 实验设计
*   **数据集与场景**：
    *   **基准分类**：在 11 个标准数据集（如 ImageNet, Caltech101, OxfordPets, EuroSAT 等）上进行 Base-to-Novel 泛化实验。
    *   **跨数据集迁移**：在 ImageNet 上训练，直接在其余 10 个数据集上测试。
    *   **领域泛化**：在 ImageNet 的四个变体（V2, Sketch, A, R）上测试鲁棒性。
    *   **少样本学习**：评估 1, 2, 4, 8, 16-shot 下的性能。
*   **对比方法 (Benchmarks)**：
    *   对比了包括 CLIP (Zero-shot), CoOp, CoCoOp, MaPLe, PromptSRC, TCP, MMA 等在内的十余种最先进的提示学习和适配器方法。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA A800 GPU** 上完成。
*   **效率指标**：
    *   **参数量**：约 0.764M 可训练参数（比 MaPLe 减少了约 4.6 倍）。
    *   **训练速度**：约 4.5ms/image。
    *   **推理速度**：达到 1282.1 FPS（Batch size=100）。
*   论文明确给出了训练效率对比表，证明了该方法在资源占用上的优势。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 11 个数据集的全面评估，并针对不同泛化场景（类泛化、域泛化、任务泛化）进行了详尽测试。
*   **消融实验**：对 MPP 结构、权重共享、进化策略、FGR 损失、知识恒定损失等每个核心组件都进行了消融分析，验证了各部分的必要性。
*   **客观性**：所有实验均采用三次随机种子的平均值，遵循了主流学术界的公平对比协议（Fair Comparison），实验设计非常充分且逻辑严密。

### 6. 主要结论与发现
*   **性能领先**：EvoPrompt 在 Novel 类别的泛化性能和调和平均值（HM）上均达到了 SOTA，尤其在处理“遗忘”问题上表现优异。
*   **演化规律**：分析发现，学习到的幅度 $\alpha$ 在训练初期迅速达到峰值，随后逐渐下降，验证了提示演化是从“构建核心特征”到“微调细节”的过程。
*   **稳定性**：FGR 正则化能显著提升特征空间的利用率，避免了少样本训练中常见的特征坍缩现象。

### 7. 优点
*   **理论与直觉结合**：将生物进化/轨迹控制的直觉与低秩分解、Soft-HGR 理论相结合，方案具有较强的解释性。
*   **极高的参数效率**：通过共享投影器和秩衰减，在保持高性能的同时显著降低了存储和计算开销。
*   **泛化性强**：在保持 Base 类准确率的同时，Novel 类的性能提升尤为明显，有效解决了 PEFT 中的过拟合顽疾。

### 8. 不足与局限
*   **超参数依赖**：引入了多个超参数（如损失权重 $\gamma, \eta$，秩衰减节点 $\mu, \nu$），虽然论文给出了敏感性分析，但在新任务上可能需要一定的调优成本。
*   **任务覆盖面**：实验主要集中在图像分类任务，尚未在视觉检测、分割或更复杂的视觉问答（VQA）等生成式任务上验证其通用性。
*   **实现复杂度**：相比于简单的线性提示，其进化策略涉及历史梯度的管理和权重的解耦累加，代码实现复杂度略高。

（完）
