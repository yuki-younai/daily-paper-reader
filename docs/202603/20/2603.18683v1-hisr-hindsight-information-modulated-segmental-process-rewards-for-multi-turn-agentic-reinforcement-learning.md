---
title: "HISR: Hindsight Information Modulated Segmental Process Rewards For Multi-turn Agentic Reinforcement Learning"
title_zh: HISR：面向多轮智能体强化学习的事后信息调制片段式过程奖励
authors: "Zhicong Lu, Zichuan Lin, Wei Jia, Changyuan Tian, Deheng Ye, Peiguang Li, Li Jin, Nayu Liu, Guangluan Xu, Wei Feng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18683v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 针对大语言模型多轮智能体强化学习的分段过程奖励
tldr: 针对大语言模型在长程智能体决策任务中面临的稀疏奖励延迟和信用分配不可靠问题，本文提出了HISR方法。该方法通过分段式过程奖励模型为子目标分配奖励，并利用事后信息构建重要性评分来调节奖励权重。实验证明，HISR能有效提升信用分配的准确性，在多个基准测试中表现优异，显著增强了多轮强化学习的效果。
selection_source: fresh_fetch
motivation: 现有的强化学习奖励模型在处理长程任务时，存在结果奖励延迟和细粒度回合奖励信用分配不准的问题。
method: 提出分段式过程奖励模型，并结合事后模型与策略模型的似然比来衡量动作重要性，进而调节各片段的奖励。
result: 在三个公开基准测试上的实验结果表明，HISR显著提升了多轮智能体强化学习的性能。
conclusion: 通过事后信息调节的分段奖励机制能更精准地进行信用分配，是提升复杂决策任务表现的有效途径。
---

## 摘要
尽管大语言模型在多个领域表现出色，但它们在复杂的长程智能体决策任务中的表现仍然有限。大多数现有方法集中于设计有效的奖励模型（RM），通过多轮强化学习来提升性能。然而，这些方法面临稀疏结果奖励中的延迟传播问题，以及由于可能过于细粒度且不集中的轮次级过程奖励导致的不可靠信用分配。在本文中，我们提出了 HISR，利用事后信息（Hindsight Information）来调制片段式过程奖励（Segmental process Rewards），使奖励与子目标紧密对齐，并强调重要片段，以增强信用分配的可靠性。具体而言，本文提出了一种片段级过程奖励模型，为任务中的每个子目标分配奖励，避免了对轮次进行过度细粒度的分配。为了强调轨迹中的重要片段，设计了一个事后模型，用于反映在已知轨迹结果后执行特定动作的偏好。利用这一特性，我们设计了事后模型与策略模型之间的序列似然比，以衡量动作的重要性。随后，这些比率被用于聚合片段重要性得分，进而调制片段式过程奖励，从而提高信用分配的可靠性。在三个公开基准测试上的广泛实验结果证明了我们方法的有效性。

## Abstract
While large language models excel in diverse domains, their performance on complex longhorizon agentic decision-making tasks remains limited. Most existing methods concentrate on designing effective reward models (RMs) to advance performance via multi-turn reinforcement learning. However, they suffer from delayed propagation in sparse outcome rewards and unreliable credit assignment with potentially overly fine-grained and unfocused turnlevel process rewards. In this paper, we propose (HISR) exploiting Hindsight Information to modulate Segmental process Rewards, which closely aligns rewards with sub-goals and underscores significant segments to enhance the reliability of credit assignment. Specifically, a segment-level process RM is presented to assign rewards for each sub-goal in the task, avoiding excessively granular allocation to turns. To emphasize significant segments in the trajectory, a hindsight model is devised to reflect the preference of performing a certain action after knowing the trajectory outcome. With this characteristic, we design the ratios of sequence likelihoods between hindsight and policy model to measure action importance. The ratios are subsequently employed to aggregate segment importance scores, which in turn modulate segmental process rewards, enhancing credit assignment reliability. Extensive experimental results on three publicly benchmarks demonstrate the validity of our method.

---

## 论文详细总结（自动生成）

### 论文总结：HISR —— 面向多轮智能体强化学习的事后信息调制片段式过程奖励

#### 1. 核心问题与整体含义
论文关注大语言模型（LLM）在执行复杂、长程的智能体决策任务（如家庭助手、网页导航）时面临的挑战。现有的强化学习（RL）方法主要存在两个痛点：
*   **结果奖励延迟（Delayed Rewards）：** 仅在任务结束时给出奖励，难以将信号有效传播到早期的关键动作。
*   **信用分配不可靠（Unreliable Credit Assignment）：** 现有的“回合级（Turn-level）”过程奖励往往过于细粒度（一个子目标可能跨越多个回合），且未能区分动作的重要性，导致优化目标不聚焦。
**HISR 的核心目标**是通过引入“片段式（Segmental）”划分和“事后信息（Hindsight Information）”调制，使奖励信号与任务子目标对齐，并突出关键动作，从而提升信用分配的准确性。

#### 2. 方法论
HISR 方法分为三个主要阶段：
*   **片段式过程奖励模型 (SPRM)：**
    *   利用 GPT-4o 将原始轨迹划分为多个“片段（Segments）”，每个片段对应一个明确的子目标。
    *   在参考策略模型上添加 MLP 线性层，训练一个进度估计模型，在每个片段结束时预测其对最终结果的增量贡献。
*   **事后信息调制 (HIM)：**
    *   **事后模型训练：** 训练一个“事后模型（Hindsight Model）”，该模型在已知完整轨迹和结果的前提下，通过掩码预测任务来学习动作的偏好。
    *   **重要性度量：** 计算事后模型与当前策略模型在相同动作上的序列似然比（Likelihood Ratio）。如果事后模型对某动作的评分显著高于策略模型，说明该动作在达成目标中具有高重要性。
    *   **奖励调制：** 将片段奖励与该片段内动作的重要性得分相乘并归一化，从而“放大”关键片段的奖励。
*   **多轮强化学习优化：**
    *   结合调制后的片段奖励与“动作落地奖励（Action Grounding Reward，判断动作是否可执行）”，使用 PPO 算法对策略进行迭代优化。

#### 3. 实验设计
*   **数据集/场景：**
    *   **Alfworld：** 具身智能家庭任务（如拿取、清洁、加热等 6 类任务）。
    *   **Virtualhome：** 复杂的家庭活动模拟。
    *   **Webshop：** 真实的在线购物导航与决策场景。
*   **对比方法（Baselines）：**
    *   **提示工程类：** Llama 3.2, GPT-4o, Gemini 2.5 Pro。
    *   **行为克隆类：** SFT, RFT, DPO。
    *   **强化学习类：** PPO, GRPO, Archer, StepAgent, RAGEN, 以及最强基准 **SPA**（回合级过程奖励归因）。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA H20 GPU**。
*   **软件框架：** 采用了 vLLM 和 FastChat 库来加速推理。
*   **模型底座：** 以 Llama 3.2-3B-Instruct 作为主干网络。
*   **训练细节：** 行为克隆阶段训练 3 个 epoch；RL 阶段使用 LoRA（rank=8）进行微调。具体训练时长未在文中详细标注，但提供了详细的超参数设置。

#### 5. 实验数量与充分性
*   **实验规模：** 在 3 个主流智能体基准测试上进行了全面评估，涵盖了从具身环境到 Web 环境的多种任务。
*   **消融实验：** 论文设计了 5 组消融变体（如移除 HIM、移除 SPR、移除动作落地奖励等），验证了每个核心组件的必要性。
*   **统计分析：** 提供了关于片段长度分布、奖励分布变化的统计图表，并结合具体案例（Case Study）进行了定性分析。
*   **评价：** 实验设计较为充分，对比了从闭源模型到最新开源 RL 算法的多种路径，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **HISR 达到 SOTA：** 在所有三个基准测试中，HISR 均显著优于现有的回合级奖励方法（如 SPA），证明了片段级粒度的优越性。
*   **重要性调制的价值：** 实验显示，原始的过程奖励模型往往倾向于给后期动作高分（因为接近完成），而 HIM 能够有效识别并提升早期关键动作（如“找到并拿取物体”）的奖励权重。
*   **子目标对齐：** 超过 50% 的片段包含两个或更多回合，证明了“子目标跨回合”现象的普遍性，片段式奖励比回合式奖励更符合逻辑。

#### 7. 优点
*   **粒度创新：** 突破了传统的回合级限制，通过片段划分解决了奖励信号过于细碎且带有噪声的问题。
*   **事后视角：** 巧妙利用事后模型（已知结果）与在线策略（未知结果）的差异来量化“动作价值”，无需额外的人工过程标注。
*   **鲁棒性：** 引入动作落地奖励，确保模型在追求高奖励的同时保持动作的合规性和可执行性。

#### 8. 不足与局限
*   **依赖外部模型进行分割：** 轨迹片段的划分依赖于 GPT-4o，这在训练流水线中引入了额外的开销和对闭源模型的依赖。
*   **事后模型是静态的：** 论文中的事后模型在 RL 阶段是冻结的，可能无法完全适应 RL 过程中不断演化的数据分布。
*   **先验偏差：** 原始片段奖励模型对后期片段的强先验（倾向于给高分）可能限制了事后信息调制的最终效果上限。

（完）
