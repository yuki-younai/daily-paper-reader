---
title: Discounted Beta--Bernoulli Reward Estimation for Sample-Efficient Reinforcement Learning with Verifiable Rewards
title_zh: 针对具有可验证奖励的样本高效强化学习的折扣 Beta-Bernoulli 奖励估计
authors: "Haechan Kim, Soohyun Ryu, Gyouk Chu, Doohyuk Jang, Eunho Yang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18444v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 用于大模型后训练的可验证奖励强化学习
tldr: 针对大语言模型推理任务中强化学习（RLVR）样本效率低的问题，本文提出了一种基于折扣 Beta-Bernoulli（DBB）的奖励估计方法。该方法将奖励建模为分布采样，利用历史统计信息缓解了传统点估计带来的高方差和方差崩溃问题。实验证明，DBB 在不增加计算开销的情况下，显著提升了模型在多种推理基准测试中的性能和泛化能力。
selection_source: fresh_fetch
motivation: 现有的 RLVR 方法依赖少量采样的点估计，导致奖励估计方差大且容易出现方差崩溃，限制了样本利用效率。
method: 提出 DBB 奖励估计法，通过引入历史奖励统计量并结合折扣因子，对非平稳的奖励分布进行更稳健的估计。
result: 在 1.7B 和 8B 模型上，DBB 显著优于原生 GRPO，在分布内和分布外推理任务中分别实现了明显的准确率提升。
conclusion: DBB 是一种高效且易于实现的奖励估计方案，能在不增加额外资源消耗的前提下，有效提升强化学习在推理任务中的稳定性和性能。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 已成为提升大语言模型推理能力的有效后训练范式。然而，现有的基于组的 RLVR 方法通常面临严重的样本效率低下问题。这种低效源于对少量采样奖励点估计的依赖，导致了高估计方差、方差崩溃以及对生成响应的利用不足。在这项工作中，我们从统计估计的角度重新审视了 RLVR，将奖励建模为从策略诱导分布中提取的样本，并将优势计算转化为从有限数据中估计奖励分布的问题。基于这一观点，我们提出了折扣 Beta-Bernoulli (DBB) 奖励估计，它利用了非平稳分布的历史奖励统计信息。尽管存在偏差，但所得估计器表现出更低且更稳定的方差，在理论上避免了估计方差崩溃，并实现了比标准点估计更低的均方误差。在六个分布内和三个分布外推理基准测试上的广泛实验表明，结合 DBB 的 GRPO 始终优于原始 GRPO，在 1.7B 和 8B 模型上，分布内平均 Acc@8 分别提高了 3.22/2.42 个百分点，分布外分别提高了 12.49/6.92 个百分点，且无需额外的计算开销或内存占用。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has emerged as an effective post-training paradigm for improving the reasoning capabilities of large language models. However, existing group-based RLVR methods often suffer from severe sample inefficiency. This inefficiency stems from reliance on point estimation of rewards from a small number of rollouts, leading to high estimation variance, variance collapse, and ineffective utilization of generated responses. In this work, we reformulate RLVR from a statistical estimation perspective by modeling rewards as samples drawn from a policy-induced distribution and casting advantage computation as the problem of estimating the reward distribution from finite data. Building on this view, we propose Discounted Beta--Bernoulli (DBB) reward estimation, which leverages historical reward statistics for the non-stationary distribution. Although biased, the resulting estimator exhibits reduced and stable variance, theoretically avoids estimated variance collapse, and achieves lower mean squared error than standard point estimation. Extensive experiments across six in-distribution and three out-of-distribution reasoning benchmarks demonstrate that GRPO with DBB consistently outperforms naive GRPO, achieving average Acc@8 improvements of 3.22/2.42 points in-distribution and 12.49/6.92 points out-of-distribution on the 1.7B and 8B models, respectively, without additional computational cost or memory usage.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：针对 RLVR 的折扣 Beta-Bernoulli 奖励估计

#### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**具有可验证奖励的强化学习（RLVR）**在提升大语言模型（LLM）推理能力时的**样本效率低下**问题。
*   **研究背景**：现有的组基强化学习方法（如 GRPO）依赖于对少量采样（Rollouts）的奖励进行“点估计”。
*   **核心痛点**：
    1.  **高方差**：在样本量较小时，点估计波动剧烈。
    2.  **方差崩溃（Variance Collapse）**：当一组内的所有响应奖励相同时（全对或全错），估计方差变为零，导致训练信号消失，浪费计算资源。
    3.  **非平稳性**：策略在训练中不断进化，历史奖励数据会迅速过时，难以直接复用。

#### 2. 方法论：折扣 Beta-Bernoulli (DBB) 奖励估计
论文提出将奖励建模为从策略诱导分布中提取的随机变量，并引入贝叶斯框架进行估计。
*   **核心思想**：利用 **Beta-Bernoulli 共轭先验** 建模奖励分布。将奖励视为伯努利分布的参数 $p$，并用 Beta 分布来表达对 $p$ 的不确定性。
*   **关键技术细节**：
    *   **折扣机制**：为了应对策略更新带来的非平稳性，引入折扣因子 $\lambda \in (0, 1]$。
    *   **更新公式**：在每个 epoch $\tau$，根据 $N$ 次采样中的成功次数 $S_\tau$，更新 Beta 分布的参数：
        *   $\alpha_\tau = \lambda \alpha_{\tau-1} + S_\tau$
        *   $\beta_\tau = \lambda \beta_{\tau-1} + (N - S_\tau)$
    *   **优势计算**：使用更新后的后验均值和方差来计算优势值（Advantage），由于 $\alpha, \beta$ 始终为正，从理论上彻底避免了方差崩溃。
*   **算法流程**：在标准 GRPO 流程中，不再只看当前 Batch 的奖励均值，而是维护一个针对每个 Prompt 的历史奖励统计量（$\alpha, \beta$），并在每次 Rollout 后进行折扣更新。

#### 3. 实验设计
*   **模型规模**：Qwen3-1.7B-Base 和 Qwen3-8B-Base。
*   **训练数据集**：DAPO-Math-17k（包含约 1.7 万条数学推理题目）。
*   **Benchmark（基准测试）**：
    *   **分布内 (ID)**：MATH500, Minerva, AIME24/25, AMC24, OlympiadBench。
    *   **分布外 (OOD)**：MMLU-Pro, GPQA-Diamond, Big-Bench Hard (BBH)。
*   **对比方法**：
    *   **Naive GRPO**：标准点估计方法。
    *   **RePO**：基于经验回放（Replay）的高级基准。
    *   **Dr.GRPO**：另一种优势函数变体。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   1.7B 模型：使用 **4 × H200** GPU。
    *   8B 模型：使用 **8 × H200** GPU。
    *   对比方法 RePO：由于内存开销大，统一使用 8 × H200。
*   **训练配置**：Rollout Batch Size 为 128，每个 Prompt 采样 8 个响应，总计训练 4 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.7B 到 8B 的模型跨度，并在 9 个主流推理基准上进行了验证。
*   **消融实验**：详细探讨了折扣因子 $\lambda$ 对性能的影响（测试了 0.25 到 1.0 的不同取值）。
*   **统计分析**：通过实证分析了均方误差（MSE），证明 DBB 在小样本（N < 16）下的估计准确度远高于点估计。
*   **客观性**：实验对比了最新的 Replay-based 方法，并展示了在不同优势函数（如 Dr.GRPO）上的通用性，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能提升**：GRPO-DBB 在所有基准上均优于原始 GRPO。1.7B 模型在 ID 任务平均提升 3.22 点，OOD 任务大幅提升 **12.49 点**；8B 模型分别提升 2.42 点和 6.92 点。
*   **稳定性**：DBB 有效缓解了训练中的熵增和响应长度缩短问题，实现了更深度的探索。
*   **MSE 降低**：DBB 通过引入微小偏差换取了方差的大幅降低，从而在统计学意义上实现了更低的均方误差。
*   **$\lambda$ 的敏感性**：较小的模型（1.7B）由于学习动态快，适合较小的 $\lambda$（0.5）；较大的模型（8B）学习较稳健，适合较大的 $\lambda$（0.75）。

#### 7. 优点
*   **零额外开销**：与需要存储 Token 概率和执行额外前向传播的 Replay 方法不同，DBB 仅需存储两个标量（$\alpha, \beta$），不增加显存和计算耗时。
*   **理论完备**：从统计估计视角重新定义 RLVR，解决了困扰组基方法的“方差崩溃”底层问题。
*   **泛化能力强**：在 OOD 任务上的巨大提升表明，更准确的奖励估计能显著增强模型的推理泛化性。

#### 8. 不足与局限
*   **超参数依赖**：折扣因子 $\lambda$ 对性能影响较大，且最佳取值随模型规模和任务动态变化，目前仍需手动调优。
*   **偏差引入**：虽然降低了方差，但 DBB 本质上是有偏估计，在奖励分布发生剧烈突变时，可能会产生滞后的估计偏差。
*   **场景限制**：目前仅在可验证奖励（二值奖励 0/1）的推理任务中验证，对于连续奖励或其他复杂奖励函数的适用性尚待研究。

（完）
