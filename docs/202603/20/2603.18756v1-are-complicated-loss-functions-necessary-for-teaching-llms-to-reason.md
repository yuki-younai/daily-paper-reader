---
title: Are complicated loss functions necessary for teaching LLMs to reason?
title_zh: 复杂的损失函数对于教大语言模型（LLMs）进行推理是必要的吗？
authors: "Gabriele Carrino, Andrea Sassella, Nicolo Brunello, Federico Toschi, Mark James Carman"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18756v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 对GRPO及大模型推理后训练技术的分析
tldr: 本研究探讨了提升大语言模型推理能力的后训练技术，重点分析了复杂的 GRPO 算法。研究发现负反馈对学习至关重要，而 PPO 风格的策略裁剪在推理任务中并非必需。基于此，作者提出了简化的 RGRA 算法，仅保留组相对优势估计而移除裁剪机制。实验证明 RGRA 在数学基准测试中表现优于 GRPO，为增强 LLM 推理能力提供了一种更高效、透明的替代方案。
selection_source: fresh_fetch
motivation: 旨在验证 GRPO 算法中复杂的 PPO 风格约束对于提升大语言模型数学推理能力是否具有必要性。
method: 提出 RGRA 算法，通过保留组相对优势估计来利用负反馈，同时剔除 PPO 中的策略比率裁剪等复杂组件。
result: 在标准数学基准测试中，简化的 RGRA 算法展现出比 GRPO 更强的性能表现。
conclusion: 研究表明简单的 REINFORCE 框架配合组相对优势即可有效提升推理能力，无需复杂的损失函数设计。
---

## 摘要
最近大语言模型（LLMs）的进展突显了后训练技术在提升推理和数学能力方面的重要性。组相对策略优化（GRPO）通过结合组相对优势估计、PPO 风格的裁剪以及 KL 正则化，在该领域展现出了前景。然而，其复杂性引发了一个疑问：是否所有组件对于培养推理行为都是必要的。我们对 GRPO 进行了系统分析，并得出两个关键发现：(1) 引入负反馈至关重要，仅在高于基准的动作上进行训练会限制学习效果；(2) PPO 风格的约束（如策略比例裁剪）对于提升数学推理或性能并非必需。基于这些见解，我们提出了带有组相对优势的 REINFORCE（RGRA），这是一种简化变体，它保留了组相对优势估计，但移除了 PPO 风格的裁剪和策略比例项。在标准数学基准测试上的实验表明，RGRA 有潜力实现比 GRPO 更强的性能。我们的结果表明，更简单的基于 REINFORCE 的方法可以有效地增强 LLMs 的推理能力，为 GRPO 提供了一个更透明且高效的替代方案。

## Abstract
Recent advances in large language models (LLMs) highlight the importance of post training techniques for improving reasoning and mathematical ability. Group Relative Policy Optimization (GRPO) has shown promise in this domain by combining group relative advantage estimation, PPO style clipping, and KL regularization. However, its complexity raises the question of whether all components are necessary for fostering reasoning behaviors. We conduct a systematic analysis of GRPO and identify two key findings: (1) incorporating negative feedback is essential training solely on actions above a baseline limits learning; and (2) PPO style constraints, such as policy ratio clipping, are not required to improve mathematical reasoning or performance. Building on these insights, we propose REINFORCE with Group Relative Advantage (RGRA), a simplified variant that retains group relative advantage estimation but removes PPO style clipping and policy ratio terms. Experiments across standard mathematical benchmarks indicate that RGRA has the potential to achieve stronger performance than GRPO. Our results suggest that simpler REINFORCE based approaches can effectively enhance reasoning in LLMs, offering a more transparent and efficient alternative to GRPO.

---

## 论文详细总结（自动生成）

这篇论文对当前大语言模型（LLM）推理训练中流行的 GRPO 算法进行了深入的“去碎片化”分析，探讨了复杂损失函数组件的必要性。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：为了教 LLM 进行复杂推理（如数学），像 GRPO 这样包含组相对优势、PPO 风格裁剪（Clipping）和 KL 正则化的复杂损失函数是否真的必要？
*   **研究动机**：DeepSeek-R1 等模型的成功让 GRPO 成为推理对齐的标准，但其公式设计较为复杂。作者怀疑其中的某些组件（尤其是源自 PPO 的策略裁剪）在 LLM 已经拥有强大预训练基座的情况下可能是冗余的。

### 2. 方法论：RGRA 算法
作者通过对 GRPO 的系统性消融，提出了一种简化的变体 —— **RGRA (REINFORCE with Group Relative Advantage)**。
*   **核心思想**：保留“组相对优势估计”以引入负反馈，但彻底移除 PPO 风格的“策略比率裁剪（Clipping）”和“策略比率项”。
*   **关键技术细节**：
    *   **组相对优势**：对同一个 Prompt 生成 $G$ 个回答，计算它们的奖励均值和标准差，将每个回答的奖励标准化为优势值 $\hat{A}$。这使得模型能识别哪些回答优于平均水平，哪些差于平均水平。
    *   **算法流程**：采用 REINFORCE 风格的梯度上升。在更新时，直接使用 $\nabla \log \pi \cdot \hat{A}$，并加上 KL 散度的梯度项。
    *   **对比变体**：
        1.  **GRPO-pos**：仅在优势为正（表现好）时训练，忽略负反馈。
        2.  **REINFORCE with Direct Rewards**：不使用组相对优势，直接用原始奖励训练。
        3.  **RAFT**：拒绝采样微调，仅在最优样本上进行监督微调。

### 3. 实验设计
*   **训练数据集**：从 **GSM8K** 训练集中随机抽取 1,800 个样本（小规模训练以验证效率）。
*   **评估基准（Benchmarks）**：
    *   **英文数学**：GSM8K, MATH, Gaokao2023-Math-En, OlympiadBench, AMC23。
    *   **中文数学**：CMATH, CN-Middle-School。
    *   **STEM 综合**：MMLU-STEM, Gaokao2024。
*   **对比方法**：GRPO、GRPO-pos、RGRA（本文方法）、RAFT、直接奖励的 REINFORCE、SFT（监督微调）。
*   **实验模型**：Qwen2.5-0.5B/1.5B-Instruct, Llama3.2-1B-Instruct。

### 4. 资源与算力
*   **微调技术**：使用 **LoRA**（Rank=128），可训练参数量约为原始模型的 10%。
*   **推理引擎**：使用 VLLM。
*   **具体算力说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但作者在结论中提到，由于“硬件限制（Hardware Constraints）”，未能测试更大规模的模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个不同架构/规模的模型，并在 9 个主流基准测试上进行了验证。
*   **消融实验**：针对“负反馈的必要性”和“裁剪的必要性”做了专门的对比实验（GRPO-pos vs RGRA vs REINFORCE）。
*   **充分性评价**：实验设计较为客观、公平。通过跨语言（中英）和跨领域（数学/STEM）的测试，证明了结论的普适性。虽然训练样本量较小（1800条），但足以观察到不同损失函数对模型行为（如推理链长度、奖励收敛）的影响。

### 6. 主要结论与发现
1.  **负反馈不可或缺**：只学习“好样本”（如 RAFT 或 GRPO-pos）会导致模型在小规模训练中迅速崩溃或陷入奖励作弊（输出极短回答），负反馈（告诉模型哪些回答不好）是稳定训练的关键。
2.  **优势估计是核心**：直接使用原始奖励的 REINFORCE 会导致训练不稳定，而组相对优势估计能有效平衡梯度。
3.  **PPO 裁剪并非必要**：在推理任务中，移除 PPO 的裁剪机制不仅不会降低稳定性，反而使 RGRA 在 27 项对比测试中的 17 项优于 GRPO。
4.  **推理行为的涌现**：RGRA 和 GRPO 都能诱导模型产生“反思”和“长链推理”行为，而简化方法（如 RAFT）则不能。

### 7. 优点
*   **简化了算法**：证明了在 LLM 场景下，复杂的 PPO 约束可以被更简单的 REINFORCE 变体取代，降低了实现难度。
*   **深入的消融分析**：清晰地拆解了 GRPO 成功的真正原因（组相对优势带来的负反馈），而非复杂的公式形式。
*   **跨语言验证**：在中文和英文基准上均表现出一致的改进。

### 8. 不足与局限
*   **模型规模有限**：实验仅限于 0.5B 到 1.5B 的小模型，尚未在大规模模型（如 70B+）上验证该结论是否依然成立。
*   **任务领域单一**：主要集中在数学和 STEM 推理，尚不清楚在创意写作或通用对话对齐中，移除裁剪是否会导致策略偏移过大。
*   **训练样本量较小**：1,800 条样本虽然能说明问题，但可能无法完全展现长周期训练下的潜在不稳定性。

（完）
