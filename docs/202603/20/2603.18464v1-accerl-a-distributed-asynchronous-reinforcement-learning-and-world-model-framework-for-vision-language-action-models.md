---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 视觉-语言-动作(VLA)模型的强化学习框架
tldr: AcceRL是一个针对大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习框架。它通过物理隔离训练、推理和采样过程，消除了同步障碍，并首次将可训练的世界模型集成到异步流水线中以生成虚拟经验。该框架在LIBERO基准测试中达到了SOTA水平，显著提升了硬件利用率、吞吐量和样本效率，为复杂控制任务提供了高效的训练方案。
selection_source: fresh_fetch
motivation: 旨在解决大规模VLA模型在强化学习训练中面临的计算效率低下和数据获取成本高昂的挑战。
method: 提出了一种完全异步且解耦的框架，通过物理隔离核心环节并集成即插即用的世界模型来生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现了吞吐量的超线性扩展以及极高的硬件利用率和样本效率。
conclusion: AcceRL证明了异步解耦架构与世界模型结合能显著提升大规模VLA模型的训练效率和稳定性。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

### AcceRL：面向视觉-语言-动作（VLA）模型的分布式异步强化学习与世界模型框架总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：将强化学习（RL）应用于大规模视觉-语言-动作（VLA）模型时，面临严重的**计算效率低下**和**数据获取瓶颈**。
*   **背景**：
    *   **同步障碍**：传统 RL 框架采用同步机制（如 PPO），训练器必须等待所有采样节点完成数据收集，导致高性能 GPU 在等待缓慢的物理模拟器时处于闲置状态（长尾效应）。
    *   **数据饥渴**：数十亿参数的 VLA 模型需要海量交互数据，而物理模拟器的运行速度和推理延迟限制了采样频率。
    *   **硬件利用率低**：大规模模型在训练、推理和模拟之间频繁切换，导致显存交换开销巨大。

#### 2. 方法论：核心思想与关键技术
AcceRL 提出了一个**完全异步且解耦**的架构，核心思想是将训练、推理和采样在物理上隔离。
*   **双层异步机制**：
    *   **宏观异步**：训练器（Trainer）与采样器（Rollout Worker）解耦。训练器持续从重放池采样更新，采样器独立生成轨迹并推送到池中，互不阻塞。
    *   **微观异步**：环境交互与模型推理解耦。采用“推理即服务”（Inference-as-a-Service）模式，采样器仅负责环境步进，推理请求发送至集中的推理池（Inference Pool），通过动态批处理（Dynamic Batching）最大化 GPU 利用率。
*   **集成世界模型（World Model）**：
    *   **虚拟经验生成**：集成可训练的扩散模型（基于 DIAMOND）作为观测模型，预测下一帧图像；配合奖励模型，实现“在想象中学习”。
    *   **样本效率提升**：通过在虚拟空间进行短程展开（Imagination Rollouts），绕过物理模拟器的速度限制，将样本效率提升了 200 倍。
*   **算法优化**：
    *   **策略滞后处理**：引入 **GIPO**（高斯重要性采样策略优化）替代标准 PPO 的硬截断，平滑处理异步带来的数据过期问题。
    *   **VLA 适配**：实施**词表瘦身**（仅保留动作 Token）、**动作感知注意力池化**（构建价值头）以及**Token 级策略优化**，增强训练稳定性。

#### 3. 实验设计
*   **基准测试（Benchmark）**：使用 **LIBERO** 机器人操作基准测试，涵盖四个子集：LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **对比方法**：
    *   **OpenVLA-OFT**：基于正交微调的模仿学习基准。
    *   **RLinf-VLA**：一种高效的 VLA 强化学习框架。
    *   **SimpleVLA-RL**：采用同步训练方案的 VLA-RL 代表。
*   **评估指标**：任务成功率、系统吞吐量（SPS，每秒样本数）、硬件利用率。

#### 4. 资源与算力
*   **硬件型号**：实验中明确提到了使用 **NVIDIA H200 GPU** 进行吞吐量扩展性测试。
*   **算力规模**：
    *   **扩展性实验**：测试了从 1 到 7 个 Trainer GPU 的扩展性，以及多达 64 个 Rollout Worker 的并行能力。
    *   **模型规模**：主要针对 7B 参数规模的 VLA 模型（如基于 Llama-2-7B 的 OpenVLA）。
*   **训练时长**：文中未给出总训练小时数，但展示了在 10,000 步环境交互内即可实现性能突破。

#### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **性能对比**：在 LIBERO 的四个维度上进行了全面的成功率对比。
    *   **系统测试**：详细测试了吞吐量随 GPU 数量增加的扩展曲线。
    *   **消融实验**：针对**价值重计算（Value Re-computation）**、**GIPO 算法**以及**世界模型集成**分别做了对比实验。
*   **充分性评价**：实验设计较为充分，不仅验证了算法的有效性（SOTA 性能），还深入探讨了分布式系统的扩展效率。通过对比同步与异步、有无世界模型等变量，客观地证明了 AcceRL 的优势。

#### 6. 主要结论与发现
*   **超线性扩展**：得益于 ZeRO-2 优化和异步架构，训练吞吐量随 GPU 数量增加呈现超线性增长（7 个 H200 GPU 达到 104.22 SPS）。
*   **SOTA 性能**：在 LIBERO-Long 长程任务中表现尤为突出，成功率达到 99.1%，远超模仿学习基准（90.7%）。
*   **极高效率**：世界模型增强版在仅需 10,000 次真实环境交互的情况下，即可使性能从 0.6 提升至 0.8 以上，样本效率提升 20,000%。
*   **稳定性**：GIPO 算法在处理异步带来的过期数据时，收敛速度比标准 PPO 快约 7.5 倍。

#### 7. 优点与亮点
*   **架构创新**：彻底解耦了训练、推理和模拟，解决了大规模模型 RL 训练中的“GPU 气泡”问题。
*   **即插即用世界模型**：首次在分布式异步 RL 流水线中无缝集成可训练的世界模型，实现了“零卸载”的想象采样。
*   **系统与算法协同**：不仅优化了系统吞吐量，还针对 VLA 的 Token 特性优化了 RL 损失函数，解决了数值不稳定问题。

#### 8. 不足与局限
*   **模型支持范围**：目前主要针对 VLA 模型的动作微调，尚未支持超大规模语言模型（LLM）的全量对齐（Post-training）。
*   **世界模型开销**：虽然世界模型减少了环境交互，但训练和运行扩散模型本身需要额外的计算资源和显存。
*   **任务复杂度**：实验主要集中在 LIBERO 模拟环境，尚未展示在真实物理机器人上的部署表现及跨场景泛化能力。

（完）
