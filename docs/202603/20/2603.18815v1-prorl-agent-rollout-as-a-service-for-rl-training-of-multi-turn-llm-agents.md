---
title: "ProRL Agent: Rollout-as-a-Service for RL Training of Multi-Turn LLM Agents"
title_zh: ProRL Agent：面向多轮 LLM 智能体强化学习训练的 Rollout 即服务
authors: "Hao Zhang, Mingjie Liu, Shaokun Zhang, Songyang Han, Jian Hu, Zhenghui Jin, Yuchi Zhang, Shizhe Diao, Ximing Lu, Binfeng Xu, Zhiding Yu, Jan Kautz, Yi Dong"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18815v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 用于多轮大模型智能体强化学习训练的基础设施
tldr: 本研究针对多轮LLM智能体在强化学习（RL）训练中面临的轨迹生成难题，提出了ProRL Agent。该系统采用“Rollout-as-a-Service”理念，通过API服务解耦了轨迹采样与训练循环，解决了现有架构难以迁移和维护的问题。它提供了可扩展的沙盒环境，支持在无根权限的HPC设置下运行多种任务。ProRL Agent已集成至NVIDIA NeMo Gym，显著提升了智能体在软件工程、数学及编程等复杂任务上的训练效率。
selection_source: fresh_fetch
motivation: 现有的LLM智能体强化学习框架通常将轨迹采样与训练循环紧密耦合，导致系统难以维护且难以在不同计算环境间迁移。
method: 提出了ProRL Agent架构，通过API提供全生命周期的轨迹生成服务，并构建了支持HPC环境的标准化、可扩展沙盒。
result: 在软件工程、数学、STEM和编程等多种任务上的强化学习训练验证了该系统的可扩展性和有效性。
conclusion: ProRL Agent为多轮LLM智能体提供了一个解耦且高效的训练基础设施，并作为开源工具集成到了NVIDIA NeMo Gym中。
---

## 摘要
多轮大语言模型（LLM）智能体在解决复杂的交互式任务中变得越来越重要，而强化学习（RL）是改善其长程行为的关键要素。然而，强化学习训练需要生成大量的沙箱化 rollout 轨迹，且现有的基础设施通常将 rollout 编排与训练循环耦合在一起，导致系统难以迁移和维护。秉持“rollout 即服务”的理念，我们提出了 ProRL Agent，这是一个可扩展的基础设施，通过 API 服务为完整的智能体 rollout 生命周期提供支持。ProRL Agent 还提供了标准化且可扩展的沙箱环境，支持在无根权限（rootless）的高性能计算（HPC）环境下执行多种智能体任务。我们通过在软件工程、数学、STEM 和编程任务上的强化学习训练验证了 ProRL Agent。ProRL Agent 已开源，并作为 NVIDIA NeMo Gym 的一部分进行了集成。

## Abstract
Multi-turn LLM agents are increasingly important for solving complex, interactive tasks, and reinforcement learning (RL) is a key ingredient for improving their long-horizon behavior. However, RL training requires generating large numbers of sandboxed rollout trajectories, and existing infrastructures often couple rollout orchestration with the training loop, making systems hard to migrate and maintain. Under the rollout-as-a-service philosophy, we present ProRL Agent , a scalable infrastructure that serves the full agentic rollout lifecycle through an API service. ProRL Agent also provides standardized and extensible sandbox environments that support diverse agentic tasks in rootless HPC settings. We validate ProRL Agent through RL training on software engineering, math, STEM, and coding tasks. ProRL Agent is open-sourced and integrated as part of NVIDIA NeMo Gym.

---

## 论文详细总结（自动生成）

### ProRL Agent: 面向多轮 LLM 智能体强化学习训练的 Rollout 即服务

#### 1. 论文的核心问题与整体含义
*   **研究动机**：多轮 LLM 智能体（Agent）在处理复杂任务（如软件工程、代码调试）时，需要通过强化学习（RL）来优化其长程决策能力。然而，现有的 RL 训练框架通常将 **Rollout（轨迹采样/环境交互）** 与 **训练循环（梯度更新）** 紧密耦合。
*   **核心矛盾**：Rollout 是 I/O 密集型的（涉及沙箱创建、工具调用、异步协调），而训练是 GPU 密集型的。这种耦合导致资源利用率低、系统难以维护、且在不同训练后端（如 VeRL 或 NeMo RL）之间迁移困难。
*   **整体含义**：论文提出了 **ProRL Agent**，倡导“Rollout-as-a-Service (RaaS)”理念，将智能体的全生命周期（环境初始化、执行、评估）解耦为一个独立的 API 服务，从而实现高效、可扩展且易于迁移的智能体 RL 训练。

#### 2. 论文提出的方法论
*   **核心思想**：通过 HTTP API 将 Rollout 逻辑从训练器中完全分离。训练器只需提交任务实例，ProRL Agent 服务器负责处理复杂的环境交互并返回带有奖励的完整轨迹。
*   **关键技术细节**：
    *   **三阶段异步流水线**：将 Rollout 分为 **INIT（沙箱初始化）**、**RUN（智能体执行）** 和 **EVAL（结果评估）**。每个阶段拥有独立的线程池，允许不同任务在不同阶段重叠执行，最大化吞吐量。
    *   **HPC 兼容的无根沙箱**：基于 **Singularity** 容器技术实现，无需 root 权限即可在 Slurm 管理的 HPC 集群上运行。每个容器分配独立的环回 IP，支持大规模并发。
    *   **Token-in/Token-out 通信**：直接传输 Token ID 而非纯文本，避免了因重新分词（Re-tokenization）导致的离策（Off-policy）偏差。
    *   **高效工具后端**：优化了 Bash（基于 ptyprocess 减少延迟）和 IPython（持久化内核减少重复设置）的执行效率。
    *   **LLM 后端管理**：使用最小堆（Min-heap）进行负载均衡，支持动态注册和检查点切换。
*   **算法流程**：采用 **DAPO（动态采样策略优化）** 算法，并针对 Agent 任务实现了异步补充机制，减少工人空闲时间并过滤无信息增益的提示词。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **软件工程 (SWE)**：使用 SWE-Bench Verified 评估模型解决 GitHub 真实问题的能力。
    *   **数学 (Math)**：使用 AMC 数据集，配合 IPython 工具进行验证。
    *   **STEM**：使用 SCP-116K 数据集，涉及 Web 搜索和数值计算。
    *   **编程 (Coding)**：使用 Codeforces 测试集，涉及文件编辑和测试驱动开发。
*   **对比方法**：
    *   基座模型：Qwen3-4B-Instruct, Qwen3-8B, Qwen3-14B。
    *   对比框架：SkyRL-v0（针对 SWE 任务的现有领先框架）。
    *   消融实验：对比了有无负载均衡、高效 Bash 优化、过期任务清理等组件的效果。

#### 4. 资源与算力
*   **GPU 资源**：所有 RL 训练均在 **32 台 NVIDIA H100 GPU** 上完成。
*   **训练配置**：Batch size 为 32，Mini-batch 为 8，每个实例生成 8 条 Rollout 轨迹。
*   **训练时长**：文中未明确给出总训练小时数，但展示了 60-100 个训练步骤（Steps）内的性能提升曲线。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 14B 的三个模型尺度，并在四个完全不同的智能体领域（软件、数学、STEM、代码）进行了验证。
*   **消融实验**：针对系统组件（负载均衡、Bash 效率等）和扩展性（节点数量与吞吐量的关系）做了专门分析。
*   **充分性评价**：实验设计较为充分，不仅验证了算法性能的提升，还重点测试了系统层面的扩展性（展示了近乎线性的扩展能力），客观地证明了“解耦”设计的优越性。

#### 6. 论文的主要结论与发现
*   **性能提升**：ProRL Agent 在所有模型尺度上均显著提升了 SWE-Bench Verified 的得分。例如，8B 模型在 RL 训练后性能提升了近一倍。
*   **通用性**：该框架能无缝适配多种工具（搜索、代码执行、文件编辑）和多种任务领域。
*   **系统效率**：通过解耦和异步流水线，系统在增加计算节点时表现出极高的扩展效率，且通过工具后端优化显著降低了 Rollout 的非推理耗时。

#### 7. 优点
*   **架构领先**：率先提出并实现了 Rollout-as-a-Service，解决了智能体 RL 训练中长期存在的工程痛点。
*   **HPC 友好**：针对科研和企业常用的 Slurm/Singularity 环境做了深度优化，具有极强的实操价值。
*   **工程严谨**：考虑了 Token 漂移、负载均衡、异步任务补充等底层细节，确保了训练的稳定性和高效性。
*   **开源贡献**：作为 NVIDIA NeMo Gym 的一部分开源，具有良好的生态支持。

#### 8. 不足与局限
*   **实验覆盖**：虽然涵盖了多个领域，但对于更复杂的长程任务（如涉及数百轮交互的任务）的稳定性讨论较少。
*   **通信开销**：虽然解耦带来了灵活性，但 HTTP API 通信在极高频交互场景下可能引入额外的网络延迟（尽管文中通过 UDS 等手段进行了优化）。
*   **环境依赖**：虽然支持无根部署，但对 Singularity 和特定 HPC 配置仍有一定依赖，在纯云原生（K8s）环境下的表现未做详细对比。

（完）
