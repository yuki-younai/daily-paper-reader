---
title: "PowerFlow: Unlocking the Dual Nature of LLMs via Principled Distribution Matching"
title_zh: PowerFlow：通过基于原则的分布匹配解锁大语言模型的双重特性
authors: "Ruishuo Chen, Yu Chen, Zhuoran Li, Longbo Huang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18363v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 针对大语言模型的无监督内部反馈强化学习 (RLIF)
tldr: 本研究针对无监督内部反馈强化学习（RLIF）中启发式奖励缺乏理论目标且易产生偏差的问题，提出了PowerFlow框架。该框架将无监督微调重构为分布匹配问题，利用GFlowNet作为变分采样器，并引入长度感知的轨迹平衡目标以消除长度偏差。通过调节α次幂分布，PowerFlow能定向强化模型的逻辑推理（分布锐化）或激发表达创意（分布平滑）。实验证明其性能优于现有RLIF方法，甚至可媲美有监督的GRPO，有效提升了生成质量与多样性。
selection_source: fresh_fetch
motivation: 现有的无监督强化学习方法依赖启发式内在奖励，缺乏明确的理论优化目标且容易产生退化偏差。
method: 提出基于GFlowNet的分布匹配框架，通过长度感知的轨迹平衡目标消除结构性长度偏差，并利用α次幂分布调节模型特性。
result: 实验显示PowerFlow在多项任务上优于现有RLIF方法，并在创意任务中实现了质量与多样性的同步提升，超越了有监督的GRPO。
conclusion: PowerFlow通过原则性的分布匹配方法，成功实现了对大模型逻辑推理与创意表达双重性质的定向激发与优化。
---

## 摘要
无监督内部反馈强化学习 (RLIF) 已成为一种在无需外部监督的情况下激发大语言模型 (LLM) 潜在能力的极具前景的范式。然而，当前的方法依赖于启发式的内在奖励，往往缺乏明确定义的理论优化目标，且容易产生退化偏差。在这项工作中，我们介绍了 PowerFlow，这是一个将无监督微调重新表述为分布匹配问题的原则性框架。通过将 GFlowNet 视为非归一化密度的摊销变分采样器，我们提出了一种长度感知的轨迹平衡 (Trajectory-Balance) 目标，显式地抵消了自回归生成中固有的结构性长度偏差。通过针对 α-幂分布，PowerFlow 能够定向激发 LLM 的双重特性：锐化分布 (α > 1) 以增强逻辑推理能力，或平滑分布 (α < 1) 以释放表达创造力。大量实验表明，PowerFlow 始终优于现有的 RLIF 方法，达到甚至超过了有监督的 GRPO。此外，通过减轻对齐模型中的过度锐化，我们的方法在多样性和质量方面同时取得了提升，移动了创造性任务中的帕累托前沿。

## Abstract
Unsupervised Reinforcement Learning from Internal Feedback (RLIF) has emerged as a promising paradigm for eliciting the latent capabilities of Large Language Models (LLMs) without external supervision. However, current methods rely on heuristic intrinsic rewards, which often lack a well-defined theoretical optimization target and are prone to degenerative biases. In this work, we introduce PowerFlow, a principled framework that reformulates unsupervised fine-tuning as a distribution matching problem. By casting GFlowNet as an amortized variational sampler for unnormalized densities, we propose a length-aware Trajectory-Balance objective that explicitly neutralizes the structural length biases inherent in autoregressive generation. By targeting $α$-power distributions, PowerFlow enables the directional elicitation of the dual nature of LLMs: sharpening the distribution ($α> 1$) to intensify logical reasoning, or flattening it ($α< 1$) to unlock expressive creativity. Extensive experiments demonstrate that PowerFlow consistently outperforms existing RLIF methods, matching or even exceeding supervised GRPO. Furthermore, by mitigating over-sharpening in aligned models, our approach achieves simultaneous gains in diversity and quality, shifting the Pareto frontier in creative tasks.

---

## 论文详细总结（自动生成）

### 论文总结：PowerFlow —— 通过原则性分布匹配解锁大语言模型的双重特性

#### 1. 核心问题与整体含义（研究动机和背景）
当前大语言模型（LLM）的无监督微调主要依赖**内部反馈强化学习（RLIF）**，即利用模型自身的确定性或一致性作为奖励信号。然而，现有 RLIF 方法存在三大痛点：
*   **缺乏理论目标**：大多依赖手工设计的启发式奖励（Heuristic Rewards），优化方向不明确。
*   **退化偏差**：在过度优化时容易出现长度坍缩（回答极短）、长度爆炸（重复啰嗦）、过度自信或模式坍缩（多样性丧失）。
*   **难以兼顾双重特性**：模型往往在强化逻辑推理（锐化分布）的同时，抑制了潜在的创意表达（平滑分布）。

**PowerFlow** 的核心动机是回归概率本质，将无监督微调重构为一个**分布匹配（Distribution Matching）**问题，通过调节单一参数 $\alpha$ 来定向激发模型的推理能力或创造力。

#### 2. 方法论
PowerFlow 框架建立在生成流网络（GFlowNet）和变分推断的基础上：
*   **核心思想**：将微调目标设定为基座模型的 **$\alpha$-幂分布**（$\alpha$-power distribution）：$p_\alpha(y|q) \propto p_{base}(y|q)^\alpha$。通过调节 $\alpha$，可以实现分布的**锐化**（$\alpha > 1$，增强推理）或**平滑**（$\alpha < 1$，释放创意）。
*   **关键技术：长度感知轨迹平衡（LA-TB）目标**：
    *   自回归模型中，路径概率随长度指数衰减，导致标准优化目标极易受长度偏差干扰。
    *   PowerFlow 引入了**长度归一化的能量表面**，将配分函数 $Z$ 重新参数化为长度相关的项 $Z_\phi(q, y) = (Z'_\phi(q))^{|y|}$。
*   **算法流程**：
    1.  使用策略网络 $\pi_\theta$ 采样生成轨迹。
    2.  计算长度感知的 TB 损失函数：
        $$L_{PowerFlow} = w \cdot \left( \log Z'_\phi(q) + \frac{1}{|y|} \log \pi_\theta(y|q) - \alpha \frac{1}{|y|} \log p_{base}(y|q) + \psi(y) \right)^2$$
        其中 $w$ 是重要性采样权重，$\psi(y)$ 是格式惩罚项。
    3.  同时更新策略参数 $\theta$ 和摊销配分函数估计器 $\phi$。

#### 3. 实验设计
*   **场景与数据集**：
    *   **逻辑推理**：使用 NuminaMath-CoT 进行训练。评估基准包括 MATH500、OlympiadBench、AIME24/25、AMC23 和 GPQA（科学推理）。
    *   **创意写作**：涵盖诗歌续写、故事创作和笑话编写。训练集 300 条，评估集 200 条，源自 PoemHunter、BookMIA 和 Reddit。
*   **对比方法**：
    *   **基准线**：Base 模型、Instruct 模型、低温采样（Low-temp）。
    *   **无监督 RLIF**：Intuitor（自确定性）、EMPO（语义熵）、TTRL（多数投票）、One-shot EM。
    *   **有监督方法**：GRPO（使用外部验证器奖励）。
    *   **推理采样**：PowerSampling（基于 MCMC 的幂分布采样）。

#### 4. 资源与算力
*   **模型规模**：实验涵盖了 Qwen2.5 (1.5B, 3B, 7B)、Qwen2.5-Math (1.5B, 7B) 以及 Llama-3.2 (3B)。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了超参数设置：Prompt Batch Size 为 128，推理任务学习率为 $1\times10^{-6}$ 至 $3\times10^{-6}$，创意任务为 $5\times10^{-7}$。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个模型系列、多个参数量级上进行了测试，涵盖了从数学竞赛到创意幽默的广泛任务。
*   **充分性**：
    *   **消融实验**：对 $\alpha$ 参数的敏感度进行了分析（$\alpha=2, 4, 6$）。
    *   **稳定性分析**：对比了标准 TB 目标与 LA-TB 目标在训练过程中的长度稳定性。
    *   **多样性评估**：利用 LLM-as-a-judge（DeepSeek-V3）对解题策略的多样性进行了量化评分。
*   **客观性**：对比了当前最先进的无监督方法，并与有监督的 GRPO 进行了直接对标，实验设计较为公平。

#### 6. 主要结论与发现
*   **推理增强（$\alpha > 1$）**：PowerFlow 在数学推理任务上显著优于现有 RLIF 方法，在多个指标上**达到甚至超过了有监督的 GRPO**。
*   **创意释放（$\alpha < 1$）**：对于已对齐的 Instruct 模型，PowerFlow 成功找回了被对齐算法抑制的创意多样性，实现了**质量与多样性的同步提升**（帕累托前沿移动）。
*   **长度稳定性**：LA-TB 目标有效解决了自回归生成中的长度坍缩问题，使训练过程更加单调稳定。
*   **多样性保持**：相比于其他 RL 方法，PowerFlow 在提升准确率的同时，能更好地保留多条解题路径。

#### 7. 优点
*   **理论严谨**：将微调目标从模糊的“奖励”转向明确的“分布匹配”，具有更强的数学根基。
*   **双向调控**：通过一个简单的 $\alpha$ 参数即可在“严谨推理”和“发散创意”之间切换，展现了极高的灵活性。
*   **解决结构偏差**：创新的长度感知目标（LA-TB）解决了 LLM 强化学习中长期存在的长度偏好问题。

#### 8. 不足与局限
*   **$\alpha$ 值的选择**：虽然论文给出了默认推荐值（推理 $\alpha=4$，创意 $\alpha=0.5$），但不同模型的最优 $\alpha$ 可能随其内在熵值的不同而变化，目前仍需手动调节。
*   **安全风险**：分布平滑（$\alpha < 1$）在释放创意的同时，可能会意外复现基座模型中被安全对齐（RLHF）抑制的有害内容。
*   **算力信息缺失**：未披露具体算力消耗，难以评估该方法的训练效率和落地成本。

（完）
