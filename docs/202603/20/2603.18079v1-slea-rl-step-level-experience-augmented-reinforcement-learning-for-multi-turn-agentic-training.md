---
title: "SLEA-RL: Step-Level Experience Augmented Reinforcement Learning for Multi-Turn Agentic Training"
title_zh: SLEA-RL：面向多轮智能体训练的步级经验增强强化学习
authors: "Prince Zizhuang Wang, Shuli Jiang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18079v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 针对多轮智能体训练的步级经验增强强化学习
tldr: 针对多轮工具使用任务中 LLM 智能体无法有效利用跨回合经验的问题，本文提出 SLEA-RL 框架。该框架通过在每个决策步骤根据当前观察动态检索相关经验，克服了传统静态检索的局限。其核心包含步骤级观察聚类、自进化经验库以及细粒度的策略优化。实验证明，SLEA-RL 在长周期多轮任务中显著优于现有强化学习基准，提升了智能体的决策质量。
selection_source: fresh_fetch
motivation: 传统经验增强方法在多轮任务中采用静态检索，导致检索到的经验随着环境状态的演变而变得不再适用。
method: 提出一种结合了步骤级观察聚类、自进化经验库以及细粒度信用分配策略优化的强化学习框架。
result: 在长周期多轮智能体基准测试中，SLEA-RL 展现出优于多种主流强化学习基准的性能表现。
conclusion: 通过在每个决策步骤动态引入实时相关的经验，能够显著增强 LLM 智能体处理复杂多轮任务的能力。
---

## 摘要
大语言模型（LLM）智能体在多轮工具使用任务中表现出色，但它们在训练过程中往往孤立运行，未能利用跨回合（episode）积累的经验。现有的经验增强方法通过将轨迹组织成可检索的库来解决这一问题，但它们仅根据初始任务描述检索一次经验，并在整个回合中保持不变。在观测值随每一步变化的多轮场景中，随着回合的推进，这种静态检索会变得越来越不匹配。我们提出了 SLEA-RL（步级经验增强强化学习），这是一个在每个决策步骤根据当前观测值检索相关经验的框架。SLEA-RL 通过三个组件运行：(i) 步级观测聚类，将结构等效的环境状态分组，以实现高效的聚类索引检索；(ii) 自进化经验库，通过基于评分的准入和速率限制的提取来提炼成功策略和失败模式；(iii) 带有步级信用分配的策略优化，用于在多轮回合中进行细粒度的优势估计。经验库通过语义分析而非梯度更新随策略同步进化。在长时程多轮智能体基准测试上的实验表明，与各种强化学习基线相比，SLEA-RL 取得了更优越的性能。

## Abstract
Large Language Model (LLM) agents have shown strong results on multi-turn tool-use tasks, yet they operate in isolation during training, failing to leverage experiences accumulated across episodes. Existing experience-augmented methods address this by organizing trajectories into retrievable libraries, but they retrieve experiences only once based on the initial task description and hold them constant throughout the episode. In multi-turn settings where observations change at every step, this static retrieval becomes increasingly mismatched as episodes progress. We propose SLEA-RL (Step-Level Experience-Augmented Reinforcement Learning), a framework that retrieves relevant experiences at each decision step conditioned on the current observation. SLEA-RL operates through three components: (i) step-level observation clustering that groups structurally equivalent environmental states for efficient cluster-indexed retrieval; (ii) a self-evolving experience library that distills successful strategies and failure patterns through score-based admission and rate-limited extraction; and (iii) policy optimization with step-level credit assignment for fine-grained advantage estimation across multi-turn episodes. The experience library evolves alongside the policy through semantic analysis rather than gradient updates. Experiments on long-horizon multi-turn agent benchmarks demonstrate that SLEA-RL achieves superior performance compared to various reinforcement learning baselines.

---

## 论文详细总结（自动生成）

以下是对论文《SLEA-RL: Step-Level Experience Augmented Reinforcement Learning for Multi-Turn Agentic Training》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 智能体（Agent）在多轮工具使用任务中存在“经验孤岛”和“检索失效”问题。
    *   **经验孤岛**：智能体在训练中通常是孤立运行的，无法利用跨回合（Episode）积累的成功或失败经验。
    *   **检索失效**：现有的经验增强方法（如 SkillRL）多采用**任务级检索**，即在任务开始时检索一次经验并保持不变。但在多轮任务中，环境状态随步骤剧烈变化，初始检索的经验会随着回合推进而变得不再适用（Stale Context）。
*   **研究动机**：提出一种能够随环境状态动态调整、在每个决策步骤提供精准指导的强化学习框架，使智能体能够“边做边学”并实时参考过去的教训。

### 2. 方法论：核心思想与关键技术
SLEA-RL 框架由三个核心组件交织而成：

*   **步级观测聚类（Step-level Observation Clustering）**：
    *   **核心思想**：将语义或结构相似的环境观测值归为等价类（Cluster）。
    *   **技术细节**：通过序列相似度函数和阈值 $\delta$ 将当前观测 $o_t$ 分配至聚类中心。这使得在某一状态下证明有效的经验可以立即惠及所有相似状态，提高了检索效率和跨轨迹的泛化能力。
*   **自进化经验库（Self-evolving Experience Library）**：
    *   **核心思想**：构建一个包含策略区（$E^+$，成功经验）和警告区（$E^-$，失败教训）的结构化仓库。
    *   **技术细节**：
        *   **语义提取**：利用辅助 LLM 对高分轨迹提取“原则、模式、示例”三层级的策略，对低分轨迹进行失败诊断。
        *   **质量控制**：采用基于得分的准入机制（Score-based Admission）和速率限制提取，确保库中存储的是当前策略下最高质量的知识，实现库与策略的协同进化。
*   **经验增强的策略优化（Policy Optimization）**：
    *   **核心思想**：在 GRPO 算法基础上引入步级信用分配。
    *   **技术细节**：
        *   **格式保持增强**：将检索到的经验放入系统提示词（System Role），保持用户观测格式不变，减少分布偏移。
        *   **多级优势估计**：最终优势函数 $\hat{A} = A_{episode} + w \cdot A_{step}$。其中 $A_{step}$ 是通过在相同观测聚类下的不同动作表现进行归一化计算得出的，从而为长序列中的中间步骤提供细粒度信号。

### 3. 实验设计
*   **数据集/场景**：
    1.  **ALFWorld**：具身决策基准，包含 6 类家务任务（Pick, Look, Clean, Heat, Cool, Pick2）。
    2.  **WebShop**：在线购物交互环境，拥有 110 万个产品和复杂的 HTML 导航。
    3.  **搜索增强 QA**：涵盖单跳（NQ, TriviaQA, PopQA）和多跳（HotpotQA, 2Wiki, MuSiQue, Bamboogle）共 7 个任务。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-2.5-Pro。
    *   **提示词/内存方法**：ReAct, Reflexion, Mem0, ExpeL, SimpleMem 等。
    *   **强化学习基线**：RLOO, GRPO, GiGPO, R3L, IGPO。
    *   **经验增强 RL**：MemRL, SkillRL（代表任务级检索）。

### 4. 资源与算力
*   **模型规模**：主要使用 **Qwen2.5-7B-Instruct**，并使用 **Qwen2.5-1.5B-Instruct** 验证扩展性。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：学习率 $1 \times 10^{-6}$，Batch Size 16，Group Size $G=8$，梯度累积 4 步。经验提取由模型自身完成，无需额外模型部署。

### 5. 实验数量与充分性
*   **实验规模**：在 3 大类、共计 10 余个子任务上进行了测试，涵盖了具身智能、Web 交互和知识检索等多种 Agent 场景。
*   **消融实验**：
    *   **步级 vs. 任务级检索**：证明了每一步动态检索的优越性。
    *   **步级优势权重 $w$**：验证了细粒度信用分配对性能的提升。
    *   **库容量 $C$**：探讨了经验库大小对检索精度的影响。
*   **充分性评价**：实验设计较为充分，通过不同规模模型的对比证明了方法的普适性，消融实验逻辑严密，能够客观支撑其核心贡献。

### 6. 主要结论与发现
*   **性能领先**：SLEA-RL 在 ALFWorld（93.5% 成功率）和 WebShop（76.3% 成功率）上均刷新了记录，显著优于 GiGPO 和 SkillRL。
*   **多步推理优势**：在需要复杂信息合成的多跳 QA 任务中（如 MuSiQue），SLEA-RL 相比基线有大幅提升（+9.0%），说明步级经验能有效引导复杂的推理链。
*   **小模型增益更大**：在 1.5B 模型上，SLEA-RL 带来的提升比例高于 7B 模型，表明经验增强可以有效弥补小模型内在知识的不足。

### 7. 优点
*   **动态性强**：突破了传统静态经验检索的限制，使经验指导与环境状态实时对齐。
*   **可解释性**：经验库以自然语言形式存在，不仅易于 LLM 理解，也方便人类开发者检查和干预。
*   **训练效率**：通过观测聚类减少了检索开销，且无需训练额外的 Critic 网络或 Value 网络。

### 8. 不足与局限
*   **计算开销**：虽然聚类索引提高了效率，但每一步都进行检索和 Prompt 拼接仍会增加推理延迟和 Token 消耗。
*   **依赖提取质量**：经验库的质量高度依赖于辅助 LLM 的总结能力，若提取出错误或误导性的策略，可能导致训练不稳定。
*   **系统复杂性**：相比纯 RL 算法，维护一个动态进化的经验库和聚类索引增加了工程实现和超参数调优的难度。

（完）
