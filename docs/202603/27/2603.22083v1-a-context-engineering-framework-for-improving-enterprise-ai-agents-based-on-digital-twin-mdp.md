---
title: A Context Engineering Framework for Improving Enterprise AI Agents based on Digital-Twin MDP
title_zh: 一种基于数字孪生 MDP 的改进企业级 AI 智能体的上下文工程框架
authors: "Xi Yang, Aurelie Lozano, Naoki Abe, Bhavya, Saurabh Jha, Noah Zheutlin, Rohan R. Arora, Yu Deng, Daby M. Sow"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22083v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 利用离线强化学习改进基于LLM的企业智能体
tldr: 针对企业级AI智能体在自动化决策中面临的数据质量差、反馈缺失等挑战，本文提出了一种基于数字孪生马尔可夫决策过程（DT-MDP）的上下文工程框架。该框架通过将智能体推理行为抽象为MDP，利用对比逆强化学习从混合质量的离线轨迹中学习奖励函数和策略，并以此指导上下文工程，显著提升了智能体在IT自动化等复杂任务中的决策性能。
selection_source: fresh_fetch
motivation: 企业级AI智能体在实际部署中受限于数据质量、推理复杂度和缺乏可靠反馈信号等问题。
method: 提出DT-MDP-CE框架，结合数字孪生MDP建模、对比逆强化学习策略诱导以及RL引导的上下文工程。
result: 在IT自动化领域的实验表明，该框架在多种评估设置下均显著优于基准智能体。
conclusion: 该框架为提升企业级智能体性能提供了一种轻量级且通用的离线强化学习解决方案。
---

## 摘要
尽管用于企业自动化和决策的 AI 智能体取得了快速进展，但其在现实世界中的部署和进一步的性能提升仍受限于数据质量和数量有限、复杂的现实推理需求、自我博弈的困难以及缺乏可靠的反馈信号。为了应对这些挑战，我们提出了一种轻量级、模型无关的框架，通过离线强化学习 (RL) 来改进基于大语言模型 (LLM) 的企业级智能体。所提出的基于 DT-MDP 的上下文工程 (DT-MDP-CE) 框架包含三个关键组件：(1) 数字孪生马尔可夫决策过程 (DT-MDP)，它将智能体的推理行为抽象为有限 MDP；(2) 鲁棒的对比逆向强化学习，利用 DT-MDP 高效地估计有据可依的奖励函数，并从质量参差不齐的离线轨迹中推导出策略；(3) 强化学习引导的上下文工程，利用从 (1) 和 (2) 的集成过程中获得的策略来改进智能体的决策行为。作为案例研究，我们将该框架应用于面向企业的 IT 自动化领域中的一个代表性任务。广泛的实验结果表明，在各种评估设置下，该框架相比基准智能体具有一致且显著的改进，这表明该框架可以推广到企业环境中具有类似特征的其他智能体。

## Abstract
Despite rapid progress in AI agents for enterprise automation and decision-making, their real-world deployment and further performance gains remain constrained by limited data quality and quantity, complex real-world reasoning demands, difficulties with self-play, and the lack of reliable feedback signals. To address these challenges, we propose a lightweight, model-agnostic framework for improving LLM-based enterprise agents via offline reinforcement learning (RL). The proposed Context Engineering via DT-MDP (DT-MDP-CE) framework comprises three key components: (1) A Digital-Twin Markov Decision Process (DT-MDP), which abstracts the agent's reasoning behavior as a finite MDP; (2) A robust contrastive inverse RL, which, armed with the DT-MDP, to efficiently estimate a well-founded reward function and induces policies from mixed-quality offline trajectories; and (3) RL-guided context engineering, which uses the policy obtained from the integrated process of (1) and (2), to improve the agent's decision-making behavior. As a case study, we apply the framework to a representative task in the enterprise-oriented domain of IT automation. Extensive experimental results demonstrate consistent and significant improvements over baseline agents across a wide range of evaluation settings, suggesting that the framework can generalize to other agents sharing similar characteristics in enterprise environments.

---

## 论文详细总结（自动生成）

### 论文总结：基于数字孪生 MDP 的企业级 AI 智能体上下文工程框架 (DT-MDP-CE)

#### 1. 核心问题与研究动机
企业级 AI 智能体（如用于 IT 运维的 SRE 智能体）在实际应用中面临四大挑战：
*   **数据困境**：高质量训练数据稀缺且多为私有，而复杂任务需要大量数据。
*   **推理复杂性**：现实世界的推理任务通常是多轮且依赖动态系统状态的。
*   **交互受限**：企业环境通常不允许大规模在线交互或自我博弈（Self-play）。
*   **反馈缺失**：缺乏可靠、可验证的中间步骤奖励信号。
**研究动机**：开发一种轻量级、模型无关的框架，利用有限且质量参差不齐的离线数据，通过强化学习（RL）优化智能体的决策行为，而无需对底层大模型进行昂贵的微调。

#### 2. 方法论
论文提出了 **DT-MDP-CE** 框架，包含三个核心组件：
*   **数字孪生 MDP (DT-MDP)**：
    *   **核心思想**：将 LLM 智能体在无限文本空间中的推理行为抽象为有限状态和动作空间的马尔可夫决策过程。
    *   **技术细节**：通过确定性抽象函数将观察结果映射为抽象状态（如基于拓扑结构的特征），将推理步骤映射为抽象动作。利用隐马尔可夫模型（HMM）进一步估计隐藏状态，提高建模精度。
*   **鲁棒对比逆强化学习 (Robust Contrastive IRL)**：
    *   **核心思想**：利用 T-REX 算法，通过对轨迹进行两两排名（由 LLM-as-a-judge 评分诱导）来学习奖励函数。
    *   **优势**：不需要手动设计奖励函数，且能从“好”和“坏”的混合轨迹中学习，超越单纯的模仿学习（Behavior Cloning）。
*   **强化学习引导的上下文工程 (RL-guided Context Engineering)**：
    *   **核心思想**：将离线学到的 RL 策略应用于在线执行阶段。
    *   **干预策略**：包括**提示建议**（在 Prompt 中加入高概率动作）、**剪枝探索**（过滤低概率动作）和**优先级排序**（重新排列探索队列）。

#### 3. 实验设计
*   **场景与数据集**：主要针对 **SRE（网站可靠性工程）诊断任务**。使用了 **ITBench** 基准测试，包含 12 个训练场景（如故障注入、网络中断）和 6 个未见过的测试场景。
*   **基准智能体**：对比了领域特定的 **EoG (Explanations over Graphs)** 智能体和通用的 **ReAct** 智能体。
*   **对比方法**：
    *   **Baseline**：原始未优化的智能体。
    *   **BC (Behavior Cloning)**：行为克隆。
    *   **RL-Sparse**：使用稀疏终端奖励的强化学习。
    *   **RL-IRL**：本文提出的基于逆强化学习的方案。
*   **评估指标**：Pass@3 Recall 和 Pass@3 F1 分数。

#### 4. 资源与算力
*   **模型使用**：实验使用了多种模型，包括 GPT-4o、Mistral-Medium-2505、Llama-3.1 系列（8B 到 405B）以及 Granite-3.2-8B。
*   **算力说明**：论文**未明确说明**训练 RL 策略的具体 GPU 型号、数量或训练时长。但提到使用了 `d3rlpy` 离线强化学习库，且由于 DT-MDP 是轻量级抽象，其计算开销远低于大模型微调。

#### 5. 实验数量与充分性
*   **实验规模**：收集了 819 条轨迹（共 12,079 轮交互）。
*   **充分性**：
    *   **多维度验证**：涵盖了不同模型家族（Mistral, Llama）、不同规模（Small, Medium, Large）以及不同领域（SRE 诊断到 SWE 软件工程）。
    *   **消融实验**：对比了三种不同的 MDP 抽象方式（基于名称、类型、拓扑）和三种上下文工程策略及其组合。
    *   **鲁棒性分析**：测试了专家轨迹数量对性能的影响，以及干预阈值的敏感度。
    *   **统计显著性**：使用了 Bonferroni 校正的配对 t 检验和 Nemenyi 检验，确保结果的客观性。

#### 6. 主要结论与发现
*   **性能提升**：DT-MDP-CE 在所有测试设置下均显著优于基准智能体，尤其在 Pass@3 指标上有大幅提升。
*   **IRL 的优越性**：基于逆强化学习（RL-IRL）诱导的策略在稳定性、鲁棒性和最终效果上均优于行为克隆（BC）和稀疏奖励 RL。
*   **泛化能力**：在 SRE 任务上训练的策略可以直接迁移到软件工程（SWE）任务中，且在不同规模的 LLM 上均有效（中等规模模型收益最大）。
*   **成本效益**：通过“剪枝探索”策略，该框架在提升准确率的同时，甚至能降低 Token 消耗和诊断时间。

#### 7. 优点与亮点
*   **轻量且模型无关**：无需微调 LLM 参数，通过外部策略干预 Prompt，易于在企业环境中部署。
*   **处理低质数据**：通过对比学习机制，能够从失败的尝试中提取有价值的信息，解决了企业数据质量差的问题。
*   **结构化抽象**：引入数字孪生 MDP，将复杂的文本推理转化为可优化的数学模型，增强了决策的可解释性。

#### 8. 不足与局限
*   **领域知识依赖**：构建 DT-MDP 的抽象函数（如状态和动作的定义）仍需要一定的领域专家知识。
*   **阈值设定**：上下文工程中的剪枝和建议阈值目前是静态设定的，可能需要针对不同任务进行手动调优。
*   **LLM 评测偏差**：实验依赖 LLM-as-a-judge（如 Gemini-2.5-Pro）进行评分，可能存在模型偏见或评估不一致的风险。
*   **应用限制**：目前主要在 IT 自动化领域验证，对于缺乏明确拓扑结构或逻辑链条的开放式任务，其抽象效果尚待验证。

（完）
