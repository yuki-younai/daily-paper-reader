---
title: "MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation"
title_zh: MMaDA-VLA：具有统一多模态指令与生成的扩散视觉-语言-动作大模型
authors: "Yang Liu, Pengxiang Ding, Tengyue Jiang, Xudong Wang, Wenxuan Song, Minghui Lin, Han Zhao, Hongyin Zhang, Zifeng Zhuang, Wei Zhao, Siteng Huang, Jinkui Shi, Donglin Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25406v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大型视觉-语言-动作模型中的跨模态对齐
tldr: 本研究针对现有视觉-语言-动作（VLA）模型在架构开销、时间一致性及环境动力学捕捉方面的不足，提出了MMaDA-VLA。这是一种原生预训练的大型扩散VLA模型，通过离散扩散公式将语言、图像和机器人控制嵌入统一的标记空间。该模型利用掩码标记去噪技术，并行生成未来目标观测和动作块，实现了多模态理解与生成的统一。实验证明，该方法在LIBERO和CALVIN等基准测试中达到了SOTA性能，显著提升了长程任务的成功率和一致性。
selection_source: fresh_fetch
motivation: 现有VLA模型存在架构冗余、时间不一致以及在缺乏额外模块时难以捕捉环境动力学的问题。
method: 提出一种原生的离散扩散框架，将语言、图像和动作统一嵌入同一标记空间，通过并行去噪同时预测未来视觉目标和动作序列。
result: "在LIBERO基准测试中实现了98.0%的平均成功率，并在CALVIN任务中取得了4.78的平均路径长度，均达到领域领先水平。"
conclusion: MMaDA-VLA通过统一的扩散生成机制，在无需辅助世界模型的情况下，有效增强了机器人操作的长期一致性和环境感知能力。
---

## 摘要
视觉-语言-动作（VLA）模型旨在根据视觉观测和自然语言指令控制机器人进行操作。然而，现有的分层和自回归范式通常会引入架构开销，面临时间不一致性和长时程误差累积的问题，并且缺乏在不增加额外模块的情况下捕获环境动力学的机制。为此，我们提出了 MMaDA-VLA，这是一个完全原生的预训练扩散 VLA 大模型，它在单一框架中统一了多模态理解与生成。我们的核心思想是一种原生的离散扩散公式，它将语言、图像和连续机器人控制嵌入到一个离散的 token 空间中，并使用掩码 token 去噪训练单个骨干网络，以并行地联合生成未来的目标观测和动作块（action chunk）。迭代去噪实现了全局、无序的细化，在不使用辅助世界模型的情况下，将动作锚定在预测的未来视觉结果中，从而提高了长时程一致性。在仿真基准测试和真实世界任务中的实验表明，该模型达到了最先进的性能，在 LIBERO 上实现了 98.0% 的平均成功率，在 CALVIN 上实现了 4.78 的平均长度。

## Abstract
Vision-Language-Action (VLA) models aim to control robots for manipulation from visual observations and natural-language instructions. However, existing hierarchical and autoregressive paradigms often introduce architectural overhead, suffer from temporal inconsistency and long-horizon error accumulation, and lack a mechanism to capture environment dynamics without extra modules. To this end, we present MMaDA-VLA, a fully native pre-trained large diffusion VLA model that unifies multi-modal understanding and generation in a single framework. Our key idea is a native discrete diffusion formulation that embeds language, images, and continuous robot controls into one discrete token space and trains a single backbone with masked token denoising to jointly generate a future goal observation and an action chunk in parallel. Iterative denoising enables global, order-free refinement, improving long-horizon consistency while grounding actions in predicted future visual outcomes without auxiliary world models. Experiments across simulation benchmarks and real-world tasks show state-of-the-art performance, achieving 98.0% average success on LIBERO and 4.78 average length on CALVIN.

---

## 论文详细总结（自动生成）

以下是对论文《MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型主要面临三大挑战：
*   **架构冗余与信息损失：** 许多模型采用分层设计（如在 VLM 上加策略头），增加了训练成本并可能导致跨模块的信息丢失。
*   **时间不一致性：** 自回归生成动作序列时，容易产生累积误差，且难以捕捉全局轨迹规划。
*   **缺乏环境动力学建模：** 大多数模型无法在不增加额外“世界模型”模块的情况下预测未来视觉状态，导致动作生成缺乏对环境演变的感知。
**研究动机：** 提出一种原生的、统一的离散扩散框架，将理解与生成整合，在单一骨干网络中同时实现动作预测和未来观测预测。

### 2. 方法论：核心思想与关键技术
**核心思想：** 采用**原生离散扩散（Native Discrete Diffusion）**公式，将语言指令、图像观测和连续机器人动作全部映射到统一的离散 Token 空间，通过掩码 Token 去噪（Masked Token Denoising）并行生成未来目标图像和动作块。

**关键技术细节：**
*   **统一 Token 化：** 文本使用 LLaDA 分词器，图像使用 MAGVIT-v2 量化器，动作则将每个维度离散化为 256 个 bin。
*   **并行生成架构：** 模型输入当前观测和指令，同时预测“目标观测图像”和“动作块（Action Chunk）”。这种设计让动作生成能隐式地参考预测的未来视觉状态。
*   **混合注意力机制（Hybrid Attention）：** 模态内部采用双向全注意力（实现全局信息交换），模态之间采用因果注意力（确保信息流向正确），避免了自回归顺序对无序动作维度的干扰。
*   **迭代去噪推理：** 推理时从全掩码状态开始，通过 $D$ 步迭代细化生成结果。引入了 **KV Cache 缓存框架**，通过选择性刷新策略降低推理延迟。

### 3. 实验设计
*   **仿真基准测试：**
    *   **LIBERO：** 包含 Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程）四个子集，评估多任务泛化能力。
    *   **CALVIN：** 采用 ABC→D 设置，评估长程语言条件下的操作及环境泛化能力。
*   **真实世界任务：** 使用 AgileX Piper 6-DoF 机械臂，涵盖简单取放、精准堆叠、复杂抽屉存储和长程桌面整理四类任务。
*   **对比方法：** 包括 OpenVLA、RT-1、π0、Diffusion Policy、UniVLA、WorldVLA、DreamVLA 等多种 SOTA 模型。

### 4. 资源与算力
*   **算力设备：** 使用了 **8 个 Nvidia H800 GPU 节点**（共 64 张显卡），每张显卡拥有 80GB 显存。
*   **训练时长：** 大规模预训练阶段耗时约 **30 小时**。
*   **数据规模：** 预训练使用了 6100 万步的跨机器人操作数据集。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了大规模预训练（61M steps）和针对性微调。在仿真中进行了数千次 rollout 评估（如 CALVIN 上的 1000 次 rollout）。
*   **消融实验：** 针对“世界模型（目标图像预测）”、“并行去噪”、“注意力机制类型”以及“预训练的作用”进行了详细对比。
*   **充分性评价：** 实验覆盖了从仿真到现实、从短程到长程、从单任务到多任务的多个维度，对比基线涵盖了最新的扩散类和自回归类 VLA 模型，实验设计较为客观、公平且充分。

### 6. 主要结论与发现
*   **性能领先：** MMaDA-VLA 在 LIBERO 上达到 98.0% 的平均成功率，在 CALVIN 上达到 4.78 的平均完成长度，均刷新了纪录。
*   **长程一致性：** 相比自回归模型，离散扩散机制显著提升了长程任务的稳定性，CALVIN 第五步任务的成功率从 80% 以下提升至约 90%。
*   **动力学感知的有效性：** 消融实验证明，并行预测未来目标图像能显著提升动作预测的准确性（平均长度提升 0.48）。
*   **预训练红利：** 大规模跨机器人预训练显著增强了模型的泛化能力和执行精度。

### 7. 优点与亮点
*   **架构优雅：** 实现了真正的“原生统一”，无需额外的策略头或独立的世界模型模块。
*   **并行解码优势：** 克服了自回归模型在处理无序动作维度时的归纳偏置问题，减少了误差累积。
*   **推理优化：** 引入训练无关的缓存机制，在保持扩散模型生成质量的同时，缓解了推理延迟问题。
*   **多模态融合：** 混合注意力机制有效地平衡了模态内的全局理解与模态间的逻辑依赖。

### 8. 不足与局限
*   **视觉保真度有限：** 生成的目标图像虽然在语义和动力学上一致，但在细节（如夹爪几何形状、微小物体）上存在模糊，这受限于 Token 数量的压缩。
*   **推理开销：** 尽管有缓存优化，但迭代去噪的推理成本仍高于单次前向传播的模型，对实时性要求极高的场景可能仍有挑战。
*   **数据依赖：** 模型性能高度依赖于大规模高质量的预训练数据，对于数据稀缺的特殊机器人形态可能存在适配成本。

（完）
