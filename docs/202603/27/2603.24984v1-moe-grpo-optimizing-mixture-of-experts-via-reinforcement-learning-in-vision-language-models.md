---
title: "MoE-GRPO: Optimizing Mixture-of-Experts via Reinforcement Learning in Vision-Language Models"
title_zh: MoE-GRPO：在视觉语言模型中通过强化学习优化混合专家模型
authors: "Dohwan Ko, Jinyoung Park, Seoung Choi, Sanghyeok Lee, Seohyun Lee, Hyunwoo J. Kim"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24984v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 基于强化学习的MoE视觉语言模型专家路由优化框架
tldr: 本研究针对视觉语言模型（VLM）中混合专家（MoE）架构的 Top-K 路由机制易导致专家过拟合及选择非最优的问题，提出了 MoE-GRPO 框架。该框架将专家选择建模为序列决策问题，利用群体相对策略优化（GRPO）算法，通过强化学习探索更优的专家组合。此外，引入模态感知路由引导以增强训练稳定性。实验证明，该方法在多模态基准测试中显著提升了专家选择的多样性和模型性能。
selection_source: fresh_fetch
motivation: 传统的确定性 Top-K 路由机制限制了专家组合的最优性，且容易导致专家过拟合。
method: 提出基于强化学习的 MoE-GRPO 框架，结合 GRPO 算法和模态感知路由引导来优化专家选择策略。
result: 在多项图像和视频多模态基准测试中，MoE-GRPO 的表现优于标准 Top-K 路由及其变体。
conclusion: 通过强化学习优化路由策略，可以有效提升 MoE 架构在视觉语言任务中的专家多样性与任务专业化能力。
---

## 摘要
混合专家模型（MoE）已成为降低 Transformer 架构计算开销的一种有效方法，它通过为每个 token 稀疏地激活一部分参数，同时保持了较高的模型容量。这一范式最近已被扩展到视觉语言模型（VLM），从而在降低计算成本的同时实现了可扩展的多模态理解。然而，广泛采用的确定性 top-K 路由机制可能会忽略更优的专家组合，并导致专家过拟合。为了解决这一局限性并提高专家选择的多样性，我们提出了 MoE-GRPO，这是一个基于强化学习（RL）的框架，用于优化基于 MoE 的 VLM 中的专家路由。具体而言，我们将专家选择建模为一个序列决策问题，并使用组相对策略优化（GRPO）对其进行优化，使模型能够通过探索和基于奖励的反馈来学习自适应专家路由策略。此外，我们引入了一种模态感知路由引导，通过阻止路由器探索针对特定模态激活频率较低的专家，从而增强了训练的稳定性和效率。在多模态图像和视频基准测试上的大量实验表明，MoE-GRPO 通过促进更多样化的专家选择，始终优于标准的 top-K 路由及其变体，从而缓解了专家过拟合，并实现了任务级的专家专业化。

## Abstract
Mixture-of-Experts (MoE) has emerged as an effective approach to reduce the computational overhead of Transformer architectures by sparsely activating a subset of parameters for each token while preserving high model capacity. This paradigm has recently been extended to Vision-Language Models (VLMs), enabling scalable multi-modal understanding with reduced computational cost. However, the widely adopted deterministic top-K routing mechanism may overlook more optimal expert combinations and lead to expert overfitting. To address this limitation and improve the diversity of expert selection, we propose MoE-GRPO, a reinforcement learning (RL)-based framework for optimizing expert routing in MoE-based VLMs. Specifically, we formulate expert selection as a sequential decision-making problem and optimize it using Group Relative Policy Optimization (GRPO), allowing the model to learn adaptive expert routing policies through exploration and reward-based feedback. Furthermore, we introduce a modality-aware router guidance that enhances training stability and efficiency by discouraging the router from exploring experts that are infrequently activated for a given modality. Extensive experiments on multi-modal image and video benchmarks show that MoE-GRPO consistently outperforms standard top-K routing and its variants by promoting more diverse expert selection, thereby mitigating expert overfitting and enabling a task-level expert specialization.

---

## 论文详细总结（自动生成）

这篇论文提出了 **MoE-GRPO**，这是首个将强化学习（RL）应用于视觉语言模型（VLM）中混合专家（MoE）路由优化的框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 MoE 架构通常采用**确定性的 Top-K 路由机制**（基于门控分数选择前 K 个专家）。
*   **研究动机**：
    *   **缺乏探索**：确定性选择限制了对不同专家组合的探索，可能错过更优的路由路径。
    *   **专家过拟合**：模型容易过度依赖一小部分专家，导致参数利用率低。
    *   **缺乏全局优化**：现有的路由算法多是局部贪婪的，没有从最终任务奖励的角度来优化专家选择策略。

### 2. 方法论
*   **核心思想**：将专家选择建模为一个**序列决策问题**，利用群体相对策略优化（GRPO）算法，通过奖励反馈来训练路由器的决策策略。
*   **关键技术细节**：
    *   **Token-GRPO**：优化 token 序列的生成质量，确保在特定专家组合下生成的回答是正确的。
    *   **Gate-GRPO**：直接优化每一层门控网络的策略。它通过比较同一组内不同随机采样路径（Rollouts）的相对奖励，强化高奖励的专家选择，抑制低奖励的选择。
    *   **模态感知路由引导（Modality-Aware Router Guidance）**：为了缩小 RL 的搜索空间，预先计算专家对视觉和文本模态的亲和力，在探索时屏蔽掉与当前 token 模态不匹配的专家，从而提高训练稳定性和效率。
    *   **奖励函数**：采用基于准确性的验证奖励（正确为 1，错误为 0），将离散的正确性信号转化为密集的梯度信号。

### 3. 实验设计
*   **模型架构**：将 **InternVL3.5-1B** 转换为 MoE 架构（8 选 2，激活参数 1.3B，总参数 2.9B）。
*   **数据集与基准测试**：
    *   **图像理解**：MME, MMBench, MMStar, MMT-Bench, AI2D。
    *   **视频理解**：VideoMME, MLVU, LongVideoBench, MVBench。
    *   **泛化性测试**：在 CLIP-MoE 上进行 ImageNet 跨数据集评估和领域泛化实验。
*   **对比方法**：
    *   **Det-FT**：标准确定性微调。
    *   **Stoch-FT-Multi/Noise**：引入多项式采样或高斯噪声的随机微调变体。
    *   **其他路由算法**：Expert Choice 路由、最优传输（Sinkhorn）路由。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA RTX Pro 6000 Blackwell Max-Q GPU**。
*   **训练配置**：从 One-Thinker 采样 100K 条多模态指令数据，训练 25,000 步。
*   **时长**：总训练时间约为 **1 天**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个主流多模态基准测试，并额外进行了 10 个数据集的跨任务泛化实验。
*   **消融实验**：详细分析了 Token-GRPO 与 Gate-GRPO 的各自贡献、模态感知引导的有效性，以及不同 RL 算法（DAPO, SAPO）的影响。
*   **客观性**：通过可视化专家利用率分布（熵值分析）和任务级专业化程度（JSD 散度），从定性和定量两个维度证明了方法的有效性，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **性能提升**：MoE-GRPO 在 7/9 的基准测试中优于所有基准方法，平均准确率提升约 1.7%~2.3%。
*   **多样性增强**：相比确定性路由，MoE-GRPO 显著提高了专家选择的熵值（从 1.05 提升至 1.82），减少了对特定专家的过度依赖。
*   **任务专业化**：模型学会了根据不同任务（如视频分类 vs. 空间推理）调用不同的专家组合，实现了更高水平的任务级专业化。
*   **泛化能力**：在未见过的领域和数据集上表现出更强的鲁棒性，有效缓解了 MoE 的过拟合问题。

### 7. 优点与亮点
*   **创新性**：首次将 GRPO 这种高效的 RL 算法引入 MoE 路由优化，打破了传统 Top-K 路由的局限。
*   **训练效率**：通过模态感知引导解决了 RL 在大规模离散动作空间中搜索效率低下的痛点。
*   **即插即用**：该框架不依赖于特定的预训练路由策略，可以从头开始训练门控网络。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 1B-3B 级别的小规模模型上，尚未在百亿或千亿级参数的大模型上验证其扩展性。
*   **奖励函数单一**：目前仅使用二值化的准确性奖励（0/1），对于需要复杂推理或长文本生成的任务，可能需要更细粒度的奖励模型（Reward Model）。
*   **计算开销**：虽然训练稳定，但 RL 训练过程中的多次 Rollout 采样相比标准微调仍需更多的计算资源。

（完）
