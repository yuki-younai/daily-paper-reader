---
title: "MSRL: Scaling Generative Multimodal Reward Modeling via Multi-Stage Reinforcement Learning"
title_zh: MSRL：通过多阶段强化学习扩展生成式多模态奖励建模
authors: "Chenglong Wang, Yifu Huo, Yang Gan, Qiaozhi He, Qi Meng, Bei Li, Yan Wang, Junfu Liu, Tianhua Zhou, Jingbo Zhu, Tong Xiao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25108v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 多模态奖励建模的多阶段强化学习
tldr: 针对多模态奖励模型（MRM）训练中多模态偏好数据获取成本高、难以扩展的问题，本文提出多阶段强化学习（MSRL）框架。该方法通过先从大规模文本数据学习奖励推理能力，再通过基于描述和全多模态的强化学习阶段逐步迁移至多模态任务，并结合跨模态知识蒸馏提升泛化性。实验证明，MSRL在无需额外多模态标注的情况下，显著提升了模型在视觉理解与生成任务上的表现。
selection_source: fresh_fetch
motivation: 现有的多模态奖励模型训练高度依赖昂贵的人工标注多模态偏好数据，限制了模型的扩展性。
method: 提出一种多阶段强化学习方法，通过文本预训练、基于描述的RL和全多模态RL三个阶段，将奖励推理能力从纯文本迁移至多模态领域。
result: "在VL-RewardBench和GenAI-Bench等基准测试中，模型性能分别从66.6%提升至75.9%以及从70.2%提升至75.7%。"
conclusion: MSRL有效地实现了生成式多模态奖励模型的规模化训练，在不增加多模态标注负担的前提下显著增强了模型的跨模态偏好建模能力。
---

## 摘要
多模态奖励建模的最新进展主要由从判别式方法向生成式方法的范式转变所驱动。在此基础上，近期的研究进一步采用了基于可验证奖励的强化学习（RLVR）来增强多模态奖励模型（MRM）。尽管取得了成功，但基于 RLVR 的训练通常依赖于标注的多模态偏好数据，这些数据的获取成本高昂且耗费人力，导致难以扩展 MRM 的训练规模。为了克服这一局限性，我们提出了一种多阶段强化学习（MSRL）方法，该方法可以在多模态数据有限的情况下实现 MRM 的可扩展强化学习。MSRL 取代了传统的基于 RLVR 的训练范式，首先从大规模文本偏好数据中学习可泛化的奖励推理能力，然后通过基于描述（caption-based）和全多模态的强化学习阶段，逐步将这种能力迁移到多模态任务中。此外，我们引入了一种跨模态知识蒸馏方法，以提高 MSRL 内部的偏好泛化能力。大量实验表明，MSRL 有效地扩展了生成式 MRM 的 RLVR 训练，并在视觉理解和视觉生成任务中显著提升了性能（例如，在 VL-RewardBench 上从 66.6% 提升至 75.9%，在 GenAI-Bench 上从 70.2% 提升至 75.7%），且无需额外的多模态偏好标注。我们的代码可在以下地址获取：https://github.com/wangclnlp/MSRL。

## Abstract
Recent advances in multimodal reward modeling have been largely driven by a paradigm shift from discriminative to generative approaches. Building on this progress, recent studies have further employed reinforcement learning from verifiable rewards (RLVR) to enhance multimodal reward models (MRMs). Despite their success, RLVR-based training typically relies on labeled multimodal preference data, which are costly and labor-intensive to obtain, making it difficult to scale MRM training. To overcome this limitation, we propose a Multi-Stage Reinforcement Learning (MSRL) approach, which can achieve scalable RL for MRMs with limited multimodal data. MSRL replaces the conventional RLVR-based training paradigm by first learning a generalizable reward reasoning capability from large-scale textual preference data, and then progressively transferring this capability to multimodal tasks through caption-based and fully multimodal reinforcement-learning stages. Furthermore, we introduce a cross-modal knowledge distillation approach to improve preference generalization within MSRL. Extensive experiments demonstrate that MSRL effectively scales the RLVR-based training of generative MRMs and substantially improves their performance across both visual understanding and visual generation tasks (e.g., from 66.6% to 75.9% on VL-RewardBench and from 70.2% to 75.7% on GenAI-Bench), without requiring additional multimodal preference annotations. Our code is available at: https://github.com/wangclnlp/MSRL.

---

## 论文详细总结（自动生成）

以下是对论文《MSRL: Scaling Generative Multimodal Reward Modeling via Multi-Stage Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态奖励模型（MRM）的训练正从判别式转向生成式（基于思维链 CoT 推理），并开始引入可验证奖励的强化学习（RLVR）。然而，RLVR 极度依赖高质量的人工标注多模态偏好数据，这类数据获取成本极高，成为限制 MRM 性能扩展的“数据瓶颈”。
*   **研究动机**：作者挑战了“必须依靠更多多模态数据才能提升性能”的假设，提出偏好建模的核心推理能力可以从海量的纯文本数据中学习，并有效地迁移到多模态领域。

### 2. 方法论
MSRL 提出了一种三阶段强化学习框架，旨在将文本领域的偏好推理能力逐步迁移至多模态任务：
*   **阶段 1：文本数据强化学习（RL on Textual Data）**
    *   利用大规模（约 400k）纯文本偏好数据，通过 GRPO（群组相对策略优化）算法训练模型。
    *   目标是建立强大的通用奖励推理基础。此阶段冻结视觉编码器。
*   **阶段 2：偏好泛化（Preference Generalization）**
    *   **基于描述的 RL（Caption-Based RL）**：将多模态数据中的图像/视频替换为详细的文本描述（Caption），在保持纯文本输入的同时引入多模态语义场景。引入“任务识别奖励”以区分不同任务类型。
    *   **跨模态知识蒸馏（CMKD）**：利用阶段 2 训练好的模型作为教师，生成高质量推理链，通过监督微调（SFT）引导学生模型在仅输入图像的情况下复现这些推理过程，从而弥合模态间隙。
*   **阶段 3：多模态数据强化学习（RL on Multimodal Data）**
    *   使用少量（约 20k）真实多模态偏好数据进行最终适配。由于前两阶段已打下基础，此阶段对数据量的需求大幅降低。

### 3. 实验设计
*   **模型骨干**：采用 InternVL3.5 系列（涵盖 1B, 4B, 8B, 14B 四种规模）。
*   **数据集与场景**：
    *   **视觉理解**：VL-RewardBench, Multimodal RewardBench。
    *   **视觉生成**：GenAI-Bench (图像), VideoGen-RewardBench (视频)。
    *   **视频理解**：ShareGPTVideo。
*   **对比方法**：
    *   **MLLM-as-a-Judge**：GPT-4o, GPT-5-mini, Gemini-1.5-Pro 等。
    *   **开源 MRM**：R1-Reward, UnifiedReward, LLaVA-Critic 等。
    *   **基准线**：在相同多模态数据上训练的判别式和生成式 MRM。

### 4. 资源与算力
*   **训练框架**：使用 `ms-swift` 框架进行 GRPO 训练。
*   **超参数**：采样大小为 8，学习率为 1e-6，Batch Size 为 128。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体的训练总时长，但考虑到模型规模（最高 14B）和多阶段 RL 流程，通常需要大规模 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：在 4 种不同参数规模的骨干网络上进行了验证，证明了方法的**可扩展性（Scaling Behavior）**。
*   **消融实验**：详细分析了三个阶段各自的贡献，以及 CMKD 和不同数据混合比例对结果的影响。
*   **任务覆盖**：涵盖了图像/视频的理解与生成共四大类任务，实验设计全面。
*   **客观性**：通过在相同数据集上对比判别式和生成式基准模型，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能飞跃**：MSRL 在不增加多模态标注的情况下，显著提升了 MRM 的准确率（如 8B 模型在 VL-RewardBench 上从 66.6% 提升至 75.9%）。
*   **扩展规律**：随着模型参数增加，MSRL 带来的增益更加显著，证明文本 RL 是增强多模态奖励建模的有效轴线。
*   **低数据效率**：一旦建立了强大的文本奖励推理能力，仅需极少量（如 5k）多模态样本即可实现优秀的适配效果。

### 7. 优点
*   **创新性路径**：通过“文本推理 -> 描述迁移 -> 多模态适配”的路径绕过了昂贵的多模态标注瓶颈。
*   **统一建模**：一个模型即可同时处理理解和生成任务的奖励预测。
*   **知识蒸馏设计**：CMKD 有效解决了文本描述与真实图像之间的模态鸿沟问题。

### 8. 不足与局限
*   **依赖高质量文本偏好**：虽然文本数据丰富，但 MSRL 的效果仍高度依赖于第一阶段文本 RL 的质量。
*   **流程复杂性**：三阶段训练相比单阶段直接训练更为复杂，涉及多次模型切换和数据处理。
*   **描述质量依赖**：阶段 2 的效果受限于自动生成的 Caption 的准确性（文中使用了 GPT-5 生成描述）。

（完）
