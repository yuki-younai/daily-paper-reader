---
title: "Train at Moving Edge: Online-Verified Prompt Selection for Efficient RL Training of Large Reasoning Model"
title_zh: 在移动边缘训练：面向大推理模型高效强化学习训练的在线验证提示词选择
authors: "Jiahao Wu, Ning Lu, Shengcai Liu, Kun Wang, Yanting Yang, Li Qing, Ke Tang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25184v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 通过在线验证的提示词选择实现高效的强化学习后训练
tldr: 针对大语言模型推理任务中强化学习（RL）训练开销巨大的问题，本文提出了一种名为HIVE的提示词选择框架。研究发现高价值样本集中在难度适中且不确定性高的“学习边缘”，且该边缘随训练动态变化。HIVE通过历史奖励轨迹进行粗选，并利用实时提示词熵进行精选，在多个数学推理基准上显著提升了训练效率且不损失性能。
selection_source: fresh_fetch
motivation: 现有的RL训练中大量提示词产生的梯度极小，导致计算资源在低效样本上被大量浪费。
method: 提出HIVE框架，利用历史奖励轨迹进行初步筛选，并结合实时提示词熵作为代理指标来剔除效用过时的样本。
result: 实验表明，HIVE在不损害模型最终推理性能的前提下，显著降低了Rollout阶段的计算开销。
conclusion: 动态选择处于“学习边缘”的高效提示词是提升大语言模型强化学习训练效率的关键路径。
---

## 摘要
强化学习（RL）已成为大语言模型（LLM）在推理任务中进行后训练的关键手段。虽然扩大采样（rollouts）规模可以稳定训练并提升性能，但计算开销是一个关键问题。在 GRPO 等算法中，每个提示词进行多次采样会产生极高的成本，因为很大一部分提示词提供的梯度微乎其微，效用较低。为了解决这一问题，我们研究了如何在采样阶段之前选择高价值的提示词。我们的实验分析表明，样本效用是不均匀且不断演变的：最强的学习信号集中在“学习边缘”（learning edge），即中等难度与高不确定性的交汇处，且这一边缘随着训练的进行而发生偏移。受此启发，我们提出了 HIVE（基于历史信息和在线验证的提示词选择），这是一个用于数据高效强化学习的双阶段框架。HIVE 利用历史奖励轨迹进行粗选，并采用提示词熵作为实时代理，以剔除效用过时的实例。通过在多个数学推理基准测试和模型上评估 HIVE，我们证明了 HIVE 在不损失性能的情况下显著提高了采样效率。

## Abstract
Reinforcement learning (RL) has become essential for post-training large language models (LLMs) in reasoning tasks. While scaling rollouts can stabilize training and enhance performance, the computational overhead is a critical issue. In algorithms like GRPO, multiple rollouts per prompt incur prohibitive costs, as a large portion of prompts provide negligible gradients and are thus of low utility. To address this problem, we investigate how to select high-utility prompts before the rollout phase. Our experimental analysis reveals that sample utility is non-uniform and evolving: the strongest learning signals concentrate at the ``learning edge", the intersection of intermediate difficulty and high uncertainty, which shifts as training proceeds. Motivated by this, we propose HIVE (History-Informed and online-VErified prompt selection), a dual-stage framework for data-efficient RL. HIVE utilizes historical reward trajectories for coarse selection and employs prompt entropy as a real-time proxy to prune instances with stale utility. By evaluating HIVE across multiple math reasoning benchmarks and models, we show that HIVE yields significant rollout efficiency without compromising performance.

---

## 论文详细总结（自动生成）

以下是对论文《Train at Moving Edge: Online-Verified Prompt Selection for Efficient RL Training of Large Reasoning Model》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：强化学习（RL，如 GRPO 算法）已成为提升大语言模型（LLM）推理能力的关键技术。然而，RL 训练中极其耗时的环节是 **Rollout（采样生成）**，通常需要对每个提示词（Prompt）生成多个响应。
*   **核心问题**：在训练过程中，大量提示词产生的梯度几乎为零（要么太简单模型已掌握，要么太难模型无法理解），导致昂贵的计算资源被浪费在低效样本上。
*   **研究动机**：如何精准、低成本地识别并选择那些能提供强学习信号的“高价值”提示词，从而在不损失性能的前提下大幅提升训练效率？

### 2. 方法论：HIVE 框架
HIVE（History-Informed and online-VErified prompt selection）采用**双阶段层次化筛选**机制：

*   **核心思想**：定位“学习边缘”（Learning Edge），即处于中等难度且模型对其输出高度不确定的样本交汇区。
*   **第一阶段：历史信息驱动的选择（History-Informed Selection）**
    *   **奖励得分**：利用历史训练轨迹，统计提示词连续出现零方差（全对或全错）的次数，通过概率衰减机制降低这些“无用”样本的采样频率。
    *   **熵得分**：利用历史响应熵（Response Entropy）衡量模型对该题目的不确定性。
    *   **粗选**：结合上述两项指标进行概率采样，初步过滤掉明显的简单或极难样本。
*   **第二阶段：在线验证的选择（Online-Verified Selection）**
    *   **解决元数据陈旧问题**：由于模型参数在实时更新，历史数据会迅速过时。
    *   **关键技术：提示词熵（Prompt Entropy）**：HIVE 提出使用提示词本身的 token 熵作为响应效用的**高效代理指标**。计算提示词熵仅需一次前向传播，成本远低于完整的 Rollout 生成。
    *   **动态门控**：计算候选池中所有提示词的熵，仅保留熵值高于中位数的样本进入最终的 Rollout 阶段。

### 3. 实验设计
*   **数据集**：
    *   **DAPO+MATH (DM)**：结合了 DAPO 和标准 MATH 数据集。
    *   **OPEN-R1 30k (OR1)**：Open-R1 数学数据集的子集。
*   **评测基准（Benchmarks）**：包含 Math500, AIME24, AMC, Gaokao, Minerva Math, Olympiad Bench 等六个主流数学推理榜单。
*   **对比方法**：
    *   **Dynamic Sampling (DS)**：动态过滤零方差样本并重新采样填补 Batch。
    *   **GRESO**：一种先进的基于历史动态的提示词选择方法。
*   **实验模型**：Qwen2.5-Math (1.5B/7B/14B/32B), DeepSeek-R1-Distill-Qwen-1.5B, Llama-3.2-3B-Instruct。

### 4. 资源与算力
*   **硬件环境**：主要实验在 **8 × NVIDIA A100-80G GPUs** 上完成。
*   **训练时长与效率**：
    *   以 Qwen2.5-Math-7B 为例，HIVE 将总训练时间从 198.4 小时（DS 方法）缩短至 **85.8 小时**，实现了 **2.2 倍的整体加速**。
    *   在 Rollout 环节，HIVE 实现了高达 **3.8 倍的单项加速**。
*   **软件框架**：基于 verl 强化学习框架和 vLLM 推理引擎实现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 32B 不同参数规模的模型，验证了方法的可扩展性。
*   **充分性分析**：
    *   **消融实验**：分别验证了“仅使用历史信息”和“仅使用在线验证”的效果，证明了两阶段结合的必要性。
    *   **理论验证**：通过 2048 个样本的统计分析，证明了提示词熵与响应熵之间存在强线性相关性（$R^2=0.92$），为方法论提供了理论支撑。
    *   **动态分析**：展示了训练过程中零方差比例的变化，客观反映了 HIVE 捕捉“学习边缘”的能力。
*   **评价**：实验设计全面，对比基准扎实，涵盖了性能、效率、理论和案例等多个维度，结果具有较高的说服力。

### 6. 主要结论与发现
*   **效率飞跃**：HIVE 在减少高达 **70%（约 920 万次）Rollout** 的情况下，依然能达到甚至超过全量采样的推理准确率。
*   **学习边缘的移动性**：实验证实了高价值样本的分布随训练动态偏移，静态筛选或单纯依赖历史数据的筛选（如 GRESO）会因“元数据陈旧”而失效。
*   **代理指标的有效性**：提示词熵是一个极低成本且高保真的实时效用预测器，能够有效指导在线样本选择。

### 7. 优点与亮点
*   **极低开销的在线验证**：Stage 2 的计算开销仅占总迭代时间的 **0.4%**，却能带来数倍的效率提升，性价比极高。
*   **理论与实践结合**：不仅提出了算法，还给出了关于“秩一致性”（Rank Consistency）的理论证明。
*   **即插即用**：该框架不改变 RL 算法（如 GRPO）的核心逻辑，易于集成到现有的后训练流水线中。

### 8. 不足与局限
*   **任务覆盖面**：目前主要集中在数学推理任务，尚未在代码生成、创意写作或通用对话等非验证性奖励（Non-verifiable rewards）场景下进行测试。
*   **超参数敏感性**：虽然引入了自适应机制，但平衡系数 $\lambda$ 和探索概率步长 $\Delta p$ 等参数在不同规模模型上的最优值可能存在差异。
*   **多模态缺失**：尚未探索提示词熵作为效用代理的原则是否适用于多模态大模型（VLM）。

（完）
