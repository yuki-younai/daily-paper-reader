---
title: "Bridging Perception and Reasoning: Token Reweighting for RLVR in Multimodal LLMs"
title_zh: 连接感知与推理：多模态大语言模型中 RLVR 的 Token 重加权
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Guoyin Wang, Jiancan Wu, Xiang Wang, Xiangnan He"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25077v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 针对多模态大模型的带验证奖励强化学习
tldr: 本研究针对多模态大语言模型在可验证奖励强化学习（RLVR）中感知与推理令牌相互交织的挑战，提出了一种名为Token-Reweighting (ToR) 的即插即用策略。通过实证分析发现，仅优化感知或推理令牌的效果均不如全面优化，说明两者存在强耦合。ToR通过识别并动态重新加权这两类关键令牌，显著提升了模型在多模态推理任务中的视觉定位与逻辑推理能力，在多个基准测试中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 多模态模型在强化学习中面临感知令牌与推理令牌相互交织且高度耦合的问题，导致单一维度的优化效果受限。
method: 提出Token-Reweighting (ToR) 策略，通过识别关键的感知和推理令牌并在训练过程中动态调整其权重来建模两者的相互依赖性。
result: 在GRPO和DAPO等方法上应用ToR后，模型在多个多模态推理基准测试中均实现了性能增益并达到SOTA水平。
conclusion: ToR策略证明了在多模态强化学习中协同优化感知与推理令牌对于提升模型综合性能至关重要。
---

## 摘要
将具有可验证奖励的强化学习（RLVR）扩展到多模态大语言模型（MLLMs）面临一个根本性挑战：它们的响应本质上交织着感知相关 token（用于定位视觉内容）和推理相关 token（用于构建推理链）。这些 token 类型实例化了不同但又相互依赖的能力——视觉定位和符号推理——这使得孤立的优化变得不足。通过 token 级别的实证分析，我们证明了仅优化感知或仅优化推理 token 的表现始终不如全面优化，这强调了它们内在的耦合性。为了解决这个问题，我们提出了一种即插即用的 Token 重加权（ToR）策略，该策略通过识别这两类关键 token 并在 RLVR 训练期间动态重新加权，显式地对这种相互依赖性进行建模。在现有方法（如 GRPO 和 DAPO）的基础上应用 ToR，在多个多模态推理基准测试中实现了持续的性能提升，在准确的视觉定位和连贯的推理方面均达到了最先进的性能。

## Abstract
Extending Reinforcement Learning with Verifiable Rewards (RLVR) to multimodal large language models (MLLMs) faces a fundamental challenge: their responses inherently interleave perception-related tokens, which ground visual content, with reasoning-related tokens, which construct reasoning chains. These token types instantiate distinct yet interdependent capacities -- visual grounding and symbolic reasoning -- making isolated optimization insufficient. Through token-level empirical analysis, we demonstrate that optimizing either perception- or reasoning-only tokens consistently underperforms full optimization, underscoring their inherent coupling. To address this, we propose a plug-and-play Token-Reweighting (ToR) strategy that explicitly models this interdependence by identifying critical tokens of both types and dynamically reweighting them during RLVR training. Applied on top of existing methods (e.g., GRPO and DAPO), ToR delivers consistent performance gains across multiple multi-modal reasoning benchmarks, achieving state-of-the-art performance with both accurate visual grounding and coherent reasoning.

---

## 论文详细总结（自动生成）

这篇论文《Bridging Perception and Reasoning: Token Reweighting for RLVR in Multimodal LLMs》深入探讨了多模态大语言模型（MLLM）在强化学习过程中的感知与推理耦合问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何有效地将“带验证奖励的强化学习”（RLVR，如 DeepSeek-R1 风格的训练）扩展到多模态模型。
*   **研究动机**：MLLM 的响应由两类 token 交织而成：**感知相关 token**（用于定位和描述视觉内容）和**推理相关 token**（用于逻辑推导）。
*   **背景**：现有方法通常孤立地优化这两者（例如仅通过思维链提升推理，或通过数据增强提升感知）。作者通过实验发现，感知与推理在 token 级别是高度耦合的，仅优化其中一类会导致另一类能力受损，从而限制整体性能。

### 2. 方法论：Token-Reweighting (ToR)
ToR 是一种即插即用的策略，旨在通过动态调整不同功能 token 的权重来平衡感知与推理的优化。
*   **核心思想**：识别响应中关键的感知和推理 token，并在计算策略梯度（Policy Gradient）时赋予它们更高的权重。
*   **关键技术细节**：
    *   **推理 Token 识别**：基于**预测熵（Entropy）**。高熵位置通常对应推理链中的关键决策点（Forking points）。
    *   **感知 Token 识别**：基于**视觉敏感度（Visual Sensitivity）**。通过计算“有图像输入”与“无图像输入（空占位符）”时 token 对数概率的差值（log-prob difference）来衡量该 token 对视觉信息的依赖程度。
    *   **重加权机制**：在 GRPO 或 DAPO 的损失函数中引入权重因子 $\gamma_r$（推理权重）和 $\gamma_p$（感知权重）。公式上，它在优势函数（Advantage）前乘以一个基于 token 类型的掩码权重，使得梯度更新集中在这些关键 token 上。
*   **算法流程**：在 Rollout 生成响应后，计算每个 token 的熵和视觉敏感度，筛选出前 $\alpha$ 比例的关键 token，应用重加权系数后进行参数更新。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Geometry3K（2.1K 样本，用于基础实验）；ViRL-39K（用于扩展实验）。
    *   **测试基准（Benchmarks）**：涵盖视觉推理（MathVerse, MathVision, MathVista, WeMath）和视觉感知（HalluBench，用于检测幻觉）。
*   **对比方法**：
    *   **开源模型**：InternVL-2.5/3, LLaVA-OneVision, Qwen2.5-VL-7B-Instruct。
    *   **RLVR 变体**：R1-VL, Vision-R1, MM-Eureka, NoisyRollout, 以及标准的 GRPO 和 DAPO。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H800 GPU (80GB)**。
*   **训练配置**：学习率 1e-6，全局 Batch Size 为 128，Rollout Batch Size 为 512，每个样本生成 12 个 Rollout 响应。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到使用了 EasyR1 框架进行高效训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **消融实验**：详细测试了不同 token 筛选比例（20% 到 80%）以及不同权重系数（$\gamma_p$ 从 0.1 到 1.5）的影响。
    *   **模型泛化**：在 Qwen2.5-VL 的 3B 和 7B 两个尺寸上验证了有效性。
    *   **数据扩展**：从 2.1K 数据扩展到 39K 数据，验证了方法的扩展性。
*   **充分性评价**：实验设计非常充分且客观。作者不仅提供了量化指标，还通过“选择性优化”实验（即只优化某一类 token 观察性能下降）有力地证明了感知与推理的耦合假设。

### 6. 主要结论与发现
*   **耦合性证明**：仅优化推理 token 会导致模型虽然逻辑连贯但视觉定位错误；仅优化感知 token 则会导致模型无法将视觉信息整合进逻辑链。
*   **性能提升**：ToR 显著优于 vanilla GRPO 和 DAPO。例如，在 WeMath 基准上，ToR-DAPO 相比基础 DAPO 提升了约 2.8%。
*   **SOTA 表现**：在相同训练数据规模下，ToR 达到了最先进的性能，且在减少视觉幻觉（HalluBench）方面表现优异。
*   **Token 特征**：感知依赖于少数视觉敏感 token 的稳定性，而推理依赖于高熵决策点的鲁棒性。

### 7. 优点
*   **即插即用**：不需要修改模型架构或增加复杂的外部 pipeline（如额外的奖励模型或图像处理工具）。
*   **自包含性**：仅利用模型自身的内在信号（熵和概率差）来识别关键 token，无需外部先验知识。
*   **理论与实证结合**：通过深入的 token 级别分析揭示了 MLLM 训练中的本质矛盾，并给出了简洁的数学解决方案。

### 8. 不足与局限
*   **识别精度**：目前的感知 token 识别基于概率差，虽然有效，但可能不如结合 SAM 等分割模型进行细粒度空间定位来得精确。
*   **权重静态性**：实验中 $\gamma$ 权重是预设的静态值，未来若能实现根据训练阶段动态调整权重可能会更优。
*   **上下文忽略**：ToR 主要关注单个 token 的重加权，可能忽略了 token 之间更复杂的语义上下文联系。

（完）
