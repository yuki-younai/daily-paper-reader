---
title: A transformer architecture alteration to incentivise externalised reasoning
title_zh: 一种旨在激励外部化推理的 Transformer 架构改进
authors: "Elizabeth Pavlova, Mariia Koroliuk, Karthik Viswanathan, Cameron Tice, Edward James Young, Puria Radmard"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21376v2"
tags: ["query:rlaif"]
score: 9.0
evidence: 使用强化学习激励推理的后训练流水线
tldr: 本研究提出一种改进Transformer架构的方法，旨在通过减少内部冗余计算来激励模型进行外部化推理。研究者在中间层引入“早期退出”机制，并结合强化学习训练模型在预测简单标记时尽早退出。该方法能根据标记难度自适应调整计算量，从而限制模型利用内部激活进行非透明规划，迫使其将复杂逻辑通过外部推理步骤呈现，提高了推理过程的可解释性与效率。
selection_source: fresh_fetch
motivation: 旨在减少大模型在简单标记上的过度内部计算，从而激励其将复杂逻辑转向可观测的外部推理。
method: 在Transformer架构中引入早期退出机制，并利用强化学习在保持性能的前提下诱导模型尽早结束前向传播。
result: 初步实验表明，小型推理模型能够根据标记预测难度自适应地减少计算层数。
conclusion: 该方法能有效限制模型的内部隐式规划，将计算资源保留给真正需要的复杂推理任务。
---

## 摘要
我们提出了一种新的架构变更及后训练流水线，通过教导模型提前截断前向传播，使大语言模型（LLM）成为更详尽的推理者。我们在现有的 Transformer 架构中增加了中间层的提前退出机制，并训练模型在无需深度计算即可预测下一个 token 时，从较浅层退出。在校准阶段之后，我们利用强化学习激励模型在保持任务性能的同时尽可能早地退出。我们针对小型推理模型提供了初步实验结果，表明它们学会了跨 token 自适应地减少计算量。我们预测，在合适的规模下应用该方法，可以最大限度地减少推理模型利用其内部激活进行非近视规划时所拥有的过剩计算量，并将其仅保留给难以预测的 token。

## Abstract
We propose a new architectural change, and post-training pipeline, for making LLMs more verbose reasoners by teaching a model to truncate forward passes early. We augment an existing transformer architecture with an early-exit mechanism at intermediate layers and train the model to exit at shallower layers when the next token can be predicted without deep computation. After a calibration stage, we incentivise the model to exit as early as possible while maintaining task performance using reinforcement learning. We provide preliminary results to this effect for small reasoning models, showing that they learn to adaptively reduce computations across tokens. We predict that, applied at the right scale, our approach can minimise the amount of excess computation that reasoning models have at their disposal to perform non-myopic planning using their internal activations, reserving this only for difficult-to-predict tokens.

---

## 论文详细总结（自动生成）

以下是对论文《A transformer architecture alteration to incentivise externalised reasoning》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的思维链（CoT）推理存在“不忠实性”和“隐蔽性”风险。由于 Transformer 是非近视的（每个前向传播不仅为当前 token 优化，还为后续 token 预存信息），模型可能在内部激活中进行隐式规划，而输出的 CoT 并不反映其真实的计算过程。
*   **核心问题**：如何通过架构改进，限制模型在单个 token 上的过度内部计算，从而迫使模型将复杂的逻辑过程“外部化”到可观测的 CoT 步骤中。

### 2. 论文提出的方法论
该研究提出了一种两阶段的后训练流水线，结合了架构修改与强化学习：
*   **架构修改**：在 Transformer 的中间层（每 5 层）添加“早期退出头”（Early-exit heads）。这些头将残差流映射为一个标量对数（logit），用于决定是否在该层停止计算。若退出，则直接将当前残差流传至输出层。
*   **阶段 1：自蒸馏校准（SFT）**：
    *   使用原始全深度模型作为教师，带有退出头的模型作为学生。
    *   通过计算中间层与最终层预测分布之间的 **KL 散度** 来确定“安全退出点”。
    *   训练目标是让学生模型在保证预测准确性的前提下，学习教师模型的退出概率分布。
*   **阶段 2：强化学习激励（RL）**：
    *   采用 **RLOO**（Reinforcement Learning from Open-source Outcomes）算法。
    *   **奖励函数**：$R_{total} = R_{task} - \lambda \cdot (\text{平均退出层索引}) - \beta \cdot D_{KL}$。
    *   核心思想是通过惩罚计算深度（$\lambda$ 参数），激励模型在不损失任务性能的情况下尽可能早地退出。

### 3. 实验设计
*   **模型规模**：主要在小型推理模型上进行实验，包括 **DeepSeek-R1-Distill-Qwen-1.5B** 和 **Qwen3-4B**。
*   **数据集**：
    *   **GSM8K**：数学文字题数据集。
    *   **Theory of Mind (ToM)**：使用 ExploreToM 生成的心理理论推理数据集。
*   **对比基准**：主要对比 SFT 前后的基础模型性能，以及不同 $\lambda$（退出积极度）和 KL 因子下的表现。
*   **评估指标**：任务准确率（Accuracy）、回答连贯性（Coherence）、平均计算量（使用的层数比例）和总计算量（Token 数 × 平均层数）。

### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。考虑到模型规模（1.5B 至 4B）和使用的 LoRA 微调技术，预计算力需求处于中等水平。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了针对两个不同规模模型的实验结果，并对 RL 训练动态进行了 5 次独立运行的平均分析。
*   **消融与验证**：进行了针对 KL 因子的敏感度分析（验证了连贯性与退出率的权衡），并展示了 token 级别的退出行为可视化。
*   **充分性评价**：作为“初步结果”（Preliminary results）和“概念验证”（Proof of concept），实验设计较为合理，但在模型规模（未上 7B+）和任务多样性上仍有提升空间。

### 6. 论文的主要结论与发现
*   **自适应计算**：模型学会了根据 token 的预测难度动态调整深度。例如，在预测高度确定的 token（如单词后缀）时会极早退出，而在复杂推理步骤时使用全深度。
*   **性能提升与计算减少**：在 Qwen3-4B 上的实验显示，RL 阶段不仅将平均计算量降低了约 5%-10%，同时任务准确率从 47% 提升至 55%-60%，甚至超过了原始基座模型。
*   **可调控性**：通过调节 $\lambda$ 参数，可以在“推理外部化压力”与“生成质量”之间找到平衡点。

### 7. 优点
*   **创新视角**：不同于传统的早期退出（旨在加速推理），本文将其作为一种**安全工具**，用于强制模型减少隐式规划。
*   **端到端流程**：提供了一套从架构修改、自蒸馏校准到 RL 激励的完整训练方案。
*   **灵活性**：引入了可调节的“拨盘”（$\lambda$ 和退出阈值），允许根据安全需求或性能需求动态调整模型行为。

### 8. 不足与局限
*   **规模限制**：实验仅限于 4B 以下的小模型，在大规模模型（如 70B+）上的有效性尚未验证。
*   **忠实性验证缺失**：虽然论文声称该方法能提高 CoT 的忠实性，但尚未进行专门的实验（如红队测试或隐藏意图检测）来量化证明这一点。
*   **泛化性问题**：目前的评估集中在特定任务（数学、ToM），缺乏跨领域通用能力的验证。
*   **性能损失风险**：过于激进的早期退出（低 KL 因子）会导致模型连贯性大幅下降。

（完）
