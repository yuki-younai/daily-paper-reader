---
title: "SumRank: Aligning Summarization Models for Long-Document Listwise Reranking"
title_zh: SumRank：面向长文档列表式重排序的摘要模型对齐
authors: "Jincheng Feng, Wenhan Liu, Zhicheng Dou"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24204v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 包含强化学习对齐的三阶段训练流水线
tldr: 针对大语言模型在长文档重排序中面临的效率与效果挑战，本文提出SumRank模型。该模型通过三阶段训练（SFT、RL数据构建、排序驱动对齐）将长文档压缩为保留关键排序信号的摘要。实验表明，SumRank在TREC DL数据集上达到了SOTA性能，显著降低了计算开销并提升了重排序效率。
selection_source: fresh_fetch
motivation: 大语言模型直接处理长文档重排序时，由于上下文过长导致效率低下且排序效果受损。
method: 提出一种与下游排序目标对齐的逐点摘要模型SumRank，通过强化学习确保摘要保留核心检索信号。
result: 在五个TREC DL基准数据集上，SumRank在保持轻量化的同时实现了最先进的排序性能并大幅提升了效率。
conclusion: 将摘要模型与排序任务对齐是解决长文档重排序中效率与精度权衡问题的有效途径。
---

## 摘要
大语言模型（LLMs）在列表式段落重排序任务中表现出了卓越的性能。然而，由于上下文长度大幅增加，直接将其应用于长文档排序会带来有效性和效率方面的问题。为了应对这一挑战，我们提出了一种与下游列表式重排序相对齐的逐点摘要模型 SumRank，旨在最终列表式重排序阶段之前，将长文档压缩为简洁且与排序对齐的摘要。为了获得 SumRank 摘要模型，我们引入了一个三阶段训练流水线，包括冷启动有监督微调（SFT）、专门的强化学习（RL）数据构建以及通过强化学习进行的排序驱动对齐。该范式使 SumRank 与下游排序目标保持一致，从而保留相关性信号。我们在来自 TREC 深度学习赛道（TREC DL 19-23）的五个基准数据集上进行了广泛的实验。结果表明，我们的轻量级 SumRank 模型在达到最先进（SOTA）排序性能的同时，通过减少摘要开销和重排序复杂度，显著提高了效率。

## Abstract
Large Language Models (LLMs) have demonstrated superior performance in listwise passage reranking task. However, directly applying them to rank long-form documents introduces both effectiveness and efficiency issues due to the substantially increased context length. To address this challenge, we propose a pointwise summarization model SumRank, aligned with downstream listwise reranking, to compress long-form documents into concise rank-aligned summaries before the final listwise reranking stage. To obtain our summarization model SumRank, we introduce a three-stage training pipeline comprising cold-start Supervised Fine-Tuning (SFT), specialized RL data construction, and rank-driven alignment via Reinforcement Learning. This paradigm aligns the SumRank with downstream ranking objectives to preserve relevance signals. We conduct extensive experiments on five benchmark datasets from the TREC Deep Learning tracks (TREC DL 19-23). Results show that our lightweight SumRank model achieves state-of-the-art (SOTA) ranking performance while significantly improving efficiency by reducing both summarization overhead and reranking complexity.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SumRank** 的新型摘要模型，旨在解决大语言模型（LLM）在处理长文档列表式重排序（Listwise Reranking）时面临的效率与效果难题。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然 LLM 在短文本重排序中表现卓越，但直接应用于长文档时存在两大瓶颈：
    1.  **效率低下**：长文档大幅增加了上下文长度，导致推理延迟呈平方级增长。
    2.  **效果受损**：过长的上下文会加重模型的建模负担，导致排序性能下降。
*   **核心目标**：提出一种“先摘要后排序”的范式，通过一个专门优化的摘要模型（SumRank）将长文档压缩为保留关键排序信号的短摘要，从而在提升排序精度的同时大幅降低计算开销。

### 2. 方法论：核心思想与技术细节
SumRank 采用了一个三阶段的训练流水线，确保摘要模型与下游排序任务高度对齐：
*   **阶段一：冷启动有监督微调（SFT）**：
    *   使用强力教师模型（如 Qwen2.5-72B）生成高质量摘要作为金标准。
    *   通过知识蒸馏，让轻量级学生模型（3B/7B）学习基础的文档压缩和查询感知摘要能力。
*   **阶段二：强化学习（RL）数据构建**：
    *   构建包含正负样本的候选文档列表。
    *   利用 SFT 后的模型预先生成“静态背景摘要列表”，在后续 RL 阶段仅替换目标文档的摘要，以降低计算成本并减少奖励方差。
*   **阶段三：排序驱动的强化学习对齐（GRPO）**：
    *   **核心算法**：采用组相对策略优化（GRPO）。
    *   **奖励机制**：直接以重排序后的 **NDCG@10** 作为奖励信号。
    *   **对齐策略**：如果模型对无关文档生成了包含“未找到相关信息”的摘要，给予高奖励；若未能识别无关文档则给予惩罚。这迫使模型在摘要中保留最相关的匹配信号。

### 3. 实验设计
*   **数据集**：使用了 TREC Deep Learning (DL) 2019-2023 共五个基准数据集（基于 MS MARCO）。
*   **下游排序器**：统一使用 Qwen2.5-32B-Instruct 作为列表式重排序器。
*   **对比方法（Baselines）**：
    *   **规则类**：BM25、FirstP（截断前 128/256/512 标记）。
    *   **传统摘要模型**：BART、LED、PEGASUS。
    *   **通用 LLM 零样本摘要**：Qwen2.5-3B/7B-Instruct。
*   **评价指标**：NDCG@10 和 MAP@100。

### 4. 资源与算力
论文明确记录了实验所用的算力资源：
*   **SFT 阶段**：使用 4 台 NVIDIA A800 (80GB) GPU，训练 5 个 epoch。
*   **RL (GRPO) 阶段**：
    *   3B 模型：使用 2 台 NVIDIA A800。
    *   7B 模型：使用 4 台 NVIDIA A800。
    *   训练 5 个 epoch，学习率为 $1 \times 10^{-6}$。
*   **推理阶段**：对比了不同模型的推理延迟（秒/查询）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同年份的 TREC DL 数据集上进行了测试，验证了跨时间的鲁棒性。
*   **消融实验**：分别验证了“无冷启动 SFT”和“无排序驱动 GRPO”的影响，证明了三阶段流水线的必要性。
*   **数据策略分析**：对比了静态背景摘要与动态更新摘要的效果，以及不同截断长度的影响。
*   **充分性评价**：实验设计较为全面，涵盖了性能、效率、模型规模对比及消融分析，对比基准涵盖了从传统方法到最新 LLM 的主流方案，结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：SumRank (7B) 在所有五个数据集上均达到了 SOTA 性能，甚至超越了直接使用 72B 模型处理长文档的效果。
*   **效率飞跃**：SumRank (3B) 相比 72B 模型实现了 **42 倍** 的加速，而 SumRank (7B) 在性能更优的前提下实现了 **12 倍** 的加速。
*   **对齐的重要性**：传统的摘要模型（如 BART）虽然生成的文本通顺，但因未针对排序信号优化，效果往往不如简单的文本截断（FirstP）。而经过排序驱动对齐的 SumRank 能精准保留检索所需的关键词。

### 7. 优点与亮点
*   **任务对齐创新**：首次提出将摘要模型直接与列表式重排序的指标（NDCG）进行强化学习对齐。
*   **效率优化技巧**：通过“静态背景摘要列表”解决了列表式排序在 RL 训练中计算复杂度过高（$O(N \times G)$）的问题。
*   **以小博大**：证明了通过精细的任务对齐，较小的模型（3B/7B）可以在特定任务上战胜参数量大得多的通用模型（72B）。

### 8. 不足与局限
*   **两阶段开销**：虽然重排序阶段变快了，但整个系统仍需维护两个模型（摘要+排序），增加了部署复杂性。
*   **模型规模限制**：受限于算力，作者未能在更大规模（如 14B 或 32B）的开源模型上验证该对齐框架。
*   **端到端缺失**：目前是“摘要+排序”的分离架构，未来若能实现端到端的压缩与排序一体化，效率可能进一步提升。

（完）
