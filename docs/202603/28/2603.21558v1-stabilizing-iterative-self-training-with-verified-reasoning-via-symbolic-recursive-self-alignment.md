---
title: Stabilizing Iterative Self-Training with Verified Reasoning via Symbolic Recursive Self-Alignment
title_zh: 通过符号递归自对齐，利用经过验证的推理稳定迭代自训练
authors: Xinyu Zhang
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21558v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 带有符号验证的推理迭代自训练
tldr: 针对迭代自训练中推理错误累积导致的“递归漂移”问题，本文提出神经符号递归自对齐（NSRSA）框架。该方法通过嵌入符号验证子系统，在推理步骤层面（而非仅结果层面）对生成数据进行质量把控，利用sympy验证算术逻辑并强制执行领域约束。实验表明，NSRSA能有效剔除“侥幸猜对”的错误推理，结合DPO进一步提升了模型的推理可靠性，为实现稳定、可衡量的递归自我提升提供了新路径。
selection_source: fresh_fetch
motivation: 迭代自训练中推理错误的累积会导致模型性能退化和模式崩溃，即所谓的“递归漂移”问题。
method: 提出NSRSA框架，利用符号验证系统在推理步骤级别对算术运算、逻辑流和领域约束进行严格校验。
result: "NSRSA成功剔除了34%虽答案正确但推理错误的样本，并使DPO偏好学习的奖励准确率从46%提升至63%。"
conclusion: 引入外部符号验证能有效稳定递归自改进过程，使模型在具备自动化验证条件的领域内实现可靠的能力增长。
---

## 摘要
递归自我提升——即模型在其自身输出上进行迭代训练——有望实现能力的持续增长，但面临着一个根本性障碍：递归漂移。随着模型在多次迭代中利用自生成数据进行训练，中间推理过程中的错误会不断累积，导致模式崩溃和性能下降。我们提出了神经符号递归自对齐（NSRSA），通过嵌入一个符号验证子系统，在推理步骤层面把控训练数据质量，从而稳定迭代自训练。与仅针对结果的过滤（这种方式会接纳带有错误推理的“侥幸猜中”）不同，NSRSA 通过 sympy 验证每个算术运算，检查推理步骤间的逻辑流一致性，并强制执行领域约束。我们在 GSM8K 数据集上使用 Qwen3-4B-Thinking 模型，在五种条件下进行了 5 次自训练迭代评估：无验证、结果验证、多数投票、完整的 NSRSA 符号验证以及结合 DPO 的 NSRSA。我们的过滤分析表明，NSRSA 拒绝了约 34% 通过结果验证但答案正确的解法，从而从训练集中消除了带有错误推理的“侥幸猜中”。我们进一步证明，利用 NSRSA 验证构建 DPO 偏好对，可以教会模型区分正确与错误的推理（奖励准确率从 46% 提升至 63%）。NSRSA 提供了一个可扩展的框架，展示了在可进行自动验证的领域中，外部符号验证如何使递归自我提升变得可衡量且可靠。

## Abstract
Recursive self-improvement--where a model iteratively trains on its own outputs--promises sustained capability growth but faces a fundamental obstacle: recursive drift. As models train on self-generated data across multiple iterations, errors in intermediate reasoning compound, leading to mode collapse and performance degradation. We propose Neuro-Symbolic Recursive Self-Alignment (NSRSA), which stabilizes iterative self-training by embedding a symbolic verification subsystem that gates training data quality at the reasoning step level. Unlike outcome-only filtering (which admits "lucky guesses" with flawed reasoning), NSRSA verifies each arithmetic operation via sympy, checks logical flow consistency across reasoning steps, and enforces domain constraints. We evaluate NSRSA on GSM8K using Qwen3-4B-Thinking across 5 self-training iterations under five conditions: no verification, outcome verification, majority voting, full NSRSA symbolic verification, and NSRSA with DPO. Our filtering analysis shows that NSRSA rejects approximately 34% of correct-answer solutions that pass outcome verification, eliminating "lucky guesses" with flawed reasoning from the training set. We further demonstrate that constructing DPO preference pairs from NSRSA verification teaches the model to distinguish sound from flawed reasoning (reward accuracy 46% to 63%). NSRSA provides an extensible framework that demonstrates how external symbolic verification can make recursive self-improvement measurable and reliable within domains where automated verification is available.

---

## 论文详细总结（自动生成）

以下是对论文《Stabilizing Iterative Self-Training with Verified Reasoning via Symbolic Recursive Self-Alignment》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：论文探讨了“递归自我提升”（Recursive Self-Improvement, RSI）中的**递归漂移（Recursive Drift）**问题。当模型在自身生成的数据上迭代训练时，中间推理步骤的错误会不断累积。
*   **研究背景**：现有的自训练方法（如 STaR）主要依赖“结果验证”（Outcome Verification），即只要最终答案正确就采纳。这会导致模型学到“侥幸猜中”（Lucky Guesses）的错误推理模式，最终引发模型崩溃或性能瓶颈。
*   **目标**：通过引入细粒度的符号验证，确保训练数据在推理步骤层面也是正确的，从而稳定递归训练过程。

### 2. 方法论：NSRSA 框架
**神经符号递归自对齐（NSRSA）**的核心是将符号验证子系统嵌入自训练循环中。其流程包含：生成 -> 验证 -> 训练 -> 评估。
*   **核心思想**：不仅检查答案，还利用外部符号工具（如 `sympy`）对推理链条（CoT）进行逐步审计。
*   **关键技术细节（四个验证维度）**：
    1.  **答案正确性**：最终数值必须与 Ground Truth 匹配。
    2.  **算术验证**：提取推理中的算术表达式（如 $A \odot B = C$），使用 `sympy` 重新计算左侧，验证其是否等于右侧。要求正确率 $\ge 80\%$。
    3.  **逻辑流验证**：跟踪变量赋值。如果同一个变量在非相邻步骤中被赋予了差异巨大的值（相对变化 $>50\%$），则判定为逻辑幻觉。
    4.  **约束满足**：强制执行领域约束，如计数变量必须为非负数、离散量必须为整数等。
*   **DPO 变体**：利用 NSRSA 构建偏好对，将“答案正确且推理通过验证”作为 Chosen，将“答案正确但推理未通过验证”作为 Rejected，教会模型区分严谨推理与侥幸猜中。

### 3. 实验设计
*   **模型**：Qwen3-4B-Thinking（具备思维链能力的 4B 参数模型）。
*   **数据集**：
    *   **GSM8K**：主要训练和评估基准（小学数学）。
    *   **MATH-500**：用于测试跨任务迁移能力（难度更高的数学竞赛题）。
*   **对比方法（Baseline）**：
    1.  **无验证（No Verification）**：随机采样自生成数据。
    2.  **结果验证（Outcome Verification）**：仅根据最终答案过滤。
    3.  **多数投票（Majority Voting）**：基于自一致性（Self-consistency）过滤。
    4.  **NSRSA (Symbolic)**：本文提出的符号验证 SFT。
    5.  **NSRSA + DPO**：在 NSRSA 基础上增加偏好对训练。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A10G GPU**（每张 24GB 显存）。
*   **训练细节**：
    *   采用 **LoRA** 微调（r=16, α=32）。
    *   有效 Batch Size 为 16，每轮迭代训练 1 个 Epoch。
    *   DPO 训练在 4,426 个偏好对上耗时约 **2.25 小时**。
    *   符号验证过程在 CPU 上运行，每轮迭代增加约 10 分钟开销，相对于训练时间可忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **5 轮完整的自训练迭代**，涵盖了 5 种不同的实验条件。
*   **充分性**：
    *   不仅关注准确率，还分析了**验证通过率**（Reasoning Quality）和 **Self-BLEU**（多样性/模式崩溃指标）。
    *   包含跨任务迁移实验（MATH-500），证明了方法的泛化性。
    *   进行了解析器覆盖范围的消融分析，评估了符号解析失败对结果的影响。
*   **客观性**：对比了不依赖标签的“多数投票”法，确保了实验对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：在 GSM8K 上，NSRSA 经过 5 轮迭代将准确率从 80.5% 提升至 **91.0%**，而结果验证仅达到 85.8%，无验证组则崩溃至 73.2%。
*   **消除“侥幸猜中”**：分析显示，NSRSA 拒绝了约 **34%** 答案正确但推理有误的样本。
*   **推理质量增长**：随着迭代进行，NSRSA 组生成的解法中，通过符号验证的比例持续上升，说明模型学会了生成更严谨的推理。
*   **抑制模式崩溃**：NSRSA 的 Self-BLEU 分数保持在较低水平（0.35），有效防止了模型陷入重复、单一的错误模式。

### 7. 优点
*   **细粒度监督**：通过符号化手段实现了自动化的“过程监督”（Process Supervision），无需昂贵的人工标注。
*   **稳定性强**：解决了递归训练中常见的性能退化问题，使长期自我提升成为可能。
*   **可解释性**：验证失败的原因（算术错、逻辑断裂等）清晰可见，便于分析模型弱点。

### 8. 不足与局限
*   **领域局限性**：目前高度依赖数学领域的符号解析（sympy），难以直接推广到常识推理或创意写作等非结构化领域。
*   **解析器脆弱性**：如果模型不使用标准数学符号（如用自然语言描述计算），解析器可能失效（虽然文中通过“默认通过”机制缓解了此问题，但仍存在漏检）。
*   **逻辑流检测较简单**：基于字符串匹配和简单阈值的逻辑流检查可能无法处理复杂的指代消解或变量重命名。

（完）
