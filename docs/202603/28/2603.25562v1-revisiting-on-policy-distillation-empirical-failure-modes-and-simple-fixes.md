---
title: "Revisiting On-Policy Distillation: Empirical Failure Modes and Simple Fixes"
title_zh: 重访同策略蒸馏：经验性失败模式与简单修复方法
authors: "Yuqian Fu, Haohuan Huang, Kaiwen Jiang, Yuanheng Zhu, Dongbin Zhao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25562v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大语言模型后训练中的在线蒸馏
tldr: 针对大语言模型后训练中的同策略蒸馏（OPD）不稳定的问题，本文深入探讨了其理论偏差与方差权衡，并识别出单标记信号失衡、前缀漂移导致教师指导不可靠及分词器不匹配等三大失效模式。研究提出了一种基于教师Top-K局部支持匹配的改进方案，通过截断逆KL散度、Top-p采样和特殊标记掩码，显著提升了数学推理和多任务智能体训练的稳定性和下游性能。
selection_source: fresh_fetch
motivation: 现有的同策略蒸馏方法在长序列任务中容易因分布偏移和单标记信号微弱而导致训练不稳定。
method: 提出了一种结合截断逆KL散度、Top-p采样和特殊标记掩码的教师Top-K局部支持匹配方法。
result: 该方法在单任务数学推理和多任务智能体训练中均表现出更稳定的优化过程和更优的下游任务性能。
conclusion: 通过修正同策略蒸馏中的局部匹配逻辑和处理分布漂移，可以有效提升大模型知识蒸馏的效率和鲁棒性。
---

## 摘要
同策略蒸馏 (On-policy distillation, OPD) 在大语言模型 (LLM) 后训练中极具吸引力，因为它在学生生成的展开 (rollouts) 而非固定的教师轨迹上评估教师反馈。然而，在长时界 (long-horizon) 场景下，常见的采样标记 (sampled-token) 变体非常脆弱：它将分布匹配简化为单标记信号，且随着展开偏离教师常用的前缀，其可靠性也随之降低。我们从估计器和实现两个方面重新审视了 OPD。理论上，标记级 OPD 相对于序列级逆向 KL 散度 (reverse-KL) 存在偏差，但其最坏情况下的方差界限更紧；我们的玩具实验在经验上也展示了同样的权衡，更强的未来奖励耦合会导致更高的梯度方差和更不稳定的学习。在经验层面，我们识别了采样标记 OPD 的三种失败模式：不平衡的单标记信号、教师对学生生成前缀的指导不可靠，以及由分词器或特殊标记不匹配引起的失真。我们通过教师 top-K 局部支持匹配来解决这些问题，具体实现为带有 top-p 展开采样和特殊标记掩码的截断逆向 KL。在单任务数学推理和多任务智能体加数学训练中，该目标函数比采样标记 OPD 产生了更稳定的优化和更好的下游性能。

## Abstract
On-policy distillation (OPD) is appealing for large language model (LLM) post-training because it evaluates teacher feedback on student-generated rollouts rather than fixed teacher traces. In long-horizon settings, however, the common sampled-token variant is fragile: it reduces distribution matching to a one-token signal and becomes increasingly unreliable as rollouts drift away from prefixes the teacher commonly visits. We revisit OPD from the estimator and implementation sides. Theoretically, token-level OPD is biased relative to sequence-level reverse-KL, but it has a much tighter worst-case variance bound; our toy study shows the same tradeoff empirically, with stronger future-reward coupling producing higher gradient variance and less stable learning. Empirically, we identify three failure modes of sampled-token OPD: an imbalanced one-token signal, unreliable teacher guidance on student-generated prefixes, and distortions caused by tokenizer or special-token mismatch. We address these issues with teacher top-K local support matching, implemented as truncated reverse-KL with top-p rollout sampling and special-token masking. Across single-task math reasoning and multi-task agentic-plus-math training, this objective yields more stable optimization and better downstream performance than sampled-token OPD.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大语言模型（LLM）后训练中的**同策略蒸馏（On-Policy Distillation, OPD）**。OPD 的优势在于让学生模型在自己生成的序列（rollouts）上接受教师模型的指导，这比离线蒸馏更能处理分布偏移。
然而，目前主流的“采样标记（sampled-token）”变体在长程任务（如数学推理、智能体操作）中表现脆弱。核心问题在于：
*   **理论层面**：在偏差与方差之间存在权衡。序列级目标方差随长度呈四次增长（$O(T^4)$），而标记级目标虽然有偏差但方差更受控（$O(T^2)$）。
*   **经验层面**：采样标记 OPD 存在三大失效模式：信号极度不平衡（仅靠一个标记的 log-ratio 驱动）、教师在前缀漂移时指导不可靠（容易陷入重复循环）、以及分词器/特殊标记不匹配导致的伪惩罚。

### 2. 论文提出的方法论：核心思想与技术细节
论文提出了一种名为**教师 Top-K 局部支持匹配（Teacher Top-K Local Support Matching）**的改进方案，旨在保留标记级更新的低方差特性，同时增强监督信号的鲁棒性。
*   **核心思想**：不再只对比学生采样出的那一个标记，而是在每个前缀位置，对比教师模型认为最合理的 $K$ 个候选标记的分布。
*   **关键技术流程**：
    1.  **Top-p 采样展开**：使用学生模型进行 top-p 采样生成序列，确保轨迹不至于过度偏离合理分布。
    2.  **确定局部支持集 $S$**：在每个步骤，选取教师模型概率最高的 $K$ 个标记。
    3.  **分布重归一化**：将学生和教师在集合 $S$ 内的概率进行重新归一化，确保两者在相同支持域内可比。
    4.  **截断逆向 KL 散度**：计算重归一化后的局部分布之间的 KL 散度作为损失函数。
    5.  **特殊标记掩码**：对 `<think>`、`<|im_end|>` 等容易因分词器差异产生歧义的特殊标记进行掩码处理，减少噪声。

### 3. 实验设计
*   **数据集/场景**：
    *   **单任务数学推理**：使用 DAPO-Math-17K 训练，在 Math500、AIME24/25、Minerva、OlympiadBench 上评估。
    *   **多任务智能体+数学**：交替训练数学任务和 ALFWorld（多轮智能体任务）。
*   **Benchmark 与对比方法**：
    *   **基座模型**：Qwen2.5-7B-Instruct（学生），OpenThinker3-7B（数学教师），GiGPO-Qwen2.5-7B-Alfworld（智能体教师）。
    *   **对比方法**：原始采样标记 OPD、带掩码修复的 OPD、以及本文提出的局部支持匹配方法（含不同 $K$ 值和采样策略的消融）。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：Batch size 为 128，微批次（mini-batch）为 64，学习率为 $2 \times 10^{-6}$，最大上下文长度 16K。
*   **实现细节**：在现有的 OPD 训练流水线上构建，使用了 top-p = 0.9 的采样设置。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从理论验证的“玩具实验（Toy Experiment）”到大规模 LLM 训练。
*   **消融实验**：非常充分。作者针对支持集大小 $K$、是否重归一化、采样策略（top-p vs unconstrained）、特殊标记掩码的影响分别做了详细对比。
*   **客观性**：通过梯度范数、策略熵、裁剪比例（clip ratio）等多种诊断指标监控训练稳定性，实验设计较为严谨且公平。

### 6. 主要结论与发现
*   **稳定性提升**：局部支持匹配显著降低了梯度范数和裁剪边界触发频率，使优化过程更平滑。
*   **性能增强**：在数学任务上，该方法显著优于采样标记 OPD（例如在 Math500 上从 80.0 提升至 82.0，在 AIME24 上从 10.0 提升至 23.3）。
*   **多任务兼容性**：在保持智能体任务高性能的同时，大幅提升了数学推理能力，证明了该方法在复杂混合任务中的有效性。
*   **理论验证**：证实了在长序列任务中，牺牲一定的序列耦合（偏差）以换取更低的方差是必要的。

### 7. 优点与亮点
*   **理论与经验结合**：不仅提出了算法，还通过方差分析解释了为什么简单的标记级更新在实践中更可行。
*   **直击痛点**：识别并解决了 OPD 中被忽视的分词器不匹配和“奖励黑客（reward-hacking）”现象（如模型在答案后不断重复无意义标记）。
*   **简单易行**：提出的修复方案（Top-K 匹配、掩码、重归一化）不需要复杂的架构改动，易于集成到现有 RLHF 或蒸馏流水线中。

### 8. 不足与局限
*   **仍是代理目标**：该方法本质上还是局部匹配，无法完全解决学生与教师在长程逻辑上的根本分歧。
*   **教师依赖性**：如果教师模型本身在学生生成的错误前缀下表现不佳（Out-of-distribution），该方法仍会受到误导。
*   **算力细节缺失**：缺乏具体的算力消耗报告，难以评估其在大规模生产环境下的训练成本。
*   **奖励黑客风险**：虽然有所缓解，但教师匹配并不等同于任务成功，模型仍可能通过模仿教师的语气而非逻辑来获得高分。

（完）
