---
title: "When Models Judge Themselves: Unsupervised Self-Evolution for Multimodal Reasoning"
title_zh: 当模型自我评判：多模态推理的无监督自我进化
authors: "Zhengxian Wu, Kai Shi, Chuanrui Zhang, Zirui Liao, Jun Yang, Ni Yang, Qiuying Peng, Luyuan Zhang, Hangrui Xu, Tianhuang Su, Zhenyu Yang, Haonan Lu, Haoqian Wang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21289v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大模型多模态推理的无监督自我进化
tldr: 本研究针对多模态大模型推理任务中对高质量标注数据和教师模型蒸馏的依赖问题，提出了一种无监督自进化训练框架。该框架通过采样多个推理路径，利用模型自身的自一致性信号作为先验，并引入基于判别器（Judge）的调制机制来动态调整不同质量路径的权重。结合组相对策略优化（GRPO）在无标注数据上进行训练，显著提升了模型在数学推理任务上的性能和泛化能力，为多模态模型的自主进化提供了可扩展的路径。
selection_source: fresh_fetch
motivation: 现有多模态推理模型高度依赖昂贵的人工标注或教师模型蒸馏，难以实现低成本的大规模扩展。
method: 提出一种基于自一致性信号和判别器调制的无监督框架，利用组相对策略优化（GRPO）对采样路径进行加权更新。
result: 在五个数学推理基准测试中，该方法在不使用外部奖励模型或标注答案的情况下，持续提升了模型的推理性能。
conclusion: 该研究证明了多模态模型可以通过无监督自进化实现性能提升，为构建可扩展的自进化智能体提供了有效方案。
---

## 摘要
近期多模态大语言模型的进展在推理任务上取得了强劲表现，但这些提升很大程度上依赖于高质量的标注数据或教师模型蒸馏，这两者都成本高昂且难以扩展。为解决这一问题，我们提出了一种用于多模态推理的无监督自我进化训练框架，在不使用人工标注答案或外部奖励模型的情况下实现了稳定的性能提升。对于每个输入，我们采样多条推理轨迹，并联合建模它们的组内结构。我们利用 Actor 的自一致性信号作为训练先验，并引入一种基于有界判别器（Judge）的调制机制，以持续对不同质量的轨迹进行重新加权。我们进一步将调制后的分数建模为组级分布，并将绝对分数转换为每组内的相对优势，从而实现更稳健的策略更新。通过在无标签数据上使用组相对策略优化（GRPO）进行训练，我们的方法在五个数学推理基准测试中持续提升了推理性能和泛化能力，为迈向自我进化的多模态模型提供了一条可扩展的路径。代码可在 https://dingwu1021.github.io/SelfJudge/ 获取。

## Abstract
Recent progress in multimodal large language models has led to strong performance on reasoning tasks, but these improvements largely rely on high-quality annotated data or teacher-model distillation, both of which are costly and difficult to scale.To address this, we propose an unsupervised self-evolution training framework for multimodal reasoning that achieves stable performance improvements without using human-annotated answers or external reward models. For each input, we sample multiple reasoning trajectories and jointly model their within group structure.We use the Actor's self-consistency signal as a training prior, and introduce a bounded Judge based modulation to continuously reweight trajectories of different quality.We further model the modulated scores as a group level distribution and convert absolute scores into relative advantages within each group, enabling more robust policy updates. Trained with Group Relative Policy Optimization (GRPO) on unlabeled data, our method consistently improves reasoning performance and generalization on five mathematical reasoning benchmarks, offering a scalable path toward self-evolving multimodal models.The code are available at https://dingwu1021.github.io/SelfJudge/.

---

## 论文详细总结（自动生成）

### 论文总结：当模型自我评判——多模态推理的无监督自我进化

#### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLM）在复杂推理任务上的进步高度依赖高质量的人工标注数据或强力教师模型的蒸馏。然而，高质量标注数据稀缺且昂贵，教师模型的能力也逐渐达到瓶颈。
*   **研究动机**：探索一种**无监督自我进化（Unsupervised Self-Evolution）**路径，即在没有人工标注答案、没有外部奖励模型的情况下，仅利用无标签数据让模型实现自主的性能提升。
*   **挑战**：无监督信号通常存在噪声和偏差（如“多数票”选出的答案未必正确），直接进行强化学习容易导致训练不稳定、策略坍塌（Entropy Collapse）或响应长度崩溃。

#### 2. 方法论：核心思想与技术细节
该论文提出了一种结合 **Actor-Judge 联合建模**与**组相对策略优化（GRPO）**的框架：
*   **Actor 采样与自一致性先验**：对于每个输入，Actor 模型采样 $n$ 条推理轨迹。利用“自一致性（Self-Consistency）”作为初始奖励信号，即某个答案出现的频率越高，其对应的轨迹初始奖励越高。
*   **有界判别器（Judge）调制**：引入一个与 Actor 结构相同但参数冻结的 Judge 模块。Judge 对每条轨迹的答案正确性、推理质量和视觉对齐进行评分。
    *   **关键技术**：设计了一个有界的、连续可微的校准函数 $g(s)$（基于 Sigmoid 构造），将 Judge 的原始分数映射为调制信号，用于修正 Actor 的自一致性奖励，防止模型陷入错误的“共识”。
*   **组内分布建模（Distributional Modeling）**：
    *   不使用绝对分数，而是将调制后的分数视为组内分布。
    *   利用 **Log-Sum-Exp** 构造基准线，将绝对分数转化为组内相对优势（Relative Advantage）。
    *   **公式逻辑**：通过能量函数（Energy-based）缩放，使策略更新目标变为匹配奖励诱导的软分布，而非简单地向单一模式坍塌。
*   **优化算法**：采用 GRPO 算法进行策略更新，在保持 KL 散度约束的同时，利用组内相对奖励实现稳健训练。

#### 3. 实验设计
*   **训练数据集**：使用了 Geometry3k、GeoQA 和 MMR1 的无标签版本。
*   **评估基准（Benchmarks）**：
    *   **数学推理**：MathVision, MathVerse, WeMath, LogicVista, DynaMath。
    *   **泛化性测试**：ChartQA（图表理解）, MMVP（通用视觉推理）。
*   **对比方法**：
    *   **基座模型**：Qwen2.5-VL-7B-Instruct。
    *   **无监督方法**：VisionZero, EvoLMM, MM-UPT（基于多数投票的自训练）。
    *   **监督方法**：SFT、基于标签的 GRPO、教师模型蒸馏方法（如 Vision-R1）。

#### 4. 资源与算力
*   **硬件环境**：单节点 8 × NVIDIA A800 (80GB) GPU。
*   **软件框架**：基于 Verl 强化学习框架实现。
*   **训练开销**：
    *   训练周期为 20 个 Epoch。
    *   **相对成本**：由于需要在线采样和 Judge 评分，其训练耗时约为监督强化学习（Supervised GRPO）的 **1.4 倍**（监督 GRPO 在该配置下约需 10.5 小时）。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个核心数学基准上进行了详尽测试，并跨越了不同规模的基座模型（从 2B 到 32B，包括 InternVL3, GLM-4, Qwen2/2.5 系列）。
*   **消融实验**：非常充分。分别验证了自一致性信号、Judge 评分、分布建模（Distributional Modeling）各自的贡献，并展示了训练过程中的熵（Entropy）变化和响应长度变化曲线。
*   **客观性**：通过对比“多数投票（Majority Voting）”这一常用基准，清晰地揭示了该方法在防止模型过早收敛和错误放大方面的优势。

#### 6. 主要结论与发现
*   **性能提升**：在无监督条件下，该方法在 MathVision 上取得了 **+5.9%** 的绝对精度提升（从 25.0% 提升至 30.9%），表现接近甚至在某些指标上超过了依赖标签的监督训练。
*   **稳定性**：相比于多数投票法，该框架能维持更健康的策略熵和响应长度，有效缓解了无监督学习中的模式坍塌问题。
*   **泛化能力**：在数学数据上训练的模型，在图表理解（ChartQA）和通用视觉推理（MMVP）上也表现出性能增长。
*   **可扩展性**：该方法在不同参数规模（2B 至 32B）的模型上均表现出一致的改进效果。

#### 7. 优点与亮点
*   **完全脱离标签**：实现了一种闭环的自我进化，不依赖任何外部奖励模型或人工干预。
*   **创新的奖励建模**：通过 Judge 调制自一致性信号，并结合组内分布建模，解决了无监督 RL 中信号噪声大、易坍塌的痛点。
*   **训练动态优雅**：实验证明该方法能诱导模型产生更长、更深入的思考过程（CoT），且不会像其他方法那样出现剧烈的长度崩溃。

#### 8. 不足与局限
*   **Judge 能力上限**：自我进化的上限受限于初始 Judge（即模型自身）的判别能力。如果模型对某个问题存在根深蒂固的错误共识且 Judge 也无法识别，进化可能会失效。
*   **计算开销**：在线采样 8 条轨迹并进行 Judge 评分增加了训练的时间成本（1.4x）。
*   **Pass@10 指标下降**：由于策略在训练后变得更加集中（虽然比多数投票法好），其采样多样性有所下降，导致 Pass@10 指标在某些情况下略低于原始模型。
*   **动态更新缺失**：目前 Judge 是冻结的，论文指出未来需要研究如何让 Judge 随着 Actor 的进步而同步进化。

（完）
