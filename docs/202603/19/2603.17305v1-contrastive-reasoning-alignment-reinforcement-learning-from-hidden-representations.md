---
title: "Contrastive Reasoning Alignment: Reinforcement Learning from Hidden Representations"
title_zh: 对比推理对齐：基于隐藏表示的强化学习
authors: "Haozheng Luo, Yimin Wang, Jiahao Yu, Binghui Wang, Yan Chen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17305v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 利用强化学习和隐藏表示对大型推理模型进行对齐
tldr: 本研究提出CRAFT对齐框架，旨在通过大模型的推理能力和隐藏层表示来增强其抵御越狱攻击的鲁棒性。与以往仅关注输出层面的防御不同，CRAFT将对比表示学习与强化学习（GRPO）相结合，在隐藏空间内优化安全推理轨迹，从而实现深层的推理级安全对齐。实验表明，该方法在推理和响应安全性上均有显著提升，有效解决了模型表层对齐的问题。
selection_source: fresh_fetch
motivation: 现有的安全对齐方法多局限于输出层面，难以有效防御针对复杂推理过程的越狱攻击。
method: CRAFT框架通过在隐藏空间中集成对比学习与强化学习，显式优化推理轨迹的潜在几何分布以分离安全与风险状态。
result: "在Qwen和Llama推理模型上的实验显示，CRAFT在推理安全性和最终响应安全性上分别实现了79.0%和87.7%的显著提升。"
conclusion: 通过隐藏空间的推理对齐，可以有效消除模型的虚假对齐现象，为构建更鲁棒的安全推理模型提供了新路径。
---

## 摘要
我们提出了 CRAFT，这是一个红队对齐框架，利用模型的推理能力和隐藏表示来提高针对越狱攻击的鲁棒性。与以往主要在输出层面运行的防御措施不同，CRAFT 通过显式优化在隐藏状态空间上定义的目标，使大型推理模型能够生成具有安全意识的推理轨迹。在方法论上，CRAFT 将对比表示学习与强化学习相结合，以区分安全和不安全的推理轨迹，从而产生支持鲁棒的推理级安全对齐的潜空间几何结构。在理论上，我们证明了将潜文本一致性（latent-textual consistency）引入 GRPO 可以通过排除局部最优解来消除表面对齐的策略。在实验上，我们在多个安全基准测试中评估了 CRAFT，使用了两个强大的推理模型 Qwen3-4B-Thinking 和 R1-Distill-Llama-8B，结果显示它始终优于 IPO 和 SafeKey 等最先进的防御方法。值得注意的是，与基础模型相比，CRAFT 在推理安全性和最终响应安全性方面分别平均提升了 79.0% 和 87.7%，证明了隐藏空间推理对齐的有效性。

## Abstract
We propose CRAFT, a red-teaming alignment framework that leverages model reasoning capabilities and hidden representations to improve robustness against jailbreak attacks. Unlike prior defenses that operate primarily at the output level, CRAFT aligns large reasoning models to generate safety-aware reasoning traces by explicitly optimizing objectives defined over the hidden state space. Methodologically, CRAFT integrates contrastive representation learning with reinforcement learning to separate safe and unsafe reasoning trajectories, yielding a latent-space geometry that supports robust, reasoning-level safety alignment. Theoretically, we show that incorporating latent-textual consistency into GRPO eliminates superficially aligned policies by ruling them out as local optima. Empirically, we evaluate CRAFT on multiple safety benchmarks using two strong reasoning models, Qwen3-4B-Thinking and R1-Distill-Llama-8B, where it consistently outperforms state-of-the-art defenses such as IPO and SafeKey. Notably, CRAFT delivers an average 79.0% improvement in reasoning safety and 87.7% improvement in final-response safety over the base models, demonstrating the effectiveness of hidden-space reasoning alignment.

---

## 论文详细总结（自动生成）

以下是对论文《Contrastive Reasoning Alignment: Reinforcement Learning from Hidden Representations》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：大型推理模型（LRMs，如 DeepSeek-R1、OpenAI o1 等）虽然在复杂任务上表现优异，但存在“表层安全对齐”（Superficial Safety Alignment, SSA）的问题。即模型在最终回答时虽然拒绝了有害请求，但在其内部推理过程（思维链/Reasoning Traces）中仍可能包含有害信息、歧视性逻辑或违规内容。
*   **核心问题**：现有的安全对齐方法大多集中在输出层面，难以约束模型内部的“暗箱”推理过程。本文旨在通过对齐模型的**隐藏层表示（Hidden Representations）**，确保推理轨迹与最终安全输出在逻辑和语义上保持一致。

### 2. 论文提出的方法论：CRAFT
CRAFT（对比推理对齐框架）结合了对比学习与强化学习，分为两个阶段：
*   **阶段一：推理潜空间对比学习（LCLR）**
    *   **核心思想**：在隐藏空间中构建几何结构，将推理轨迹分为“安全”、“不安全”和“重新思考（Rethink）”三个区域。
    *   **技术细节**：使用三元组损失（Triplet Loss）拉近安全轨迹与安全原型的距离，推开不安全轨迹；引入实例级不变性损失（类似 SimCLR）以应对文本扰动；通过潜空间安全校准损失，将隐藏状态映射为可解释的安全概率。
*   **阶段二：基于推理潜空间的强化学习（R2L）**
    *   **核心思想**：利用 GRPO（群体相对策略优化）算法，直接在推理轨迹的隐藏状态上施加奖励。
    *   **奖励函数设计**：
        1.  **潜空间语义奖励 ($R_{ls}$)**：根据隐藏向量在潜空间中靠近安全原型的程度给予奖励。
        2.  **文本安全奖励 ($R_{txt}$)**：基于外部评估器对最终输出的安全性评分。
        3.  **潜文本一致性奖励 ($R_{cons}$)**：这是关键创新，通过最小化“潜空间预测的安全概率”与“文本实际安全得分”之间的差异，强制模型“言行一致”，消除 SSA。

### 3. 实验设计
*   **数据集/场景**：
    *   **安全评估**：JailbreakBench（越狱基准）、StrongReject（强拒绝基准）。
    *   **能力评估**：AIME24、MATH-500、Minerva（数学推理）；LiveCodeBench（代码生成）。
*   **对比方法**：对比了 6 种最先进的方法，包括 SafeChain、RealSafe、STAR、SafeKey、IPO 和 ReasoningShield。
*   **实验模型**：Qwen3-4B-Thinking 和 DeepSeek-R1-Distill-Llama-8B。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备了 **4 张 NVIDIA H100 (80GB) GPU** 的系统上运行。
*   **软件环境**：基于 PyTorch 和 Hugging Face Transformers 库。
*   **训练细节**：使用了 2,500 条有害提示词进行训练，推理参数设置温度为 0.6，top-p 为 0.95。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了主流的推理模型和多个维度的基准测试。
*   **消融实验**：详细分析了 LCLR、一致性奖励 ($R_{cons}$) 和潜空间奖励 ($R_{ls}$) 的贡献，证明了每个模块的必要性。
*   **鲁棒性测试**：针对 GPTFuzzer 和 AutoDAN 等高级越狱攻击进行了额外测试。
*   **评价**：实验设计全面且客观，不仅关注安全性提升，还通过数学和代码测试验证了模型是否因对齐而导致能力退化（即“安全税”问题）。

### 6. 论文的主要结论与发现
*   **安全性显著提升**：相比基础模型，CRAFT 在推理过程安全性上平均提升了 **79.0%**，在最终响应安全性上提升了 **87.7%**。
*   **能力不降反升**：CRAFT 在保持安全性的同时，推理能力（数学/代码）平均提升了 **4.7%**。作者认为这是因为高质量的推理对齐过程本身也强化了模型的逻辑严密性。
*   **理论验证**：通过数学证明显示，引入潜文本一致性奖励可以将 SSA 状态排除在 GRPO 的局部最优解之外。

### 7. 优点
*   **深度对齐**：突破了传统的“黑盒”对齐，深入到模型的隐藏表示层，解决了推理模型特有的 SSA 风险。
*   **理论与实践结合**：不仅有显著的实验效果，还提供了严谨的理论证明。
*   **高效性**：利用 GRPO 算法，无需额外的奖励模型（Reward Model）即可实现高效对齐。

### 8. 不足与局限
*   **依赖外部评估器**：在计算文本安全奖励时，仍高度依赖 GPT-4o 等外部强模型作为评估器，可能存在评估偏差。
*   **训练成本**：虽然使用了 GRPO，但针对隐藏层表示的优化和对比学习仍需要较高的计算资源（如 H100）。
*   **泛化限制**：目前主要在数学和代码推理模型上验证，对于更广泛的开放域对话推理的适用性仍有待进一步探索。

（完）
