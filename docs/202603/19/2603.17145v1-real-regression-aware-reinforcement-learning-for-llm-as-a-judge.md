---
title: "REAL: Regression-Aware Reinforcement Learning for LLM-as-a-Judge"
title_zh: REAL：面向 LLM-as-a-Judge 的回归感知强化学习
authors: "Yasi Zhang, Tianyu Chen, Mingyuan Zhou, Oscar Leong, Ying Nian Wu, Michal Lukasik"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17145v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 针对LLM作为评委场景的回归感知强化学习框架
tldr: 针对LLM作为自动评分员（LLM-as-a-Judge）时，传统强化学习（RL）采用二元奖励而忽略数值回归序数特性的问题，本文提出了REAL框架。该框架将回归目标引入RL，通过广义策略梯度估计器，结合思维链（CoT）探索与最终评分的回归感知优化。实验表明，REAL在不同规模模型上均显著提升了评分相关性及泛化能力，为提升LLM评估准确性提供了新路径。
selection_source: fresh_fetch
motivation: 现有的LLM评分方法中，传统RL忽略了分数的序数结构，而回归感知型SFT则缺乏对最优推理路径的探索能力。
method: 提出REAL框架，利用广义策略梯度估计器将优化分解为思维链轨迹探索和最终分数的回归感知预测微调。
result: 在Qwen3-32B模型上，REAL相比SFT基准在Pearson和Spearman相关性上分别提升了8.40和7.20，且泛化性能优异。
conclusion: 将回归目标集成到强化学习的探索过程中，对于提升大语言模型作为自动评估器的准确性和鲁棒性至关重要。
---

## 摘要
大语言模型 (LLM) 越来越多地被部署为自动评估器，为模型输出分配数值分数，这种范式被称为 LLM-as-a-Judge。然而，标准的强化学习 (RL) 方法通常依赖于二元奖励（例如 0-1 准确率），从而忽略了回归任务中固有的序数结构；例如，当真实值为 5 时，它们无法识别出预测为 4 明显优于预测为 1。相反，现有的回归感知方法通常局限于有监督微调 (SFT)，限制了它们探索最优推理路径的能力。为了弥补这一差距，我们提出了 REAL (REgression-Aware Reinforcement Learning)，这是一个旨在优化回归奖励的原则性强化学习框架，并被证明对相关性指标也是最优的。一个关键的技术挑战是回归目标显式地依赖于策略，从而使标准的策略梯度方法失效。为了解决这个问题，我们采用了广义策略梯度估计器，它自然地将优化分解为两个互补的部分：(1) 在思维链 (CoT) 轨迹上的探索，以及 (2) 最终分数的回归感知预测细化。在不同模型规模（8B 到 32B）上的广泛实验表明，REAL 一致地优于回归感知 SFT 基准和标准 RL 方法，在域外基准测试中表现出显著更好的泛化能力。特别是在 Qwen3-32B 上，与 SFT 基准相比，我们实现了 +8.40 的 Pearson 相关性和 +7.20 的 Spearman 相关性提升，与基础模型相比提升了 +18.30/+11.20。这些发现强调了将回归目标集成到 RL 探索中对于准确 LLM 评估的关键价值。

## Abstract
Large language models (LLMs) are increasingly deployed as automated evaluators that assign numeric scores to model outputs, a paradigm known as LLM-as-a-Judge. However, standard Reinforcement Learning (RL) methods typically rely on binary rewards (e.g., 0-1 accuracy), thereby ignoring the ordinal structure inherent in regression tasks; for instance, they fail to recognize that predicting 4 is significantly better than predicting 1 when the ground truth is 5. Conversely, existing regression-aware approaches are often confined to Supervised Fine-Tuning (SFT), limiting their ability to explore optimal reasoning paths. To bridge this gap, we propose \textbf{REAL} (\underline{RE}gression-\underline{A}ware Reinforcement \underline{L}earning), a principled RL framework designed to optimize regression rewards, and also proven to be optimal for correlation metrics. A key technical challenge is that the regression objective is explicitly policy-dependent, thus invalidating standard policy gradient methods. To address this, we employ the generalized policy gradient estimator, which naturally decomposes optimization into two complementary components: (1) exploration over Chain-of-Thought (CoT) trajectory, and (2) regression-aware prediction refinement of the final score. Extensive experiments across model scales (8B to 32B) demonstrate that REAL consistently outperforms both regression-aware SFT baselines and standard RL methods, exhibiting significantly better generalization on out-of-domain benchmarks. On Qwen3-32B specifically, we achieve gains of +8.40 Pearson and +7.20 Spearman correlation over the SFT baseline, and +18.30/+11.20 over the base model. These findings highlight the critical value of integrating regression objectives into RL exploration for accurate LLM evaluation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **REAL (Regression-Aware Reinforcement Learning)** 的新型强化学习框架，专门用于优化大语言模型作为自动评委（LLM-as-a-Judge）时的数值评分能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：LLM 越来越多地被用作自动评分员（如给文本打 1-5 分）。目前的训练方法主要分为两类：
    1.  **有监督微调 (SFT)**：如 RAFT 或 TRACT，虽然引入了回归损失，但局限于静态数据集，缺乏对推理路径（CoT）的自主探索能力。
    2.  **标准强化学习 (RL)**：通常使用二元奖励（对/错），忽略了分数的**序数结构**（例如：目标是 5 分时，预测 4 分显然比预测 1 分更好，但二元 RL 视两者为同等错误）。
*   **核心问题**：如何在一个统一的强化学习框架中，既能让模型探索最优的推理路径，又能尊重数值评分的连续性和序数特性？

### 2. 方法论
*   **核心思想**：将回归目标（均方误差 MSE）直接引入 RL 的奖励函数，并利用**广义策略梯度（Generalized Policy Gradient）**解决奖励函数显式依赖于策略参数（Policy-Dependent Reward）的技术难题。
*   **关键技术细节**：
    *   **REAL 目标函数**：结合了回归损失（预测值与真实值的平方差）和最终答案的对数似然（Log-likelihood）辅助项。
    *   **梯度分解**：通过数学推导，REAL 将梯度更新分解为两个互补部分：
        1.  **思维链（CoT）更新**：利用策略梯度鼓励模型探索能导致准确评分的推理轨迹。
        2.  **预测细化更新**：通过标准反向传播，直接对最终数值预测进行回归感知的监督。
    *   **理论证明**：论文证明了最小化平方误差损失等同于优化 Pearson 相关性指标，为回归奖励的合理性提供了理论支撑。
    *   **训练稳定性**：采用了 **RLOO (Leave-one-out)** 估计器来降低方差，并引入动态采样策略（优先训练部分正确的样本）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Feedback Collection（约 10 万个样本）。
    *   **评估集（Benchmark）**：包含域内测试集 **Feedback Bench**，以及三个域外（OOD）测试集：**FLASK**、**Vicuna Bench** 和 **MT Bench**。
*   **对比方法**：
    *   **零样本基准**：基础模型（Mistral, Qwen3）。
    *   **SFT 基准**：Prometheus 1/2, RAFT, TRACT。
    *   **RL 基准**：采用二元准确率奖励的标准 RL（Standard RL），以及复现的 JEPO 算法。
*   **评估指标**：Pearson 相关系数 ($r$)、Spearman 秩相关系数 ($\rho$)、Kendall’s $\tau$、RMSE 和 MAE。

### 4. 资源与算力
*   **硬件使用**：
    *   **Mistral2-7B 和 Qwen3-8B**：使用 8 台 NVIDIA **A100** GPU。
    *   **Qwen3-32B**：使用 16 台 NVIDIA **A100** GPU。
*   **训练细节**：采用全参数微调（Full-parameter fine-tuning），学习率在 $5 \times 10^{-8}$ 到 $1 \times 10^{-6}$ 之间。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B、8B 到 32B 三种不同架构和规模的模型，验证了算法的可扩展性。
*   **消融实验**：非常充分，涵盖了组大小 ($K$)、对数似然权重 ($\lambda$)、预测权重 ($\beta$)、采样策略、初始化检查点以及温度系数 ($T$) 的影响。
*   **客观性**：通过在多个完全不同的域外基准上测试，证明了该方法不是简单的过拟合，而是真正提升了模型的泛化评估能力。

### 6. 主要结论与发现
*   **性能领先**：REAL 在所有模型规模和所有基准测试中均显著优于 SFT 和标准 RL。
*   **泛化能力强**：在域外任务（如 Vicuna Bench）上提升尤为明显。例如，Qwen3-32B 在相关性指标上比 SFT 基准提升了约 8 个点。
*   **推理效率**：REAL 训练的模型在推理时只需单次采样（N=1）即可达到极高准确度，无需多次采样取平均，具有更高的部署效率。
*   **标准 RL 的局限**：实验证实，在回归任务中使用二元奖励会导致相关性指标崩溃，证明了“回归感知”在 RL 中的必要性。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了算法，还给出了关于相关性优化的数学证明。
*   **解决策略依赖奖励问题**：通过广义策略梯度优雅地处理了动态奖励函数的梯度回传。
*   **解耦优化**：将“推理逻辑（CoT）”的探索与“数值预测”的精准度分开处理，符合 LLM 评分任务的直觉。

### 8. 不足与局限
*   **任务局限性**：目前仅聚焦于**点分式（Pointwise）**评估，尚未直接扩展到两两比较（Pairwise）的偏好学习场景。
*   **推理偏差**：模型仍依赖于自生成的 CoT，如果基础模型本身存在严重的系统性偏差，REAL 可能会继承这些偏差。
*   **计算成本**：虽然推理高效，但 RL 训练阶段（尤其是采样多个 CoT 轨迹）相比 SFT 仍需要更多的计算资源。

（完）
