---
title: A Diffusion Analysis of Policy Gradient for Stochastic Bandits
title_zh: 随机多臂老虎机策略梯度的扩散分析
authors: Tor Lattimore
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10219v1"
tags: ["query:rlaif"]
score: 6.0
evidence: 随机多臂老虎机中策略梯度方法的分析
tldr: 本研究利用连续时间扩散近似方法分析了随机多臂老虎机中的策略梯度算法。研究证明，当学习率设定为 η = O(Δ²/log(n)) 时，算法的累积遗憾界为 O(k log(k) log(n) / η)。此外，研究还通过构造特定实例证明，若学习率不满足 η = O(Δ²) 条件，即使在臂数较少的情况下，算法也可能产生线性遗憾，这强调了学习率选择对算法性能的决定性影响。
selection_source: fresh_fetch
motivation: 旨在通过连续时间扩散近似框架，深入探究策略梯度算法在随机多臂老虎机问题中的理论性能与收敛特性。
method: 采用连续时间扩散过程来近似离散的策略梯度更新，并在此数学框架下推导算法的累积遗憾界。
result: 证明了在特定学习率下算法可达到对数级遗憾，并揭示了学习率过大时会导致线性遗憾的失效场景。
conclusion: 该研究明确了策略梯度算法在老虎机任务中实现次线性遗憾所需的学习率约束及其与最小间隙 Δ 的关键关系。
---

## 摘要
我们研究了 $k$ 臂随机多臂老虎机策略梯度的连续时间扩散逼近。我们证明了当学习率 $η= O(Δ^2/\log(n))$ 时，其遗憾（regret）为 $O(k \log(k) \log(n) / η)$，其中 $n$ 是时间跨度，$Δ$ 是最小间隙。此外，我们构造了一个仅包含对数数量臂的实例，证明了除非 $η= O(Δ^2)$，否则其遗憾将是线性的。

## Abstract
We study a continuous-time diffusion approximation of policy gradient for $k$-armed stochastic bandits. We prove that with a learning rate $η= O(Δ^2/\log(n))$ the regret is $O(k \log(k) \log(n) / η)$ where $n$ is the horizon and $Δ$ the minimum gap. Moreover, we construct an instance with only logarithmically many arms for which the regret is linear unless $η= O(Δ^2)$.