---
title: "InfoDensity: Rewarding Information-Dense Traces for Efficient Reasoning"
title_zh: InfoDensity：奖励信息密集型轨迹以实现高效推理
authors: "Chengwei Wei, Jung-jae Kim, Longyin Zhang, Shengkai Chen, Nancy F. Chen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17310v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 利用强化学习优化大语言模型的推理路径
tldr: 针对大语言模型推理过程冗长且冗余的问题，本文提出InfoDensity强化学习框架。研究发现高质量推理具有低不确定性收敛和单调进展的特性。该框架通过结合AUC奖励、单调性奖励及长度缩放因子，引导模型生成信息密度高且简洁的推理链。实验证明，该方法在保持或提升数学推理准确率的同时，显著降低了Token消耗，实现了效率与性能的平衡。
selection_source: fresh_fetch
motivation: 现有的强化学习方法往往只优化响应长度而忽视了中间推理质量，导致模型产生冗余推理并容易出现奖励作弊现象。
method: 提出一种结合AUC奖励、单调性奖励和长度缩放因子的统一奖励框架，旨在衡量并提升推理步骤的信息密度。
result: 在数学推理基准测试中，该方法在达到或超过现有最先进模型准确率的同时，显著减少了推理所需的Token数量。
conclusion: 通过优化推理过程的信息密度而非单纯限制长度，可以有效提升大语言模型在复杂推理任务中的计算效率和逻辑质量。
---

## 摘要
具有扩展推理能力的大语言模型（LLMs）通常会生成冗长且冗余的推理轨迹，从而产生不必要的计算开销。虽然现有的强化学习方法通过优化最终响应长度来解决这一问题，但它们忽略了中间推理步骤的质量，使模型容易受到奖励作弊（reward hacking）的影响。我们认为，冗长不仅仅是一个长度问题，而是中间推理质量低下的表现。为了研究这一点，我们进行了一项实证研究，追踪推理步骤中答案分布的条件熵。我们发现，高质量的推理轨迹表现出两个一致的特性：低不确定性收敛和单调进展。这些发现表明，高质量的推理轨迹在信息上是密集的，即相对于总推理长度，每一步都贡献了有意义的熵减。受此启发，我们提出了 InfoDensity，这是一个用于强化学习训练的奖励框架，它结合了基于 AUC 的奖励和单调性奖励，作为推理质量的统一衡量标准，并由长度缩放项进行加权，以鼓励更简洁地实现同等质量。在数学推理基准测试上的实验表明，InfoDensity 在准确率上达到或超过了最先进的基准模型，同时显著减少了 token 使用量，实现了强大的准确率-效率权衡。

## Abstract
Large Language Models (LLMs) with extended reasoning capabilities often generate verbose and redundant reasoning traces, incurring unnecessary computational cost. While existing reinforcement learning approaches address this by optimizing final response length, they neglect the quality of intermediate reasoning steps, leaving models vulnerable to reward hacking. We argue that verbosity is not merely a length problem, but a symptom of poor intermediate reasoning quality. To investigate this, we conduct an empirical study tracking the conditional entropy of the answer distribution across reasoning steps. We find that high-quality reasoning traces exhibit two consistent properties: low uncertainty convergence and monotonic progress. These findings suggest that high-quality reasoning traces are informationally dense, that is, each step contributes meaningful entropy reduction relative to the total reasoning length. Motivated by this, we propose InfoDensity, a reward framework for RL training that combines an AUC-based reward and a monotonicity reward as a unified measure of reasoning quality, weighted by a length scaling term that favors achieving equivalent quality more concisely. Experiments on mathematical reasoning benchmarks demonstrate that InfoDensity matches or surpasses state-of-the-art baselines in accuracy while significantly reducing token usage, achieving a strong accuracy-efficiency trade-off.

---

## 论文详细总结（自动生成）

### InfoDensity: 奖励信息密集型轨迹以实现高效推理 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLMs）向推理模型（LRMs）演进，模型生成的思维链（CoT）轨迹变得越来越长。虽然这提升了复杂问题的解决能力，但也带来了严重的**计算冗余和 Token 浪费**。
*   **现有问题**：目前的强化学习（RL）优化方法（如长度惩罚或设定长度目标）往往只关注“缩短长度”和“最终答案正确性”，忽略了中间推理步骤的质量。这导致模型容易产生“奖励作弊”（Reward Hacking），即生成表面简洁但逻辑断裂或不完整的推理。
*   **核心观点**：论文认为“冗长”是推理质量低下的症状。高质量的推理应该是**信息密集**的，即每一步都应显著降低对最终答案的不确定性。

#### 2. 方法论：核心思想与关键技术
论文提出了 **InfoDensity** 框架，通过信息论指标（条件熵）来量化推理质量，并将其作为 RL 的奖励信号。

*   **核心思想**：利用一个固定的“评判模型”（Judge Model）计算推理过程中每一步的条件熵 $H(Z | X, Y_{\le t})$，衡量模型对最终答案 $Z$ 的不确定性。
*   **关键奖励组件**：
    1.  **AUC 奖励 ($R_{AUC}$)**：衡量不确定性收敛的效率。计算熵曲线下的面积（AUC），面积越小说明模型越快、越稳定地锁定了正确答案。
    2.  **单调性奖励 ($R_{mono}$)**：衡量推理的连贯性。计算熵值严格下降的步骤比例，鼓励模型每一步都产生实质性的进展，而非原地踏步或引入逻辑混乱。
    3.  **质量评分 ($R_{quality}$)**：上述两者的加权平均（实验中 $\alpha=0.5$）。
    4.  **长度缩放因子 ($R_L$)**：采用组内相对长度缩放，奖励比平均长度短的轨迹，惩罚冗长轨迹。
*   **最终公式**：$R_{InfoDensity} = R_{quality} \cdot R_L$（仅对最终答案正确的轨迹发放）。

#### 3. 实验设计
*   **基础模型**：Qwen3-0.6B 和 DeepSeek-R1-Distill-Qwen-1.5B。
*   **数据集**：
    *   **训练集**：GSM8K 和 MATH 的训练部分。
    *   **测试集（分布内）**：GSM8K, MATH500。
    *   **测试集（分布外/挑战性）**：AIME 24, OlympiadBench。
*   **Benchmark 与对比方法**：
    *   **GRPO-Acc**：仅奖励准确率。
    *   **GRPO-LP**：准确率奖励 + 长度惩罚。
    *   **Direct-Scoring (DS)**：利用 Judge 模型直接对推理质量打分（Prompt-based）。
    *   **PEAR**：最先进的基于相位熵的效率优化方法。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量以及总训练时长。
*   **实现细节**：提到了使用 Qwen3-4B-Instruct 作为固定的外部评判模型来计算熵轨迹，这在训练过程中会增加额外的推理开销。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.6B 到 1.5B 的模型，并在 4 个主流数学基准上进行了测试。
*   **消融实验**：详细探讨了 $\alpha$ 参数（AUC 与单调性的平衡）对训练稳定性的影响，并对比了不同长度缩放强度 $\lambda$ 的效果。
*   **充分性评价**：实验设计较为充分。通过对比“直接打分（DS）”和“信息论指标（InfoDensity）”，客观证明了基于概率分布的熵信号比 LLM 的主观打分更稳定、更有效。同时，跨分布（OOD）的测试验证了该方法的泛化能力。

#### 6. 主要结论与发现
*   **高质量推理特征**：正确轨迹表现出“低不确定性收敛”和“单调熵减”；错误轨迹则在首个错误点后熵值停滞或反弹。
*   **性能表现**：InfoDensity 在 DeepSeek-R1-1.5B 上实现了 **2.5% 的平均准确率提升**，同时**减少了 30% 的 Token 消耗**。
*   **克服奖励作弊**：相比于单纯的长度惩罚（GRPO-LP），InfoDensity 能够防止模型为了缩短长度而牺牲推理完整性，保持了极高的训练稳定性。

#### 7. 优点（亮点）
*   **无监督质量评估**：无需人工标注步骤级的正确性标签，纯粹利用模型自身的概率分布特性提取信号。
*   **动态过程监控**：不同于只看结果的奖励，InfoDensity 监控了推理的“轨迹形状”，捕捉了逻辑推进的动态过程。
*   **效率与精度的平衡**：成功解决了“想得太久”和“想得太浅”之间的矛盾。

#### 8. 不足与局限
*   **领域局限性**：实验仅集中在数学推理任务，其“熵减”特性在代码生成或开放式创意写作等任务中是否同样适用尚待验证。
*   **计算开销**：训练期间需要调用外部 Judge 模型计算每个步骤的熵，这显著增加了训练时的推理成本。
*   **Judge 模型依赖**：奖励信号的质量高度依赖于 Judge 模型的能力，若 Judge 模型本身对问题理解有偏，可能会误导训练。

（完）
