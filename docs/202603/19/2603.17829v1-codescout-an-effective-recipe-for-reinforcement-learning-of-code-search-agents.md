---
title: "CodeScout: An Effective Recipe for Reinforcement Learning of Code Search Agents"
title_zh: CodeScout：代码搜索智能体强化学习的有效方案
authors: "Lintang Sutawika, Aditya Bharat Soni, Bharath Sriraam R R, Apurva Gandhi, Taha Yassine, Sanidhya Vijayvargiya, Yuchen Li, Xuhui Zhou, Yilin Zhang, Leander Melroy Maben, Graham Neubig"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17829v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 针对代码搜索智能体的强化学习方案
tldr: 本研究针对大型代码库中的代码定位难题，提出了 CodeScout 框架。不同于以往依赖复杂静态分析工具的方法，CodeScout 仅通过标准 Unix 终端，利用强化学习（RL）训练智能体进行代码搜索。通过优化环境复用、奖励设计和 RL 策略，该模型在 SWE-Bench Verified、Pro 和 Lite 基准测试中表现卓越，不仅超越了规模大 2-18 倍的开源模型，甚至接近 Claude Sonnet 等顶级闭源模型的性能，为构建高效代码搜索智能体提供了新范式。
selection_source: fresh_fetch
motivation: 现有代码搜索智能体通常依赖复杂的专用工具（如静态分析图），限制了其在不同环境下的通用性。
method: 提出 CodeScout，通过强化学习训练仅配备标准 Unix 终端的智能体，并重点优化了环境适配、奖励函数和 RL 训练流程。
result: 在 SWE-Bench 系列基准测试中，CodeScout 显著优于规模大 2-18 倍的模型，且性能逼近 Claude Sonnet 等顶级闭源模型。
conclusion: 研究证明了通过有效的强化学习配方，即使仅使用基础工具，代码搜索智能体也能在复杂仓库定位任务中取得顶尖性能。
---

## 摘要
编程智能体在大型代码库中执行任务的前提是代码定位——即识别出需要处理的相关文件、类和函数。虽然代码库级别的代码定位通常使用基于嵌入的检索方法（如向量搜索）来完成，但最近的研究重点在于开发能够定位相关代码的智能体，这种定位既可以作为独立的前置步骤，也可以与实际工作交替进行。大多数先前的智能体代码搜索方法为智能体配备了复杂的专用工具，例如通过静态分析生成的代码库图。在本文中，我们证明了通过有效的强化学习方案，仅配备标准 Unix 终端的编程智能体也能通过训练获得出色的结果。我们在三个基准测试（SWE-Bench Verified、Pro 和 Lite）上的实验表明，我们的模型始终优于或等同于比其大 2-18 倍的基础及经过后训练的大语言模型（LLM），且在某些情况下，即使后者使用了专门的脚手架，我们的模型也能接近 Claude Sonnet 等闭源模型的性能。我们的工作特别关注于将现有的编程智能体环境重新用于代码搜索的技术、奖励设计以及强化学习优化。我们发布了由此产生的模型系列 CodeScout，以及所有代码和数据，供社区在此基础上进一步开发。

## Abstract
A prerequisite for coding agents to perform tasks on large repositories is code localization - the identification of relevant files, classes, and functions to work on. While repository-level code localization has been performed using embedding-based retrieval approaches such as vector search, recent work has focused on developing agents to localize relevant code either as a standalone precursor to or interleaved with performing actual work. Most prior methods on agentic code search equip the agent with complex, specialized tools, such as repository graphs derived from static analysis. In this paper, we demonstrate that, with an effective reinforcement learning recipe, a coding agent equipped with nothing more than a standard Unix terminal can be trained to achieve strong results. Our experiments on three benchmarks (SWE-Bench Verified, Pro, and Lite) reveal that our models consistently achieve superior or competitive performance over 2-18x larger base and post-trained LLMs and sometimes approach performance provided by closed models like Claude Sonnet, even when using specialized scaffolds. Our work particularly focuses on techniques for re-purposing existing coding agent environments for code search, reward design, and RL optimization. We release the resulting model family, CodeScout, along with all our code and data for the community to build upon.

---

## 论文详细总结（自动生成）

这是一份关于论文《CodeScout: An Effective Recipe for Reinforcement Learning of Code Search Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在处理大型代码库任务（如 SWE-Bench）时，智能体必须首先进行“代码定位”（识别相关文件、类和函数）。
*   **核心挑战**：现有方法通常依赖复杂的、特定语言的静态分析工具（如 AST 解析、代码依赖图），这增加了工程复杂性且难以跨语言迁移。
*   **整体含义**：论文探讨了是否可以通过**强化学习（RL）**训练一个仅配备标准 Unix 终端（如 `ls`, `grep`, `rg`）的通用智能体，使其在代码搜索任务上达到或超过拥有复杂工具的专用智能体。

### 2. 方法论
*   **核心思想**：利用强化学习优化智能体使用基础终端工具进行仓库导航和搜索的能力，而非开发新工具。
*   **关键技术细节**：
    *   **智能体脚手架 (OpenHands-Bash)**：仅提供一个 Bash 终端工具和一个用于提交结果的 `LocalizationFinish` 工具。
    *   **奖励设计**：采用多粒度 F1 分数（文件级、模块级、函数级）。公式为：$R = F1_{file} + F1_{module} + F1_{func}$。针对 14B 模型，还加入了鼓励在步数限制内完成任务的二元奖励。
    *   **训练算法**：使用 **GSPO (Group Sequence Policy Optimization)**，这是一种改进的策略梯度算法，去除了优势标准化和 KL 正则化，提高了长序列智能体训练的稳定性。
    *   **数据策略**：从 SWE-Smith 提取了 39K 个 Python 任务实例。对于 1.7B 小模型，先用 14B 模型的成功轨迹进行拒绝采样微调（RFT），再进行 RL。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **SWE-Bench Verified** (500 实例)
    *   **SWE-Bench Lite** (300 实例)
    *   **SWE-Bench Pro** (266 Python 实例，难度最高)
*   **对比方法**：
    *   **开源基准**：RepoNavigator, LocAgent, OrcaLoca, CoSIL, Agentless, RepoSearcher（涵盖了 7B 到 32B 规模）。
    *   **闭源模型**：GPT-5 (o1-preview/chat), Claude 3.7 Sonnet, Claude 4.5 Sonnet。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   **4B 模型**：训练 200 步，Batch Size 为 8，每实例采样 8 条轨迹。
    *   **14B 模型**：训练 300 步，Batch Size 为 32，每实例采样 4 条轨迹。
    *   **1.7B 模型**：RFT 训练 1 个 Epoch，随后进行 100 步 RL。
    *   **上下文长度**：训练时为 32K-50K，评估时扩展至 132K。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准上进行了全面测试，并针对 RL 算法选择（GSPO vs GRPO vs SAPO）进行了消融实验。
*   **充分性评价**：实验设计较为充分。不仅对比了定位准确率，还分析了定位结果对下游“问题解决（Issue Resolution）”成功率的影响，并可视化了智能体在 RL 过程中工具使用行为的演变（从乱用工具到集中使用 `rg` 和 `sed`）。

### 6. 主要结论与发现
*   **参数效率极高**：CodeScout-1.7B 在多个指标上优于规模大 8 倍的 Qwen3-14B 基础模型；CodeScout-14B 显著超越了 32B 的开源 SOTA 模型。
*   **逼近闭源模型**：在函数级定位上，CodeScout-14B 的表现甚至超过了配备复杂工具的 Claude 3.7 Sonnet。
*   **工具简化可行性**：证明了复杂的静态分析工具并非必须，通过 RL 训练，智能体可以学会仅靠 `ripgrep` 等基础工具高效定位代码。
*   **下游增益**：准确的代码定位能显著提升 Bug 修复的成功率，并减少 Token 消耗和推理步数。

### 7. 优点
*   **语言无关性**：由于只使用终端工具，该方案理论上可以轻松扩展到 Python 以外的编程语言。
*   **工程简洁**：避免了昂贵的预索引（如构建代码图）和复杂的脚手架设计。
*   **开源贡献**：完整发布了模型权重、训练代码和处理后的数据集。

### 8. 不足与局限
*   **语言覆盖偏差**：虽然方法论是通用的，但目前的实验和数据提取脚本仍主要集中在 **Python** 语言。
*   **真值提取限制**：目前的 Ground Truth 仅基于补丁修改过的位置，可能忽略了那些对理解上下文很重要但未被修改的文件。
*   **提示词敏感性**：实验发现闭源模型（如 GPT-5）对是否包含“提交提醒”极其敏感，这可能导致对比时的基准波动。

（完）
