---
title: Efficient Exploration at Scale
title_zh: 大规模高效探索
authors: "Seyed Mohammad Asghari, Chris Chute, Vikranth Dwaracherla, Xiuyuan Lu, Mehdi Jafarnia, Victor Minden, Zheng Wen, Benjamin Van Roy"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17378v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 用于高效大模型训练的在线RLHF算法
tldr: 本研究提出一种在线学习算法，旨在显著提升从人类反馈中强化学习（RLHF）的数据效率。该方法通过增量更新奖励和语言模型，引入认知神经网络来建模奖励不确定性，并结合信息导向探索（IDS）和肯定性微调信号。实验表明，在 Gemma 大模型上，该算法仅需不到 2 万条标签即可匹配传统离线 RLHF 使用 20 万条标签的性能，实现了 10 倍以上的数据效率提升，且预估在更大规模下可达 1000 倍增益。
selection_source: fresh_fetch
motivation: 针对当前 RLHF 过程中数据效率低下、依赖海量人工标注的问题，探索更高效的在线学习与探索机制。
method: 开发了一种结合认知神经网络建模奖励不确定性、信息导向探索（IDS）以及增量式模型更新的在线 RLHF 算法。
result: 在 Gemma 模型上仅用不到 2 万条标签便达到了离线 RLHF 20 万条标签的效果，实现了超过 10 倍的数据效率提升。
conclusion: 该研究证明了通过高效的探索策略，大规模语言模型的微调可以实现 10 到 1000 倍的数据效率增益。
---

## 摘要
我们开发了一种在线学习算法，显著提高了人类反馈强化学习（RLHF）的数据效率。我们的算法在接收到选择数据时增量更新奖励模型和语言模型。奖励模型拟合选择数据，而语言模型通过 REINFORCE 算法的一种变体进行更新，强化信号由奖励模型提供。几项特性实现了效率提升：在每个强化信号中加入微小的肯定性微调（affirmative nudge）、一个建模奖励不确定性的认知神经网络（epistemic neural network），以及信息导向探索（information-directed exploration）。使用 Gemma 大语言模型（LLMs），我们的算法仅用不到 2 万条标签就达到了在 20 万条标签上训练的离线 RLHF 的性能，代表了超过 10 倍的数据效率提升。根据我们的结果进行推断，我们预计在 100 万条标签上训练的算法可以匹配在 10 亿条标签上训练的离线 RLHF。这代表了 1000 倍的提升。据我们所知，这是首个证明如此巨大的改进是可能的结果。

## Abstract
We develop an online learning algorithm that dramatically improves the data efficiency of reinforcement learning from human feedback (RLHF). Our algorithm incrementally updates reward and language models as choice data is received. The reward model is fit to the choice data, while the language model is updated by a variation of reinforce, with reinforcement signals provided by the reward model. Several features enable the efficiency gains: a small affirmative nudge added to each reinforcement signal, an epistemic neural network that models reward uncertainty, and information-directed exploration. With Gemma large language models (LLMs), our algorithm matches the performance of offline RLHF trained on 200K labels using fewer than 20K labels, representing more than a 10x gain in data efficiency. Extrapolating from our results, we expect our algorithm trained on 1M labels to match offline RLHF trained on 1B labels. This represents a 1,000x gain. To our knowledge, these are the first results to demonstrate that such large improvements are possible.

---

## 论文详细总结（自动生成）

这篇由 Google DeepMind 团队发表的论文《Efficient Exploration at Scale》探讨了如何通过在线学习和主动探索机制，极大地提升大语言模型（LLM）在人类反馈强化学习（RLHF）阶段的数据效率。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的 RLHF 方法（尤其是离线方法）数据效率低下，需要海量的人类偏好标注才能获得微小的性能提升。
*   **研究动机**：随着模型规模扩大，获取高质量人类反馈成为瓶颈。研究者旨在探索是否存在一种算法，能够通过“聪明地选择数据”和“实时更新模型”，在极少量标注的情况下达到甚至超越传统方法在大规模数据上的表现。

### 2. 方法论
该论文提出了一套在线 RLHF 框架，其核心包含三个关键技术创新：
*   **在线增量更新（Online Incremental Updates）**：不同于传统的先收集数据再训练（离线）或定期重新训练（周期性），该算法在接收到每一批次（Batch）选择数据后，立即增量更新奖励模型（RM）和语言模型（LM）。
*   **肯定性微调信号（Affirmative Nudge）**：在强化学习信号中加入一个微小的正标量 $\epsilon$。这解决了在线 RLHF 中常见的“性能崩塌（Tanking）”问题，使模型在没有大幅降低学习率的情况下保持稳定增长。
*   **信息导向探索（Information-Directed Exploration, IDS）**：
    *   **认知神经网络（ENN）**：使用一种特殊的奖励模型架构，通过集成（Ensemble）多个 MLP 头部和随机先验函数来建模奖励的不确定性。
    *   **IDS 采样策略**：在生成响应对时，算法会选择那些让奖励模型“最困惑”（即选择概率方差最大）的响应对提交给人类（或模拟器）标注，从而确保每一条标签都能提供最大的信息增益。

### 3. 实验设计
*   **模型对象**：使用 **Gemma 9B** 作为基础策略模型。
*   **反馈模拟器**：为了模拟真实人类反馈并保证实验可重复，使用了基于 **Gemini 1.5 Pro** 训练的奖励模型作为模拟器。
*   **数据集**：使用了 20.2 万条内部提示词（Prompts），涵盖写作、代码、摘要、数学、科学等多种任务。
*   **Benchmark 与对比方法**：
    *   **Offline RLHF**：传统的离线训练基准。
    *   **Periodic RLHF**：每隔一定步数重新训练的周期性方法。
    *   **Online RLHF**：不带主动探索的在线增量方法。
    *   **Information-Directed Exploration**：本文提出的完整方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：明确提到了策略模型为 9B 参数，奖励模型后端同样基于 9B 规模，模拟器则使用了更大规模的 Gemini Pro。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从 1 万到 20 万条标签的完整训练曲线，并对 100 万至 10 亿条标签的性能进行了对数尺度的外推预测。
*   **消融实验**：针对“奖励模型的作用”、“肯定性微调信号（Nudge）的影响”以及“学习率调节”做了专门的对比实验。
*   **充分性评价**：实验设计较为充分，通过对数坐标轴清晰地展示了不同算法在扩展性（Scaling Laws）上的本质区别。使用 Gemini 1.5 Pro 作为模拟器虽然不是真实人类，但其复杂程度足以模拟人类行为的非线性特征。

### 6. 主要结论与发现
*   **效率飞跃**：该算法仅需 **2 万条**标签即可匹配离线 RLHF 在 **20 万条**标签上的表现（10 倍增益）。
*   **扩展潜力**：根据外推曲线，该算法在 **100 万条**标签下的表现预计可匹配离线方法在 **10 亿条**标签下的表现，实现 **1000 倍**的数据效率提升。
*   **解决崩塌**：通过“肯定性微调信号”，成功克服了在线强化学习中模型性能容易突然下降的顽疾。

### 7. 优点
*   **突破性的效率**：首次在 LLM 规模上证明了通过主动探索可以实现量级上的数据节省。
*   **算法优雅**：引入的 ENN 和 IDS 机制在增加极少参数（不到 5%）的前提下，显著提升了模型对不确定性的感知。
*   **实用性强**：在线增量更新机制比周期性重新训练更节省计算资源且更符合实际部署逻辑。

### 8. 不足与局限
*   **模拟器偏差**：实验核心依赖于 Gemini 1.5 Pro 模拟的人类反馈，虽然模拟器很强大，但可能无法完全捕捉真实人类偏好的多样性和细微矛盾。
*   **计算开销**：虽然数据效率高，但在线生成 16 个响应并进行 ENN 方差计算会增加推理阶段的计算负担。
*   **外推风险**：1000 倍增益的结论基于数学外推，尚未在真实的 10 亿级别标签规模下进行实证验证。

（完）
