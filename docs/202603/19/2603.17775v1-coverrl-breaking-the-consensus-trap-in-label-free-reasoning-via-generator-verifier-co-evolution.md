---
title: "CoVerRL: Breaking the Consensus Trap in Label-Free Reasoning via Generator-Verifier Co-Evolution"
title_zh: CoVerRL：通过生成器-验证器协同演化打破无标签推理中的共识陷阱
authors: "Teng Pan, Yuchen Yan, Zixuan Wang, Ruiqing Zhang, Gaiyang Han, Wanqi Zhang, Weiming Lu, Jun Xiao, Yongliang Shen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17775v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 无标签推理强化学习中的生成器-验证器协同演化
tldr: 针对无标签强化学习中因过度追求自一致性而导致的“共识陷阱”（即模型自信地强化系统性错误），本文提出CoVerRL框架。该框架通过让单一模型交替担任生成器和验证器角色，实现双向能力互促：利用多数投票提供初步监督训练验证器，再由不断进化的验证器过滤伪标签中的错误，从而打破性能瓶颈，显著提升数学推理能力。
selection_source: fresh_fetch
motivation: 传统的无标签强化学习依赖多数投票生成伪标签，容易导致模型陷入自信地强化系统性错误且多样性崩溃的“共识陷阱”。
method: 提出CoVerRL框架，通过生成器与验证器的协同演化，利用验证器动态识别并过滤掉伪标签中具有误导性的自一致错误。
result: "在Qwen和Llama模型上的实验显示，CoVerRL在数学推理基准上比基线提升了4.7-5.9%，自验证准确率从55%提升至85%以上。"
conclusion: 生成器与验证器的角色交替与协同进化能有效克服无标签学习中的共识陷阱，是提升大模型推理能力的有效途径。
---

## 摘要
无标签强化学习使大语言模型能够在没有地面真值监督的情况下提高推理能力，通常将多数投票的答案视为伪标签。然而，我们发现了一个关键的失败模式：随着训练最大化自一致性，输出多样性会发生崩溃，导致模型自信地强化了那些难以检测的系统性错误。我们将此称为“共识陷阱”。为了摆脱这一困境，我们提出了 CoVerRL，这是一个让单个模型在生成器和验证器角色之间交替切换的框架，每种能力都能引导另一种能力的提升。多数投票为训练验证器提供了带有噪声但具有信息量的监督，而不断改进的验证器则逐步从伪标签中过滤掉自一致的错误。这种协同演化创造了一个良性循环，在整个训练过程中保持了较高的奖励准确率。在 Qwen 和 Llama 模型系列上的实验表明，CoVerRL 在数学推理基准测试中比无标签基准模型高出 4.7-5.9%。此外，自验证准确率从约 55% 提高到 85% 以上，证实了这两种能力确实在协同演化。

## Abstract
Label-free reinforcement learning enables large language models to improve reasoning capabilities without ground-truth supervision, typically by treating majority-voted answers as pseudo-labels. However, we identify a critical failure mode: as training maximizes self-consistency, output diversity collapses, causing the model to confidently reinforce systematic errors that evade detection. We term this the consensus trap. To escape it, we propose CoVerRL, a framework where a single model alternates between generator and verifier roles, with each capability bootstrapping the other. Majority voting provides noisy but informative supervision for training the verifier, while the improving verifier progressively filters self-consistent errors from pseudo-labels. This co-evolution creates a virtuous cycle that maintains high reward accuracy throughout training. Experiments across Qwen and Llama model families demonstrate that CoVerRL outperforms label-free baselines by 4.7-5.9\% on mathematical reasoning benchmarks. Moreover, self-verification accuracy improves from around 55\% to over 85\%, confirming that both capabilities genuinely co-evolve.

---

## 论文详细总结（自动生成）

以下是对论文《CoVerRL: Breaking the Consensus Trap in Label-Free Reasoning via Generator-Verifier Co-Evolution》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对**无标签推理强化学习（Label-Free RL）**中的一个关键瓶颈——**“共识陷阱”（Consensus Trap）**展开研究。
*   **背景**：当前的无标签训练通常采用“多数投票”（Majority Voting）作为伪标签。其假设是：如果多个推理路径得到相同答案，该答案大概率正确。
*   **核心问题**：随着训练的进行，模型为了最大化奖励会过度追求自一致性，导致输出多样性崩溃。一旦模型在某个错误答案上达成共识（系统性错误），强化学习会不断加深这一错误，形成恶性循环。
*   **研究动机**：多数投票只能捕捉“一致性”而无法检测“正确性”。论文旨在通过引入自验证机制，打破这种对共识的盲目依赖。

### 2. 方法论：核心思想与关键技术
论文提出了 **CoVerRL** 框架，核心思想是让单一模型在**生成器（Generator）**和**验证器（Verifier）**两个角色间交替演化，实现双向引导（Bootstrapping）。

*   **伪标签生成与过滤**：
    1.  **多数投票**：生成器产生多个候选方案，选出频率最高的答案作为初步伪标签。
    2.  **自一致性验证过滤**：模型切换为验证器角色，对高频答案的推理过程进行审查。只有当验证器也认可该过程时，该样本才会被用于训练。这有效过滤了“自信的错误”。
*   **在线双角色协同演化**：
    1.  **对比验证器训练**：利用多数答案（正样本）和少数答案（负样本）构建对比对，训练验证器识别错误逻辑。
    2.  **自纠错（Self-Correction）**：当验证器发现错误时，触发生成器进行二次修正，训练模型从错误中恢复的能力。
*   **关键技术：Answer-Anchored GRPO**：
    *   改进了传统的组相对策略优化（GRPO）。它将针对同一目标答案的所有验证路径锚定在一起进行优势计算（Advantage Calculation），以隔离验证方差并稳定训练。

### 3. 实验设计
*   **基座模型**：Qwen3-1.7B-Base（开启思维链模式）、Llama-3.2-3B-Instruct、Qwen2.5-7B。
*   **训练数据**：使用标准的 MATH 训练集（不使用其地面真值标签）。
*   **评测基准（Benchmarks）**：
    *   **域内数学**：MATH500、AMC、AIME24。
    *   **域外泛化**：GPQA-Diamond（研究生水平科学问题）。
    *   **验证能力**：RewardBench (mathprm)。
*   **对比方法**：主要对比 **TTRL**（Test-Time RL，基于纯多数投票的无标签 RL 基线）。

### 4. 资源与算力
*   **实现框架**：基于 VeRL 分布式强化学习框架。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）和数量，也未提及总训练时长。
*   **超参数**：训练批大小为 64，学习率为 5e-7，每个查询生成 32-64 个候选响应。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.7B 到 7B 不同规模和家族的模型，验证了方法的普适性。
*   **消融实验**：非常充分。分别验证了验证过滤、验证器更新、自纠错机制以及 AA-GRPO 算法的贡献。
*   **对比分析**：对比了“简单阈值过滤”与“验证器过滤”的区别，证明了基于推理的验证优于简单的频率统计。
*   **客观性**：通过 RewardBench 等第三方基准测试验证了验证器的迁移能力，证明模型并非仅仅记住了训练集。

### 6. 主要结论与发现
*   **性能提升**：CoVerRL 在数学推理任务上平均超越 TTRL 基线 **4.7% 至 5.9%**。
*   **验证能力飞跃**：自验证准确率显著提升（例如 Qwen2.5-7B 从 54% 提升至 86.5%），且该能力可迁移至其他模型生成的答案。
*   **解决崩溃**：实验证明 CoVerRL 能在训练全过程保持 90% 以上的奖励准确率，而 TTRL 的奖励准确率会随多样性崩溃而大幅下降。
*   **思维模式的重要性**：验证器必须开启“思维模式”（Thinking Mode）才能有效工作，否则会发生回复长度崩溃，失去判别能力。

### 7. 优点
*   **理论深刻**：准确识别并定义了无标签学习中的“共识陷阱”问题。
*   **角色统一**：通过单一模型实现双角色演化，无需额外的验证器模型，节省了参数空间。
*   **闭环自进化**：构建了“生成-验证-过滤-纠错”的完整闭环，使模型能够从自身产生的噪声数据中提取高质量信号。
*   **鲁棒性强**：通过平衡采样和改进的 GRPO 算法，解决了多轮 RL 容易出现的模型崩溃问题。

### 8. 不足与局限
*   **领域局限**：实验主要集中在数学和科学推理等具有明确客观答案的领域，在主观性较强的任务（如创意写作）中是否适用尚待验证。
*   **计算开销**：相比单轮 RL，CoVerRL 需要进行多次推理（生成、验证、纠错），训练时的计算成本更高。
*   **初始能力依赖**：模型仍需具备一定的初始推理能力以通过多数投票产生正确的“种子”信号，若模型初始水平极低，协同演化可能难以启动。

（完）
