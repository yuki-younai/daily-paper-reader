---
title: "DyJR: Preserving Diversity in Reinforcement Learning with Verifiable Rewards via Dynamic Jensen-Shannon Replay"
title_zh: DyJR：通过动态 Jensen-Shannon 回放保持具有可验证奖励的强化学习中的多样性
authors: "Long Li, Zhijian Zhou, Tianyi Wang, Weidi Xu, Zuming Huang, Wei Chu, Zhe Wang, Shirui Pan, Chao Qu, Yuan Qi"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16157v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大模型强化学习后训练中的经验回放以保持多样性
tldr: 针对强化学习（如GRPO）在提升大模型推理能力时存在的样本效率低和多样性缺失问题，本文提出DyJR框架。该框架通过动态Jensen-Shannon回放机制，利用时间敏感动态缓冲区保留近期样本，并结合JS散度正则化约束分布，在提高训练效率的同时防止模式崩溃。实验证明DyJR在数学推理和Text-to-SQL任务上显著优于现有基准，有效维持了生成多样性。
selection_source: fresh_fetch
motivation: 现有的经验回放方法在提升强化学习效率时，往往因过度拟合准确样本而导致模型出现模式崩溃和多样性丧失。
method: 提出DyJR框架，利用时间敏感动态缓冲区同步模型演化，并通过Jensen-Shannon散度正则化代替直接梯度更新以维持分布多样性。
result: 在数学推理和Text-to-SQL基准测试中，DyJR在性能上显著优于GRPO及RLEP等基准，且训练效率与原始GRPO相当。
conclusion: DyJR通过动态参考分布约束，成功解决了强化学习中准确性与多样性的平衡问题，有效缓解了模型对高概率Token的过度依赖。
---

## 摘要
虽然强化学习（RL）增强了大语言模型的推理能力，但像 GRPO 这样的同策略（on-policy）算法由于丢弃了过去的采样序列（rollouts），导致样本效率低下。现有的经验回放方法通过重用准确的样本进行直接策略更新来解决这一问题，但这通常会带来高昂的计算成本，并因过拟合导致模式崩溃（mode collapse）。我们认为，历史数据应优先用于维持多样性，而非仅仅是强化准确性。为此，我们提出了动态 Jensen-Shannon 回放（DyJR），这是一个简单而有效的正则化框架，利用来自近期轨迹的动态参考分布。DyJR 引入了两项创新：（1）时间敏感动态缓冲区，采用先进先出（FIFO）和自适应大小调整，仅保留时间上邻近的样本，从而与模型演化保持同步；（2）Jensen-Shannon 散度正则化，用分布约束取代直接的梯度更新，以防止多样性崩溃。在数学推理和 Text-to-SQL 基准测试上的实验表明，DyJR 显著优于 GRPO 以及 RLEP 和 Ex-GRPO 等基准方法，同时保持了与原始 GRPO 相当的训练效率。此外，从 Rank-$k$ Token 概率演化的角度来看，我们展示了 DyJR 增强了多样性并减轻了对 Rank-1 Token 的过度依赖，阐明了 DyJR 的特定子模块如何影响训练动态。

## Abstract
While Reinforcement Learning (RL) enhances Large Language Model reasoning, on-policy algorithms like GRPO are sample-inefficient as they discard past rollouts. Existing experience replay methods address this by reusing accurate samples for direct policy updates, but this often incurs high computational costs and causes mode collapse via overfitting. We argue that historical data should prioritize sustaining diversity rather than simply reinforcing accuracy. To this end, we propose Dynamic Jensen-Shannon Replay (DyJR), a simple yet effective regularization framework using a dynamic reference distribution from recent trajectories. DyJR introduces two innovations: (1) A Time-Sensitive Dynamic Buffer that uses FIFO and adaptive sizing to retain only temporally proximal samples, synchronizing with model evolution; and (2) Jensen-Shannon Divergence Regularization, which replaces direct gradient updates with a distributional constraint to prevent diversity collapse. Experiments on mathematical reasoning and Text-to-SQL benchmarks demonstrate that DyJR significantly outperforms GRPO as well as baselines such as RLEP and Ex-GRPO, while maintaining training efficiency comparable to the original GRPO. Furthermore, from the perspective of Rank-$k$ token probability evolution, we show that DyJR enhances diversity and mitigates over-reliance on Rank-1 tokens, elucidating how specific sub-modules of DyJR influence the training dynamics.

---

## 论文详细总结（自动生成）

### 论文总结：DyJR —— 通过动态 Jensen-Shannon 回放保持强化学习中的多样性

#### 1. 核心问题与研究动机
在具有可验证奖励的强化学习（RLVR）中，如数学推理和代码生成，主流的同策略（on-policy）算法（如 GRPO）存在**样本效率低下**的问题，因为它们在单次更新后会丢弃昂贵的采样序列。虽然现有的经验回放（Experience Replay）方法尝试通过重用历史正确样本来解决此问题，但往往面临两个挑战：
*   **模式崩溃（Mode Collapse）：** 简单地通过最大似然估计强化历史正确路径，会导致模型过度拟合特定解法，丧失探索多样性。
*   **资源消耗大：** 存储完整的历史轨迹需要巨大的显存开销。
**研究动机：** 作者认为历史数据的主要价值应在于**维持生成多样性**，而非仅仅是重复准确性。

#### 2. 方法论
DyJR 提出了一个正则化框架，包含两个核心创新：
*   **时间敏感动态缓冲区（Time-Sensitive Dynamic Buffer）：**
    *   **FIFO 机制：** 仅保留最近 $M$ 个步骤内的“完美样本”（奖励为 1），确保参考分布与当前模型演化同步。
    *   **自适应数据选择：** 采用“由高到低”的置信度分层策略，优先选择高置信度样本，但在困难任务中自动放宽标准以防止数据饥饿。
    *   **早期多样性保护：** 在训练初期（前 20 步）临时扩大缓冲区容量，捕捉高熵的探索模式。
*   **Jensen-Shannon (JS) 散度正则化：**
    *   不同于传统方法直接进行梯度更新，DyJR 将历史策略的混合分布作为“锚点”。
    *   使用 JS 散度作为正则化项（$L_{JS}$），约束当前策略不要偏离多样化的成功路径。相比 KL 散度，JS 散度是对称且有界的，在处理非平稳的历史数据分布时更稳定，能有效防止“模式覆盖”导致的过度平滑。

#### 3. 实验设计
*   **任务与数据集：**
    *   **长链数学推理：** 使用 Qwen3-4B-Base 模型，在 Reinforce-Ada-Hard 数据集上训练，并在 AIME25、AMC23、HMMT25、Minerva 等 6 个竞赛级基准上评估。
    *   **SQL 生成：** 使用 Llama-3.1-8B-Instruct 模型，在 BIRD 数据集上训练，并在 BIRD 和 Spider 上评估跨域泛化能力。
*   **对比基准（Baselines）：**
    *   基础算法：GRPO、DAPO。
    *   经验回放类：RLEP、Ex-GRPO。
    *   散度约束类：DPH-RL（使用静态 JS 约束）。

#### 4. 资源与算力
*   **硬件环境：** 使用了 **32 张 NVIDIA A800 GPU**。
*   **算力开销：** 论文强调了 DyJR 的高效性。由于缓冲区仅存储索引和 Token 概率，显存额外开销**小于 1GB**。
*   **训练效率：** 实验表明，DyJR 的训练吞吐量（Rollout 和 Update 阶段的耗时）与原始 GRPO 几乎持平，远优于需要大规模存储的 RLEP。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了数学和 SQL 两个异构领域，使用了不同架构（Qwen, Llama）和参数规模的模型。
*   **消融实验：** 针对散度类型（JS vs. Forward KL）、正则化系数 $\alpha_{JS}$、缓冲区最大寿命 $M$、以及初始采样率进行了详细的消融研究。
*   **深度分析：** 引入了 **Rank-$k$ Token 概率演化分析**，从微观层面展示了模型如何避免对 Rank-1 Token 的过度依赖。
*   **评价：** 实验设计全面，对比了多种最先进的变体，且通过 Pass@k 评估了长尾采样能力，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升：** 在数学任务上，DyJR 的平均准确率达到 34.1%，比 GRPO 基准提升了 4.3%；在 SQL 任务上，Pass@16 提升显著（Spider 提升 7.0%）。
*   **多样性是关键：** 实验证明，早期高熵的探索数据对最终性能至关重要。如果降低初期的采样率，性能会大幅下降。
*   **JS 散度的优越性：** 在处理快速演化的策略分布时，JS 散度比 Forward KL 更能平衡“模式覆盖”与训练稳定性。
*   **缓解模式崩溃：** DyJR 成功让模型在保持高准确率的同时，提升了 Rank-2 和 Rank-3 Token 的概率，维持了健康的探索空间。

#### 7. 优点
*   **理念新颖：** 重新定义了经验回放在推理任务中的角色——从“强化准确”转向“保护多样”。
*   **极高效率：** 动态缓冲区设计极大地降低了显存占用，且不增加显著的计算延迟。
*   **鲁棒性强：** 在不同难度、不同领域的任务中均表现出优于静态约束方法的适应性。
*   **分析深入：** 通过 Token 排名概率的动态可视化，清晰地揭示了算法防止模式崩溃的内在机制。

#### 8. 不足与局限
*   **任务局限性：** 目前仅在具有“可验证奖励”（Verifiable Rewards）的任务上验证，对于主观性较强的对齐任务（如对话、写作）是否适用尚待研究。
*   **超参数敏感性：** 正则化系数 $\alpha_{JS}$ 和缓冲区寿命 $M$ 对性能有一定影响，可能需要针对不同模型进行微调。
*   **模型规模：** 实验主要集中在 4B 和 8B 模型，尚未在超大规模参数（如 70B+）模型上验证其扩展性。

（完）
