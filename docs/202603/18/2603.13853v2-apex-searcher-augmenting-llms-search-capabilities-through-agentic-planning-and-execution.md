---
title: "APEX-Searcher: Augmenting LLMs' Search Capabilities through Agentic Planning and Execution"
title_zh: APEX-Searcher：通过智能体规划与执行增强大语言模型的搜索能力
authors: "Kun Chen, Qingchao Kong, Zhao Feifei, Wenji Mao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13853v2"
tags: ["query:rlaif"]
score: 9.0
evidence: 通过强化学习进行多轮迭代检索和推理
tldr: APEX-Searcher是一个旨在增强大语言模型在复杂多跳任务中搜索能力的智能体规划与执行框架。针对现有RAG方法在处理复杂问题时检索路径模糊和强化学习奖励稀疏的问题，该框架将检索过程解耦为规划和执行两个阶段。通过分解特定奖励的强化学习优化战略规划，并利用高质量轨迹的监督微调提升执行能力，显著提高了多跳检索增强生成的性能。
selection_source: fresh_fetch
motivation: 现有的多轮检索方法在处理复杂多跳问题时，面临检索路径不明确以及端到端强化学习中奖励稀疏导致的性能下降挑战。
method: 提出一种两阶段框架，将检索解耦为规划（利用分解特定奖励的强化学习优化）和执行（基于高质量轨迹的监督微调）两个过程。
result: 实验结果表明，该框架在多个基准测试中显著提升了多跳检索增强生成（RAG）和任务规划的性能。
conclusion: APEX-Searcher通过解耦规划与执行并结合针对性优化，有效增强了LLM在复杂知识检索任务中的推理和搜索能力。
---

## 摘要
基于大语言模型（LLMs）的检索增强生成（RAG）是在各种领域应用中检索和利用外部知识的重要方法。当面对复杂的多跳问题时，单轮检索往往不足以进行准确的推理和问题解决。为了增强复杂任务的搜索能力，现有的大多数工作通过端到端训练将多轮迭代检索与推理过程相结合。虽然这些方法显著提高了问题解决性能，但它们在任务推理和模型训练方面仍面临挑战，特别是端到端强化学习（RL）过程中模糊的检索执行路径和稀疏奖励，导致检索结果不准确和性能下降。为了解决这些问题，本文提出了 APEX-Searcher，一种新型的智能体规划与执行框架，旨在增强 LLM 的搜索能力。具体而言，我们引入了一个两阶段智能体框架，将检索过程解耦为规划和执行：首先，它采用具有分解特定奖励的强化学习来优化战略规划；在子任务分解的基础上，它随后对高质量的多跳轨迹进行监督微调，使模型具备鲁棒的迭代子任务执行能力。广泛的实验表明，我们提出的框架在多个基准测试中显著提升了多跳 RAG 和任务规划的性能。

## Abstract
Retrieval-augmented generation (RAG), based on large language models (LLMs), serves as a vital approach to retrieving and leveraging external knowledge in various domain applications. When confronted with complex multi-hop questions, single-round retrieval is often insufficient for accurate reasoning and problem solving. To enhance search capabilities for complex tasks, most existing works integrate multi-round iterative retrieval with reasoning processes via end-to-end training. While these approaches significantly improve problem-solving performance, they are still faced with challenges in task reasoning and model training, especially ambiguous retrieval execution paths and sparse rewards in end-to-end reinforcement learning (RL) process, leading to inaccurate retrieval results and performance degradation. To address these issues, in this paper, we proposes APEX-Searcher, a novel Agentic Planning and Execution framework to augment LLM search capabilities. Specifically, we introduce a two-stage agentic framework that decouples the retrieval process into planning and execution: It first employs RL with decomposition-specific rewards to optimize strategic planning; Built on the sub-task decomposition, it then applies supervised fine-tuning on high-quality multi-hop trajectories to equip the model with robust iterative sub-task execution capabilities. Extensive experiments demonstrate that our proposed framework achieves significant improvements in both multi-hop RAG and task planning performances across multiple benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《APEX-Searcher: Augmenting LLMs' Search Capabilities through Agentic Planning and Execution》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统检索增强生成（RAG）在处理复杂**多跳问题（Multi-hop Questions）**时表现不佳。虽然现有的迭代式或智能体（Agentic）RAG尝试通过多轮检索解决，但面临两大挑战：
    1.  **执行路径模糊**：缺乏全局规划，容易陷入死循环（如重复查询相同关键词）。
    2.  **训练效率低下**：端到端强化学习（RL）存在严重的奖励稀疏问题，且错误累积导致优化目标不明确。
*   **研究动机**：通过将复杂的检索推理过程**解耦为“规划”与“执行”**两个独立阶段，并针对性地采用强化学习与监督微调，从而增强大语言模型（LLM）的搜索与推理能力。

### 2. 方法论：核心思想与技术细节
APEX-Searcher 框架分为两个阶段：
*   **阶段 I：基于 RL 的智能体规划（Agentic Planning）**
    *   **核心思想**：将复杂问题分解为 2-4 个具有逻辑依赖关系的子任务。
    *   **关键技术**：采用 **GRPO（组相对策略优化）** 算法。
    *   **奖励函数设计**：不直接使用最终答案作为奖励（避免稀疏），而是计算生成规划与标准规划（Gold Plan）的语义相似度。利用 Sentence-Transformer 编码子问题，通过**匈牙利算法（Hungarian Algorithm）**进行二分图匹配，计算 F1 分数作为奖励信号。
*   **阶段 II：基于 SFT 的智能体执行（Agentic Execution）**
    *   **核心思想**：通过监督微调（SFT）使模型掌握子任务的迭代执行逻辑。
    *   **技术细节**：构建了 14,604 条高质量多轮检索轨迹数据。执行过程包含：知识充足性评估、动态查询生成、文档检索与去重、以及是否继续检索的决策。
*   **推理流程**：
    1.  **规划**：生成带引用标识（如 #1, #2）的子问题序列。
    2.  **执行**：按序解决子问题，利用前序答案填充后续问题的占位符。
    3.  **综合**：汇总所有子答案，生成最终回复并给出置信度评分。

### 3. 实验设计
*   **数据集**：使用了四个具有挑战性的多跳 QA 基准：**2WikiMultiHopQA、HotpotQA、MuSiQue 和 Bamboogle**。
*   **评估指标**：主要采用 **Exact Match (EM)**，因为目标答案多为短事实实体。
*   **对比方法**：
    *   非检索方法：Direct Inference, CoT。
    *   标准 RAG：Standard RAG。
    *   迭代式 RAG：IRCoT。
    *   智能体 RAG：Search-o1, Search-R1, ZeroSearch, StepSearch, ReasonRAG, DecEx-RAG 等。

### 4. 资源与算力
*   **模型底座**：Qwen2.5-3B-Instruct 和 Qwen2.5-7B-Instruct。
*   **训练框架**：使用 `verl` 框架实现 GRPO，使用 `360Llamafactory` 进行全参数 SFT。
*   **技术配置**：启用了 DeepSpeed ZeRO-3、Flash Attention 2 和梯度检查点（Gradient Checkpointing）以优化内存。
*   **算力细节**：文中提到了微批次大小（Micro-batch size）为 16，学习率为 5e-6，但**未明确说明具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流数据集上进行了测试，涵盖了从 3B 到 7B 不同参数规模的模型。
*   **消融实验**：非常充分。分别验证了“仅规划”、“规划+RL”、“仅执行 SFT”以及“全组合”的效果，证明了规划模块对小模型（3B）的挑战以及 RL 优化的必要性。
*   **敏感性分析**：研究了检索文档数量（Num）和推理跳数（Hop）对性能的影响。
*   **客观性**：使用了统一的检索环境和语料库，确保了与基线方法对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：APEX-Searcher 在所有基准测试中均显著优于现有方法，相比标准 RAG，平均性能提升了约 23%。
*   **规划的重要性**：单纯增加规划模块若不配合 RL 训练，在小模型上反而可能导致性能下降（引入噪声）；而经过 RL 优化的规划能显著提升逻辑严密性。
*   **SFT 的补偿作用**：SFT 能显著增强模型利用工具和处理特定结构化模式的能力，对 3B 模型的提升尤为巨大（Eval 分数从 13.42 升至 33.45）。
*   **参数规律**：检索文档超过 3 个后收益递减甚至下降（噪声干扰）；而推理跳数在 1-5 跳范围内与准确率正相关。

### 7. 优点
*   **解耦设计**：将复杂的端到端任务拆解，降低了训练难度，使模型在规划时更具全局观。
*   **创新的奖励机制**：利用语义相似度匹配而非简单的字符串匹配作为 RL 奖励，更符合自然语言逻辑。
*   **鲁棒性**：通过引用机制（#n）处理子任务间的依赖，有效避免了多跳推理中的信息丢失。

### 8. 不足与局限
*   **依赖高质量规划数据**：RL 阶段需要人工标注或高质量模型生成的“标准规划”作为参考，这在某些新兴领域可能难以获取。
*   **推理延迟**：由于采用了“规划-多次执行-综合”的流程，其推理时间成本显著高于单轮 RAG。
*   **算力细节缺失**：未披露具体的训练资源消耗，难以评估其训练成本的经济性。
*   **应用范围**：目前主要在百科类（Wikipedia）多跳问答上验证，在非结构化程度更高或实时性要求极强的 Web 搜索场景下的表现尚待观察。

（完）
