---
title: "Dual Consensus: Escaping from Spurious Majority in Unsupervised RLVR via Two-Stage Vote Mechanism"
title_zh: 双重共识：通过两阶段投票机制在无监督 RLVR 中摆脱伪多数
authors: "Kaixuan Du, Meng Cao, Hang Zhang, Yukun Wang, Xiangzhou Huang, Ni Li"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16223v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 用于复杂推理任务的两阶段共识强化学习
tldr: 针对无监督强化学习（RLVR）中大模型易陷入“伪多数”错误答案的问题，本文提出双重共识强化学习（DCRL）。该方法通过两阶段投票机制，结合锚点模型的主导响应与探索者模型的多元信号（通过临时遗忘产生），利用调和平均值生成更可靠的学习信号。在8个基准测试中，DCRL显著提升了推理性能且训练更稳定，为无标签推理优化提供了新路径。
selection_source: fresh_fetch
motivation: 现有的无监督强化学习方法容易受到错误但普遍的答案（伪多数）干扰，导致模型陷入局部最优并限制性能提升。
method: 提出DCRL方法，通过“锚点”生成主导响应和“探索者”通过临时遗忘生成多样化信号，并以两者的调和平均值作为最终训练目标。
result: "在八个跨领域基准测试中，DCRL在Pass@1指标上一致优于多数投票法，并展现出更稳定的训练动态。"
conclusion: DCRL证明了在无需外部监督的情况下，通过内部两阶段共识机制可以有效克服伪多数问题，实现更强的推理能力。
---

## 摘要
当前针对大语言模型（LLMs）的无标签 RLVR 方法（如 TTRL 和 Self-reward）已证明在提升 LLMs 复杂推理任务性能方面具有有效性。然而，这些方法严重依赖准确的伪标签估计，并容易收敛于虚假但普遍的答案，从而陷入主导模式并限制了进一步的提升。基于此，我们提出了双重共识强化学习（DCRL），这是一种新颖的自监督训练方法，能够通过两阶段共识机制生成更可靠的学习信号。模型最初作为锚点（anchor），产生主导响应；随后作为探索者（explorer），通过临时的遗忘（unlearning）过程生成多样化的辅助信号。最终的训练目标源自这两组信号集的调和平均值。值得注意的是，该过程完全在没有外部模型或监督的情况下运行。在八个基准测试和不同领域中，DCRL 在 Pass@1 指标上始终优于多数投票（majority vote），同时产生了更稳定的训练动态。这些结果表明，DCRL 为在无标签情况下实现更强的推理能力建立了一条可扩展的路径。

## Abstract
Current label-free RLVR approaches for large language models (LLMs), such as TTRL and Self-reward, have demonstrated effectiveness in improving the performance of LLMs on complex reasoning tasks. However, these methods rely heavily on accurate pseudo-label estimation and converge on spurious yet popular answers, thereby trapping in a dominant mode and limiting further improvements. Building on this, we propose Dual Consensus Reinforcement Learning (DCRL), a novel self-supervised training method which is capable of generating more reliable learning signals through a two-stage consensus mechanism. The model initially acts as an anchor, producing dominant responses; then it serves as an explorer, generating diverse auxiliary signals via a temporary unlearning process. The final training target is derived from the harmonic mean of these two signal sets. Notably, the process operates entirely without external models or supervision. Across eight benchmarks and diverse domains, DCRL consistently improves Pass@1 over majority vote while yielding more stable training dynamics. These results demonstrate that DCRL establishes a scalable path toward stronger reasoning without labels.

---

## 论文详细总结（自动生成）

以下是对论文《Dual Consensus: Escaping from Spurious Majority in Unsupervised RLVR via Two-Stage Vote Mechanism》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：强化学习（RLVR）在提升大语言模型（LLM）推理能力方面表现卓越，但通常依赖人工标注或可验证的环境反馈。
*   **核心问题**：现有的无监督 RLVR 方法（如基于多数投票的方法）存在“伪多数（Spurious Majority）”偏差。即在训练后期，模型容易收敛到错误但普遍的答案模式，导致奖励信号失效、模型陷入局部最优，并出现熵坍缩（输出分布过于单一）。
*   **研究动机**：探索如何在完全无标签的情况下，利用模型自身的鲁棒性来识别更准确的伪标签，从而实现持续的自我进化。

### 2. 方法论
论文提出了 **双重共识强化学习（DCRL）**，其核心流程包括三个关键技术：
*   **先遗忘再探索（Unlearn Then Explore）**：
    *   将当前策略模型克隆为“锚点（Anchor）”模型。
    *   对锚点模型执行一次临时的“遗忘”梯度更新（通过特定的损失函数抑制高概率 Token），将其转化为“探索者（Explorer）”模型。这一步旨在拉平概率分布，强制模型探索非主导的推理路径。
*   **调和选举（Harmonic Election）**：
    *   分别从锚点和探索者模型中采样多条路径，计算各答案的出现概率 $p_0$ 和 $p_1$。
    *   使用**调和平均值** $S(a) = \frac{2p_0p_1}{p_0 + p_1}$ 作为评分标准。只有在两个模型中都具有鲁棒性的答案才会获得高分，从而有效过滤掉仅在主导模式中出现的错误答案。
*   **自适应采样（Adaptive Sampling）**：
    *   根据共识率（Consensus Rate）动态调整训练数据。当模型信心不足时，仅使用锚点样本；当模型趋于稳定共识时，引入探索者样本以增强泛化能力。
*   **奖励设计**：采用保守奖励策略，为伪标签分配 1 分，为锚点模型的多数答案分配 0.5 分（保留奖励），其余为 0 分，以稳定训练动态。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DAPO-Math-14k（数学推理）。
    *   **评估集**：涵盖 8 个基准测试，包括 MATH-500, GSM8K, AIME24, Minerva-math, AMC, OlympiadBench（数学类）以及 MMLU-Pro, GPQA-Diamond（通用/科学类）。
*   **实验场景**：大规模无监督预训练和测试时自适应（TTA）。
*   **对比基准（Baselines）**：
    *   **RENT**：基于确定性的方法。
    *   **TTRL**：基于标准多数投票的方法。
    *   **Co-Rewarding I/II**：基于多样本聚合的自监督方法。
    *   **GRPO (Supervised)**：作为性能上限的带标签监督学习。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 VeRL 框架，超参数包括 Batch Size 为 128，学习率为 $1 \times 10^{-6}$，每个样本采样 16 条路径进行投票。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 种不同规模的模型（Llama3.2-3B, Qwen3-4B/8B）上进行了验证。
    *   针对 8 个不同领域的基准测试进行了全面评估。
    *   进行了详细的消融实验，验证了调和选举、保守奖励和动态采样三个组件的必要性。
    *   进行了超参数敏感性分析（如遗忘学习率 $\eta_u$）。
*   **充分性评价**：实验设计较为充分且客观。通过 Pass@1（16 次独立采样平均）指标进行评估，并对比了多种无监督基线，证明了 DCRL 在不同任务和模型规模下的普适性。

### 6. 主要结论与发现
*   **性能领先**：DCRL 在所有 8 个基准测试中均优于现有的无监督方法，在 Qwen3-8B 上甚至接近了有监督 GRPO 的表现。
*   **纠偏能力**：实验证明 DCRL 能够识别出被多数投票忽略的正确答案（低共识但正确的答案），有效缓解了伪多数偏差。
*   **训练稳定性**：相比于直接使用多数投票，DCRL 提供了更准确的奖励信号，训练曲线更平滑，且能有效防止熵坍缩。

### 7. 优点
*   **机制创新**：巧妙地利用“临时遗忘”来模拟分布扰动，通过调和平均值捕捉推理路径的鲁棒性。
*   **完全自主**：无需外部奖励模型（RM）或任何人工标注，实现了真正的闭环自监督学习。
*   **通用性强**：不仅在数学任务上有效，在 MMLU-Pro 和 GPQA 等通用知识任务上也表现出良好的泛化性。

### 8. 不足与局限
*   **系统性先验偏差**：如果模型对某个错误答案存在极强的系统性偏见（锚点和探索者都一致认为错的是对的），该方法仍无法纠正。
*   **计算开销**：由于需要两阶段采样（锚点+探索者）以及额外的临时梯度更新，其训练阶段的计算成本高于传统的多数投票方法。
*   **极难任务限制**：对于模型预训练分布之外、完全无法生成正确路径的极难题目，该方法无法凭空创造出正确答案。

（完）
