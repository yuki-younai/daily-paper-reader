---
title: Internalizing Agency from Reflective Experience
title_zh: 从反思性经验中内化代理能力
authors: "Rui Ge, Yichao Fu, Yuyang Qian, Junda Su, Yiming Zhao, Peng Zhao, Hao Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16843v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 在大模型后训练中从反思经验中内化智能体能力
tldr: "针对大语言模型在长程交互任务中难以有效利用环境反馈的问题，本文提出 LEAFE 框架。传统强化学习往往只关注最终结果，导致模型仅能重复有限的成功路径。LEAFE 通过在探索阶段总结反馈、回溯并尝试修正行为，将这些反思后的纠错经验通过监督微调内化至模型中。实验证明，该方法在编程和智能体任务中显著提升了模型的纠错能力和 Pass@k 性能，有效增强了模型的自主代理能力。"
selection_source: fresh_fetch
motivation: 现有的基于结果的强化学习方法忽视了丰富的环境反馈，导致模型在长程任务中缺乏纠错和扩展解空间的能力。
method: 提出 LEAFE 框架，通过总结反馈、回溯决策并探索修正路径，将反思后的经验通过监督微调内化到模型策略中。
result: "在多项交互式编程和智能体任务中，LEAFE 显著优于 GRPO 等基线，在 Pass@128 指标上最高提升了 14%。"
conclusion: 通过内化反思经验，模型能够更有效地从错误中恢复，从而在有限的交互预算下显著提升复杂问题的解决能力。
---

## 摘要
大语言模型越来越多地被部署为自主代理，必须通过与提供丰富反馈的环境进行长程交互，来执行规划、行动并从错误中恢复。然而，主流的结果驱动型后训练方法（例如带有可验证奖励的强化学习）主要优化最终的成功信号，导致丰富的环境反馈未被充分利用。因此，这些方法通常会导致分布收缩：策略变得更擅长复现一小部分已经成功的行为，却未能提升在长程场景中扩展问题解决能力（如 Pass@k）所需的基于反馈的代理能力。为了解决这一问题，我们提出了 LEAFE（从反思性经验中学习基于反馈的代理能力），这是一个从反思性经验中内化恢复代理能力的框架。具体而言，在探索过程中，代理将环境反馈总结为可操作的经验，回溯到早期的决策点，并使用修正后的动作探索替代分支。随后，我们通过监督微调将这些经验引导的修正蒸馏到模型中，使策略能够在未来的交互中更有效地恢复。在固定交互预算下的一系列多样化交互式编程和代理任务中，LEAFE 在 Pass@1 上始终优于基础模型，并且比结果驱动的基准方法（如 GRPO）以及基于经验的方法（如 Early Experience）实现了更高的 Pass@k，在 Pass@128 上提升高达 14%。

## Abstract
Large language models are increasingly deployed as autonomous agents that must plan, act, and recover from mistakes through long-horizon interaction with environments that provide rich feedback. However, prevailing outcome-driven post-training methods (e.g., RL with verifiable rewards) primarily optimize final success signals, leaving rich environment feedback underutilized. Consequently, they often lead to distribution sharpening: the policy becomes better at reproducing a narrow set of already-successful behaviors, while failing to improve the feedback-grounded agency needed to expand problem-solving capacity (e.g., Pass@k) in long-horizon settings.   To address this, we propose LEAFE (Learning Feedback-Grounded Agency from Reflective Experience), a framework that internalizes recovery agency from reflective experience. Specifically, during exploration, the agent summarizes environment feedback into actionable experience, backtracks to earlier decision points, and explores alternative branches with revised actions. We then distill these experience-guided corrections into the model through supervised fine-tuning, enabling the policy to recover more effectively in future interactions. Across a diverse set of interactive coding and agentic tasks under fixed interaction budgets, LEAFE consistently improves Pass@1 over the base model and achieves higher Pass@k than outcome-driven baselines (GRPO) and experience-based methods such as Early Experience, with gains of up to 14% on Pass@128.

---

## 论文详细总结（自动生成）

这是一份关于论文《Internalizing Agency from Reflective Experience》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是：**如何让大语言模型（LLM）智能体在长程交互任务中真正具备“代理能力”（Agency）**，即能够根据环境反馈识别错误并自主纠错。

*   **研究动机**：当前主流的后训练方法（如基于结果的强化学习 RLVR，代表算法为 GRPO）主要依赖最终的成功/失败信号（Scalar Reward）。这种方式会导致“分布收缩”（Distribution Sharpening），即模型只是提高了采样到已有成功路径的概率，而没有学会如何处理失败路径。
*   **核心矛盾**：在长程任务中，Pass@1 的提升往往掩盖了模型在探索能力上的停滞。当采样预算增加时（如 Pass@128），传统 RL 方法的性能提升会迅速遇到瓶颈。

### 2. 论文提出的方法论：LEAFE 框架
LEAFE（Learning Feedback-Grounded Agency from Reflective Experience）通过两个阶段将“反思纠错”这一过程内化到模型参数中：

*   **阶段 1：带回溯的树状经验生成（Tree-Based Experience Generation with Rollback）**
    *   **周期性反思**：代理在每隔 $K$ 步或任务失败时，调用自身进行反思，识别出导致失败的决策点 $\tau$ 并生成一段简洁的经验总结 $e$（诊断及修正建议）。
    *   **回溯与分支探索**：环境重置到 $\tau$ 时刻，模型在经验 $e$ 的引导下生成修正动作 $a'_\tau$，并继续完成后续轨迹。这形成了一个隐式的“回溯树”，从而挖掘出模型原生分布之外的成功路径。
*   **阶段 2：经验蒸馏（Experience Distillation）**
    *   **行为排演（Behavior Rehearsal）**：对所有成功的轨迹（包括回溯修正后的）进行标准的监督微调（SFT），以保持基础任务能力。
    *   **经验到策略蒸馏（Experience-to-Policy Distillation）**：这是核心创新。将“有经验引导下的修正动作 $a'_\tau$”作为目标，但在输入端**去掉**显式的经验总结 $e$，只保留原始历史 $h_\tau$。
    *   **损失函数**：$L(\theta') = L_{cf} + \beta L_{reh}$。其中 $L_{cf}$ 负责将纠错逻辑内化，使模型在推理时不依赖额外反思步骤也能做出正确修正。

### 3. 实验设计
*   **数据集/场景**：涵盖了五类具有挑战性的交互式任务：
    1.  **WebShop**：网页导航与购物。
    2.  **ALFWorld**：具身智能体家庭任务。
    3.  **ScienceWorld**：科学实验模拟。
    4.  **Sokoban**：逻辑规划推箱子游戏。
    5.  **CodeContests**：竞赛级编程题目（需根据执行反馈纠错）。
*   **对比基准（Baselines）**：
    *   **Base**：未微调的指令模型。
    *   **GRPO-RLVR**：目前最先进的结果驱动强化学习方法。
    *   **EarlyExp**：一种通过隐式世界建模学习早期经验的方法。
    *   **ACE**：一种基于 Prompt 和外部“剧本”存储经验的非训练方法。
*   **评估指标**：Pass@1（单次成功率）和 Pass@128（多次采样下的能力上限）。

### 4. 资源与算力
*   **软件框架**：使用了 `verl` 和 `verl-agent` 分布式强化学习框架。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号（如 H100 或 A100）及具体数量。
*   **训练配置**：提到 Batch Size 为 128，学习率为 $1 \times 10^{-6}$，训练轮数为 2-3 Epochs。

### 5. 实验数量与充分性
*   **实验规模**：实验非常充分。研究涵盖了从 7B、8B 到 32B、70B/72B 不同量级的模型（Qwen2.5 系列和 Llama-3 系列）。
*   **消融实验**：
    *   验证了阶段 1 的回溯采样比单纯的迭代优化更有效。
    *   验证了阶段 2 中 $L_{cf}$ 损失函数对提升 Pass@k 的关键作用。
    *   进行了 **OOD（分布外）泛化测试**：在 CodeContests 上训练，在 MBPP 上测试，证明了 LEAFE 学习的是通用代理能力而非过拟合。
*   **客观性**：通过固定交互预算（Sampling Budget）来对比不同方法，确保了对比的公平性。

### 6. 主要结论与发现
*   **突破能力天花板**：LEAFE 在 Pass@128 上的表现显著优于 GRPO。例如在 CodeContests 上，Pass@128 提升了 14%。
*   **内化胜过外挂**：相比于 ACE 这种依赖长上下文 Prompt 的方法，LEAFE 通过权重更新内化了能力，推理效率更高且不受上下文长度限制。
*   **纠正分布收缩**：实验证明 GRPO 往往只是让模型在已知的成功路径上更“自信”，而 LEAFE 扩展了模型解决新问题的边界。

### 7. 优点（亮点）
*   **数据效率高**：通过回溯机制，将失败的尝试转化为高质量的纠错对，极大地利用了环境反馈。
*   **训练目标新颖**：通过“有经验引导采样 -> 无经验监督微调”的逻辑，巧妙地解决了如何让模型在不增加推理开销的情况下学会反思的问题。
*   **通用性强**：在编程、具身、导航等多个完全不同的领域均表现出一致的增益。

### 8. 不足与局限
*   **环境依赖性**：LEAFE 依赖于环境必须是**可回溯的（Rollback-able）**。在某些不可逆的现实场景或非确定性环境中，该方法难以实施。
*   **反馈质量要求**：如果环境提供的反馈极其稀疏或具有误导性，模型生成的“经验总结 $e$”质量会下降，从而影响蒸馏效果。
*   **计算开销**：虽然推理时没有额外开销，但在训练阶段（阶段 1）需要进行大量的树状分支探索，采样成本高于普通的 RL。

（完）
