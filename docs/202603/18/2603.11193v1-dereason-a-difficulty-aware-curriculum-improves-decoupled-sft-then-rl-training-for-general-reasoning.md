---
title: "DeReason: A Difficulty-Aware Curriculum Improves Decoupled SFT-then-RL Training for General Reasoning"
title_zh: DeReason：一种难度感知的课程学习方法，旨在改进通用推理中的解耦式 SFT 后 RL 训练
authors: "Hanxu Hu, Yuxuan Wang, Maggie Huan, Jannis Vamvas, Yinya Huang, Zhijiang Guo, Rico Sennrich"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11193v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大模型解耦SFT与RL训练的难度感知课程学习
tldr: 本研究针对通用STEM领域中强化学习（RL）训练效率低的问题，提出了DeReason框架。该框架采用基于难度的课程学习策略，利用大模型评分将数据解耦：非推理密集型数据用于SFT以构建基础知识，推理密集型数据则用于RL以培养复杂推理能力。实验证明，这种有原则的数据分配方式在多个基准测试中显著优于单一SFT、RL或随机划分方法，为提升大模型通用推理能力提供了高效的后训练方案。
selection_source: fresh_fetch
motivation: 针对通用STEM领域中RL训练效率低且与SFT协同机制不明的问题，探索如何优化SFT与RL的数据分配。
method: 提出DeReason策略，利用LLM评分将数据按推理强度解耦，简单数据用于SFT建立基础，复杂数据用于RL强化推理。
result: 实验证明该解耦课程训练在通用STEM和数学基准上显著优于仅SFT、仅RL及随机划分数据的基线。
conclusion: 科学的数据解耦与课程化训练能有效结合SFT的知识覆盖与RL的推理能力，是提升通用推理能力的有效方案。
---

## 摘要
带有可验证奖励的强化学习（RLVR）已成为激发大语言模型推理能力的强大范式，特别是在数学和编程领域。尽管最近的研究已将这一范式扩展到更广泛的通用科学（STEM）领域，但在这些背景下，有监督微调（SFT）与强化学习（RL）之间复杂的相互作用仍有待探索。在本文中，我们通过受控实验揭示了一个关键挑战：对于通用 STEM 领域，直接在基座模型上应用 RL 的样本效率极低，且其表现始终逊于在具有中等质量响应的数据上进行的有监督微调（SFT）。然而，先进行 SFT 再进行 RL 的顺序训练可以进一步提升性能，这表明这两个阶段起到了互补作用，且训练数据在两者之间的分配方式至关重要。因此，我们提出了 DeReason，一种针对通用推理的基于难度的数据解耦策略。DeReason 通过基于 LLM 评分估算的推理强度，将训练数据划分为推理密集型和非推理密集型子集。它将覆盖面广、非推理密集型的问题分配给 SFT 以建立基础领域知识，并保留一个集中的困难问题子集给 RL 以培养复杂的推理能力。我们证明，这种有原则的解耦比随机拆分数据进行顺序 SFT 和 RL 具有更好的性能。在通用 STEM 和数学基准测试上的广泛实验表明，我们的解耦课程训练显著优于仅 SFT、仅 RL 以及随机拆分的基准方法。我们的工作系统地研究了通用推理中 SFT 与 RL 之间的相互作用，提供了一种高效且通用的后训练方案。

## Abstract
Reinforcement learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for eliciting reasoning capabilities in large language models, particularly in mathematics and coding. While recent efforts have extended this paradigm to broader general scientific (STEM) domains, the complex interplay between supervised fine-tuning (SFT) and RL in these contexts remains underexplored. In this paper, we conduct controlled experiments revealing a critical challenge: for general STEM domains, RL applied directly to base models is highly sample-inefficient and is consistently surpassed by supervised fine-tuning (SFT) on moderate-quality responses. Yet sequential SFT followed by RL can further improve performance, suggesting that the two stages play complementary roles, and that how training data is allocated between them matters. Therefore, we propose DeReason, a difficulty-based data decoupling strategy for general reasoning. DeReason partitions training data by reasoning intensity estimated via LLM-based scoring into reasoning-intensive and non-reasoning-intensive subsets. It allocates broad-coverage, non-reasoning-intensive problems to SFT to establish foundational domain knowledge, and reserves a focused subset of difficult problems for RL to cultivate complex reasoning. We demonstrate that this principled decoupling yields better performance than randomly splitting the data for sequential SFT and RL. Extensive experiments on general STEM and mathematical benchmarks demonstrate that our decoupled curriculum training significantly outperforms SFT-only, RL-only, and random-split baselines. Our work provides a systematic study of the interplay between SFT and RL for general reasoning, offering a highly effective and generalized post-training recipe.

---

## 论文详细总结（自动生成）

以下是对论文《DeReason: A Difficulty-Aware Curriculum Improves Decoupled SFT-then-RL Training for General Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在通用科学（STEM）领域，如何高效地结合有监督微调（SFT）与强化学习（RL）来提升大语言模型（LLM）的推理能力？
*   **研究背景**：虽然带有可验证奖励的强化学习（RLVR）在数学和代码领域表现卓越，但在通用 STEM 领域，直接在基座模型上进行 RL 存在**样本效率极低**的问题。
*   **研究动机**：作者发现，在数据量相同的情况下，使用中等质量数据进行的 SFT 往往优于纯 RL。然而，SFT 擅长知识获取，RL 擅长复杂推理。因此，如何科学地在两个阶段之间分配训练数据（即“数据解耦”），是提升模型通用推理能力的关键。

### 2. 方法论：DeReason 框架
DeReason 提出了一种基于难度的课程学习策略，将 SFT 和 RL 的角色进行明确分工：
*   **核心思想**：将“简单/广博”的知识类数据分配给 SFT 阶段，用于构建领域知识基础；将“困难/推理密集型”的数据保留给 RL 阶段，用于激发复杂的思维链（CoT）能力。
*   **关键技术流程**：
    1.  **难度估计（Difficulty Estimation）**：使用一个与策略模型同规模的指令微调模型（如 Qwen3-4B-Instruct）作为裁判，对训练题目进行 1-5 分的“推理强度”打分。
    2.  **数据划分（Data Partitioning）**：设定阈值 $\tau$，得分 $\le \tau$ 的数据进入 SFT 子集（DSFT），得分 $>\tau$ 的数据进入 RL 子集（DRL）。
    3.  **课程训练（Curriculum Training）**：
        *   **第一阶段**：在 DSFT 上进行 SFT，使模型获得基础知识。
        *   **第二阶段**：以 SFT 后的模型为起点，在 DRL 上使用 **GRPO（Group Relative Policy Optimization）** 算法进行强化学习。
*   **验证机制**：针对通用领域无法使用规则校验的问题，采用模型基验证器（Model-based Verifier）来判断答案的语义等价性。

### 3. 实验设计
*   **数据集**：使用了两个大规模 STEM 训练集：**WebInstruct-Verified** 和 **Webscale-RL**。
*   **Benchmark（评估基准）**：
    *   **通用推理**：MMLU-Pro, GPQA-Diamond, SuperGPQA, BBEH（Big-Bench Extra Hard）。
    *   **数学推理**：AIME24, AIME25, MATH500。
*   **对比方法**：
    *   **SFT-only**：仅在全量数据上进行 SFT。
    *   **RL-only**：直接在基座模型上进行全量 RL。
    *   **Random SFT-then-RL**：随机划分数据进行顺序训练。
    *   **SFT-then-selected-SFT**：两阶段均为 SFT。

### 4. 资源与算力
*   **模型规模**：主要基于 **Qwen3-4B-Base** 进行实验。
*   **训练框架**：SFT 使用 Llama-Factory，RL 使用 VeRL。
*   **超参数**：SFT 学习率 1e-5，Batch Size 128；RL 学习率 1e-6，最大响应长度 8192。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 A100/H100）及具体训练时长，仅在致谢中提到计算资源由瑞士 AI 倡议（Swiss AI Initiative）提供。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的主流 STEM 数据集上分别验证，并在 7 个不同的推理基准测试上进行评估。
*   **消融与分析**：
    *   对比了不同难度分布（Score 1-5）对训练奖励、响应长度和策略熵（Entropy）的影响。
    *   进行了 SFT 与 RL 样本效率的受控对比实验。
*   **充分性评价**：实验设计较为系统，通过控制变量法证明了“先易后难、先 SFT 后 RL”的优越性。分析部分深入探讨了模型行为的变化（如 RL 如何压缩 SFT 带来的冗长回复），实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **SFT 是不可或缺的冷启动**：对于小参数模型，SFT 的样本效率远高于纯 RL，是获取领域知识的最快途径。
*   **难度解耦的有效性**：DeReason 在 GPQA-Diamond 和 BBEH 等高难度基准上显著优于随机划分和纯 SFT/RL。
*   **模型行为差异**：RL 过程会显著降低策略熵，使模型输出更确定；同时，RL 具有“压缩”作用，能缩短 SFT 阶段产生的冗长回复，且这种缩短与答案正确性正相关。
*   **领域分布**：高难度题目（Score 4-5）高度集中在数学和物理领域，而低难度题目分布更均匀。

### 7. 优点（亮点）
*   **数据驱动的解耦策略**：不依赖复杂的算法修改，仅通过优化数据分配逻辑即可获得显著增益，具有极强的工程普适性。
*   **正交性**：该方法与现有的 RL 算法改进（如 GRPO 的变体）是正交的，可以轻松集成到现有的后训练流水线中。
*   **深入的行为分析**：论文不仅给出了分数，还详细分析了训练过程中熵、长度和奖励的变化，揭示了 SFT 和 RL 在参数空间中的不同作用。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 4B 小模型上，尚未在大规模模型（如 70B+）上验证该解耦策略的缩放效应（Scaling Laws）。
*   **打分器偏差**：难度评估依赖于同规模的 Instruct 模型，如果打分模型本身存在偏见或能力不足，可能会导致数据划分不合理。
*   **验证器依赖**：在通用领域仍依赖模型基验证器（Model-based Verifier），这可能引入“奖励作弊”或判定不准的风险，不如数学领域的规则校验（Rule-based）严谨。

（完）
