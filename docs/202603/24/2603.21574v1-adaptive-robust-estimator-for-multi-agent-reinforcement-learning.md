---
title: Adaptive Robust Estimator for Multi-Agent Reinforcement Learning
title_zh: 面向多智能体强化学习的自适应鲁棒估计器
authors: "Zhongyi Li, Wan Tian, Jingyu Chen, Kangyao Huang, Huiming Zhang, Hui Yang, Tao Ren, Jinyang Jiang, Yijie Peng, Yikun Ban, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21574v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 用于大语言模型协作推理的多智能体强化学习
tldr: 针对多智能体强化学习在协作推理中面临的信用分配困难及奖励噪声导致的训练不稳定问题，本文提出了一种鲁棒的多智能体强化学习框架。该框架包含DACR结构化推理流程，通过“回答-评判-重写”三阶段实现明确的贡献归因；同时引入ARE自适应鲁棒估计器，有效抵御重尾和噪声奖励对策略优化的干扰。实验证明，该方法在数学推理和具身智能任务中表现优异，显著提升了训练的稳定性和鲁棒性。
selection_source: fresh_fetch
motivation: 多智能体协作推理存在交互模糊导致的信用分配难题，且易受重尾和噪声奖励影响导致训练不稳定。
method: 提出了由DACR（双智能体回答-评判-重写）结构化流水线和ARE（自适应鲁棒估计器）组成的强化学习框架。
result: 在数学推理和具身智能基准测试中，该方法在存在奖励噪声的情况下仍能显著优于基准模型。
conclusion: 该框架通过结构化交互和鲁棒估计，有效解决了多智能体强化学习中的信用分配与噪声干扰问题，增强了系统的稳定性。
---

## 摘要
多智能体协作已成为增强大语言模型推理能力的强大范式，但它面临着交互层面的模糊性，这种模糊性混淆了生成、批判和修正过程，导致智能体间的信用分配变得困难。此外，在这种设定下的策略优化容易受到重尾和噪声奖励的影响，这可能会使优势估计产生偏差，并引发不稳定甚至发散的训练。为了解决这两个问题，我们提出了一种用于协作推理的鲁棒多智能体强化学习框架，由两个组件组成：双智能体“回答-批判-重写”（DACR）和自适应鲁棒估计器（ARE）。DACR 将推理分解为一个结构化的三阶段流水线：回答、批判和重写，同时能够显式地归因每个智能体对其合作伙伴性能的边际贡献。ARE 在多智能体策略优化过程中提供批次经验均值的鲁棒估计。在数学推理和具身智能基准测试中，即使在存在奖励噪声的情况下，我们的方法在同构和异构设置下均一致优于基准方法。这些结果表明，该方法对奖励噪声具有更强的鲁棒性，且训练动态更稳定，有效防止了由噪声奖励信号引起的优化失败。

## Abstract
Multi-agent collaboration has emerged as a powerful paradigm for enhancing the reasoning capabilities of large language models, yet it suffers from interaction-level ambiguity that blurs generation, critique, and revision, making credit assignment across agents difficult. Moreover, policy optimization in this setting is vulnerable to heavy-tailed and noisy rewards, which can bias advantage estimation and trigger unstable or even divergent training. To address both issues, we propose a robust multi-agent reinforcement learning framework for collaborative reasoning, consisting of two components: Dual-Agent Answer-Critique-Rewrite (DACR) and an Adaptive Robust Estimator (ARE). DACR decomposes reasoning into a structured three-stage pipeline: answer, critique, and rewrite, while enabling explicit attribution of each agent's marginal contribution to its partner's performance. ARE provides robust estimation of batch experience means during multi-agent policy optimization. Across mathematical reasoning and embodied intelligence benchmarks, even under noisy rewards, our method consistently outperforms the baseline in both homogeneous and heterogeneous settings. These results indicate stronger robustness to reward noise and more stable training dynamics, effectively preventing optimization failures caused by noisy reward signals.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Robust Estimator for Multi-Agent Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）通过多智能体协作（如“解决者-评估者”配对）能有效缓解单智能体的确认偏差。然而，将强化学习（RL）应用于多智能体推理时面临两大挑战。
*   **核心问题**：
    1.  **信用分配困难**：多智能体交互（生成、批判、修改）过程模糊，难以量化单个智能体对最终结果的边际贡献。
    2.  **奖励信号不稳定**：推理任务的奖励通常由验证器或奖励模型生成，具有**重尾分布（Heavy-tailed）**和**高噪声**特征。传统的 GRPO 算法依赖批次均值进行归一化，极少数离群值（Outliers）就会导致优势估计偏差，引发训练震荡甚至不收敛。

### 2. 方法论：DACR 与 ARE
论文提出了一个结合结构化交互与鲁棒优化的框架：
*   **DACR（双智能体回答-评判-重写协议）**：
    *   **三阶段流程**：1. 独立回答；2. 交换答案并互相提供批判性意见；3. 根据对方建议重写答案。
    *   **交叉评估奖励机制**：智能体 $i$ 的总奖励由其最终答案质量和“协作效用”组成。协作效用定义为：由于智能体 $i$ 提供的批判意见，导致合作伙伴答案质量提升的幅度。这实现了显式的信用归因。
*   **ARE（自适应鲁棒估计器）**：
    *   **核心思想**：改进传统的“均值之中的中位数”（Median-of-Means, MoM）估计器。
    *   **技术细节**：ARE 在每个数据块内部不再计算简单的算术平均，而是通过最小化**自适应鲁棒损失函数（Barron Loss）**来寻找鲁棒中心。
    *   **算法流程**：利用**渐进非凸性（GNC）**优化框架和**迭代重权最小二乘法（IRLS）**，动态调整形状参数 $\alpha$ 和尺度参数 $c$，从而在保留有效信号的同时自动抑制离群值的干扰。

### 3. 实验设计
*   **数学推理任务**：
    *   **数据集**：在 MATH 7.5k 上训练；在 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外/泛化性测试）上评估。
    *   **模型**：Qwen2.5 (1.5B/7B)、Llama3.1 (8B)，涵盖同构和异构智能体配对。
    *   **对比方法**：以标准 GRPO 为基准（Base），对比了独立回答与三阶段交互的效果。
*   **具身智能任务（Aerial VLN）**：
    *   **场景**：无人机视觉语言导航。
    *   **模型**：Qwen2-VL-2B。
    *   **指标**：导航误差（NE）、成功率（SR）、路径长度加权成功率（SPL）等。

### 4. 资源与算力
*   **硬件环境**：使用了单节点 **8 张 NVIDIA A800 GPU**。
*   **软件框架**：使用 Ray 进行分布式训练编排，vLLM 作为推理引擎（4 个引擎，张量并行度为 2）。
*   **训练细节**：启用了梯度检查点（Gradient Checkpointing）和 Adam 参数卸载（Offloading）以优化显存。具体训练时长未明确说明，但提到了最大 Prompt 长度为 6144 Tokens。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同规模和家族的模型。
    *   进行了 6 个数学基准测试集的广泛评估。
    *   **消融实验**：验证了三阶段交互（DACR）相对于独立回答的增益。
    *   **鲁棒性测试**：专门注入了**柯西噪声（Cauchy Noise）**来模拟极端奖励污染，测试不同污染比例下的性能退化情况。
*   **充分性评价**：实验设计较为充分，不仅验证了推理任务，还跨领域验证了具身智能任务，证明了 ARE 估计器的通用性。对比实验考虑了噪声环境，符合其“鲁棒性”的研究定位。

### 6. 主要结论
*   **性能提升**：在数学推理中，ARE 显著优于 GRPO 基准，尤其在 AMC23 等困难任务上，准确率提升高达 10% 以上。
*   **训练稳定性**：在存在重尾噪声奖励的情况下，ARE 能有效防止梯度尖峰，使训练曲线更加平滑，收敛更稳定。
*   **泛化能力**：在具身智能任务中，ARE 在未见过的环境（Unseen environments）中表现出比 FlightGPT 更强的泛化性能。
*   **协作价值**：DACR 的三阶段结构比简单的独立尝试更能激发模型的推理潜力。

### 7. 优点
*   **理论与实践结合**：为 ARE 估计器提供了严谨的数学证明（包括有限方差和重尾分布下的偏差界限）。
*   **解决痛点**：直接针对 RLHF/RLAIF 中常见的奖励模型噪声问题，具有很强的实用价值。
*   **结构化交互**：DACR 巧妙地解决了多智能体信用分配这一经典难题，且无需复杂的数学建模。

### 8. 不足与局限
*   **计算开销**：ARE 涉及 GNC 和 IRLS 的多次迭代，虽然在批次级别计算，但相比简单均值计算，会增加一定的优化耗时。
*   **超参数敏感性**：尽管 ARE 具有自适应性，但 DACR 中的奖励权重 $\gamma$ 以及 ARE 的块数量 $k$ 仍可能需要针对不同任务进行微调。
*   **多轮对话限制**：目前主要集中在三阶段交互，对于更长程、多轮的复杂对话推理，其信用分配的有效性尚待验证。

（完）
