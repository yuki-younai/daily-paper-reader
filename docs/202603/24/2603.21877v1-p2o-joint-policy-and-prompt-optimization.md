---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：联合策略与提示优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 大语言模型推理的策略与提示词联合强化学习优化
tldr: 针对大语言模型在强化学习（RLVR）中面临的“硬样本”探索效率低、奖励稀疏问题，本文提出了 P^2O 框架。该框架将提示词优化与策略优化相结合，利用 GeneticPareto 算法为硬样本进化出更优提示词，引导模型发现成功路径，并将这些推理增益蒸馏至模型参数中。实验证明，P^2O 在分布内和分布外基准测试中均显著提升了模型性能和收敛速度。
selection_source: fresh_fetch
motivation: 传统的 RLVR 在面对成功率接近零的硬样本时，由于奖励稀疏导致模型缺乏有效的监督信号，限制了推理能力的进一步提升。
method: 提出 P^2O 框架，通过 GeneticPareto 算法动态优化硬样本的提示词，并将优化后的推理路径作为密集监督信号蒸馏到模型策略中。
result: "实验显示 P^2O 不仅在分布内任务表现优异，在分布外（OOD）基准测试中也实现了平均 4.7% 的显著性能提升。"
conclusion: P^2O 通过协同提示词与策略优化，有效解决了强化学习中的探索难题，增强了语言模型的复杂推理能力和泛化性。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLMs）推理能力的强大范式。然而，原生 RLVR 面临探索效率低下的问题，尤其是在面对成功率接近于零的“困难样本”时。在此类场景下，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有很高的信息价值，但实际上却使模型缺乏监督信号。为解决这一问题，我们提出了 P^2O，这是一个协同提示优化与策略优化的新颖框架。P^2O 在训练迭代过程中识别困难样本，并利用 GeneticPareto (GEPA) 提示优化算法来演化提示模板，从而引导模型发现成功的轨迹。至关重要的是，与依赖输入增强的传统提示工程方法不同，P^2O 将这些优化提示所带来的推理增益直接蒸馏到模型参数中。该机制为困难样本提供了更密集的正向监督信号，并加速了收敛。大量实验表明，P^2O 不仅在分布内数据集上取得了卓越性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

以下是对论文《P^2O: Joint Policy and Prompt Optimization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对**具有可验证奖励的强化学习（RLVR）**在提升大语言模型（LLM）推理能力时面临的**探索瓶颈**问题。
*   **核心矛盾**：在处理复杂推理任务时，训练集往往包含大量“硬样本”（模型在多次采样中成功率为零）。由于奖励极其稀疏，模型无法获得有效的梯度信号（优势估计为零），导致模型在简单样本上过拟合，而在复杂逻辑上停滞不前。
*   **研究动机**：作者发现，虽然模型在标准指令下无法解决硬样本，但通过精心设计的提示词（Prompt）往往能激发其潜在的推理路径。因此，论文提出将提示词优化与策略优化结合，利用提示词作为“桥梁”跨越奖励稀疏的低谷，并将这种能力内化到模型参数中。

### 2. 方法论：核心思想与关键技术
P^2O 框架采用**交替优化**的策略，主要分为两个阶段：

*   **阶段 1：带上下文蒸馏的策略优化（Policy Optimization with Context Distillation）**
    *   **硬样本挖掘**：在训练迭代中，动态识别成功率低于阈值 $\tau$ 的样本。
    *   **上下文蒸馏**：对于硬样本，使用上一阶段优化出的提示词 $z$ 增强输入 $\tilde{x}$，引导模型生成成功的推理轨迹。
    *   **关键细节**：在更新参数时，模型虽然是基于增强输入生成的轨迹，但计算梯度时却针对**原始输入 $x$**。这种“脱钩”机制强制模型学习推理逻辑本身，而非依赖提示词，从而实现能力的内化。
*   **阶段 2：进化提示词优化（Evolutionary Prompt Optimization）**
    *   **GEPA 算法**：利用遗传-帕累托（Genetic-Pareto）算法。使用一个“反思模型”（Reflection LLM）分析硬样本的错误轨迹，生成针对性的提示词变体（突变）。
    *   **帕累托筛选**：维护一个多样化的提示词群体，通过帕累托前沿选择在准确性和简洁性等维度表现优异的模板。
    *   **贪婪分配**：通过贪婪集合覆盖算法，为下一轮训练的硬样本分配最有效的提示词模板。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 DeepScaler（5K 样本）和 DeepMath（5K 样本，难度 $\ge 7$）中抽样。
    *   **测试基准（Benchmarks）**：包含 AIME24、AIME25、AMC、MATH500、MINERVA 和 OLYMPIAD 六个极具挑战性的数学推理数据集。
*   **对比方法**：
    *   基础模型：Qwen3-0.6B/1.7B/4B。
    *   基准算法：标准的 **GRPO**（Group Relative Policy Optimization）。
    *   变体对比：P^2O Self-Ref（自反思）和 P^2O Teacher-Ref（使用 Kimi-K2 作为老师进行反思）。

### 4. 资源与算力
*   **模型规模**：主要使用 **Qwen3-4B** 作为骨干模型。
*   **训练参数**：学习率 $1 \times 10^{-6}$，全局 Batch Size 为 128，最大生成长度 12k tokens，训练 5 个 Epoch。
*   **采样配置**：每条提示词采样 $K=6$ 条轨迹，温度 $T=0.6$。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 H100/A100）及具体数量，但提到 GRPO 算法相比 PPO 节省了约一半的显存（因为去除了 Critic 网络）。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的 5K 训练分布上进行了完整实验，并在 6 个主流基准上验证。
*   **消融实验**：
    1.  **上下文蒸馏的必要性**：验证了如果不进行蒸馏（直接在带提示词的输入上训练），模型会产生“提示词依赖”，导致推理能力下降。
    2.  **提示词多样性**：验证了在 Rollout 组内使用多样化的帕累托提示词比使用单一提示词效果更好。
*   **充分性评价**：实验设计较为全面，涵盖了从模型性能、训练曲线到定性案例分析的多个维度。通过对比不同反思模型（Self vs Teacher），探讨了知识内化的边界，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能显著提升**：P^2O 在所有基准上均优于 GRPO。在 DeepScaler-5K 上平均提升 **4.7%**，在极难的 AIME 任务上提升高达 **12.3%**。
*   **解决硬样本**：实验观察到随着训练进行，硬样本的数量稳步下降，证明了优化后的提示词确实帮助模型攻克了原本无法触及的搜索空间。
*   **内化效应**：通过上下文蒸馏，模型在推理时无需任何特殊提示词即可达到高性能，证明了提示词引导的增益已成功转化为模型参数。
*   **反思源的影响**：在某些任务中，自反思（Self-Ref）甚至优于外部老师反思，说明模型有时更易于吸收符合自身推理逻辑的改进建议。

### 7. 优点
*   **创新的双层优化**：将离散的提示词搜索与连续的参数优化结合，解决了强化学习中常见的“冷启动”和探索效率问题。
*   **推理内化机制**：通过蒸馏技术避免了推理时的额外开销，保持了模型的简洁性。
*   **强大的泛化性**：在分布外（OOD）任务上表现强劲，证明该方法学习的是通用推理模式而非简单的模式匹配。

### 8. 不足与局限
*   **计算开销**：虽然 GRPO 节省了显存，但 Phase 2 的提示词进化过程（涉及多次反思和验证集评估）会增加额外的计算耗时。
*   **领域局限**：实验主要集中在数学推理领域，尚未在代码生成、创意写作或多模态任务中验证其通用性。
*   **依赖反思模型**：提示词优化的质量高度依赖于反思模型（Reflection LLM）的水平，如果反思模型无法提供高质量的突变建议，框架的效果可能会受限。

（完）
