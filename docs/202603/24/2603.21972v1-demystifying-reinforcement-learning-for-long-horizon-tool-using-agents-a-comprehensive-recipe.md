---
title: "Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe"
title_zh: 揭秘长程工具使用智能体的强化学习：一份全面的方案
authors: "Xixi Wu, Qianguo Sun, Ruiyang Zhang, Chao Song, Junlong Wu, Yiyan Qi, Hong Cheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21972v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 在大模型智能体的复杂多轮环境中扩展强化学习
tldr: 本研究针对大语言模型在长程规划和工具使用中缺乏实用强化学习（RL）训练方案的问题，利用TravelPlanner基准进行了系统性实证研究。通过探索奖励建模、模型规模、数据组成、算法选择和环境稳定性五个核心维度，总结出7项关键实践准则。实验表明，该方案能显著提升智能体性能，在TravelPlanner上超越了现有领先模型，为构建自主智能体提供了可扩展的RL训练指南。
selection_source: fresh_fetch
motivation: 旨在填补大语言模型在复杂、多轮工具使用场景下缺乏高效强化学习训练实践方案的空白。
method: 基于TravelPlanner任务，从奖励函数设计、模型缩放、数据配比、算法优化及环境稳定性五个维度开展受控实验。
result: 发现1K左右的平衡难度样本是训练甜点位，且大模型在简单密集奖励下即可高效收敛，最终取得SOTA性能。
conclusion: 本文提炼的强化学习训练“食谱”为开发具备长程规划能力的自主工具使用智能体提供了重要参考。
---

## 摘要
强化学习 (RL) 对于将大语言模型 (LLMs) 演进为具备长程规划能力的自主智能体至关重要，但在复杂的、多轮交互的环境中扩展强化学习的实用方案仍然难以捉摸。本文利用 TravelPlanner 这一具有挑战性的测试基准进行了一项系统的实证研究，该基准要求通过工具编排来满足多方面的约束。我们将智能体强化学习的设计空间分解为五个维度：奖励塑造、模型缩放、数据组成、算法选择和环境稳定性。我们的受控实验得出了 7 个关键结论，例如：(1) 奖励和算法的选择具有规模依赖性，较小的模型受益于阶段性奖励和增强的探索，而较大的模型则能通过更简单的密集奖励高效收敛；(2) 约 1000 个具有平衡难度混合的训练样本是域内和域外性能的最佳平衡点；(3) 环境稳定性对于防止策略退化至关重要。基于我们提炼的方案，我们经过强化学习训练的模型在 TravelPlanner 上实现了最先进的性能，显著优于领先的大语言模型。

## Abstract
Reinforcement Learning (RL) is essential for evolving Large Language Models (LLMs) into autonomous agents capable of long-horizon planning, yet a practical recipe for scaling RL in complex, multi-turn environments remains elusive. This paper presents a systematic empirical study using TravelPlanner, a challenging testbed requiring tool orchestration to satisfy multifaceted constraints. We decompose the agentic RL design space along 5 axes: reward shaping, model scaling, data composition, algorithm selection, and environmental stability. Our controlled experiments yield 7 key takeaways, e.g., (1) reward and algorithm choices are scale-dependent as smaller models benefit from staged rewards and enhanced exploration, whereas larger models converge efficiently with simpler dense rewards, (2) ~ 1K training samples with a balanced difficulty mixture mark a sweet spot for both in-domain and out-of-domain performance, and (3) environmental stability is critical to prevent policy degradation. Based on our distilled recipe, our RL-trained models achieve state-of-the-art performance on TravelPlanner, significantly outperforming leading LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：《揭秘长程工具使用智能体的强化学习：一份全面的方案》

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向自主智能体演进，**长程规划（Long-Horizon Planning）**和**复杂工具调用**成为核心挑战。现有的强化学习（RL）研究多集中在短程推理或简单交互，而在需要数十步操作、满足多重约束（如预算、偏好、逻辑一致性）的复杂场景下，如何有效扩展 RL 训练仍缺乏系统性的指导方案（Recipe）。本论文旨在通过深入的实证研究，拆解智能体 RL 的设计空间，为开发者提供一套可落地的训练准则。

#### 2. 论文提出的方法论
论文提出了 **STAR (Synthesis, Training, And Reinforcement)** 统一后训练流水线：
*   **数据合成 (Synthesis)**：针对 TravelPlanner 场景，通过采样原子元素并验证可行性，合成 10K+ 具有难度控制（简单、中等、困难）的查询。
*   **监督微调 (SFT)**：利用强教师模型（如 DeepSeek-V3.2-Exp-Thinking）进行拒绝采样，获取约 1.2K 条高质量成功轨迹，为 RL 提供任务感知的初始策略，避免“冷启动”问题。
*   **强化学习 (Reinforcement)**：
    *   **核心算法**：采用 **GRPO (Group Score Policy Optimization)**，通过组内奖励归一化计算优势函数，无需额外的价值模型（Critic）。
    *   **设计空间探索**：从奖励塑造（从密集到稀疏）、模型缩放（1.5B 到 7B）、数据配比（数量与难度）、算法变体（标准 GRPO vs. 增强探索型算法）以及环境稳定性（注入随机错误）五个维度进行受控实验。

#### 3. 实验设计
*   **主要测试床 (Testbed)**：**TravelPlanner**。这是一个模拟真实旅行社场景的基准，要求智能体调用 6 种工具（搜索航班、酒店、餐厅等），在满足硬约束（预算、房型等）和常识约束（不幻觉、逻辑一致）的前提下生成多日行程。
*   **评估指标**：微观/宏观常识得分、微观/宏观硬约束得分，以及最终的**成功率 (Success Rate)**。
*   **对比基准 (Benchmark)**：
    *   **闭源/领先模型**：GPT-4o, GPT-5, Gemini-1.5-Pro, Kimi-K2.5, Qwen3.5 等。
    *   **域外 (OOD) 泛化评估**：在 7 个知识密集型 QA 数据集（如 Natural Questions, HotpotQA）上测试模型的信息检索与推理能力。

#### 4. 资源与算力
论文详细记录了训练开销（见 Table 4）：
*   **硬件**：1.5B 和 3B 模型使用 1 台 8×A100-80G 节点；7B 模型使用 2 台节点（共 16×A100）。
*   **时长与效率**：
    *   1.5B/3B 模型训练约需 160-200 GPU 小时。
    *   7B 模型使用标准 GRPO 约需 368 GPU 小时。
    *   每步训练时长约 8-13 分钟。

#### 5. 实验数量与充分性
实验设计非常充分且具有高度的客观性：
*   **多维度消融**：针对 5 个核心维度分别进行了详尽的对比实验，每组实验均严格控制变量。
*   **规模覆盖**：涵盖了从 1.5B 到 7B 的主流轻量化模型规模。
*   **泛化性验证**：不仅关注域内（旅行规划）的成功率，还通过 7 个 OOD 任务评估了 RL 训练是否导致模型能力退化（即“对齐税”）。
*   **统计显著性**：通过训练曲线（Entropy, Success Rate）展示了收敛过程的稳定性。

#### 6. 主要结论与发现
1.  **规模依赖性**：小模型（1.5B）极度依赖**阶段性课程奖励 (Curriculum Reward)** 和增强探索算法；而大模型（7B）在简单的**密集奖励 (Sum Reward)** 下表现更好。
2.  **数据甜点位**：**1K 个训练样本**是性能与泛化的平衡点。超过此数量后，域内提升微小，但 OOD 性能会因过拟合而显著下降。
3.  **难度配比**：混合难度的训练数据至关重要。纯困难数据会导致奖励过稀疏而无法学习，纯简单数据则无法处理复杂约束。
4.  **算法选择**：对于 7B 模型，标准的 GRPO 效率最高；复杂的探索机制（如 ARPO）在小模型上更有价值。
5.  **环境鲁棒性**：智能体能容忍 5% 以下的工具调用错误率，但 10% 的错误率会导致策略严重退化。

#### 7. 优点
*   **实战导向**：提供了一套清晰的“食谱”，对开发者在不同算力预算下选择模型和策略具有极强的指导意义。
*   **性能卓越**：基于该方案训练的 7B 模型在 TravelPlanner 上的成功率（62.8%）远超 Kimi-K2.5（11.8%）和 GPT-4o（6.7%）。
*   **开源贡献**：开源了 STAR 框架和训练细节，促进了社区对长程智能体 RL 的理解。

#### 8. 不足与局限
*   **环境局限性**：TravelPlanner 虽然复杂，但仍属于模拟沙盒环境，真实世界的 API 交互可能存在更不可预测的动态性。
*   **模型规模上限**：实验最高仅覆盖到 7B 规模，更大规模模型（如 70B+）是否遵循相同的规律尚待验证。
*   **任务单一性**：主要集中在旅行规划这一种长程任务，跨领域的长程任务（如代码工程、科研辅助）的通用性有待进一步加强。
*   **缺乏全局回溯**：案例分析显示，模型在遇到下游死胡同时仍缺乏全局重规划（Backtracking）的能力。

（完）
