---
title: "KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning"
title_zh: KG-Hopper：通过强化学习赋能轻量级开源大语言模型的知识图谱推理能力
authors: "Shuai Wang, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21440v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 大模型多跳推理的强化学习
tldr: 针对大语言模型在知识图谱问答中多步推理灵活性不足及错误累积的问题，本文提出KG-Hopper框架。该框架利用强化学习训练紧凑型开源LLM，使其能在单次推理中通过统一的“思考”阶段完成完整的知识图谱遍历与决策。KG-Hopper支持跨步骤依赖的全局推理和动态路径回溯，在8个基准测试中表现优异，以7B参数量实现了超越70B模型及媲美GPT-4o-mini的性能。
selection_source: fresh_fetch
motivation: 现有的KBQA方法通常采用预定义的顺序推理流水线，导致推理过程缺乏灵活性且容易产生步骤间的错误累积。
method: 提出一种强化学习框架，将整个知识图谱遍历和决策过程整合进LLM的单个推理轮次中，实现具备回溯能力的全局动态推理。
result: 实验显示KG-Hopper在8个基准测试中一致优于参数量高达70B的多步推理系统，并达到了与GPT-4o-mini相当的水平。
conclusion: 通过强化学习赋能紧凑型模型进行集成化多步推理，是提升开源LLM处理复杂知识密集型任务效率与准确性的有效途径。
---

## 摘要
大语言模型（LLMs）展现出卓越的自然语言处理能力，但在处理知识密集型推理任务时往往面临挑战。利用结构化知识图谱（KGs）的知识库问答（KBQA）由于需要准确的多跳推理，充分体现了这一挑战。现有方法通常在预定义流水线的指导下执行顺序推理步骤，这限制了灵活性，并因每一步的孤立推理而导致错误级联。为了解决这些局限性，我们提出了 KG-Hopper，这是一个新颖的强化学习（RL）框架，旨在使轻量级开源 LLM 能够在单轮推理中执行集成的多跳知识图谱推理。与逐步推理不同，我们训练了一个推理 LLM，将整个知识图谱遍历和决策过程嵌入到一个统一的“思考”阶段，从而实现对跨步依赖关系的全局推理以及带有回溯的动态路径探索。在八个知识图谱推理基准测试上的实验结果表明，基于 7B 参数 LLM 的 KG-Hopper 始终优于更大规模的多步系统（高达 70B），并与 GPT-3.5-Turbo 和 GPT-4o-mini 等闭源模型相比具有竞争力的性能，同时保持了轻量、开源和数据高效的特点。代码已在 https://github.com/Wangshuaiia/KG-Hopper 公开。

## Abstract
Large Language Models (LLMs) demonstrate impressive natural language capabilities but often struggle with knowledge-intensive reasoning tasks. Knowledge Base Question Answering (KBQA), which leverages structured Knowledge Graphs (KGs) exemplifies this challenge due to the need for accurate multi-hop reasoning. Existing approaches typically perform sequential reasoning steps guided by predefined pipelines, restricting flexibility and causing error cascades due to isolated reasoning at each step. To address these limitations, we propose KG-Hopper, a novel Reinforcement Learning (RL) framework that empowers compact open LLMs with the ability to perform integrated multi-hop KG reasoning within a single inference round. Rather than reasoning step-by-step, we train a Reasoning LLM that embeds the entire KG traversal and decision process into a unified ``thinking'' stage, enabling global reasoning over cross-step dependencies and dynamic path exploration with backtracking. Experimental results on eight KG reasoning benchmarks show that KG-Hopper, based on a 7B-parameter LLM, consistently outperforms larger multi-step systems (up to 70B) and achieves competitive performance with proprietary models such as GPT-3.5-Turbo and GPT-4o-mini, while remaining compact, open, and data-efficient. The code is publicly available at: https://github.com/Wangshuaiia/KG-Hopper.

---

## 论文详细总结（自动生成）

以下是对论文《KG-Hopper: Empowering Compact Open LLMs with Knowledge Graph Reasoning via Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）在处理知识密集型任务时容易产生幻觉。现有的知识库问答（KBQA）方法通常采用多步顺序推理流水线（Pipeline），存在**灵活性差**（难以处理不完整数据）、**局部最优**（缺乏回溯机制）和**错误级联**（前步错误导致后续全错）等问题。
*   **研究动机**：借鉴 OpenAI o1 等模型的“思考（Thinking）”机制，探索如何通过强化学习（RL）让轻量级开源模型在单次推理中完成完整的多跳知识图谱（KG）遍历与决策，从而实现更具鲁棒性和全局观的推理。

### 2. 方法论
KG-Hopper 的核心思想是将多跳推理过程整合进 LLM 的单个“思考”阶段，利用强化学习优化推理策略。
*   **冷启动（Cold Start）**：首先通过少量（500条）高质量的思维链（CoT）数据进行有监督微调（SFT），教会模型如何调用 KG 检索工具及遵循特定的输出格式（`<think>`、`<search>`、`<answer>` 标签）。
*   **强化学习框架**：采用 **GRPO（Group Relative Policy Optimization）** 算法进行优化。
*   **关键技术细节**：
    *   **复合奖励函数**：由四部分组成：
        1.  **检索奖励**：鼓励模型主动搜索，但设有上限以防过度调用。
        2.  **格式奖励**：确保模型严格遵守标签规范。
        3.  **推理奖励**：利用外部大模型（如 Llama-3-70B）对 `<think>` 块内的逻辑严密性进行评分。
        4.  **答案奖励**：利用 LLM 评估预测答案与真实答案的语义一致性。
    *   **掩码技术**：在计算损失时掩盖检索到的三元组内容，防止模型简单地复制外部知识，而是学习推理逻辑。
    *   **历史重采样（History Resampling）**：在训练后期移除简单的一跳问题，强制模型专注于复杂的多跳推理（课程学习策略）。

### 3. 实验设计
*   **数据集**：涵盖了 8 个主流基准测试，分为两大类：
    *   **基于 Freebase**：ComplexWebQuestions (CWQ), WebQSP, WebQuestions, GrailQA。
    *   **基于 WikiData**：QALD10-en, T-REx, Zero-Shot RE, Creak（事实核查）。
*   **对比方法**：
    *   **Prompt-only**：Qwen-2.5-7B, Llama-3.1-8B/70B, GPT-4o, GPT-4o-mini。
    *   **KG-Augmented（无微调）**：上述模型结合 KG 检索工具。
    *   **SFT-based**：Interactive-KBQA, KG-CoT 等现有先进多步推理框架。
*   **评估指标**：Hit@1（问答/槽填充）和 Accuracy（事实核查）。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练配置**：训练 2 个 Epoch，Batch Size 为 16，学习率为 1e-6。
*   **推理设置**：每个查询生成 16 个 Rollouts（采样路径）。
*   **模型底座**：主要基于 Qwen-2.5-7B 和 Llama-3.1-8B。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同性质的数据集上进行了测试，涵盖了从简单问答到复杂推理和事实核查的多种场景。
*   **消融实验**：设计了四组关键消融实验：
    1.  **RL vs SFT**：验证了强化学习在处理长程依赖上的优越性。
    2.  **奖励组件分析**：证明了“推理奖励”对性能贡献最大。
    3.  **无标注训练**：测试了在没有 Ground Truth 时，利用 LLM 作为裁判（Judge）的可行性。
    4.  **重采样策略**：验证了动态调整训练难度对提升多跳能力的作用。
*   **评价**：实验设计全面，对比了不同参数规模（7B 到 70B）和不同厂商（开源与闭源）的模型，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **以小博大**：基于 7B 参数的 KG-Hopper 在多个任务上超越了 70B 规模的多步推理系统。
*   **媲美闭源**：其性能达到了与 GPT-4o-mini 相当的水平，甚至在某些复杂多跳任务上表现更优。
*   **RL 的必要性**：相比 SFT，RL 能显著提升模型在面对 KG 数据缺失或路径复杂时的回溯和自我修正能力。
*   **数据高效**：仅需极少量的标注数据（500条冷启动 + 2000条 RL 训练）即可实现强大的推理性能。

### 7. 优点
*   **单轮集成推理**：将复杂的“检索-推理-再检索”循环整合进单次模型调用，降低了系统复杂度和推理延迟。
*   **动态回溯能力**：通过“思考”阶段的自我纠错，有效解决了传统方法中“一步错步步错”的级联错误问题。
*   **开源且轻量**：证明了通过精细的 RL 训练，轻量级模型也能具备极强的结构化知识推理能力。

### 8. 不足与局限
*   **外部模型依赖**：训练过程中需要更强大的模型（如 70B 模型）作为奖励评分器，这增加了训练阶段的成本和环境依赖。
*   **KG 覆盖率限制**：尽管具备回溯能力，但如果 KG 本身存在严重缺失，模型仍需依赖内部参数知识，存在潜在的幻觉风险。
*   **推理成本**：虽然是单轮推理，但 `<think>` 阶段生成的 Token 数量较多，可能会增加单次请求的计算开销。

（完）
