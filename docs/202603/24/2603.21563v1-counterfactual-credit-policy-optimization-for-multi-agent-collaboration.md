---
title: Counterfactual Credit Policy Optimization for Multi-Agent Collaboration
title_zh: 面向多智能体协作的反事实贡献策略优化
authors: "Zhongyi Li, Wan Tian, Yikun Ban, Jinju Chen, Huiming Zhang, Yang Liu, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21563v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 基于反事实信用分配的多智能体强化学习，用于大模型协作
tldr: 针对多智能体大语言模型协作中的信用分配难题，本文提出反事实信用策略优化（CCPO）框架。该框架通过模拟移除特定智能体贡献后的反事实轨迹，估算每个智能体的边际贡献，从而提供角色敏感的学习信号。结合全局历史感知归一化方案，CCPO 有效解决了全局奖励导致的更新方差大和“搭便车”问题。在数学和逻辑推理任务上的实验证明，CCPO 显著提升了协作效率，优于现有的多智能体强化学习基准。
selection_source: fresh_fetch
motivation: 协作式多智能体系统中共享的全局奖励难以区分个体贡献，导致强化学习训练中出现高方差和“搭便车”现象。
method: 提出 CCPO 框架，通过构建动态反事实基准来估算智能体的边际贡献，并利用全局历史感知归一化来稳定异构任务下的优势函数。
result: 在顺序推理和多智能体投票等拓扑结构下，CCPO 在数学和逻辑推理基准测试中均优于强基线模型，有效缓解了搭便车问题。
conclusion: CCPO 为协作式 LLM 训练提供了一种更精细、有效的信用分配机制，显著提升了多智能体系统的推理性能和训练稳定性。
---

## 摘要
协作式多智能体大语言模型（LLM）可以通过角色分解和多样化假设聚合来解决复杂的推理任务。然而，此类系统的强化学习（RL）往往受到贡献分配（credit assignment）问题的困扰：共享的全局奖励掩盖了单个智能体的贡献，导致更新方差增大并诱发“搭便车”现象。我们提出了反事实贡献策略优化（CCPO），该框架通过反事实轨迹估计每个智能体的边际贡献，从而分配特定于智能体的学习信号。CCPO 构建了动态反事实基准，模拟移除某个智能体贡献后的结果，为策略优化生成角色敏感的优势函数。为了进一步提高在异构任务和数据分布下的稳定性，我们提出了一种全局历史感知的归一化方案，利用全局展开（rollout）统计数据来校准优势函数。我们在两种协作拓扑结构上评估了 CCPO：顺序的“思考-推理”二元组和多智能体投票。在数学和逻辑推理基准测试中，CCPO 缓解了搭便车现象，且性能优于强力的多智能体强化学习基线，为协作式 LLM 训练提供了更细粒度且更有效的贡献分配。我们的代码可在 https://github.com/bhai114/ccpo 获取。

## Abstract
Collaborative multi-agent large language models (LLMs) can solve complex reasoning tasks by decomposing roles and aggregating diverse hypotheses. Yet, reinforcement learning (RL) for such systems is often undermined by credit assignment: a shared global reward obscures individual contributions, inflating update variance and encouraging free-riding. We introduce Counterfactual Credit Policy Optimization (CCPO), a framework that assigns agent-specific learning signals by estimating each agent's marginal contribution through counterfactual trajectories. CCPO builds dynamic counterfactual baselines that simulate outcomes with an agent's contribution removed, yielding role-sensitive advantages for policy optimization. To further improve stability under heterogeneous tasks and data distributions, we propose a global-history-aware normalization scheme that calibrates advantages using global rollout statistics. We evaluate CCPO on two collaboration topologies: a sequential Think--Reason dyad and multi-agent voting. Across mathematical and logical reasoning benchmarks, CCPO mitigates free-riding and outperforms strong multi-agent RL baselines, yielding finer-grained and more effective credit assignment for collaborative LLM training. Our code is available at https://github.com/bhai114/ccpo.

---

## 论文详细总结（自动生成）

以下是对论文《Counterfactual Credit Policy Optimization for Multi-Agent Collaboration》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多智能体大语言模型（LLM）协作系统中，如何解决**信用分配（Credit Assignment）**难题。
*   **研究背景**：当前的协作式 LLM（如“思考者-推理者”架构或“投票”机制）在强化学习（RL）训练时，通常依赖共享的全局奖励（如最终答案是否正确）。
*   **研究动机**：共享奖励存在三大缺陷：
    1.  **稀疏且延迟**：难以确定是哪个智能体改进或损害了最终结果。
    2.  **搭便车（Free-riding）**：即使某个智能体输出了冗余或有害信息，只要团队获胜，它也会获得正向更新，导致梯度噪声大且偏置高。
    3.  **角色敏感性缺失**：不同协作拓扑（顺序 vs. 并行）对个体贡献的要求不同，简单共享奖励无法体现这种差异。

### 2. 方法论：核心思想与关键技术
*   **核心思想（CCPO）**：通过**反事实（Counterfactual）**推理来估算每个智能体的边际贡献。即询问：“如果移除智能体 $i$ 的贡献，团队表现会如何？”
*   **关键技术细节**：
    *   **边际贡献计算**：$\Delta_i = R_{joint} - R_{\neg i}$。其中 $R_{joint}$ 是团队总奖励，$R_{\neg i}$ 是移除智能体 $i$ 后的反事实奖励。
    *   **两种拓扑实例化**：
        1.  **顺序协作（Think-Solve）**：计算“智能体2独立回答”与“智能体1提供思路后智能体2回答”的差值，以此作为智能体1的信用。
        2.  **并行协作（Voting）**：计算“所有智能体投票结果”与“排除智能体 $i$ 后的投票结果”的差值。
    *   **全局历史感知归一化**：为了稳定训练，使用指数移动平均（EMA）维护边际贡献的均值和方差，对优势函数（Advantage）进行标准化处理。
    *   **策略优化**：基于 GRPO 算法框架，将计算出的个体优势函数应用于剪切后的策略梯度更新。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理**：训练集使用 MATH 7.5k；测试集包括 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外/OOD）。
    *   **逻辑推理**：使用 LogiQA 数据集。
*   **协作模式**：双智能体“思考-推理”模型、三智能体“投票”模型。
*   **对比方法（Baselines）**：
    *   单智能体（Single-agent）。
    *   未微调的协作模型（Untrained）。
    *   共享奖励的 GRPO。
    *   **ReMA**（一种近期的多智能体元思考强化学习方法）。

### 4. 资源与算力
*   **硬件设备**：实验使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：论文对比了 CCPO 与 ReMA 的运行时间。在相同设置下，CCPO 的训练时间约为 15.24 小时，比 ReMA（11.57 小时）增加了约 **31.7%**。这主要是因为在顺序协作模式下需要额外的反事实轨迹采样。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 72B 不同参数规模的模型（Qwen2.5 系列、Llama3.1-8B）。
*   **充分性**：
    *   **多维度评估**：不仅测试了准确率，还进行了 OOD 泛化性测试。
    *   **消融/验证实验**：设计了专门实验验证智能体 2 是否真的利用了智能体 1 的输出（而非忽略它），并可视化了奖励分布。
    *   **公平性**：对比了相同参数规模下的不同算法表现，实验设计较为客观。

### 6. 主要结论与发现
*   **性能提升**：CCPO 在所有数学和逻辑基准测试中均优于共享奖励基线和 ReMA。例如，在 Qwen2.5-1.5B 上，CCPO 比 CoT 策略平均提升 4.6%，比 ReMA 提升 1.8%。
*   **缓解搭便车**：通过奖励分布可视化发现，CCPO 能更准确地识别负面贡献。当联合回答正确但个体贡献为负时，CCPO 会给予惩罚，而共享奖励则会错误奖励。
*   **协作有效性**：实验证明智能体之间形成了真正的互补，而非简单的性能叠加。

### 7. 优点（亮点）
*   **理论支撑**：论文提供了关于收敛性、稳定性和方差缩减的理论证明（定理 5.1 和 5.3）。
*   **计算效率优化**：在投票模式下，反事实奖励的计算不需要额外解码，极大地节省了算力。
*   **通用性**：该框架可灵活应用于异构（顺序）和同构（并行）的多种多智能体拓扑。

### 8. 不足与局限
*   **计算开销**：在顺序协作（如 Think-Solve）中，为了获得反事实基准，需要额外的推理步骤，导致训练时间增加约 30%。
*   **拓扑限制**：目前仅验证了简单的顺序和投票拓扑，对于更复杂的图状交互或多轮对话协作的适用性尚待研究。
*   **奖励函数依赖**：依然依赖于能够自动判题的客观奖励（如数学/逻辑的 0/1 奖励），对于主观性较强的任务（如创意写作协作）可能难以定义反事实基准。

（完）
