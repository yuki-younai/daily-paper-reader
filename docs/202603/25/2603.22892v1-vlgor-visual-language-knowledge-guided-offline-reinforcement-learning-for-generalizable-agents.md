---
title: "VLGOR: Visual-Language Knowledge Guided Offline Reinforcement Learning for Generalizable Agents"
title_zh: VLGOR：面向可泛化智能体的视觉-语言知识引导的离线强化学习
authors: "Pengsen Liu, Maosen Zeng, Nan Tang, Kaiyuan Li, Jing-Cheng Pang, Yunan Liu, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22892v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 结合离线强化学习的视觉语言模型微调
tldr: "本研究针对大语言模型在强化学习中缺乏物理感知、泛化能力受限的问题，提出了VLGOR框架。该框架通过微调视觉语言模型（VLM），结合初始视觉观察和高层指令生成具有时空一致性的虚构交互数据。利用反事实提示增强数据多样性，使智能体在离线强化学习中更好地将语言指令与环境感知对齐。实验证明，该方法在机器人操纵任务中显著提升了对未知任务的泛化性能，成功率比基准方法高出24%以上。"
selection_source: fresh_fetch
motivation: 大语言模型在强化学习中因缺乏对物理环境的直接感知，限制了其对环境动态的理解及在未知任务上的泛化能力。
method: VLGOR通过微调视觉语言模型来预测未来状态和动作，并利用反事实提示生成多样化的想象回放数据以增强离线训练。
result: "在机器人操纵基准实验中，VLGOR在处理需要新策略的未知任务时，成功率比现有基准方法提升了超过24%。"
conclusion: 通过整合视觉与语言知识生成高质量的模拟交互数据，可以显著增强离线强化学习智能体的环境对齐能力和任务泛化性。
---

## 摘要
将大语言模型（LLMs）与强化学习（RL）相结合，使智能体能够更有效地理解语言指令以执行任务。然而，LLMs 通常缺乏对物理环境的直接感知，这限制了它们对环境动力学的理解以及对未知任务的泛化能力。为了解决这一局限性，我们提出了视觉-语言知识引导的离线强化学习（VLGOR），这是一个整合视觉和语言知识以生成想象轨迹（imaginary rollouts）的框架，从而丰富了交互数据。VLGOR 的核心前提是微调一个视觉-语言模型，以初始视觉观察和高层指令为条件预测未来的状态和动作，确保生成的轨迹在时间上连贯且在空间上合理。此外，我们采用反事实提示（counterfactual prompts）为离线强化学习训练生成更多样化的轨迹，使智能体能够获取有助于遵循语言指令的知识，同时基于视觉线索在环境中实现具身落地（grounding）。在机器人操纵基准测试上的实验表明，VLGOR 显著提高了需要新型最优策略的未知任务的性能，其成功率比基准方法高出 24% 以上。

## Abstract
Combining Large Language Models (LLMs) with Reinforcement Learning (RL) enables agents to interpret language instructions more effectively for task execution. However, LLMs typically lack direct perception of the physical environment, which limits their understanding of environmental dynamics and their ability to generalize to unseen tasks. To address this limitation, we propose Visual-Language Knowledge-Guided Offline Reinforcement Learning (VLGOR), a framework that integrates visual and language knowledge to generate imaginary rollouts, thereby enriching the interaction data. The core premise of VLGOR is to fine-tune a vision-language model to predict future states and actions conditioned on an initial visual observation and high-level instructions, ensuring that the generated rollouts remain temporally coherent and spatially plausible. Furthermore, we employ counterfactual prompts to produce more diverse rollouts for offline RL training, enabling the agent to acquire knowledge that facilitates following language instructions while grounding in environments based on visual cues. Experiments on robotic manipulation benchmarks demonstrate that VLGOR significantly improves performance on unseen tasks requiring novel optimal policies, achieving a success rate over 24% higher than the baseline methods.

---

## 论文详细总结（自动生成）

### 论文总结：VLGOR (视觉-语言知识引导的离线强化学习)

#### 1. 核心问题与整体含义（研究动机和背景）
开发能够执行多样化任务的通用智能体是人工智能的核心目标。虽然大语言模型（LLM）具备丰富的语义知识，但其在强化学习（RL）中面临两个主要挑战：
*   **缺乏物理感知（Grounding）：** LLM 处理的是离散的语言符号，而物理环境是连续的动力学过程。仅依赖语言生成的模拟轨迹往往违反物理定律（如物体穿透、运动不合理）。
*   **泛化能力受限：** 离线强化学习受限于训练数据的覆盖范围，难以应对未见过的任务或指令。
**VLGOR 的核心动机**是利用视觉-语言模型（VLM）将视觉感知与语言理解结合，生成符合物理规律的“想象轨迹”来增强离线数据集，从而提升智能体在未知任务上的泛化性能。

#### 2. 方法论：核心思想与关键技术
VLGOR 框架分为两个主要阶段：
*   **轨迹生成阶段（Rollout Generation）：**
    *   **VLM 微调：** 使用 BAGEL 模型作为骨干，通过 LoRA 适配器进行微调。模型输入初始视觉观察、高层指令和初始动作，预测未来的视觉状态（图像帧）和动作。
    *   **环境落地：** 通过监督学习（MSE 损失）让 VLM 学习环境动力学，确保生成的轨迹在空间和时间上具有一致性。
    *   **反事实提示（Counterfactual Prompts）：** 为了增加数据多样性，通过修改指令中的“操作目标（Target）”或“行为模式（Behavior）”来生成原始数据中不存在的虚构任务轨迹。
*   **技能获取阶段（Skill Acquisition）：**
    *   将原始真实数据与 VLM 生成的虚构数据合并，构建增强数据集。
    *   使用离线强化学习算法（如 BC、CQL、TD3 等）训练目标条件策略（Goal-conditioned Policy），使智能体学会将语言指令与视觉状态对齐并执行动作。

#### 3. 实验设计
*   **实验场景/数据集：**
    *   **CLEVR-Robot：** 操纵 5 个可移动球体达到特定配置。
    *   **Meta-World：** 使用 Sawyer 机械臂执行开关门、按按钮、取放物体等任务。
*   **任务难度分级（Benchmark）：**
    *   **指令改写（Rephrasing）：** 语义相同但表达不同。
    *   **反事实任务（Counterfactual）：** 修改目标或行为，产生新任务。
    *   **困难任务（Hard）：** 目标与行为的复杂组合，与训练集差异巨大。
*   **对比方法：**
    *   基础离线 RL：BC（行为克隆）、CQL（保守 Q 学习）。
    *   语言引导方法：KALM（仅依赖语言生成轨迹的方法）。
    *   模型辅助方法：COMBO。

#### 4. 资源与算力
*   **硬件：** 使用了单张 **NVIDIA H800 80GB PCIe GPU**。
*   **训练细节：**
    *   VLM 微调：20 个 epoch，batch size 为 16。
    *   离线 RL 训练：50 个 epoch，使用 Adam 优化器。
    *   数据集规模：原始离线数据包含 10 万个轨迹对，微调 VLM 使用了 1 万个样本。

#### 5. 实验数量与充分性
*   **实验规模：** 针对两个基准环境，分别在三种难度等级下进行了测试。
*   **重复性：** 所有实验均使用 **3 个随机种子**取平均值，并提供了标准差（误差棒）。
*   **消融实验：** 探讨了 VLM 微调样本数量对生成质量（PSNR、SSIM、MED 指标）的影响，以及不同类型的反事实提示对成功率的贡献。
*   **充分性评价：** 实验设计较为全面，涵盖了从感知质量到决策性能的多维度评估，对比了多种主流离线 RL 算法，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能显著提升：** VLGOR 在未知任务上的成功率比基准方法高出 **24% 以上**。
*   **视觉引导的重要性：** 相比于仅依赖语言的 KALM，VLGOR 生成的轨迹在物理上更真实（解决了物体重叠等问题），这直接导致了更好的策略学习效果。
*   **泛化能力：** 即使在“困难任务”下，VLGOR 依然能保持一定的成功率（约 20%），而传统方法几乎失效（1%-8%）。
*   **数据增强有效性：** 反事实提示能有效扩展数据分布，帮助智能体在未见过的状态空间中进行探索。

#### 7. 优点
*   **物理一致性：** 成功将视觉感知引入轨迹生成，解决了 LLM 在具身智能中“幻觉”导致的物理违和感。
*   **数据效率：** 通过 VLM 生成高质量合成数据，缓解了离线 RL 对大规模高质量人工标注数据的依赖。
*   **通用性：** 框架不绑定特定的离线 RL 算法，具有良好的兼容性和扩展性。

#### 8. 不足与局限
*   **场景复杂度：** 实验目前集中在相对简单的机器人仿真环境（CLEVR, Meta-World），尚未在复杂的真实世界场景或高维动作空间中验证。
*   **联合学习压力：** VLGOR 同时生成状态和动作，这要求模型同时承担环境建模和策略模仿的任务，增加了学习难度。
*   **计算开销：** 虽然使用了 LoRA，但微调大型 VLM 并生成大量图像帧轨迹仍需要较高的计算资源。

（完）
