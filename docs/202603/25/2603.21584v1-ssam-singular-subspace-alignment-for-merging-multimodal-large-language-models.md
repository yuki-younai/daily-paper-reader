---
title: "SSAM: Singular Subspace Alignment for Merging Multimodal Large Language Models"
title_zh: SSAM：用于合并多模态大语言模型的奇异子空间对齐
authors: "Md Kaykobad Reza, Ameya Patil, Edward Ayrapetian, M. Salman Asif"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21584v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 跨视觉和音频的多模态大语言模型的对齐与合并
tldr: 针对多模态大语言模型（MLLM）训练成本高、数据需求大的问题，本文提出了一种无需训练的模型合并框架SSAM。该方法通过将不同模态专家模型的语言相关参数更新映射到共享的低秩奇异子空间进行对齐与合并，有效解决了参数干扰和表示差异挑战。SSAM在不使用任何多模态训练数据的情况下，在多个数据集上达到了SOTA性能，证明了参数空间对齐是构建多模态模型的一种高效且可扩展的替代方案。
selection_source: fresh_fetch
motivation: 传统的联合多模态训练耗费大量计算资源和配对数据，因此探索如何将现有的单模态专家模型高效合并为统一模型具有重要意义。
method: SSAM通过分离模态特定更新，并在共享的低秩子空间内对齐语言相关的参数更新，从而在减少干扰的同时保留互补知识。
result: 在四个数据集上的实验表明，SSAM在无训练条件下超越了现有的合并方法，甚至优于部分联合训练的多模态模型。
conclusion: 在参数空间进行子空间对齐为多模态模型的扩展提供了一种资源节约且性能卓越的新路径。
---

## 摘要
多模态大语言模型 (MLLM) 通过联合处理来自视觉、音频和语言等多种模态的输入，实现了强大的性能。然而，构建此类模型或将其扩展到新模态通常需要大规模的配对数据集和大量的计算资源。鉴于许多预训练的 MLLM（例如视觉-语言或音频-语言模型）已经公开，我们探究是否可以将它们合并为一个能够处理多种模态的单一 MLLM？合并具有不同输入模态的 MLLM 仍然具有挑战性，部分原因在于学习到的表示存在差异以及它们参数空间之间的干扰。为了应对这些挑战，我们提出了奇异子空间对齐与合并 (SSAM)，这是一种无需训练的模型合并框架，它将独立训练的专家级 MLLM 统一为一个能够处理任何输入模态组合的单一模型。SSAM 分别保留特定模态的参数更新，并为语言相关的参数更新识别出一个共享的低秩子空间，在该子空间内对齐它们并进行合并，从而在最小化参数干扰的同时保留互补知识。在不使用任何多模态训练数据的情况下，SSAM 在四个数据集上均达到了最先进的性能，超越了以往的无需训练的合并方法，甚至优于联合训练的多模态模型。这些结果表明，在参数空间中对齐模型为传统的联合多模态训练提供了一种可扩展且资源高效的替代方案。

## Abstract
Multimodal large language models (MLLMs) achieve strong performance by jointly processing inputs from multiple modalities, such as vision, audio, and language. However, building such models or extending them to new modalities often requires large paired datasets and substantial computational resources. Since many pretrained MLLMs (e.g., vision-language or audio-language) are publicly available, we ask whether we can merge them into a single MLLM that can handle multiple modalities? Merging MLLMs with different input modalities remains challenging, partly because of differences in the learned representations and interference between their parameter spaces. To address these challenges, we propose Singular Subspace Alignment and Merging (SSAM), a training-free model merging framework that unifies independently trained specialist MLLMs into a single model capable of handling any combination of input modalities. SSAM maintains modality-specific parameter updates separately and identifies a shared low-rank subspace for language-related parameter updates, aligns them within this subspace, and merges them to preserve complementary knowledge while minimizing parameter interference. Without using any multimodal training data, SSAM achieves state-of-the-art performance across four datasets, surpassing prior training-free merging methods and even jointly trained multimodal models. These results demonstrate that aligning models in parameter space provides a scalable and resource-efficient alternative to conventional joint multimodal training.

---

## 论文详细总结（自动生成）

以下是对论文《SSAM: Singular Subspace Alignment for Merging Multimodal Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：构建能够处理多种模态（视觉、音频、点云等）的多模态大语言模型（MLLM）通常需要海量的多模态配对数据和巨大的计算资源。
*   **核心问题**：既然社区已经存在许多针对特定模态的专家模型（如视觉-语言、音频-语言模型），能否在**无需额外训练（Training-free）**的情况下，将这些异构专家模型合并为一个统一的多模态模型？
*   **挑战**：不同专家模型在微调过程中学习到的参数更新方向存在差异，直接合并会导致严重的**参数干扰（Parameter Interference）**，从而损害模型性能。

### 2. 核心方法论：SSAM 框架
SSAM（奇异子空间对齐与合并）的核心思想是：在参数空间中识别并利用不同专家模型之间共享的低秩结构。
*   **参数解耦**：将模型参数分为模态特定参数（编码器和投影器）和语言相关参数（LLM 解码器）。合并时保留各自的模态参数，仅对语言参数进行操作。
*   **语言向量（Language Vectors）计算**：定义任务向量 $\Delta_i = W_i - W_0$，即微调后的专家模型权重与原始预训练权重之差。
*   **共享共识子空间构建**：
    1.  计算所有专家模型语言向量的行向和列向协方差矩阵。
    2.  利用**奇异值分解（SVD）**提取主成分方向，构建一个能够捕捉各模型间一致性更新方向的低秩子空间（Shared Consensus Subspace）。
*   **子空间投影与合并**：将每个专家的语言向量投影到该共享子空间中，过滤掉冲突的噪声或模态特定的冗余更新，最后在子空间内进行加权合并。
*   **LoRA 兼容性**：该方法支持 LoRA 权重的直接处理，通过重构低秩矩阵实现高效合并。

### 3. 实验设计
*   **数据集与场景**：
    *   **AVQA 任务**：使用 MUSIC-AVQA 和 AVQA 数据集，评估模型在视频、音频和图像组合下的问答能力。
    *   **MCUB 任务**：使用多模态共同性理解基准（MCUB-3 和 MCUB-4），测试模型在从未见过的模态组合（如图像+音频+点云）下的推理能力。
    *   **领域特定基准**：MMLU（语言理解）、OCRBench（视觉文本）、MMAU（音频理解），用于验证合并后是否保留了专家能力。
*   **对比方法**：
    *   **联合训练模型**：ChatBridge, OneLLM, ImageBind-LLM, X-InstructBLIP 等。
    *   **无需训练的合并方法**：Task Arithmetic, TIES-Merging, WUDI, OptMerge, DAMC 等。

### 4. 资源与算力
*   **模型规模**：实验主要基于 Vicuna-7B-v1.5 作为语言解码器，配合 CLIP（视觉）、BEATs（音频）、LanguageBind（视频）和 PointLLM（点云）编码器。
*   **算力说明**：论文**未明确列出**具体的 GPU 训练时长或数量，因为该方法的核心卖点是 **Training-free（无需训练）**。合并过程仅涉及矩阵运算（SVD 和投影），其计算开销远小于任何形式的微调。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个多模态数据集和 3 个单领域基准上进行了详尽测试，涵盖了从 2 模态到 4 模态的多种组合。
*   **消融实验**：针对子空间秩 $k$ 的选择、不同合并策略（简单平均 vs. 子空间对齐）进行了对比。
*   **充分性评价**：实验设计非常充分且客观。通过对比联合训练模型，证明了该方法在零样本（Zero-shot）多模态组合下的优越性；通过领域基准测试，证明了合并过程没有发生“灾难性遗忘”。

### 6. 主要结论与发现
*   **性能领先**：SSAM 在所有测试的数据集上均达到了 SOTA 性能，显著优于现有的无需训练的合并方法。
*   **超越联合训练**：在某些任务（如 MUSIC-AVQA）上，SSAM 的表现甚至超过了使用配对数据进行联合训练的模型，证明了参数空间对齐的强大潜力。
*   **跨模态泛化**：合并后的模型能够处理训练时从未见过的模态组合（如音频+点云），展现出强大的涌现推理能力。
*   **低秩特性**：实验发现 $k=128$ 左右的低秩子空间最能平衡知识保留与干扰抑制。

### 7. 优点（亮点）
*   **高效性**：完全无需训练，极大地降低了扩展 MLLM 到新模态的门槛。
*   **数学严谨性**：利用 SVD 显式处理参数干扰问题，比简单的权重平均（Naive Averaging）更具理论支撑。
*   **灵活性**：可以轻松集成来自不同机构、针对不同模态独立训练的专家模型。

### 8. 不足与局限
*   **架构限制**：要求所有专家模型共享相同的语言解码器架构（如都基于 Vicuna-7B），无法直接合并异构架构（如 Llama-3 与 Qwen）。
*   **超参数依赖**：虽然 $k=128$ 在实验中表现良好，但在处理差异极大的模型时，可能仍需要针对特定任务微调缩放系数 $\lambda$ 或秩 $k$。
*   **模态编码器独立**：虽然语言层实现了对齐，但模态编码器仍然是独立的，这可能导致在处理极多模态输入时，内存开销随模态数量线性增加。

（完）
