---
title: Off-Policy Value-Based Reinforcement Learning for Large Language Models
title_zh: 面向大语言模型的离策基于价值的强化学习
authors: "Peng-Yuan Wang, Ziniu Li, Tian Xu, Bohan Yang, Tian-Shuo Liu, ChenYang Wang, Xiong-Hui Chen, Yi-Chen Li, Tianyun Yang, Congliang Chen, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23355v1"
tags: ["query:rlaif"]
score: 10.0
evidence: 针对大语言模型的离策价值强化学习框架
tldr: 针对大语言模型（LLM）强化学习中策略梯度方法采样效率低的问题，本文提出了一种基于价值的离策（Off-Policy）强化学习框架 ReVal。该方法通过结合步进式内部一致性信号与轨迹级结果验证信号，利用 Bellman 更新和经验回放机制实现数据的多次复用。实验证明，ReVal 在数学推理等任务上比 GRPO 收敛更快且性能更优，为 LLM 训练提供了一种高效的替代方案。
selection_source: fresh_fetch
motivation: 现有的 LLM 强化学习方法多为同策（On-policy），导致数据利用率低且在长程任务中采样成本高昂。
method: 提出 ReVal 框架，通过结合步进式信号与轨迹级验证的 Bellman 更新，并利用经验回放缓冲区实现离策学习。
result: "在数学推理基准测试中，ReVal 的收敛速度和最终性能均优于 GRPO，在 AIME24 和 GPQA 任务上分别提升了 2.7% 和 4.5%。"
conclusion: 基于价值的离策强化学习是提升 LLM 训练效率和性能的一种切实可行且高效的路径。
---

## 摘要
提高数据利用效率对于在生成轨迹成本高昂的长程任务中扩展强化学习（RL）至关重要。然而，目前主流的大语言模型（LLM）强化学习方法大多是同策（on-policy）的：它们对每批数据仅更新一次后便将其丢弃，随后采集新样本，导致样本效率低下。在这项工作中，我们探索了一种替代的基于价值的 LLM 强化学习框架，该框架能够自然地实现离策（off-policy）学习。我们提出了 ReVal，这是一种基于贝尔曼更新（Bellman-update）的方法，它结合了捕捉内部一致性的逐步信号和源自结果验证的轨迹级信号。ReVal 自然地支持基于经验回放池（replay-buffer）的训练，从而能够高效地复用过去的轨迹。在标准数学推理基准测试上的实验表明，ReVal 不仅收敛更快，而且在最终性能上也优于 GRPO。在 DeepSeek-R1-Distill-1.5B 上，ReVal 提高了训练效率，且相比 GRPO 在 AIME24 上提升了 2.7%，在域外基准测试 GPQA 上提升了 4.5%。这些结果表明，基于价值的强化学习是 LLM 训练中替代基于策略（policy-based）方法的一种实用方案。

## Abstract
Improving data utilization efficiency is critical for scaling reinforcement learning (RL) for long-horizon tasks where generating trajectories is expensive. However, the dominant RL methods for LLMs are largely on-policy: they update each batch of data only once, discard it, and then collect fresh samples, resulting in poor sample efficiency. In this work, we explore an alternative value-based RL framework for LLMs that naturally enables off-policy learning. We propose ReVal, a Bellman-update-based method that combines stepwise signals capturing internal consistency with trajectory-level signals derived from outcome verification. ReVal naturally supports replay-buffer-based training, allowing efficient reuse of past trajectories. Experiments on standard mathematical reasoning benchmarks show that ReVal not only converges faster but also outperforms GRPO in final performance. On DeepSeek-R1-Distill-1.5B, ReVal improves training efficiency and achieves improvement of 2.7% in AIME24 and 4.5% in out-of-domain benchmark GPQA over GRPO. These results suggest that value-based RL is a practical alternative to policy-based methods for LLM training.

---

## 论文详细总结（自动生成）

以下是对论文《Off-Policy Value-Based Reinforcement Learning for Large Language Models》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：当前大语言模型（LLM）的主流强化学习（RL）方法（如 PPO、GRPO）大多是**同策（On-policy）**的。这意味着每批采集的轨迹数据在进行一次梯度更新后即被丢弃，导致采样效率极低。
*   **背景挑战**：随着 LLM 转向长程推理（Long-horizon reasoning）和智能体（Agentic）任务，生成轨迹的计算成本已成为训练的主要瓶颈。在这些场景下，如何通过**离策（Off-policy）**学习实现数据的多次复用，是降低训练成本、提升模型性能的关键。

### 2. 方法论：ReVal 框架
*   **核心思想**：将 LLM 的 Logits 直接解释为动作价值（Q-values），从而在不增加额外 Critic 模型（低成本）的前提下，将 LLM 转化为一个天然支持贝尔曼更新（Bellman Update）的价值模型。
*   **关键技术细节**：
    *   **Logit-as-Q 参数化**：基于前人研究，证明预训练 LLM 的 Logits 编码了价值信息，ReVal 直接利用 Logits 定义 $Q_\theta(s, a)$。
    *   **校准初始化（Calibrated Initialization）**：针对前序工作 TBRM 在无奖励时会产生策略漂移的问题，ReVal 引入了**奖励塑造（Reward Shaping）**项。该项确保在奖励为零时，最优策略会自动退化为参考策略（Reference Policy），保证了训练初期的稳定性。
    *   **目标函数**：构建了一个轨迹级的贝尔曼残差损失函数，结合了缩放后的环境奖励、参考策略的引导信号以及状态价值（V-function）的偏移。
    *   **经验回放池（Replay Buffer）**：引入 FIFO（先进先出）缓冲区存储历史轨迹。在每次训练迭代中，从缓冲区中采样旧数据进行多次梯度更新（离策学习），从而实现“一份数据，多次利用”。

### 3. 实验设计
*   **模型与数据集**：
    *   **基础模型**：DeepSeek-R1-Distill-Qwen-1.5B 和 Qwen2.5-Math-7B。
    *   **训练数据**：DeepScaleR 数据集。
*   **基准测试（Benchmarks）**：涵盖 AIME24、AIME25、AMC、MATH、MINERVA、Olympiad Bench 和 GPQA（域外泛化测试）。
*   **对比方法**：
    *   **GRPO**：目前主流的同策、无 Critic 策略梯度方法。
    *   **TBRM**：另一种基于价值但通常以同策方式运行的方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 H100 或 A100）及具体数量。
*   **训练效率对比**：
    *   在单次生成仅 1 条轨迹（N=1）的受限设置下，ReVal 达到 SOTA 性能所需的时间为 6.3 小时，而 GRPO 需要 7.5 小时（时间缩短约 18%）。
    *   在收敛速度上，ReVal 在不同难度的任务中实现了平均 **4.3 倍**的加速。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的基准测试（7 个主流榜单）和多维度消融实验。
*   **消融实验内容**：包括参考策略更新频率、超参数 $\beta$（控制 KL 正则化强度）的影响、以及不同奖励设计（0/1 奖励 vs 归一化优势奖励）的对比。
*   **客观性与公平性**：实验在相同的训练框架（Verl）和超参数基准下进行，并特别设置了“有限采样（N=1）”场景，以验证离策学习在极端资源受限下的真实价值，实验设计较为严谨客观。

### 6. 主要结论与发现
*   **性能超越**：在 DeepSeek-R1-Distill-1.5B 上，ReVal 在 AIME24 上比 GRPO 高出 2.7%，在 GPQA 上高出 4.5%。
*   **采样效率极高**：在每步仅采样 1 条轨迹的情况下，ReVal 的优势更加显著（AIME 提升 4.8%），证明了其对稀缺数据的高效利用能力。
*   **收敛加速**：通过增加回放池的更新频率，模型能以更少的生成轮数达到目标精度。
*   **泛化能力**：在域外基准（GPQA）上的显著提升表明，基于价值的学习可能比单纯的策略梯度更能捕捉到推理任务的底层逻辑。

### 7. 优点（亮点）
*   **架构优雅**：无需额外的 Critic 网络，保持了 Actor-only 方法的显存优势，同时获得了离策学习的能力。
*   **理论完备**：通过奖励塑造解决了价值学习中的初始化校准问题，提升了训练稳定性。
*   **实用性强**：直接兼容现有的分布式训练框架，且在生成成本高昂的长文本推理任务中具有极高的经济价值。

### 8. 不足与局限
*   **任务覆盖面**：目前主要集中在数学推理任务，尚未在通用对话、代码生成或多模态任务中验证其普适性。
*   **回放策略简单**：目前仅使用简单的 FIFO 缓冲区，未探索更高级的优先经验回放（Prioritized Experience Replay），可能仍有提升空间。
*   **超参敏感度**：实验显示 $\beta$ 参数对训练稳定性影响较大，且不同长度的模型输出需要精细调整该参数。

（完）
