---
title: "SortedRL: Accelerating RL Training for LLMs through Online Length-Aware Scheduling"
title_zh: SortedRL：通过在线长度感知调度加速大语言模型的强化学习训练
authors: "Yiqi Zhang, Huiqiang Jiang, Xufang Luo, Zhihe Yang, Chengruidong Zhang, Yifei Shen, Dongsheng Li, Yuqing Yang, Lili Qiu, Yang You"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23414v1"
tags: ["query:rlaif"]
score: 9.0
evidence: 通过长度感知调度加速大模型的强化学习训练
tldr: 针对大语言模型强化学习训练中长序列生成导致的效率瓶颈，本文提出SortedRL调度策略。该方法通过对Rollout样本按长度排序并优先处理短样本，实现了更早的策略更新和微课程学习。结合缓存机制控制离策程度，SortedRL在显著减少训练空置率的同时，在逻辑推理和数学任务上展现出更强的性能表现。
selection_source: fresh_fetch
motivation: 解决LLM强化学习中长轨迹生成导致的Rollout阶段耗时过长及计算资源同步开销大的效率瓶颈。
method: 提出一种在线长度感知调度策略，通过对样本排序优先更新短序列，并利用缓存机制和状态控制器优化流水线。
result: "实验显示SortedRL减少了50%以上的训练空置率，并在相同数据量下使模型性能提升了3.9%至18.4%。"
conclusion: SortedRL通过优化Rollout与更新的调度逻辑，在不牺牲训练稳定性的前提下显著提升了RL训练效率和模型推理能力。
---

## 摘要
扩展强化学习（RL）在增强大语言模型（LLMs）的推理能力方面展现出巨大潜力，特别是在需要长思维链（CoT）生成的任务中。然而，RL 训练效率通常受限于采样（rollout）阶段。在生成长轨迹（如 16k token）时，由于自回归生成速度慢以及采样与策略更新之间的同步开销，采样阶段可占总训练时间的 70%。我们提出了 SortedRL，这是一种在线长度感知调度策略，旨在通过提高采样效率并保持训练稳定性来解决这一瓶颈。SortedRL 根据输出长度对采样样本进行重新排序，优先将短样本成组以进行早期更新。这使得大批量采样、灵活的更新批次以及近乎同策略（on-policy）的微课程构建能够同时实现。为了进一步加速流水线，SortedRL 引入了一种基于缓存的机制来控制离策略（off-policy）训练的程度，并由专门的 RL 基础设施支持，该设施通过有状态控制器和采样缓冲区管理采样与更新。在包括逻辑谜题以及 AIME 24、Math 500 和 Minerval 等数学挑战在内的多种任务上，使用 LLaMA-3.1-8B 和 Qwen-2.5-32B 进行的实验表明，SortedRL 将 RL 训练的空泡率（bubble ratios）降低了 50% 以上，同时在相同数据量下，性能比基准模型提升了 3.9% 至 18.4%。

## Abstract
Scaling reinforcement learning (RL) has shown strong promise for enhancing the reasoning abilities of large language models (LLMs), particularly in tasks requiring long chain-of-thought generation. However, RL training efficiency is often bottlenecked by the rollout phase, which can account for up to 70% of total training time when generating long trajectories (e.g., 16k tokens), due to slow autoregressive generation and synchronization overhead between rollout and policy updates. We propose SortedRL, an online length-aware scheduling strategy designed to address this bottleneck by improving rollout efficiency and maintaining training stability. SortedRL reorders rollout samples based on output lengths, prioritizing short samples forming groups for early updates. This enables large rollout batches, flexible update batches, and near on-policy micro-curriculum construction simultaneously. To further accelerate the pipeline, SortedRL incorporates a mechanism to control the degree of off-policy training through a cache-based mechanism, and is supported by a dedicated RL infrastructure that manages rollout and update via a stateful controller and rollout buffer. Experiments using LLaMA-3.1-8B and Qwen-2.5-32B on diverse tasks, including logical puzzles, and math challenges like AIME 24, Math 500, and Minerval, show that SortedRL reduces RL training bubble ratios by over 50%, while attaining 3.9% to 18.4% superior performance over baseline given same amount of data.

---

## 论文详细总结（自动生成）

### SortedRL：通过在线长度感知调度加速大语言模型的强化学习训练

#### 1. 论文的核心问题与整体含义
*   **研究背景**：强化学习（RL）已成为提升大语言模型（LLM）推理能力（如长思维链 CoT）的关键技术。
*   **核心痛点**：RL 训练的效率瓶颈在于**采样阶段（Rollout）**，其耗时可占总训练时间的 70%。由于 LLM 生成是自回归的，且不同样本的输出长度差异巨大（长尾分布），传统的同步更新机制会导致严重的 **GPU 计算空泡（Bubbles）**，即硬件在等待最长样本生成完成时处于闲置状态。
*   **研究动机**：现有的解决方法（如增大 Batch Size）虽能提高吞吐，但会导致模型在同一批数据上多次更新，产生“离策略（Off-policy）”偏差，损害训练稳定性。论文旨在寻找一种既能消除计算空泡，又能保持同策略（On-policy）稳定性的调度方案。

#### 2. 方法论
SortedRL 的核心思想是**在线长度感知调度**，通过算法与基础设施的协同设计来优化训练流水线：
*   **在线长度感知调度（Online Length-Aware Scheduling）**：
    *   **超额订阅与早期终止**：控制器向生成引擎输入超过容量的 Prompt，并根据生成进度动态截断。
    *   **长度排序更新**：将生成的轨迹按长度排序，优先将已完成的短样本组成 Batch 送入训练器更新模型。
*   **微课程构建（Micro-curriculum）**：由于短样本通常对应较简单的任务，这种按长度排序的更新方式自然形成了一种从易到难的“微课程”，有助于模型稳定收敛。
*   **可控离策机制（Controllable Off-policiness）**：
    *   **全同策模式**：仅使用最新策略生成的完整轨迹，丢弃未完成部分。
    *   **部分模式（Partial Mode）**：利用缓存机制存储未完成的片段及其 Log-probabilities，在下一轮继续生成，通过重要性采样修正偏差。
*   **基础设施协同设计**：开发了专门的 **Stateful Rollout Buffer（有状态采样缓冲区）** 和控制器，支持断点续传和细粒度的 Batch 管理。

#### 3. 实验设计
*   **数据集与场景**：
    1.  **逻辑推理**：LogicRL（包含 5000 个合成的骑士与无赖谜题）。
    2.  **数学任务**：DAPO-Math-17k（混合数学数据集）。
*   **Benchmark**：GSM8k、MATH500、Minerva Math、OlympiadBench、AIME 2024、AMC 2023。
*   **对比方法**：Vanilla Reinforce++（标准强化学习基准）和 PPO。
*   **实验模型**：LLaMA-3.1-8B-Instruct（用于逻辑任务）和 Qwen-2.5-32B（用于数学任务）。

#### 4. 资源与算力
*   **硬件环境**：实验在 NVIDIA H100 和 AMD MI300X GPU 混合集群上运行。
*   **计算配置**：配备了 96 核 Intel Xeon Platinum 8480C CPU。
*   **软件框架**：基于 VeRL 框架开发，使用 SGLang 作为后端推理引擎。
*   **训练细节**：文中未明确给出总的训练小时数，但提到在数学任务中进行了 600 个更新步数（Update Steps）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 8B 到 32B 不同规模的模型，并在逻辑和数学两个具有代表性的推理领域进行了验证。
*   **消融实验**：针对“分组大小（Group Size）”、“排序策略（Post-hoc vs Online）”以及“有无分组”进行了详细的消融研究。
*   **客观性**：实验采用了 Mean@32（多次采样平均准确率）等标准指标，并对比了相同数据量下的性能表现，确保了对比的公平性。

#### 6. 主要结论与发现
*   **效率大幅提升**：SortedRL 将采样阶段的空泡率从 **74% 降低至 6% 以下**，Rollout 吞吐量提升了约 **40%**。
*   **性能显著增强**：在相同训练数据量下，SortedRL 在 AIME 2024 等竞赛级数学任务上的准确率比基准提升了 **3.9% 至 18.4%**。
*   **样本效率**：在逻辑推理任务中，SortedRL 仅需减少 **40.74%** 的样本量即可达到与基准相同的评分。
*   **长度与性能关系**：实验观察到模型性能的提升往往伴随着响应长度的增加，SortedRL 的调度策略更好地利用了这一特性。

#### 7. 优点
*   **工程实用性强**：直接解决了大规模 RL 训练中昂贵的计算资源浪费问题。
*   **算法与系统结合**：不仅是算法优化，还通过底层基础设施的改造实现了真正的端到端加速。
*   **无需额外标注**：利用样本长度作为自然代理指标构建微课程，降低了训练设计的复杂度。

#### 8. 不足与局限
*   **超参数敏感性**：分组大小（Group Size）对训练效果影响较大，过大或过小都可能导致性能退化，需要针对不同任务调优。
*   **任务覆盖面**：主要集中在具有明确规则和答案的逻辑与数学任务，在创意写作或开放式对话等长度分布更不可控的任务上效果尚待验证。
*   **离策风险**：虽然提供了部分离策模式，但在某些极长序列任务中，缓存片段的累积可能仍会带来难以预测的训练不稳定性。

（完）
