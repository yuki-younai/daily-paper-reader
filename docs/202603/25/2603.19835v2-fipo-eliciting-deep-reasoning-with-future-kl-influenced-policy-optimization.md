---
title: "FIPO: Eliciting Deep Reasoning with Future-KL Influenced Policy Optimization"
title_zh: FIPO：通过受未来 KL 影响的策略优化激发深度推理
authors: "Chiyu Ma, Shuo Yang, Kexin Huang, Jinda Lu, Haoming Meng, Shangshang Wang, Bolin Ding, Soroush Vosoughi, Guoyin Wang, Jingren Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19835v2"
tags: ["query:rlaif"]
score: 9.0
evidence: 用于大模型策略优化的强化学习算法
tldr: 针对大语言模型在复杂推理中面临的信用分配不均问题，本文提出FIPO算法。传统GRPO依赖结果奖励（ORM）将优势均匀分配给所有token，难以区分关键逻辑点。FIPO通过引入折扣未来KL散度，构建稠密优势函数，根据token对后续轨迹的影响进行重加权。实验显示，FIPO显著提升了推理长度和准确率，在AIME 2024上超越了o1-mini等模型，证明了稠密奖励对激发模型深度推理潜力的重要性。
selection_source: fresh_fetch
motivation: 传统的基于结果奖励的强化学习算法存在信用分配粗糙的问题，无法有效区分推理过程中的关键逻辑节点。
method: 提出FIPO算法，通过引入折扣未来KL散度来构建稠密优势函数，从而根据每个token对后续生成轨迹的影响力进行差异化权重分配。
result: "在Qwen2.5-32B上，FIPO将思维链长度从4千提升至1万以上，并将AIME 2024的Pass@1准确率提升至最高58%，优于o1-mini。"
conclusion: 建立稠密优势表述是优化基于结果奖励算法、释放基础模型深度推理能力的关键路径。
---

## 摘要
我们提出了受未来 KL 影响的策略优化（FIPO），这是一种旨在克服大语言模型推理瓶颈的强化学习算法。虽然 GRPO 风格的训练能够有效扩展，但它通常依赖于基于结果的奖励（ORM），将全局优势均匀地分配给轨迹中的每一个 token。我们认为，这种粗粒度的信用分配由于无法区分关键逻辑转折点与平凡 token，从而限制了性能上限。FIPO 通过将折扣未来 KL 散度引入策略更新来解决这一问题，从而创建了一种稠密优势公式，根据 token 对后续轨迹行为的影响对其进行重新加权。实验表明，FIPO 使模型能够突破标准基准测试中出现的长度停滞现象。在 Qwen2.5-32B 上的评估显示，FIPO 将平均思维链长度从约 4,000 个 token 延长至 10,000 个以上，并将 AIME 2024 Pass@1 准确率从 50.0% 提升至 58.0% 的峰值（收敛于约 56.0%）。这一表现优于 DeepSeek-R1-Zero-Math-32B（约 47.0%）和 o1-mini（约 56.0%）。我们的结果表明，建立稠密优势公式是演进基于 ORM 的算法以释放基础模型全部推理潜力的关键路径。我们开源了基于 verl 框架构建的训练系统。

## Abstract
We present Future-KL Influenced Policy Optimization (FIPO), a reinforcement learning algorithm designed to overcome reasoning bottlenecks in large language models. While GRPO style training scales effectively, it typically relies on outcome-based rewards (ORM) that distribute a global advantage uniformly across every token in a trajectory. We argue that this coarse-grained credit assignment imposes a performance ceiling by failing to distinguish critical logical pivots from trivial tokens. FIPO addresses this by incorporating discounted future-KL divergence into the policy update, creating a dense advantage formulation that re-weights tokens based on their influence on subsequent trajectory behavior. Empirically, FIPO enables models to break through the length stagnation seen in standard baselines. Evaluated on Qwen2.5-32B, FIPO extends the average chain-of-thought length from roughly 4,000 to over 10,000 tokens and increases AIME 2024 Pass@1 accuracy from 50.0% to a peak of 58.0% (converging at approximately 56.0\%). This outperforms both DeepSeek-R1-Zero-Math-32B (around 47.0%) and o1-mini (approximately 56.0%). Our results suggest that establishing dense advantage formulations is a vital path for evolving ORM-based algorithms to unlock the full reasoning potential of base models. We open-source our training system, built on the verl framework.

---

## 论文详细总结（自动生成）

以下是对论文《FIPO: Eliciting Deep Reasoning with Future-KL Influenced Policy Optimization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的强化学习算法（如 GRPO）在提升大模型推理能力时，主要依赖**基于结果的奖励（ORM）**。这种方式将最终的对错奖励均匀地分配给推理路径上的每一个 token（粗粒度信用分配）。
*   **研究动机**：作者认为这种“大锅饭”式的奖励分配存在性能天花板，因为它无法区分推理过程中的“关键逻辑转折点”和“无关痛痒的废话”。这导致模型在训练中会出现“长度停滞”现象，难以自发演化出更深层次的思考和自我反思行为。
*   **整体含义**：论文旨在证明，无需复杂的判别器（Critic）模型，仅通过改进策略优化公式，就能实现**稠密（Dense）的信用分配**，从而激发模型产生超长思维链（Long-CoT）并突破推理性能极限。

### 2. 方法论：FIPO 核心思想与技术细节
FIPO 的核心是在策略更新中引入了**未来 KL 散度（Future-KL）**来对 token 的优势函数进行重加权。
*   **概率偏移（$\Delta \log p$）**：定义为当前策略与旧策略在同一 token 上的对数概率差。它反映了模型在训练过程中对某个推理步骤的“偏好改变”。
*   **未来 KL 估计（Future-KL）**：计算从当前步骤 $t$ 到序列结束 $T$ 的累积概率偏移。其直观理解是：如果一个 token 导致后续整个轨迹都被模型更加看好（Future-KL 为正），那么这个 token 就是关键的逻辑锚点。
*   **关键技术组件**：
    *   **软衰减窗口（Soft Decay Window）**：引入折扣因子 $\gamma$，使近期的后续 token 对当前 token 的影响权重更大，远期的影响逐渐减小，以降低长程噪声。
    *   **极端值过滤与屏蔽（Masking）**：为了防止训练不稳定，FIPO 会自动识别并屏蔽掉那些超出安全阈值的异常概率偏移，避免梯度爆炸。
    *   **影响权重裁剪（Influence Weight Clipping）**：将计算出的 Future-KL 指数映射限制在一定范围内（如 [1.0, 1.2]），确保优势函数的调节是平滑的。
*   **算法流程**：在 GRPO 的框架下，计算组内相对优势后，再乘以这个基于 Future-KL 的影响权重 $f_t$，从而实现对关键 token 的强化或对错误路径起始点的严厉惩罚。

### 3. 实验设计
*   **基础模型**：主要使用 **Qwen2.5-32B-Base**（强调是纯净基座，未经过长思维链 SFT 蒸馏），部分消融实验使用 Qwen2.5-7B-Math。
*   **数据集**：使用公开的 **DAPO-17K** 数学竞赛数据集。
*   **Benchmark**：以 **AIME 2024** 为核心验证集，辅以 AIME 2025。
*   **对比方法**：
    *   **DAPO**（改进版 GRPO 基准）。
    *   **DeepSeek-R1-Zero-Qwen-32B**。
    *   **o1-mini**（OpenAI 的闭源推理模型）。

### 4. 资源与算力
*   **框架**：基于 **verl** 强化学习框架构建。
*   **训练细节**：全局 Batch Size 为 512 个 prompt，每个 prompt 采样 16 个响应（共 8192 个样本/迭代）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长。但从其使用的 32B 模型规模和超长序列（2万 token）来看，该实验需要大规模 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 的模型扩展，验证了算法的可迁移性。
*   **消融实验**：非常详尽。作者针对裁剪比例（Clip-high）、最大长度限制、极端值过滤机制、影响权重范围以及衰减率 $\tau$ 均做了对比实验。
*   **公平性**：通过使用相同的开源数据集和基础模型（Qwen2.5-Base），与 DAPO 等基准进行了严格的受控对比，证明了性能提升确实源于 FIPO 算法本身。

### 6. 主要结论与发现
*   **突破长度瓶颈**：FIPO 使模型平均推理长度从 4,000 token 飙升至 **10,000 token 以上**，且长度增长与准确率提升呈强正相关。
*   **性能超越**：在 AIME 2024 上，FIPO 达到了 **58.0%** 的峰值准确率，不仅远超 DeepSeek-R1-Zero-32B（47%），也超越了 **o1-mini**（56%）。
*   **自发涌现行为**：模型在训练后期自发产生了复杂的**自我反思、多路径验证和符号重推导**等深度推理行为。
*   **稳定性**：相比于标准 GRPO，FIPO 在处理超长序列时表现出更平滑的策略漂移和更稳定的梯度。

### 7. 优点
*   **高效性**：在不需要 Critic 模型（节省显存和计算开销）的前提下，实现了类似 PPO 的稠密优势估计效果。
*   **理论联系实际**：将信息论中的 KL 散度转化为动态的信用分配工具，解决了 RLVR（可验证奖励强化学习）中的核心痛点。
*   **开源贡献**：完整开源了训练代码和配置，对复现类 o1 模型具有极高的参考价值。

### 8. 不足与局限
*   **推理成本高昂**：虽然性能提升显著，但 10,000+ token 的推理长度带来了巨大的计算开销和延迟，如何将其“蒸馏”回短序列仍是挑战。
*   **任务局限性**：目前主要集中在数学领域（具有明确的 ORM 奖励），在代码或非结构化推理任务上的泛化能力尚待验证。
*   **对基座依赖性**：实验指出 7B 模型由于上下文窗口限制和预训练偏差，难以像 32B 模型那样通过 FIPO 激发超长推理，说明算法效果受限于基座模型的固有容量。

（完）
